namespace BDP.Core.AttackExecution.RangedProtocol.Prepare
{
    /// <summary>
    /// Prepare 阶段当前需要覆盖裁决的正式维度。
    /// 其余准备字段继续按门槛或叠加规则合并。
    /// </summary>
    internal enum PrepareStageDimension
    {
        Abort = 0
    }
}
