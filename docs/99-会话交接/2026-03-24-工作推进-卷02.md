### 本卷说明
- 这一卷从第一阶段主链注释补强收尾开始继续记录
- 旧卷过长，后续工作改记到这里

### 本次工作
- 已按 `docs/plans/2026-03-26-正式芯片定义契约实施计划-第一版.md` 进入执行首批任务
- 已在主模组新增 `Source/BDP/Core/Chips/Config` 与 `Source/BDP/Core/Chips/Contract` 最小骨架，建立 `ChipDefinitionConfig / ChipIdentityConfig / ChipLoadoutConfig / ChipResourceConfig / ChipExtensionConfig`
- 已新增正式契约对象与解释器：`ChipDefinitionContract / ChipIdentityContract / ChipLoadoutContract / ChipExpressionContractHandle / ChipResourceContract / ChipExtensionContract / ChipDefinitionDeclaredBlock / IChipDefinitionContractResolver / DefaultChipDefinitionContractResolver`
- 当前表达块接入策略已按设计落实为“芯片定义层只持有表达块句柄，不复制表达系统内部条目对象”

### 当前结论
- `Core/Chips` 的最小输入层和正式解释层已经成立
- 当前尚未进入的是统一 Access 读取层与最低合法性 Validation 层

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次工作
- 已新增 `Source/BDP/Core/Chips/Validation`，建立 `ChipDefinitionValidationSeverity / ChipDefinitionValidationMessage / ChipDefinitionValidationResult / IChipDefinitionValidator / DefaultChipDefinitionValidator`
- 已新增 `Source/BDP/Core/Chips/Access`，建立 `IChipDefinitionReader / ChipDefinitionReadResult / ChipSurfaceAccess / ChipDefinitionReaderSurface`
- 当前芯片定义层已经能统一回答：目标是否是正式芯片、最低合法性是否成立、正式契约结果是什么
- 表达系统已开始改从芯片定义层接表达块：`DefaultExpressionSourceDeclarationProvider` 现在优先通过 `IChipDefinitionReader` 读取表达块句柄，`ExpressionReaderSurface` 的主链拼装和契约诊断汇总也已改经芯片定义层转交表达配置

### 当前结论
- 芯片定义层已经从“纯骨架”推进到“可被表达系统正式消费”的状态
- 当前主模组里，表达系统不再只依赖 `ThingDef.GetModExtension<ChipExpressionConfig>()` 这条旧上游

### 本次验证
- 已再次执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次工作
- 已按“严格只认新设计、不做兼容”的原则回收过渡思路，删除芯片定义解释层里刚出现的旧 Trigger 约束折算逻辑
- Trigger 主链已改为经芯片定义层读取 `Loadout` 块，不再直接读取旧 `TriggerChipConstraintConfig`
- `TriggerActivationRules` 现在会拒绝非正式芯片或未通过正式定义校验的输入，避免垃圾物品混入 Trigger 正式主链
- 旧文件 `Source/BDP/Core/Trigger/Loadout/TriggerChipConstraintConfig.cs` 已删除，不再保留空壳
- `BorderDefenseProtocol.DevHarness` 当前测试芯片 Def 已迁到 `ChipDefinitionConfig` 总入口，旧的直挂表达块和直挂 Trigger 约束块写法已退出样本主路径
- 已补入 `BDP_TestChipInvalidDefinition` 非法芯片样本，供后续正式校验与说明诊断使用

### 当前结论
- 当前主模组已经从“芯片定义层存在”推进到“表达系统和 Trigger 都开始依赖芯片定义层”
- 旧的芯片输入写法已不再作为当前样本主路径存在

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过
- 已执行 `dotnet msbuild BDP.DevHarness.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次工作
- 已把 `docs/需求说明/2026-03-26-正式芯片定义契约说明-第一版.md` 从骨架补成正式上位文档，明确芯片是主模组承认的物品契约，而不是表达系统附属物
- 已正式把芯片定义拆成 `Identity / Loadout / Expression / Resource / Extension` 五块，并写清哪些属于芯片声明、哪些属于运行结果，禁止再把主攻击、按钮、当前执行结果之类真值塞回芯片定义
- 已同步把“从芯片到攻击按钮”的当前链路纳入文档审查：确认前半段边界已基本清楚，后半段仍存在 `ExpressionManualGizmoBridge / ExpressionVerbBridge / TriggerEquippedGizmoService / 两处 Pawn Patch` 这类内部桥；它们不是违禁翻墙，但也还不是最终正式动作边界
- 已回写 `芯片标准结构说明`，明确它现在服从上位的正式芯片契约文档，不再单独承担“什么叫正式芯片”的总裁定；同时把总结构命名收紧到 `identity / loadout / resource / expression / modes / extensions`

### 当前结论
- 当前主模组已经有“芯片作为物品”和“芯片表达契约”的部分事实，但还没有把“正式芯片识别入口、最低合法校验、统一读取名录”这层总边界封口
- 下一步更合理的推进方向，不是继续加零散桥接，而是补正式芯片识别与校验骨架，再决定各下游系统如何通过统一入口读取

### 本次工作
- 已新增 `docs/plans/2026-03-26-正式芯片定义契约骨架设计-第一版.md`
- 这份设计文档已把“芯片定义层为何必须独立、建议目录、五块声明结构、统一读取入口、最低合法性校验、各系统读取边界、与当前按钮链路的关系”一次收口
- 当前已推进到“可以直接写实施计划”的停点，但还没有进入实施计划和代码落地阶段

### 当前结论
- 当前关于正式芯片定义契约，已经同时具备：
- 上位边界文档
- 下位骨架设计文档
- 按钮链路边界记账
- 后续下一步不应再回到抽象争论，而应直接转实施计划

### 本次工作
- 已新增 `docs/plans/2026-03-26-正式芯片定义契约实施计划-第一版.md`
- 该计划前部已补齐简略提示词，明确背景、参考、约束、需求、目标
- 计划正文已完整覆盖从 `Core/Chips` 目录落地、契约解释、最低合法性校验、统一读取入口、Trigger / Expressions 接线迁移、DevHarness 示例迁移、说明面诊断接入，到文档回写和最终验证的整条任务链

### 当前结论
- 当前关于“正式芯片定义契约”这条任务，已经不再缺设计与计划层文档
- 后续若继续推进，应直接按该实施计划进入执行，而不是再补新的概念性提纲

### 本次工作
- 第一阶段主链的表达层、投影层、语义层、投射物与 Verb 文件，已按逐成员短注释风格补齐
- 主模组与测试模组重新编译通过
- 准备回到主线功能与架构推进

### 本次工作
- 已回顾 `docs` 下总纲、决策、计划、研究记录主干文档
- 已按“功能机制 + 规则约束 + 阶段分层”整理出全局需求清单
- 已新增 `docs/plans/2026-03-24-全局需求清单-第一版.md`，供后续长期对照执行

### 本次工作
- 已新建 `docs/需求说明` 目录，集中收纳需求清单与专项需求说明
- 已将全局需求清单迁入 `docs/需求说明/2026-03-24-全局需求清单-第一版.md`
- 已新增 `2026-03-24-攻击表达共存与多层投影说明-第一版.md`
- 已把“表达结果、按钮载体、可直接操作入口、主攻击投影、外部兼容表面、视觉跟随、形态切换连锁重算”明确拆层，并在需求清单中建立引用

### 本次工作
- 已按最新理解补足“视觉渲染旧 BDP 已是既有需求、信息兼容是重构新增强化需求”的区分
- 已更新全局需求清单与攻击表达说明中的相关引用和边界表述
- 已新增 `2026-03-24-多表达视觉渲染与信息兼容说明-第一版.md`
- 已把“真实多表达状态、视觉渲染、场景默认主攻击决策、多表达明细、外部正式接口、定点桥接/补丁边界”拆成独立专项说明
### 本次工作
- 已回查旧 BDP 远程链相关代码，重新确认远程攻击不是单点动作，而是贯穿表达、瞄准、发射、投射物、命中、落地、信息投影、视觉投影的一条攻击链
- 已新增 `docs/需求说明/2026-03-24-远程攻击链与模块化真实需求说明-第一版.md`
- 已把“旧 BDP 局部做对了什么、真正没做好的断裂点在哪里、真实目标不是旧业务名清单而是统一攻击链规则”压成正式需求说明
- 已在全局需求清单与攻击表达说明中补入该专项说明引用

### 本次工作
- 已对照新 BDP 当前代码事实、旧 BDP 已有功能与现有需求说明，重新梳理“当前真正落地到哪里、还遗漏哪些需求、哪些主题颗粒度仍不足”
- 已新增 `docs/需求说明/2026-03-24-当前实现对照与需求缺口说明-第一版.md`
- 已在全局需求清单前部补入该专项说明入口，后续可据此继续决定先深化哪一组规则

### 本次工作
- 已新增 `docs/plans/2026-03-24-阶段路线图与新会话继承提示词.md`
- 已把“全局阶段、当前阶段、当前环节、下一步动作、长期原则、踩坑结论、新会话必读资料、可直接复用的继承提示词”整理成正式交接文件

### 本次工作
- 已按旧 BDP `CompTriggerBody` 代码事实与当前需求文档，新增 `docs/需求说明/2026-03-24-Trigger装载规则说明-第一版.md`
- 已把 Trigger 收缩回“装载 owner、局部激活 owner、局部禁用 owner、局部切换协调者”，明确排除资源真值、战斗体整体相位真值、表达结果真值
- 已补充 Trigger 文档中的“名词释义与辨析”，明确区分芯片、槽位、侧、单槽位与单侧、局部激活、切换中目标槽位等概念，避免后续误读

### 本次工作
- 已新增 `docs/需求说明/2026-03-24-多表达解析规则说明-第一版.md`
- 这次不再让旧 BDP 实现主导文档层次，而是以 `攻击表达共存与多层投影说明` 作为更高优先级指导，再用旧 BDP 代码校验“单侧表达、双武器表达、组合第三表达、按钮载体、直接操作入口、主攻击投影”这些层次的既有事实
- 已把“表达来源 -> 表达结果集合 -> 手动按钮载体 -> 直接操作入口 -> 默认主攻击投影 -> 外部兼容表面/视觉跟随”的分层关系正式写实

### 本次工作
- 已新增 `docs/需求说明/2026-03-24-战斗体闭环说明-第一版.md`
- 已把战斗体文档收束成“相位真值 owner + 外层流程协调 + 收尾事务协调”三层，而不是旧实现功能清单
- 已明确“多来源 -> 单崩解入口 -> 延时解除 / 收尾”的总体结构，以及“手部损坏等局部失效不自动等于整体崩解”的边界

### 本次工作
- 原先生成的“远程攻击链正式输入输出说明”方向被判定为过于具体业务化，问题在于仍试图从旧 BDP 表现形状反推协议，而不是先抽出对未知功能也成立的统一标准
- 已按“阶段协议、标准字段分类、意图提交、宿主合并、冲突裁定、扩展挂点、链内 / 链外边界”重写 `docs/需求说明/2026-03-24-远程攻击链正式输入输出说明-第一版.md`
- 这次重写的目标不再是“更清楚地描述旧远程链”，而是“在不知道未来具体功能长什么样的前提下，先定义一套能稳定容纳未知远程能力的正式协议标准”

### 本次工作
- 已新增 `docs/需求说明/2026-03-24-视觉渲染与信息兼容读取模型说明-第一版.md`
- 这次不再从旧 BDP 具体画了什么、显示了什么出发，而是先定义“谁允许读取哪一层正式结果”的读取模型标准
- 已把读取层次拆成“真实多表达状态、当前执行表达、视觉读取结果、场景默认主攻击决策、多表达信息明细、外部正式接口、定点桥接结果”，并明确 patch 只允许做最后桥接，不允许反推业务

### 本次工作
- 在继续 `FireMode` 主题前，已进一步确认方向：这块不应被设计成“有特权去插入攻击系统并强改参数的中心功能”
- 后续将改按“攻击系统先公开标准调制通道，FireMode 只是其中一个无耦合调制参与者”的思路来写

### 本次工作
- 已把 `FireMode参数调制说明` 再向前收束一步，明确“攻击系统先开放标准调制接口，调制功能只能顺着接口接入，不能反向要求系统为自己开私口”
- 已新增 `docs/需求说明/2026-03-24-存读档与恢复说明-第一版.md`
- 这份恢复文档按“恢复责任协议”落笔，正式区分了哪些状态必须持久化、哪些必须重算、哪些必须清空、哪些允许续接，并把边界压回 `CompTrion / CombatBodyState / CompTriggerBody / 正式在途宿主`

### 本次工作
- 已对 `远程攻击链正式输入输出说明 / FireMode参数调制说明 / 存读档与恢复说明` 做一轮交叉校对
- 已修正 `存读档与恢复说明` 中把 Trigger 切换表现上下文混入 owner 真值层的问题，改回“允许续接的局部表现过程”
- 已补强 `FireMode参数调制说明` 中“表达参数调制”的边界，明确它只能作用于已成立表达的公开参数面，不能倒流进表达成立判定与主投影选择

### 本次工作
- 已按交叉校对结论继续追加 `远程攻击链正式输入输出说明`
- 新增“链内执行阶段 / 链后读取阶段分层说明”，明确 `信息投影 / 视觉投影` 仍可被称为正式读取阶段，但不属于执行裁定链
- 已把“执行链扩展口”和“读取链扩展口”正式拆开，防止后续实现时以显示、兼容、视觉名义偷拿执行链裁定权限

### 本次工作
- 已新增 `docs/plans/2026-03-24-二次重构总路线图-执行版.md`
- 这份路线图不再只讲“阶段概念”，而是正式给出 A 到 H 八段执行路线、每段进入条件、完成标准、禁止越级项与固定顺序
- 从这份路线图开始，后续推进应统一用“当前路线 / 当前节点 / 当前任务”表述，而不再反复给漂浮的“下一步建议”

### 本次工作
- 已和用户逐组确认“后期预留冻结项”局部清单，范围限定为协议骨架与边界约束，不扩写后期具体业务细节
- 已新增 `docs/需求说明/2026-03-24-后期预留冻结项说明-第一版.md`
- 已把四类正式扩展口、八类正式结果对象、字段级合并规则、四分法恢复分类、两级外部读取层、原版自然通道家族优先与 patch 严格准入、测试隔离与稳定标识正式冻结，并补入总路线图的 A 段完成标准

### 本次工作
- 已对照总路线图逐项核对路线 A 收口标准，确认高风险需求文档、后期预留冻结项、执行路线图与边界一致性均已满足进入条件
- 已新增 `docs/plans/2026-03-24-阶段1收口核对-第一版.md`
- 已把总路线图中的“当前正式位置”切换为路线 B，并明确当前进入点为 `B1：Trigger 装载规则落地`

### 本次工作
- 用户进一步明确：正式计划必须落成文件、行动必须留日志痕迹、实现节奏必须短阶段可测可视可反馈易诊断，且禁止应试思维
- 已新增 `docs/plans/2026-03-24-B1-Trigger装载规则落地-执行清单.md`
- 已把 B1 的临时 GUI 测试面、关键诊断日志点、每个子块的最小游戏验证面与禁止扩张项正式写入执行清单，并补入总路线图的 B1 交付物与完成标准

### 本次工作
- 已正式进入 `B1-a：槽位与正式容器一致性`
- 新增开发模式下的 `BDP Trigger诊断` 按钮与 `Window_TriggerBodyDiagnostics` 临时诊断窗口，可视化显示三侧槽位、正式容器芯片列表和槽位引用一致性
- `CompTriggerBody` 已补入装载/卸载/一致性修复相关的关键诊断日志与调试查询口，并提供“执行一致性修复”入口
- 主模组重新编译通过；B1-a 当前已形成第一个可在游戏中测试的环节

### 本次工作
- 用户指出更大的问题：推进过程中开始出现“为了尽快测试而往 `CompTriggerBody` 继续塞临时能力”的架构退化倾向
- 已新增 `docs/01-决策记录/2026-03-24-避免架构退化与测试污染-第一版.md`
- 已把“短反馈回路不能成为乱塞功能的理由”“测试与正式必须物理隔离”“任何测试用、临时用、验证用的东西都必须做到直接删除后对主模组零影响”提升为正式硬约束，并同步写入总路线图与 B1 执行清单

### 本次工作
- 已按新硬约束回审 `B1-a` 现状，确认主模组内置 `BDP Trigger诊断` 按钮、`Window_TriggerBodyDiagnostics` 窗口以及 `CompTriggerBody` 中的测试专用查询口构成明确污染
- 已新增 `docs/01-决策记录/2026-03-24-主模组污染审计与切除裁定-第一版.md`，正式记录污染项、保留项、切除原则与正确补位方向
- 已把 B1 执行清单中的错误成果表述改回“主模组只保留正式业务、关键诊断日志与正式扩展口；游戏内可视化验证面必须走外置测试层”

### 本次工作
- 已删除 `Source/BDP/Core/Trigger/Window_TriggerBodyDiagnostics.cs`，并从 `CompTriggerBody` 切除开发模式诊断按钮与测试专用查询口
- 已重新编译主模组通过，并确认相关测试污染符号已不再残留于主模组代码
- 已新增 `docs/plans/2026-03-24-B1-a-切污后的正确补位方案-第一版.md`，把 `B1-a` 后续可视化验证恢复方式正式限定为“主模组最小正式读取口 + 正式扩展口 + 外置测试层”

### 本次工作
- 已在主模组补入 `按侧槽位读取 / 局部切换状态只读快照 / 正式容器与槽位一致性诊断读取` 三类正式最小读取面，并把 `TriggerExternalGizmoContext` 收紧为接口上下文而非直接暴露具体 `CompTriggerBody`
- 已在 `BorderDefenseProtocol.DevHarness` 中新增基于正式读取口的 `Window_TriggerLoadoutDiagnostics`，并把测试按钮提供器改为只经 `ITriggerLoadoutState / ITriggerLoadoutDiagnostics` 接入
- 当前第一个“可在游戏中测试的 Trigger 装载环节”已重新落到外置测试层，主模组不再承载测试 GUI

### 本次工作
- 已按“第一性原理 + 旧 BDP 痛点 + 当前重构硬约束”对本轮已实现代码做一次架构重审，重点排查 owner 膨胀、测试污染回流、过度抽象与新半正式层漂移
- 审查结论显示：当前实现已明显优于把测试面塞进主模组的错误方向，但 `CompTriggerBody` 仍有继续兼任运行时桥与多类读取口聚合器的膨胀风险，`ITriggerLoadoutDiagnostics` 也有继续外扩成测试层专用协议的风险
- 已按用户新要求冻结后续工作纪律：以后每次代码改动准备交付前，都必须先做一次同等级的架构重审，再给出结果

### 本次工作
- 已按审查结论完成正式接口修正：新增 `ITriggerLoadoutReader / ITriggerLoadoutCommands`，并从代码层删除旧的一体化 `ITriggerLoadoutState`
- 已把 `CompTriggerBody` 上的 `GetPrimaryRangedVerb / GetPrimaryMeleeVerb / BuildManualCommands` 外移到 `TriggerProjectionFacade`，Patch 与手动按钮入口均改经外层门面，不再要求 owner 直接兼任投影入口宿主
- 已把 `TriggerExternalGizmoContext` 改为显式区分 `LoadoutReader / LoadoutCommands / IntegrityDiagnostics` 三类能力，修正了原先“名义只读、实际可写”的语义污染
- 已把 `ITriggerLoadoutDiagnostics` 收紧并重命名为 `ITriggerIntegrityDiagnostics`，明确它只服务槽位结构与正式容器一致性问题定位，不再作为宽泛测试协议继续生长
- 已新增 `ITriggerProjectionHost`，让 `BdpProjectionRuntime` 不再直接强转 `CompTriggerBody` 具体类获取缓存桥与投影宿主信息，而只依赖最小投影宿主能力
- 主模组与 `BorderDefenseProtocol.DevHarness` 已重新编译通过

### 本次工作
- 已新增 `docs/plans/2026-03-24-DevHarness-Trigger可视化改造计划.md`，把这轮测试层 UI 改造的目标、边界、人工验证点与“只改 DevHarness”约束正式落档
- 已在 `BorderDefenseProtocol.DevHarness` 新增 `Gizmo_TriggerLoadoutSummary`，形成“缩略摘要 + 详细窗口”两层测试可视化结构，借鉴旧 BDP 的信息分层方式，但不复刻旧业务形状
- 已重构 `Window_TriggerLoadoutDiagnostics`：补入名词辨析、三侧总览摘要、`Special=0` 明示、容器成员到侧/槽位映射、底层 `holdingOwner` 友好化显示
- 已修正缩略摘要里空侧默认激活槽位误判风险，避免测试层出现可视化假阳性
- 本轮架构复审结论：改动仍完全停留在外置测试层，没有向主模组新增测试专用接口，也没有把测试 GUI 责任回流进正式 owner
- `BorderDefenseProtocol.DevHarness` 已重新编译通过，可进入下一轮游戏内验证

### 本次工作
- 用户校正了切换语义：`WarmingUp` 结束才算正式激活，`WindingDown` 结束才算正式关闭；若中途有打断，则正式状态不提前变化
- 已回查 `Trigger装载规则说明` 与旧 BDP 切换状态机代码，确认当前主模组原实现确实把正式真值提交得过早，只是额外挂了表现上下文，属于时机错误而非文档理解偏差
- 已重构 `CompTriggerBody` 的切换结算方式：`RequestActivate / RequestDeactivate` 现在只创建阶段上下文，正式激活与正式关闭改为在读取口/输入口访问时懒结算到期阶段后提交
- 当前实现语义已改为：`WindingDown` 到期前旧槽位仍保持正式激活；`WarmingUp` 到期前目标槽位仍未正式激活；到期时再广播正式 `SlotDeactivated / SlotActivationCommitted`
- 已修复 `DevHarness` 右侧“正式容器芯片快照”面板的布局问题：窗口尺寸和左右栏占比已调整，容器成员改为分行卡片显示，不再把芯片名、持有方、Spawned 与槽位映射硬压成一行
- 主模组 `BDP.csproj` 与测试模组 `BDP.DevHarness.csproj` 已重新编译通过
- 本轮架构复审结论：主模组新增的只是 Trigger 自身应持有的局部切换阶段结算责任，没有引入新的 owner 污染；测试层修复只停留在外置显示层，没有向主模组索取额外测试接口

### 本次工作
- 已正式进入 `B1-d：局部禁用规则`，当前采用“Trigger 在正式读取/输入边界按需同步原版身体事实”的方案，不把禁用成立依赖到 tick 主循环，也不把 patch 当业务主裁定者
- 已新增正式禁用原因码 `TriggerDisableReason`，并把 `DisabledReason` 扩入 `ITriggerSlotState`，使“禁用为何成立”成为正式读取结果而不是测试层私口
- `CompTriggerBody` 已补入宿主身体事实到局部禁用真值的同步逻辑：当前阶段以 `Hand / Arm / Shoulder` 缺失作为最小正式来源，并按内部约定 `Main=右 / Sub=左` 映射到对应侧
- 为减少测试窗口和按钮重复读取带来的开销，身体禁用同步已加入“同宿主同 tick 只同步一次”的轻量缓存；同时输入口仍可强制重扫，避免伤害后同 tick 手动操作拿到旧结果
- 槽位禁用一旦成立，会立即清掉该侧切换上下文，并使该侧当前正式激活失效；事件层只广播 `SlotDisableStateChanged` 与必要的正式关闭，不持有真值
- `TriggerResolvedExpressionCache` 已接入禁用状态变化失效监听，避免禁用后仍沿用旧表达缓存
- `DevHarness` 诊断窗口已显示每槽 `禁用=是/否` 与 `原因=无/关键部位缺失`
- 主模组与测试模组已重新编译通过；`DevHarness` 首次编译一度未刷新到新引用，顺序重编后恢复正常
- 本轮架构复审结论：新增内容仍留在 Trigger 的正式边界内，属于“局部禁用真值 owner + 局部切换协调”范围，没有把身体系统、测试层或表达层责任反灌进 owner

### 本次工作
- 用户新增架构重审重点：后续每次全局重审除“架构污染 / 上帝膨胀 / 过度抽象”外，还必须显式排查“不必要耦合增加”
- 已把这条新重点正式补入 `避免架构退化与测试污染` 决策记录，并把当前架构指导方向明确为“多核心骨架 + 局部模块化 + 事件通信”
- 已据此重审主模组现有结构，确认本轮最需要先切掉的不必要耦合，是“patch 若直接触发 Trigger 私有标脏/私有同步，将把原版身体事实变化与具体业务 owner 绑死”
- 已把 `B1-d` 修正为三层结构：`PawnBodyConstraintSignalHub` 负责发布中性身体约束变化；`TriggerBodyDisableEvaluator` 负责把原版身体事实折算成 Trigger 侧别禁用结果；`CompTriggerBody` 只消费中性信号版本并持有自己的局部禁用真值
- 当前全局复审中记录到的主要耦合观察点：
- `CompTriggerBody` 仍是路线 B 当前最重的 owner，但本轮已把“身体事实解释”外移为局部模块，避免继续把身体映射细节塞进 owner
- `TriggerProjectionFacade` 仍以 `CompTriggerBody` 为门面输入，这是当前阶段可接受耦合，但后续路线 B2/B3 若继续扩张，应继续警惕不要把投影选择规则重新压回 owner
- `TriggerResolvedExpressionCache` 目前通过 Trigger 事件口失效缓存，属于可接受的事件通信耦合；它未反向持有 Trigger 真值，暂未见污染
- 新增身体约束 patch 只负责发布中性上游变化，不再直连 Trigger，也不承载禁用裁定，当前判定为符合“patch 只接线”的允许范围

### 本次工作
- 用户暂停当前路线，改为追查“当前芯片到底是什么、是否正式存在、是否只是测试壳”
- 已回查新 BDP `CompTriggerBody / BdpExpressionConfig / TriggerExpressionCollector`，确认新架构当前只把“芯片”当作由 Trigger 正式持有的 `Thing`；其正式协议只覆盖装载、激活、容器持有与从 Def 读取最小表达来源
- 已回查 DevHarness `ThingDefs_BDP_TestChips.xml`，确认当前游戏里可直接测试的三枚芯片 `BDP_TestChipRanged / BDP_TestChipMelee / BDP_TestChipExplosive` 全部来自测试模组，而不是主模组正式芯片库
- 已回查旧 BDP `CompTriggerChip / CompProperties_TriggerChip / ThingDefs_Chips.xml`，确认旧架构中的“芯片”是明确正式物品类型，承载形态、资源成本、双手占用、互斥标签、槽位限制与多类效果协议
- 已确认新 BDP 当前 `TryLoadChip` 只检查槽位与容器条件，不检查“是否为正式芯片类型”，因此现阶段“芯片”仍是约定名称，不是已封口的正式类型身份

### 本次工作
- 用户要求暂停抽象讨论，先回查旧 BDP 芯片定义与现有需求文档中所有和芯片直接或间接相关的约束
- 已复核旧 BDP 芯片 Def、`CompProperties_TriggerChip` 与 `CompTriggerChip`，确认旧芯片实际把四层内容压在一起：物品实体、Trigger 装载约束、资源成本协议、以及 `Verb / Ability / Hediff / Passive / FireMode / WeaponDraw` 等多类业务入口扩展
- 已复核 `全局需求清单 / 当前实现对照与需求缺口 / Trigger装载规则 / 攻击表达共存与多层投影 / 多表达视觉渲染与信息兼容 / 远程攻击链`，确认需求文档对芯片的共同指向是：芯片先提供表达来源与约束信息，上游是 Trigger 装载入口，中游影响表达与攻击链，下游影响视觉与兼容，但芯片自身不能重新膨胀成总攻击中心
- 当前讨论口已形成新的事实基线：芯片对玩家必须是 RimWorld 里的正式物品实体；对架构则应被理解为“高信息密度、低业务耦合”的正式载体，而不是直接承载全链业务执行

### 本次工作
- 用户指出我对 `allocationCost` 的理解有根本性误差：它不是“芯片激活时才占用锁定”，而是“战斗体进入 / 离开的一套原子流程中的一环”，按当时已装载芯片声明汇总后统一锁定 / 释放
- 已回查旧文档 `2.5_Trion战斗系统流程` 与 `2.4_Trion能量全生命周期过程分析`，确认旧 BDP 对占用值的正式语义是：战斗体生成时，已装载芯片注册占用 Trion；解除战斗体时统一释放；占用值属于战斗体阶段资源锁定，不属于单芯片局部激活流程
- 已回查旧代码 `CompTriggerBody.SlotManagement` 与战斗体相关接口，确认旧实现存在“已装载芯片总占用量计算 / 预占用同步 / 战斗体激活时统一 Allocate / 解除时统一 Release”这条链，进一步坐实“占用值声明来自芯片，执行时机属于战斗体原子事务”
- 当前芯片与 Trion 的讨论前提已修正为：芯片只声明自己的占用贡献参数；战斗体系统在进入 / 离开流程中汇总所有已装载芯片的占用声明并向 `CompTrion` 提交正式锁定 / 释放

### 本次工作
- 已新增芯片结构需求文档 `docs/需求说明/2026-03-24-芯片标准结构说明-第一版.md`
- 文档已正式收束：芯片的双层定位、五类基础信息、四个表达顶层大类、多来源规则、Trion 参数层级边界、形态覆盖语义、远程 `Verb` 模块化的基础地位、模块与非模块边界、以及旧 BDP 芯片按需求本质的归类
- 当前该文档可作为后续“新定义一枚芯片”时的基础参考说明第一版；后续若继续深化，应优先围绕各大类内部复杂子结构补写，而不是再回到芯片总结构层反复改名和改层级

### 本次工作
- 已回到主线高优先级文档补写，新增 `docs/需求说明/2026-03-24-多表达解析规则说明-第一版.md`
- 这份文档不是重复“攻击表达共存与多层投影说明”，而是把它进一步压成偏正式规则层：明确表达解析层输入、单侧先成立、双侧后重算、双武器与组合技属于高层新结果、按钮层与主攻击投影层分离、以及正式重算触发点
- 文档当前已把旧 BDP 中可确认的关键事实收束进规则骨架：`Command_BDPChipAttack` 的单按钮双入口、双武器组合器的高层合成语义、组合技作为额外新表达而不是按钮附加功能、主攻击投影独立于全部表达集合
- 当前该文档可作为后续继续补“远程攻击链正式输入输出说明”“视觉渲染与信息兼容读取模型说明”“FireMode 参数调制说明”的上游规则基础

### 本次工作
- 用户指出“多表达解析规则说明”被我写偏成了“多攻击解析规则”，忽略了芯片不是只有武器
- 已据此回修文档，补入 `Ability / Hediff / Passive` 的正式结果地位，明确非攻击表达不是攻击的附属挂件，而是与攻击型表达并列的正式结果
- 已补入非攻击型双侧关系的正式语义：双侧重算不只回答双武器与攻击型组合技，也应允许回答叠加、联动和非攻击型高层结果
- 已新增“非攻击结果读取层”概念，用于明确：非攻击表达不能被硬塞进主攻击投影层才能拥有正式地位
- 当前该文档已从“偏武器解析”修正为更接近真正的“多表达解析规则”第一版；下一步若回到主线，应优先恢复到被芯片问题打断前的代码推进，而不是继续无止境扩写文档

### 本次工作
- 已恢复路线 `B1-e` 代码推进，正式落地“双手绑定最小伪装语义”：装载后一侧为主槽位、对侧同索引自动生成镜像副本占位，双侧激活/关闭/挤占同步，但表达采集只认主槽位
- 已新增 `TriggerChipConstraintConfig` 作为极小芯片约束扩展位，当前只承载 `isTwoHandBinding / exclusionTags`，避免把 Trigger 约束重新塞回庞大芯片 Comp 或混进表达层
- 已在 `TriggerSlotState / ITriggerSlotState` 增加绑定主从元数据；这层只描述“是否镜像、副本根是谁、绑定到哪一格”，不持有任何业务真值
- 已在 `CompTriggerBody` 收口双手绑定装载、卸载、主副同步切换、同步后摇/前摇结算、以及禁用时的成对关闭；保持 owner 仍只负责局部装载与局部激活真值
- 已在 `TriggerExpressionCollector` 明确跳过镜像副本，守住“视觉双占位、表达单来源”的本质边界
- 已在 `BorderDefenseProtocol.DevHarness` 增补双手绑定测试芯片 `BDP_TestChipTwoHandRanged`，并让诊断窗口直接显示“主槽位 / 镜像副本 / root / partner / 表达采集是否跳过 / 是否双侧同步”
- 主模组 `Source/BDP/BDP.csproj` 与测试模组 `Source/BDP.DevHarness/BDP.DevHarness.csproj` 已重新编译通过
- 本轮架构重审结论：
- 约束来源被收束在独立 `DefModExtension`，没有回流成新的芯片上帝模块
- 镜像关系只落在槽位元数据层，没有把镜像副本做成新的 owner
- 表达层通过正式只读事实跳过镜像副本，没有为双手绑定开私有投影通道
- 测试芯片与可视化继续停留在外置 DevHarness，没有向主模组追加测试专用协议
- 当前剩余风险主要在“主副同步切换”这组新路径的运行时行为，还需要进游戏验证，尤其是：已激活双手芯片被普通芯片挤占、双手芯片从副侧作为主槽位加载、以及禁用/恢复与同步切换并发时的边缘情况

### 本次工作
- 用户要求测试装载交互收缩，不再继续平铺“每种芯片一个按钮”，而改为“按侧一个装载按钮 + 右键菜单选芯片”
- 已按该要求重构 `DevHarnessTriggerGizmoProvider`：删除按芯片种类平铺的装载按钮，改为 `测 装主 / 测 装副 / 测 装特` 三个装载按钮
- 每个装载按钮现在通过右键动态菜单列出测试芯片候选，并自动装入该侧第一个空槽位；当前候选包括远程、近战、爆炸、双手绑定远程四类测试芯片
- 由于原版 `Command_Action.RightClickFloatMenuOptions` 是只读属性，已在测试模组新增极小辅助类 `Command_ActionWithRightClickMenu` 负责接入右键菜单；该类只存在于 DevHarness，不进入主模组
- 测试模组 `Source/BDP.DevHarness/BDP.DevHarness.csproj` 已重新编译通过
- 本轮架构重审结论：这次改动纯属测试层交互收束，方向正确；它减少了测试 UI 本身的按钮膨胀，没有向主模组新增任何测试协议，也没有把“芯片类型词典”反向推入正式层

### 本次工作
- 用户反馈上一版在“激活芯片”时会导致游戏闪退，已按系统化排查流程先回收事实而非直接猜修
- 已检查 `Player.log / Player-prev.log`，崩溃前最后一条正式日志停在 `请求激活槽位 -> 进入 WarmingUp`，没有常规托管异常堆栈；这更符合“内部递归重入导致硬崩/栈溢出”的表现
- 已定位根因：`CompTriggerBody` 内部阶段结算路径中，`FinalizeWarmingUp / FinalizeWindingDown / ShouldUseSynchronizedHandTransition / HasExclusionConflict` 会调用公开读取口 `GetActiveSlot / GetActiveSlots / GetActiveSwitchContext`，而这些公开读取口又会再次触发 `ResolveDueSwitchTransitions()`，形成结算链重入
- 已修正策略：新增内部原始读取 helper `GetActiveSlotRaw / GetActiveSlotsRaw`，只给 Trigger 内部阶段结算与内部判定使用；同时从 `GetActiveSwitchContext()` 中移除内部再次 `ResolveDueSwitchTransitions()` 的行为，避免在结算链内部递归重入结算链
- 该修正没有改动外部正式读取协议，也没有禁用双手功能，而是把“公开懒结算读取”和“内部原始真值读取”做了边界分离
- 主模组与测试模组已重新编译通过
- 本轮架构重审结论：
- 这次修正属于边界净化，不是补丁堆叠；它减少了 Trigger 内部“公开读取口”和“内部阶段推进”之间的耦合
- `CompTriggerBody` 仍然偏重，但这次是把递归耦合切开，而不是继续堆条件分支
- 当前建议先回游戏只测最小复现：普通远程/近战芯片装载后激活是否还会闪退；若不再闪退，再继续测双手绑定路径

### 本次工作
- 用户要求在他自行测试期间，我回到主模组做一轮“全代码、全局、按问题而不是按对象”的架构二审，并且基于模组自身正式需求重新判断哪些问题成立、哪些需要调整分级
- 已完成二审重分级：
- `CompTriggerBody` 膨胀仍维持严重风险
- `CombatBody` 双轨职责与主链未接通维持高风险
- 表达 / 投影链的假解耦与过度分层维持高风险

### 本次工作
- 已正式切换到“芯片表达契约重构实施计划”主线，不再围绕旧四列表继续修补
- 已回审 `芯片表达契约重构裁定 / 芯片标准结构说明 / 表达系统骨架设计` 三份主文档，并补强“不做长期双轨”“新增 `Contract` 层”“`Config -> Contract -> Pipeline -> Projection`”的正式边界
- 已确认当前代码事实仍以 `VerbSources / AbilitySources / HediffSources / PassiveSources` 作为主结构，因此接下来会直接改 `Config` 与新增 `Contract` 骨架，而不是继续在旧结构上叠补丁
- 当前第一批执行目标固定为：`Task 1` 文档定稿、`Task 2` 契约骨架、`Task 3` 统一条目配置；完成前不扩张到后续投影细节

### 本次工作
- 已完成第一批代码切换：`ChipExpressionConfig` 已改为 `Entries + Modes`，`ChipExpressionModeConfig` 已改为 `ModeKey + Operations`
- 已新增 `Core/Expressions/Contract` 目录与最小契约对象、契约枚举、契约解释器、解释结果与校验结果对象，并全部补入逐成员注释
- `ExpressionReaderSurface` 已改为经 `DefaultChipExpressionContractInterpreter -> DefaultExpressionSourceDeclarationProvider` 进入结果链，主路径不再直接读取旧四列表
- 旧 `VerbExpressionSourceConfig / AbilityExpressionSourceConfig / HediffExpressionSourceConfig / PassiveExpressionSourceConfig` 已从主模组删除，旧四列表正式退出主设计路径
- DevHarness 测试芯片 Def 已完成第一轮迁移，统一改为 `Entries` 写法，并显式填写 `Kind`
- 当前 `BDP.csproj` 与 `BDP.DevHarness.csproj` 均已重新编译通过
- 下一步将继续补“契约层最小诊断 -> 说明投影”以及“是否需要补一个混合/形态示例 Def”这两项

### 本次工作
- 已把契约层最小诊断正式接入说明投影：`ExpressionReaderSurface.GetInfoProjection()` 现在会汇总当前活跃芯片的契约接受情况，并追加到 `ExpressionInfoProjection`
- 已补入 `ExpressionContractDiagnosticEntry`，当前最小诊断会诚实返回：芯片是否有效、主模组接受了多少条目、错误数、警告数；不回流泄漏内部解析对象
- 已扩充契约校验：除条目 id 冲突、Replace / Disable 目标缺失外，现也会校验 `Verb 缺 VerbProps / Ability 缺 AbilityDefName / Hediff 缺 HediffDefName / Passive 缺 PassiveKey`
- 已新增 DevHarness 示例芯片 `BDP_TestChipMixed`，用于验证统一条目契约下“一枚芯片同时声明攻击条目与 Attached 被动条目”的真实写法
- 这次没有补“假模式示例”：当前如果主模组还没有诚实的正式模式来源，就不为了看起来完整而伪造一个模式样例
- 当前主模组 `BDP.csproj` 与 DevHarness `BDP.DevHarness.csproj` 已再次编译通过

### 本次工作
- 已继续按 `中文文件与终端规则` 复核本轮中文文档与 DevHarness XML 的读取和回读过程，确认后续中文改动继续按 `UTF-8 + 先读后改 + 改后回读` 执行
- 已把“当前已落地事实”同步补入 `芯片标准结构说明 / 表达系统骨架设计 / 芯片表达契约重构实施计划`
- 当前文档层已明确：统一条目契约、最小契约解释器、最小契约诊断、DevHarness 混合芯片样例都已是既成事实；完整模式生态仍不是既成事实
- 主模组 `BDP.csproj` 与 DevHarness `BDP.DevHarness.csproj` 已做一轮新的收尾编译确认，并再次通过

### 本次工作
- 已按 `中文文件与终端规则` 对当前任务链路再做一轮乱码扫描，范围限定为 `Core/Expressions` 与 `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness`
- 扫描结论：当前这条链路里，本轮新增或重写过的文件未再命中常见乱码特征；因此这一步不再盲目扩大到无关目录做整仓重写
- 当前对“顺便解决乱码”的执行口径已固定为：优先清理当前任务链路内的乱码，不把整仓历史乱码治理混入本轮契约重构任务

### 本次工作
- 已开始把“正式芯片定义契约”从表达系统上方独立出来，并新增上位文档 `2026-03-26-正式芯片定义契约说明-第一版.md`
- 这份新文档当前先只立骨架，正式回答“芯片是什么、哪些信息块属于芯片、各系统分别从芯片读什么”，不再让表达契约代替整个芯片契约
- 已对当前游戏中“芯片攻击按钮生成链路”做一轮边界审计，结论是：
- `ThingDef -> Trigger 装载/激活 -> Expressions Snapshot/ManualProjection` 主链基本走的是正式入口
- `TriggerEquippedGizmoService -> ExpressionManualGizmoBridge` 属于主模组内部半正式桥，不是最上层 formal surface
- `Patch_Pawn_TryGetAttackVerb -> ExpressionVerbBridge` 与 `Patch_Pawn_MeleeVerbs_TryMeleeAttack -> ExpressionVerbBridge` 也属于内部桥，不是外部正式接口
- 当前最值得继续收口的“翻窗点”是：Trigger/patch 侧仍直接依赖 `ExpressionManualGizmoBridge / ExpressionVerbBridge` 这种桥类，而不是统一经一个更高层正式动作表面
- `TriggerExternalGizmoRegistry` 因用户明确裁定为正式长期扩展需求，不再判定为测试污染；但其宿主位置与边界规范化仍被判为高风险设计点
- 已新增正式设计文档 `docs/plans/2026-03-24-架构二审与高风险修复设计-第一版.md`
- 已新增正式实施计划 `docs/plans/2026-03-24-架构高风险修复实施计划-第一版.md`
- 设计文档中已补入“当前架构 vs 预期修复后架构”的 ASCII 对比图，并明确建议先做 `CompTriggerBody` 瘦身，再做 `CombatBody` 单主链化，再处理表达 / 投影链瘦身与正式外部命令扩展口规范化

### 本次工作
- 已按正式实施计划开始执行 `P1：CompTriggerBody 瘦身第一轮`，本轮只处理 Trigger owner 的局部规则外移，不推进 `CombatBody`、表达投影链或外部命令扩展口
- 已新增三个不持真值的局部模块：
- `Source/BDP/Core/Trigger/TriggerActivationRules.cs`
- `Source/BDP/Core/Trigger/TriggerSwitchEngine.cs`
- `Source/BDP/Core/Trigger/TriggerDisableSync.cs`
- 其中：
- `TriggerActivationRules` 收口双手绑定归一、互斥检查、双手装载前校验等局部规则
- `TriggerSwitchEngine` 收口前摇 / 后摇上下文构造与到期阶段结算
- `TriggerDisableSync` 收口宿主身体约束到 Trigger 局部禁用真值的同步逻辑
- 已改造 `CompTriggerBody`，把上述三块职责改为 owner 显式调用局部模块；`CompTriggerBody` 当前继续保留真值字段、正式读取/请求口、事件广播口、存读档、IThingHolder 与必要的正式桥接
- 本轮没有新增新的真值 owner，也没有把业务塞进 patch；三个新模块均只通过 owner 提供的最小回调面访问真值
- 主模组与 `BorderDefenseProtocol.DevHarness` 已重新编译通过
- 本轮架构复审结论：
- `CompTriggerBody` 行数已由上一轮审计时的约 `1455` 行下降到当前 `1217` 行，属于实质瘦身，不是单纯换文件名
- 递归重入修复未回退，`GetActiveSwitchContext()` 仍保持不在内部再次触发懒结算
- 新增的两个 `*Context` 仍偏胖，当前判定为可接受的过渡形态，但已记录为 `P1` 后续第二刀需要继续压缩的中风险点，避免演变为“万能上下文包”
- 当前下一步应继续正式计划的 `P2：CombatBody 单主链化接通`，但在进入前仍需先结合你最新游戏测试反馈判断是否要对 `P1` 再补一轮行为修正

### 本次工作
- 用户进一步要求“继续瘦身 TriggerBody，把能拆且合适拆的都拆走”，并且追加约束：同步检查已经拆出去的局部模块里，有没有本就应该合并在一起的概念簇
- 已据此完成 Trigger 第二刀重整，不再只是继续细碎拆分，而是做“边拆边并”：
- 新增 `Source/BDP/Core/Trigger/TriggerBindingEngine.cs`
- 新增 `Source/BDP/Core/Trigger/TriggerCommandFacade.cs`
- 其中：
- `TriggerBindingEngine` 统一收拢了原先散落在 `CompTriggerBody + TriggerActivationRules + TriggerSwitchEngine` 周边的绑定拓扑解释与双手同步切换流程，包括：主/副侧判定、镜像槽归一、绑定 partner 解析、同步激活请求、同步关闭请求、成对立即激活、成对立即关闭
- `TriggerCommandFacade` 统一收拢了装备状态下额外命令的正式拼装，把“外部扩展命令 + 手动表达命令”的组装责任从 `CompTriggerBody.CompGetEquippedGizmosExtra()` 继续外移
- 同时已把 `TriggerActivationRules` 收窄回更纯的“约束判断模块”：保留芯片约束读取、装载前绑定合法性判定、互斥检查；不再继续承载绑定拓扑 helper
- 本轮删除了 `CompTriggerBody` 中整组双手同步流程残留，以及已经失效的同步前摇/后摇死代码
- 主模组与 `BorderDefenseProtocol.DevHarness` 已重新编译通过
- 本轮架构复审结论：
- `CompTriggerBody` 行数已进一步降到当前约 `932` 行，已经从“重 owner + 重流程”明显回落为“仍偏大但边界明显的 owner 门面”
- `TriggerBindingEngine` 的出现不是新增上帝模块，而是把本就同属于“绑定槽位局部语义”的碎片重新收拢，属于正确合并
- `TriggerActivationRules / TriggerBindingEngine / TriggerSwitchEngine / TriggerDisableSync` 四者当前边界分别对应：约束判断 / 绑定语义 / 通用切换推进 / 身体禁用同步，职责切面已明显比上一轮干净
- `TriggerCommandFacade` 的外移是正确方向，因为命令拼装属于正式扩展接线，不应继续贴着 Trigger owner 生长
- 当前剩余可继续评估的 Trigger 瘦身点，主要只剩：容器一致性修复辅助逻辑、少量绑定 wrapper、以及未来 `P4` 会继续处理的正式外部命令扩展边界；这些暂未继续拆，是因为再拆下去会开始触碰 owner 必需的容器 / 存读档 / 正式门面边界

### 本次工作
- 用户要求暂停进入战斗体代码实现，先回到文档与旧 BDP 事实，重新从全局架构视角理解战斗体，并强调“模组整体架构正确合理优雅且符合需求永远第一”
- 已重新细读旧文档 `2.2_真身-战斗体-真身领域过程分析`，并结合旧代码 `CombatBodyOrchestrator / Gene_TrionGland / CompTriggerBody.CombatBodySupport`、以及现有新文档 `战斗体闭环说明 / 三核心边界草案 / 旧BDP与原版宿主映射 / 旧BDP非Verb表达接入与真值边界再确认`
- 当前重新确认的核心认识是：
- 战斗体不是局部功能，而是 `真身域 <-> 战斗域` 的正式域切换主轴
- 它会反向约束 `Trion / Trigger / Snapshot / Hediff / Host / 事件通信` 的整体结构
- 它不能膨胀成第四真值核心，仍必须维持 `CompTrion / CombatBodyState / CompTriggerBody` 三核心骨架
- 已新增正式文档 `docs/需求说明/2026-03-25-战斗体对整体架构约束说明-第一版.md`
- 这份文档不重复战斗体局部流程，而是专门写清：
- 战斗体如何约束三核心边界
- 局部模块化应该怎样切
- 事件通信在战斗体上下文中怎样用
- 为什么 `Snapshot / Cleanup / Host / Hediff` 都必须回到自然位置，而不能继续互相越位
- 当前文档结论会直接约束后续现有代码的结构调整方向：
- Trigger 继续收束为装载 owner，不再承担战斗体镜像真值与事务
- CombatBody 协调层必须是单主链，而不是与 Cleanup 并列双轨
- Snapshot 恢复链必须进入正式主链，不能散装
- 资源请求与相位提交必须继续分离，事件只通知不持真值

### 本次工作
- 已切换当前主线为“主模组现阶段全局架构自省与对象裁定”，不再继续直接补某一个局部功能
- 已重新扫描 `Source/BDP/Core/*` 与 `Source/BDP/Patches/*` 当前真实对象分布，并结合最近两轮 Trigger 瘦身后的代码事实复核哪些对象是真核心、哪些对象只是局部模块、哪些对象属于空骨架或假抽象
- 这轮判断的重点不是“哪个文件大”，而是：
- 谁在错误占用系统级位置
- 谁会诱导未来非核心能力没有自然接入口
- 谁会让主链看起来很多层、实际上仍强耦合
- 已新增正式裁定文档 `docs/plans/2026-03-25-主模组现阶段架构自省与对象裁定-第一版.md`
- 文档已把主模组对象按四类正式裁定：
- 保留为核心
- 保留但重定职责
- 降级为局部模块
- 删除 / 压平 / 内化
- 本轮核心结论：
- `CompTrion / CombatBodyState / CompTriggerBody` 继续保留为三核心，不新增新 owner
- `CombatBodyCoordinator` 保留但必须收成唯一主协调链
- `CombatBodyCleanupCoordinator` 应退回离开侧事务协作者，不再占平行主链位置
- `ICollapseCleanupCoordinator / IVisualProjection / ISemanticDamageSource / 多个 Projection 接口壳` 已被裁定为当前优先删除或内化对象
- `PawnCombatBodyHost` 不应删除，但也不能继续长期空置，必须尽快以最小正式方式接回主链
- `TriggerExternalGizmo* / 表达结果骨架 / 语义桥接骨架 / BodyConstraints 中性信号链` 被裁定为需要保留的“薄、硬、诚实”源码级长期记忆锚点
- 当前这份裁定文档已经把后续整顿的进入点定清：先处理 `CombatBody / Cleanup / Host` 这组“位置已占、主链未接”的问题，再回头收束表达 / 投影层的假解耦

### 本次工作
- 用户指出 `Cleanup` 这种独立层命名与位置本身就破坏战斗体进入 / 离开的对称主轴，并进一步追问“三核心对外提供信息、接收请求的正式面”是否需要现在就定下来
- 已据此新增执行计划 `docs/plans/2026-03-25-CombatBody单主链化与三核心外部面第一刀-执行计划.md`
- 这轮不是泛泛而谈地“统一接口风格”，而是按边界诚实原则落最小正式面：
- `CompTrion`：新增 `ITrionReader / ITrionCommands`，旧 `ITrionState` 退为兼容总口
- `CombatBody`：新增 `ICombatBodyReader / ICombatBodyCommands`，`ICombatBodyPhaseState` 与 `ICombatBodyCoordinator` 改为兼容总口
- `Trigger`：维持现有 `Reader / Commands / Events` 结构，不在这轮继续扩散
- 已删除 `Core/Cleanup/ICollapseCleanupCoordinator.cs`
- 已删除 `Core/Cleanup/CombatBodyCleanupCoordinator.cs`
- 已新增 `Core/CombatBody/CombatBodyExitPipeline.cs`
- 这意味着：`Cleanup` 不再以平行系统存在，而是正式退回“离开事务步骤簇”
- 已修改 `CombatBodyCoordinator`：
- Trion 依赖由 `ITrionState` 收窄为 `ITrionCommands`
- 战斗体相位依赖由具体 `CombatBodyState` 收窄为 `ICombatBodyPhaseState`
- 手动退出不再直接内联“释放 / 恢复”，而是显式调用 `CombatBodyExitPipeline`
- 当前崩解仍只先提交崩解相位，未在这刀强行补全后续闭环，避免为求完整而扩张
- 已修改 `CombatBodyState`，使其成为 `ICombatBodyReader` 的正式承载者
- 已修改 `CompTrion`，使其显式实现 `ITrionReader / ITrionCommands / ITrionEvents`
- 已修改 `PawnCombatBodyHost` 注释与定位：它不再被表述为空桥位，而是“已进入主链、但细节尚未补完的正式宿主挂接点”
- 主模组与 `BorderDefenseProtocol.DevHarness` 已重新编译通过
- 本轮架构复审结论：
- 没有新增新的真值 owner
- `Cleanup` 没有换个名字继续占平行系统位置，而是确实降为离开事务步骤簇
- 新增的 `Reader / Commands` 接口目前都直接贴 owner 边界，没有长成新的空 facade
- 当前剩余的不完全对称点是：进入侧仍在 `CombatBodyCoordinator` 内联，离开侧已抽成 `ExitPipeline`
- 这不是新的污染，而是下一刀应继续处理的局部结构差异；后续若继续整顿 `CombatBody`，最自然的方向是把进入侧也收成对应步骤簇，但仍保持只有一条主链

### 本次工作
- 已继续推进 `CombatBody` 第二刀，把进入侧也从 `CombatBodyCoordinator` 中抽成 `Core/CombatBody/CombatBodyEnterPipeline.cs`
- 当前 `CombatBodyCoordinator` 已形成更清楚的单主链结构：
- 进入：`CanActivate -> EnterPipeline -> CombatBodyState.EnterActive -> PhaseChanged`
- 离开：`CanManualDeactivate -> ExitPipeline -> CombatBodyState.EnterCooldown -> PhaseChanged`
- `CombatBodyEnterPipeline` 当前只承担“Trion 锁定 + 宿主进入动作”两步，并在宿主进入动作抛异常时回滚本次锁定，避免资源半提交
- 这次没有新增新的相位 owner、资源 owner 或 Trigger owner；进入 / 离开步骤簇都仍由 `CombatBodyCoordinator` 主链裁定与调用
- 主模组重新编译通过；编译时出现一次 `BDP.dll` 正被其它进程占用的复制重试警告，但重试后已成功输出
- `BorderDefenseProtocol.DevHarness` 重新编译通过
- 本轮架构复审结论：
- `CombatBody` 现在在结构层面已经不再是“进入内联、退出清理”的不对称口径
- 进入 / 离开都已回收到同一条主链内部的步骤簇模型
- 当前真正剩下的核心缺口不再是内部事务结构，而是“战斗体正式附着点仍未接回原版宿主”
- 这个缺口不能为了尽快接通而硬造半正式新载体；后续应先围绕自然宿主裁定，再决定把 `CombatBody` 正式挂到哪里

### 本次工作
- 已继续回查旧 BDP 文档 `2.2_真身-战斗体-真身领域过程分析`、现有 `战斗体对整体架构约束说明` 与 `旧BDP与RimWorld原版宿主映射` 相关结论，专门确认“战斗体正式附着点”不应再悬空
- 已新增决策记录 `docs/01-决策记录/2026-03-25-战斗体正式附着点裁定-第一版.md`
- 当前正式裁定为：
- 战斗体主链下一步应优先回到 `Hediff / HediffComp` 宿主
- `Hediff` 只是正式附着点，不是总协调者
- `CombatBodyCoordinator + CombatBodyState + Enter/ExitPipeline` 继续负责主链与相位，不回退到 `Hediff` 里持流程真值
- 这次没有继续贸然新增 `HediffDef` 或 `HediffComp` 代码，是因为当前要先把“附着点方向”钉死，避免为了接通而临时塞进 `CompTriggerBody / CompTrion / 平行 ThingComp`

### 本次工作
- 已把战斗体正式附着点真正落成最小代码宿主：
- 新增 `Core/CombatBody/HediffCompProperties_CombatBodyHost.cs`
- 新增 `Core/CombatBody/HediffComp_CombatBodyHost.cs`
- 新增 `Core/CombatBody/CombatBodyHostAccess.cs`
- 新增 `Core/Defs/BDP_DefOf.cs`
- 新增 XML `1.6/Defs/Combat/HediffDefs_CombatBody.xml`
- 这层宿主的当前职责非常收束：
- 把 `CombatBodyState + PawnCombatBodyHost + EnterPipeline + ExitPipeline + CombatBodyCoordinator` 正式附着到 Pawn 的 `Hediff/HediffComp` 宿主语义上
- 暴露 `ICombatBodyReader / ICombatBodyCommands / ICombatBodyEvents`
- 只负责“正式宿主位”和“正式访问工具”，不冒充激活来源、不冒充总协调者
- 为了让这层宿主长期可用而不持死引用，`CombatBodyEnterPipeline / CombatBodyExitPipeline` 已改为通过当前 Pawn 动态解析 `CompTrion / CompTriggerBody`
- 主模组与 `BorderDefenseProtocol.DevHarness` 已重新编译通过
- 本轮全局复审结论出现了一个更真实的下一阻塞点：
- 战斗体正式宿主位已经有了
- 但 `Trion` 当前还没有正式附着在 Pawn 身上
- 所以现在如果急着在 DevHarness 里加“战斗体激活按钮”，它大概率只会因为资源宿主缺失而失败
- 这不是测试口不足，而是全局骨架真实还没闭合
- 因此当前没有为了“尽快能点”而往测试层塞一组会误导判断的战斗体按钮；下一步更自然的方向，应先审 `Trion` 的 Pawn 正式宿主问题

### 本次工作
- 已根据 `旧BDP与RimWorld原版宿主映射` 的明确结论，把 `Trion` 正式宿主接回 Pawn `ThingComp`
- 新增 `Core/Trion/PawnTrionCompInjector.cs`
- 并在 `BDPMod` 启动时执行一次宿主接线：为所有人形 Pawn `ThingDef` 注入 `CompProperties_Trion`
- 这一步被明确限制为“宿主接线”，不承载战斗体、Trigger 或角色资格业务
- 主模组与 `BorderDefenseProtocol.DevHarness` 已重新编译通过
- 本轮架构复审结论：
- 没有新建平行资源宿主
- 没有把资源逻辑塞进注入器
- `CompTrion` 仍然只持有资源真值；变化只是现在它有了 Pawn 自然宿主

### 本次工作
- 已把下一轮可视化测试口补到 `DevHarness`，且完全停留在测试层：
- 新增 `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Window_CombatBodyDiagnostics.cs`
- 更新 `DevHarnessTriggerGizmoProvider.cs`
- 当前开发模式下，Trigger 测试按钮区新增：
- `测 挂战体宿`
- `测 看战体`
- `测 开战体`
- `测 关战体`
- 这些测试按钮全部通过主模组正式入口工作：
- `CombatBodyHostAccess.EnsureHostComp(...)`
- `ICombatBodyCommands`
- `ICombatBodyReader`
- 当前第一轮战斗体可测口的目标不是复现旧 BDP 完整表现，而是验证：
- 战斗体正式宿主能否正确附着
- `CompTrion` 是否已作为 Pawn 正式资源宿主存在
- `CombatBody` 主链是否能最小成立：`Inactive -> Active -> Cooldown/Inactive`
- 主模组与 `BorderDefenseProtocol.DevHarness` 在补入这组测试口后再次编译通过

### 本次工作
- 用户在游戏中反馈 `BDP_CombatBodyHost` 出现 XML config error：缺少 `description`
- 已按系统化排查确认根因是新加 `HediffDef` 缺少原版要求字段，而不是战斗体主链逻辑问题
- 已对 `1.6/Defs/Combat/HediffDefs_CombatBody.xml` 做最小修补，补入 `description`
- 主模组与 `BorderDefenseProtocol.DevHarness` 重新编译通过

### 本次工作
- 用户明确裁定：战斗体不允许靠 `Hediff` 挂给 Pawn；`Hediff` 只能作为战斗体激活或崩解时的表现层
- 已据此整套回切错误实现：
- 删除 `Core/CombatBody/HediffCompProperties_CombatBodyHost.cs`
- 删除 `Core/CombatBody/HediffComp_CombatBodyHost.cs`
- 删除 `Core/Defs/BDP_DefOf.cs`
- 删除 `1.6/Defs/Combat/HediffDefs_CombatBody.xml`
- 新增 `Core/CombatBody/CompProperties_CombatBodyHost.cs`
- 新增 `Core/CombatBody/CompCombatBodyHost.cs`
- 新增 `Core/CombatBody/PawnCombatBodyCompInjector.cs`
- `BDPMod` 现已在启动时为人形 Pawn 注入战斗体主链宿主 `ThingComp`
- `CombatBodyHostAccess` 已改成只暴露正式外部面：
- `GetReader(Pawn)`
- `GetCommands(Pawn)`
- `GetEvents(Pawn)`
- `DevHarness` 已同步回切：
- 删除“测 挂战体宿”依赖
- `测 开战体 / 测 关战体 / 测 看战体` 全部改为只走战斗体正式读取面与正式请求面
- 期间发现一个构建层污染：
- `DevHarness` 原先通过磁盘上的 `BDP.dll` 文件引用主模组
- 当 DLL 被其它进程占用时，测试层会吃到陈旧接口面
- 已改为 `ProjectReference` 直接引用主工程，避免被旧 DLL 锁文件状态误导
- 已新增决策记录 `docs/01-决策记录/2026-03-25-战斗体正式附着点裁定-第二版.md`
- 当前有效裁定变更为：
- 战斗体主链正式附着点 = Pawn `ThingComp`
- `Hediff` = 战斗体阶段表现层，不再承担主链宿主
- 主模组与 `BorderDefenseProtocol.DevHarness` 已重新编译通过
- 本轮全局复审结论：
- 没有把 `CompCombatBodyHost` 做成第四个真值 owner；相位真值仍在 `CombatBodyState`
- `DevHarness` 对主模组的依赖已回到正式 `Reader / Commands / Events` 面，不再直接拿具体宿主实现类
- 当前主要剩余架构风险不在宿主附着点，而在 `CombatBodyExitPipeline` 仍直接通过 `Primary equipment -> CompTriggerBody` 关闭 Trigger 局部激活，这条依赖后续需要收成正式 Trigger 外部接入面

### 本次工作
- 用户游戏内反馈：
- `测 看战体` 显示“未附着战斗体正式宿主 Comp”
- `测 开战体` 提示“当前 Pawn 没有战斗体主链正式请求面”
- 已据此做系统化回查，并确认根因不在 `CombatBodyHostAccess` 或 `DevHarness`
- 根因确认：
- `BDPMod` 构造函数执行时机早于 `LoadModXML / ParseAndProcessXML`
- 此时 `DefDatabase<ThingDef>` 仍未完成 Def 装载
- 所以 `PawnTrionCompInjector.Apply()` 与 `PawnCombatBodyCompInjector.Apply()` 在错误时机执行，实际注入目标数为 0
- 这正是游戏内 Pawn 没有 `CompTrion / CompCombatBodyHost` 的直接原因
- 已按 RimWorld 正式时序修正：
- 保留 `BDPMod` 只负责 Harmony 注册
- 新增 `Core/Runtime/BdpPawnCompBootstrap.cs`
- 通过 `[StaticConstructorOnStartup]` 在 Def 全部解析完成后执行 Pawn comp 注入
- 当前这条修正只调整“宿主接线发生时机”，不改战斗体、Trion、Trigger 的任何业务逻辑
- 主模组与 `BorderDefenseProtocol.DevHarness` 已重新编译通过
- 当前建议的下一轮游戏复测目标：
- 打开同一存档或新局后，先看 `测 看战体` 是否出现正式宿主 Comp
- 若宿主已出现，再继续测 `测 开战体 / 测 关战体`

### 本次工作
- 用户复测后反馈：
- 战斗体正式宿主 Comp 已经出现
- 但 `测 开战体` 仍提示“战斗体激活请求失败”
- 已回查进入链事实，确认根因是：
- `CompProperties_CombatBodyHost.activationCost` 当前默认值为 `0f`
- `CombatBodyEnterPipeline.ExecuteEnter(...)` 会无条件执行 `CompTrion.Allocate(activationCost)`
- `CompTrion.Allocate(0)` 明确返回 `false`
- 因此当前并不是资源不足，而是“零成本进入”被误判成一次失败的资源占用事务
- 已按边界修正到进入链：
- `CombatBodyEnterPipeline` 仅在 `activationCost > 0f` 时才执行 `Allocate / Release`
- 资源层 `CompTrion` 的语义保持不变，不把“零值占用”改成正式资源动作
- 同时修正 `DevHarness` 的测试反馈：
- `测 关战体` 现在会先检查 `CanManualDeactivate()`
- 不再把无效关闭请求提示成“已提交战斗体解除请求”
- 主模组与 `BorderDefenseProtocol.DevHarness` 已重新编译通过

### 本次工作
- 已根据会话中反复确认过的全局原则、方法与禁忌，
  新增项目宪章文件：
- `docs/00-项目宪章/长期硬约束与工作方法总纲-第一版.md`
- 这份文件的定位不是单次计划，也不是单次日志，
  而是面向整个二次重构过程的长期全局约束锚点
- 内容重点已刻意优先收束为：
- 通用原则
- 全局架构判断准绳
- 真值边界
- 工作方法
- 测试与正式隔离
- 文档与日志义务
- 长期禁忌事项
- 以后继续推进时，
  这份文件应与 `二次重构总纲-第一版.md` 一起作为最高频回查入口之一

### 本次工作
- 根据本轮架构裁定，现有主模组中的“表达系统”不再被视为正式版本，而被判定为未经确认的临时样品
- 已将当前这套样品实现从主模组整块拆除，删除范围包括：
- `Source/BDP/Core/Expressions/*`
- `Source/BDP/Core/Projections/*`
- `Source/BDP/Core/Trigger/Projection/*`
- 同步拆除 Trigger 与 Pawn 攻击桥上的表达系统接线：
- `TriggerEquippedGizmoService` 不再生成表达驱动的手动攻击按钮
- `Patch_Pawn_TryGetAttackVerb` 与 `Patch_Pawn_MeleeVerbs_TryMeleeAttack` 退回空桥，不再补 Trigger 攻击入口
- `CompTriggerBody` 中的表达缓存与投影宿主字段已删除，避免继续保留“像是已成立表达系统”的伪骨架
- 本轮调整目标不是临时修功能，而是让主模组明确回到“表达系统缺席”的诚实状态
- 调整后已重新编译 `Source/BDP/BDP.csproj`，构建通过

### 当前结论
- 当前主模组仍保留了 Trigger 的正式装载/交互边界，但已经不再假装拥有表达结果解析、默认主攻击投影或表达驱动按钮链
- 这次回切是为了给后续“按需求文档正式搭建表达系统骨架”腾干净地基，不是回避问题
- 下一步不应急着补对外读取面，而应先重新设计主模组内部表达系统的正式层级、职责和最小骨架

### 本次涉及文件
- `Source/BDP/Core/Trigger/State/CompTriggerBody.cs`
- `Source/BDP/Core/Trigger/State/CompTriggerBody.Contexts.cs`
- `Source/BDP/Core/Trigger/State/CompTriggerBody.Reads.cs`
- `Source/BDP/Core/Trigger/State/CompTriggerBody.Lifecycle.cs`
- `Source/BDP/Core/Trigger/External/TriggerEquippedGizmoService.cs`
- `Source/BDP/Core/Trigger/Access/Surfaces/TriggerSurfaceAccess.cs`
- `Source/BDP/Patches/Patch_Pawn_TryGetAttackVerb.cs`
- `Source/BDP/Patches/Patch_Pawn_MeleeVerbs_TryMeleeAttack.cs`
- `Source/BDP/Core/Expressions/*`
- `Source/BDP/Core/Projections/*`
- `Source/BDP/Core/Trigger/Projection/*`

### 本次工作
- 用户新增两条表达系统正式裁定：
- 若特殊侧存在武器类表达，则拦截 Main / Sub 的武器类表达，以特殊侧武器作为当前唯一武器类单侧表达
- 特殊槽只允许正式表达出一个武器类结果，禁止多个武器类同时成立
- 某些双手锁定类武器虽然在结果层仍是单侧表达，但视觉层允许把它们伪装成双侧表达
- 已将这两条裁定补入表达需求文档，避免后续骨架设计继续默认“只有左右两侧”或把视觉伪装误写成结果层事实

### 当前结论
- 特殊侧不应被当成普通第三路并列武器输入，而应在武器类表达上拥有唯一化优先级
- 双手锁定武器的“双侧感”应明确收在视觉层，不能反向污染结果层和控制层
- 后续表达系统骨架必须同时容纳：
- 左右普通并列关系
- 特殊侧武器类拦截关系
- 结果单侧但视觉双侧的正式例外

### 本次涉及文件
- `docs/需求说明/2026-03-24-多表达解析规则说明-第一版.md`
- `docs/需求说明/2026-03-24-攻击表达共存与多层投影说明-第一版.md`

### 本次工作
- 已把本轮讨论确认过的表达系统内部骨架正式整理成设计文档
- 文档重点钉死了：
- 表达系统与 Trigger / CombatBody / Trion 的边界
- 八层内部骨架
- 第一版最小字段要求
- 正式数据流顺序
- Special 武器类表达唯一化裁定
- 双手锁定武器“结果单侧、视觉双侧”的视觉例外
- 这份文档当前定位是后续实现与继续裁定时的正式锚点，不是实现计划，也不是临时代码草稿

### 当前结论
- 当前主模组已经不再有旧样品表达系统，同时也已经拥有一份新的正式骨架设计锚点
- 后续继续推进时，应围绕这份骨架文档逐层落对象与接口，而不是重新回到“先补按钮 / 先补主攻击”的样品实现思路

### 本次涉及文件
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`

### 本次工作
- 已按 `docs/plans/2026-03-26-攻击执行边界与运行时驱动实施计划-第一版.md` 正式开始执行 `Task 2`
- 已在主模组新增 `Source/BDP/Core/AttackExecution/` 最小骨架，当前先不接 UI、不接 patch、不接近战/远程具体执行器，只先把“正式接单口”立起来
- 本轮新增的正式对象包括：
- `AttackExecutionRequest`
- `AttackExecutionReason`
- `IAttackExecutionEntry`
- `DefaultAttackExecutionEntry`
- `IAttackExecutionResolver`
- `AttackExecutionResolvedRequest`
- 当前这层已经明确：
- 外部系统只能把请求送进 `IAttackExecutionEntry`
- 入口当前只负责参数校验、读取 `ExpressionSnapshot`、按 `ResultId` 命中正式结果
- 若当前快照里没有这条结果，则入口正式拒绝，不再让外部旁路自己碰裸 `Verb`
- 当前还没有做的是：
- 近战/远程执行分流
- 手动按钮接管
- 自动远程接管
- 自动近战接管
- 这些仍属于后续 Task 3 及以后

### 当前结论
- 主模组内部现在已经出现第一版正式攻击执行边界雏形
- 这层边界当前只回答“这单能不能正式成立”，还没有回答“该怎么打出去”
- `Expressions` 继续负责“当前成立了什么”，`AttackExecution` 开始负责“能否正式接单”，边界没有混回去

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/AttackExecution/AttackExecutionRequest.cs`
- `Source/BDP/Core/AttackExecution/AttackExecutionReason.cs`
- `Source/BDP/Core/AttackExecution/IAttackExecutionEntry.cs`
- `Source/BDP/Core/AttackExecution/DefaultAttackExecutionEntry.cs`
- `Source/BDP/Core/AttackExecution/IAttackExecutionResolver.cs`
- `Source/BDP/Core/AttackExecution/AttackExecutionResolvedRequest.cs`

### 本次工作
- 已继续执行 `docs/plans/2026-03-26-攻击执行边界与运行时驱动实施计划-第一版.md` 的 `Task 3`
- 已在 `Source/BDP/Core/AttackExecution/` 新增：
- `IAttackExecutor`
- `IRangedAttackExecutor`
- `IMeleeAttackExecutor`
- `DefaultRangedAttackExecutor`
- `DefaultMeleeAttackExecutor`
- `DefaultAttackExecutionEntry` 已从“只验单”升级为“验单后分流”：
- 先经 `IAttackExecutionResolver` 把请求解析成当前快照里的正式结果
- 只接受 `ResultKind == Verb` 的正式结果
- `WeaponMode == Ranged` 交给远程执行器
- `WeaponMode == Melee` 交给近战执行器
- 其它结果当前一律正式拒绝
- 当前远程/近战执行器都先复用现有 `DefaultExpressionVerbHostResolver`
- 当前最小落地方式是：
- 先把正式结果解析成真实 `Verb`
- 再调用原版 `Verb.TryStartCastOn()` 把请求送回原版 verb 启动链
- 这样这一层已经能清楚回答：
- 谁负责分流：`DefaultAttackExecutionEntry`
- 谁负责把远程结果送进远程启动链：`DefaultRangedAttackExecutor`
- 谁负责把近战结果送进近战启动链：`DefaultMeleeAttackExecutor`
- 同时已在 `ExpressionReaderSurface` 补了一个内部辅助读取口：
- `TryGetSelectedResult()`
- 这个口只负责“从当前正式总表里按 ResultId 读结果”，不承担执行逻辑，避免把执行又拖回表达系统

### 当前结论
- 主模组现在已经不只是有“正式接单口”，而是有了第一版“正式接单 + 正式分流”结构
- 这层目前仍然只接 `Verb` 类结果，`Ability / Hediff / Passive` 还没有纳入攻击执行器范围
- 远程和近战目前都还是“最小回接”到原版 `Verb` 启动链，还没有进入后续 `JobDriver` 正式持续驱动阶段

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/AttackExecution/IAttackExecutor.cs`
- `Source/BDP/Core/AttackExecution/IRangedAttackExecutor.cs`
- `Source/BDP/Core/AttackExecution/IMeleeAttackExecutor.cs`
- `Source/BDP/Core/AttackExecution/DefaultRangedAttackExecutor.cs`
- `Source/BDP/Core/AttackExecution/DefaultMeleeAttackExecutor.cs`
- `Source/BDP/Core/AttackExecution/DefaultAttackExecutionEntry.cs`
- `Source/BDP/Core/Expressions/Access/Surfaces/ExpressionFormalSurfaces.cs`

### 本次工作
- 用户进一步确认：旧 BDP 的单侧齐射、双侧齐射、双侧混合、独立副攻击都要作为当前正式架构目标收进来
- 同时明确要求：
- 架构才是目标，结果只是自然形成
- 新增结构必须天然支持这些需求，但不能只能支持这些需求
- 副攻击在执行本质上不应被单独实现成另一套系统
- 已据此更新正式实施计划与正式裁定：
- 在 `AttackExecution` 中补入“攻击计划层”作为正式内部编排层
- 统一执行边界从“接单 + 分流”升级为“接单 + 计划 + 分流”
- 副攻击被正式裁定为与主攻击复用同一套执行边界、计划层与运行时链
- 当前计划已改为：
- 先补 `AttackExecutionPlan / AttackExecutionStep / AttackPattern / IAttackPlanner`
- 再让后续手动入口、自动远程、自动近战、JobDriver 接入都围绕攻击计划层推进

### 当前结论
- 当前主模组方向不需要推翻
- 但现在的 `AttackExecution` 还不够，必须升级成“会先形成攻击计划”的正式边界
- 后续如果实现齐射、双侧攻击、独立副攻击时还需要大量平行入口或平行宿主，就说明计划层设计失败，而不是继续加特判

### 本次涉及文件
- `docs/plans/2026-03-26-攻击执行边界与运行时驱动实施计划-第一版.md`
- `docs/01-决策记录/2026-03-26-攻击执行边界与运行时驱动裁定-第一版.md`

### 本次工作
- 已继续把刚新增的攻击编排层代码从“结果导向”收成“通用编排结构”
- 已删除 `AttackPattern` 这类会把架构形状绑死在具体攻击表现上的对象
- 改为正式使用：
- `AttackDriveMode`
- `AttackStepFireMode`
- `AttackExecutionPlan.GroupCount`
- `AttackExecutionStep.GroupIndex`
- 这样当前编排层只回答：
- 这次执行有几组
- 每组里有几步
- 每步是一发还是一步内多发
- 整体是即时完成还是持续推进
- 而不再回答“这次是不是齐射/双重攻击/混合攻击”这种结果导向问题

### 当前结论
- 当前 `AttackExecution` 的内部骨架已经更接近真正的“通用攻击编排能力”
- 后续双侧、混合、并列推进等内容，应在这套组/步/推进方式结构里自然展开
- 如果后续又想回到“再发明几个模式枚举对应旧 BDP 表现”，应视为架构退化

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/AttackExecution/AttackDriveMode.cs`
- `Source/BDP/Core/AttackExecution/AttackStepFireMode.cs`
- `Source/BDP/Core/AttackExecution/AttackExecutionPlan.cs`
- `Source/BDP/Core/AttackExecution/AttackExecutionStep.cs`
- `Source/BDP/Core/AttackExecution/DefaultAttackPlanner.cs`
- `Source/BDP/Core/AttackExecution/IAttackPlanner.cs`
- `Source/BDP/Core/AttackExecution/AttackExecutionResolvedRequest.cs`

### 本次工作
- 已按更新后计划执行 `Task 5：接管手动按钮正式入口`
- 已新增 `AttackExecutionSurfaceAccess`，把手动入口正式接单口收成一条稳定读取面
- 已扩充 `ManualEntryProjectionItem`，当前手动入口项现在能明确携带：
- `ResultId`
- `IsPrimary`
- `ResultKind`
- `WeaponMode`
- `DefaultManualEntryProjector` 已改为在投影阶段就把这些正式身份带出来
- `DefaultManualEntryGizmoResolver` 已不再用 `Command_VerbTarget` 把 UI 当最终执行边界
- 当前改法是：
- 按钮先通过内部 `verbHostResolver` 读取目标选择所需的原版参数
- 玩家选定目标后，再由 `AttackExecutionSurfaceAccess.ResolveEntry()` 把正式请求送进 `AttackExecution`
- 也就是说现在手动按钮负责的是：
- 启动目标选择
- 把请求送进正式执行边界
- 不再负责：
- 自己直接持有并执行裸 `Verb`
- `ExpressionManualGizmoBridge` 也已收紧为：
- 只有在 `AttackExecution` 正式入口存在时，才生成手动按钮

### 当前结论
- 手动按钮现在已经不再把自己当执行本体
- 手动入口已经正式并入统一执行边界
- 当前为保留 RimWorld 原版目标选择手感，按钮内部仍会解析 verb 来取 `targetParams` / 高亮 / 校验
- 但这一步只发生在手动入口内部辅助阶段，不再由 UI 决定正式执行边界

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/AttackExecution/AttackExecutionSurfaceAccess.cs`
- `Source/BDP/Core/Expressions/Model/ManualEntryProjectionItem.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultManualEntryProjector.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultManualEntryGizmoResolver.cs`
- `Source/BDP/Core/Expressions/Projection/ExpressionManualGizmoBridge.cs`

### 本次工作
- 已按更新后计划执行 `Task 6：接管自动远程入口`
- `Patch_Pawn_TryGetAttackVerb` 已不再直接向表达层索要远程 verb
- 当前改为：
- patch 只向 `DefaultRangedAttackExecutor.TryGetAutoAttackVerb()` 读取正式远程入口
- 由远程执行器先确认当前默认主远程结果是否合法
- 再把真实 verb 交回原版自动远程链
- `ExpressionVerbBridge` 已被收紧：
- 不再承担自动远程事实执行边界
- 当前只保留：
- `TryResolveFormalVerb()` 作为执行器内部辅助 verb 解析桥
- `TryGetAutoMeleeVerb()` 作为 `Task 7` 完成前的近战过渡读取口
- `DefaultRangedAttackExecutor` 当前已经同时承担两种职责：
- 正式远程执行时，把计划/结果送进原版远程 verb 启动链
- 自动远程读取时，作为自动远程入口的正式读取/分发口

### 当前结论
- 自动远程入口现在已经不再由 patch 直接临时决定执行对象
- 自动远程最后交给 RimWorld 的仍然是正式远程 verb
- 但这个 verb 现在已经先经过远程执行器确认来源
- `ExpressionVerbBridge` 现在更接近真正的“内部辅助桥”，而不是正式执行边界

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Patches/Patch_Pawn_TryGetAttackVerb.cs`
- `Source/BDP/Core/Expressions/Projection/ExpressionVerbBridge.cs`
- `Source/BDP/Core/AttackExecution/DefaultRangedAttackExecutor.cs`

### 本次工作
- 已按更新后的正式计划继续推进，先把 `AttackExecution` 的内部攻击计划层骨架正式落到代码
- 本轮新增：
- `AttackPattern`
- `AttackExecutionPlan`
- `AttackExecutionStep`
- `IAttackPlanner`
- `DefaultAttackPlanner`
- 同时把 `AttackExecutionResolvedRequest` 扩成可携带 `Plan`
- `DefaultAttackExecutionEntry` 已升级为：
- 先接单
- 再解析正式结果
- 再生成攻击计划
- 最后才分流到近战/远程执行器
- 当前计划层先只做最小落地：
- 能正式表达 `Single / Volley / DualSequential / DualSimultaneous / DualMixed / SecondaryOnly`
- 当前真正会自动推断出的仍只有：
- `Single`
- `Volley`
- `SecondaryOnly`
- 双侧与混合模式当前是先把“计划类型位置”腾出来，还没有进入正式组装逻辑
- 为后续计划层接入补了最小计划提示字段到 `FormalExpressionResult`：
- `ExecutionSlotKey`
- `IsSecondaryAttack`
- `BurstShotCountHint`
- 这些字段当前只是计划层轻量提示，不是新的真值 owner
- 远程/近战执行器当前已改成优先消费计划首步信息，而不再默认只盯着原始结果对象

### 当前结论
- 主模组的正式执行边界现在已经不是“接单后直接开打”，而是“接单 -> 计划 -> 分流”
- 副攻击在代码层也开始落实为复用同一套计划层和执行链，而不是另立平行系统
- 当前还没有做的是：
- 真正把双侧攻击、双侧齐射、混合攻击从正式结果翻译成多步计划
- 那部分应在后续任务里继续推进，不能重新退回特殊 verb 特判堆积

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/AttackExecution/AttackPattern.cs`
- `Source/BDP/Core/AttackExecution/AttackExecutionPlan.cs`
- `Source/BDP/Core/AttackExecution/AttackExecutionStep.cs`
- `Source/BDP/Core/AttackExecution/IAttackPlanner.cs`
- `Source/BDP/Core/AttackExecution/DefaultAttackPlanner.cs`
- `Source/BDP/Core/AttackExecution/AttackExecutionResolvedRequest.cs`
- `Source/BDP/Core/AttackExecution/DefaultAttackExecutionEntry.cs`
- `Source/BDP/Core/AttackExecution/DefaultRangedAttackExecutor.cs`
- `Source/BDP/Core/AttackExecution/DefaultMeleeAttackExecutor.cs`
- `Source/BDP/Core/Expressions/Model/FormalExpressionResult.cs`

### 本次工作
- 用户进一步纠正了架构口径：
- 不能为了旧 BDP 的具体表现倒推设计
- 必须把旧 BDP 的表现只当成验收样本，而不是架构形状本身
- 已据此同步修正文档措辞：
- 正式从“攻击计划层”统一改口为“攻击编排层”
- 正式目标从“支持某些具体结果”改为“建立结果无关的通用攻击编排能力”
- 旧 BDP 的齐射、双侧、混合、副攻击只保留为架构验收样本

### 当前结论
- 当前主模组后续推进时，不能再以“先做出某种攻击效果”为设计驱动
- 正确驱动应是：
- 一次攻击请求可以被正式组织成一到多步
- 这些步骤可以顺序执行，也可以并列执行
- 整体可以即时完成，也可以进入持续推进
- 具体攻击表现只能从这套通用能力里自然长出来

### 本次涉及文件
- `docs/plans/2026-03-26-攻击执行边界与运行时驱动实施计划-第一版.md`
- `docs/01-决策记录/2026-03-26-攻击执行边界与运行时驱动裁定-第一版.md`

### 本次工作
- 已完成“攻击执行边界与运行时驱动”方案定稿，确认当前主问题不是“远程按钮坏了”，而是主模组尚未把表达结果正式接成一个可被 RimWorld 完整驱动的运行时攻击宿主
- 已正式裁定当前方向为：
- 采用 Pawn 侧薄执行入口
- 保持 `CompTriggerBody` 继续只承接 Trigger 装载/激活/形态事实
- 保持 `Expressions` 继续只回答“当前成立了什么”与“怎么投影”
- 不先新增新的攻击 `Comp`
- 不把执行重新塞回 `CompTriggerBody`
- 已新增正式计划文件：
- `docs/plans/2026-03-26-攻击执行边界与运行时驱动实施计划-第一版.md`
- 已新增正式裁定文件：
- `docs/01-决策记录/2026-03-26-攻击执行边界与运行时驱动裁定-第一版.md`

### 当前结论
- 后续实现将以“统一执行入口 -> 近战/远程正式执行器 -> RimWorld 自然运行时链”为主轴推进
- `JobDriver` 当前正式定位为推进器之一，不再被误当成整个运行时本体
- 下一步进入代码实施时，应先搭 `Core/AttackExecution` 的最小骨架，再逐步接手手动按钮、自动远程、自动近战入口

### 本次涉及文件
- `docs/plans/2026-03-26-攻击执行边界与运行时驱动实施计划-第一版.md`
- `docs/01-决策记录/2026-03-26-攻击执行边界与运行时驱动裁定-第一版.md`

## 2026-03-26 芯片骨架重构补记

### 本次工作
- 已把 `ChipLoadout` 的正式插入权限语义收口为 `HandsOnly / SpecialOnly`
- 已明确主副侧只共享“手部区”插入权限，不再在芯片定义层拆成主侧可插、副侧可插两套声明
- 已把正式双持语义统一命名为 `IsDualWieldBinding`，不再继续保留 `TwoHand / IsTwoHandBinding`
- 已同步修正 `DevHarness` 芯片样例，包含 `SidePolicy`、双持样例 `defName`、分类、标签与条目标签

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -p:UseSharedCompilation=false -t:Build -v:minimal`
- 结果：通过
- 已执行 `dotnet msbuild BDP.DevHarness.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

## 2026-03-26 DevHarness 样例贴图噪音清理

### 本次工作
- 已把 `BDP_TestChipAbility / BDP_TestChipHediff / BDP_TestChipPassive / BDP_TestChipInvalidDefinition` 的物品贴图路径改成确定存在的原版资源
- 这一步只清理测试样例物品的日志噪音，不涉及表达系统结果、按钮生成、攻击执行或视觉层正式语义

### 当前结论
- 当前这些贴图仅服务“芯片物品 Def 自己在世界中的显示”
- 它们不是表达系统攻击贴图，也不是主模组视觉层正式输出

### 当前进度
- 芯片正式骨架重构的当前活动路径里，旧的 `AllowedSides / IsTwoHandBinding / BDP_TestChipTwoHandRanged` 已从代码与样例定义路径移除
- 当前仍保留的 `TwoHand / 双手` 主要位于历史工作记录，用于保留当时讨论语境，不再作为现行正式术语

## 2026-03-26 芯片被动附加数据载入修正

### 本次问题
- 游戏内调试日志出现 `ThingDefs_BDP_TestChips.xml` 反序列化失败
- 失败点落在 `Passive` 条目的 `ExposedData`
- 直接表现为混合芯片与被动芯片样例在 XML 载入阶段报错

### 根因判断
- 问题不在芯片总骨架，也不在表达结果链主结构
- 根因是 `PassiveExpressionExposedDatumConfig` 继续使用 `Key / Value` 成员名
- 这组名字在当前 XML 载入路径里与 Verse 反序列化习惯不兼容，导致没有按预期读成“被动附加数据项”

### 本次修正
- 已把定义层成员名收口为 `DataKey / DataValue`
- 已同步修正 `DevHarness` 中 `Passive` 与 `Mixed` 样例的 `ExposedData`
- 已顺手把双持远程样例的无效 `soundCast` 从 `Shot_ChargeLance` 改为现有可解析项

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -p:UseSharedCompilation=false -t:Build -v:minimal`
- 结果：通过
- 已执行 `dotnet msbuild BDP.DevHarness.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次工作
- 已暂停继续推进“正式芯片定义契约”实现，先回到架构审查
- 原因不是某个字段小问题，而是这一轮芯片定义层设计出现了明显的“抽象先行、主题后置”偏航
- 用户已明确指出：
- `Identity.IsFormalChip` 自我重复
- `ContractVersion` 缺乏真实使用对象
- `ChipKindKey` 增加理解成本
- `Resource` 命名脱离 BDP 的 `Trion` 语义
- 当前判断：
- `Core/Chips` 作为上位层、`Access / Contract / Validation` 分层方向本身可以保留
- 但这轮芯片定义层里的若干核心字段、校验前提、文档表述方式已受“企业契约模板化”思路污染
- 下一步优先事项：
- 先给出正式问题清单、根因和影响范围
- 再决定哪些骨架保留，哪些对象和文档需要激进删改

### 当前结论
- 这不是“命名再润一下”就能解决的问题
- 问题核心是：设计起点没有始终站在 BDP 玩家语义、模组主题语义和主模组长期边界上
- 因此本轮芯片定义层需要先做一次去模板化审查，再决定后续保留面

### 本次工作
- 已根据用户重新确认的 BDP 芯片骨架，另立新版实施计划：
- `docs/plans/2026-03-26-BDP芯片正式骨架重构实施计划-第一版.md`
- 新计划明确收口为：
- `profile`
- `loadout`
- `expression`
- `trion`
- `extensions`
- 并明确：
- 形态留在 `expression` 内部
- 基础分类采用“单主类 + 标签”
- 芯片本体级 `trion` 承担占用、激活成本、功率要求
- 条目级 `trion` 承担使用消耗、持续消耗
- 单一总扩展区保留，但不再允许通用三元组扩展结构

### 当前结论
- 旧 `Identity / Resource / Extension` 骨架已不再适合作为继续实现的依据
- 后续执行应以新版实施计划为准，激进删除旧坏字段与旧坏命名，不留兼容污染

### 本次工作
- 已按“小块改、改完即回读”的方式继续执行新版芯片骨架重构
- 已重写并收口上位文档：
- `2026-03-26-正式芯片定义契约说明-第一版.md`
- `2026-03-26-正式芯片定义契约骨架设计-第一版.md`
- 已将旧 `2026-03-26-正式芯片定义契约实施计划-第一版.md` 标记为停用
- `Core/Chips/Config` 已切到新总结构：
- `Profile`
- `Loadout`
- `Expression`
- `Trion`
- `Extensions`
- 已新增：
- `ChipProfileConfig`
- `ChipTrionConfig`
- `ChipExtensionBlockConfig`
- 已新增：
- `ChipProfileContract`
- `ChipTrionContract`
- `ChipExtensionBlockContract`
- 已重写：
- `ChipDefinitionContract`
- `ChipDefinitionDeclaredBlock`
- `DefaultChipDefinitionContractResolver`
- `DefaultChipDefinitionValidator`
- 已删除旧污染文件：
- `ChipIdentityConfig`
- `ChipResourceConfig`
- `ChipExtensionConfig`
- `ChipIdentityContract`
- `ChipResourceContract`
- `ChipExtensionContract`
- 已删除读取结果与诊断层里的自证式总开关：
- `IsRecognizedChip`
- `IsFormalChip`
- 当前判断改为直接看：
- 契约是否存在
- 校验是否通过
- DevHarness 已开始迁移到新总结构，当前已补齐：
- 远程样例
- 近战样例
- 爆炸样例
- Ability 样例
- Hediff 样例
- Passive 样例
- Mixed 样例
- TwoHand 样例
- 非法样例已改成缺少主分类的真实非法结构

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过
- 已执行 `dotnet msbuild BDP.DevHarness.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 当前结论
- 主模组代码主链已经切离旧 `Identity / Resource / Extension` 对象
- 当前仍需继续清理的是：
- 剩余文档中的旧名词说明
- Access 命名收口
- DevHarness 全量样例与说明文案统一

### 本次工作
- 已继续参考旧 BDP，但只提取两条可取事实，不回退到旧版 `IChipEffect` 总控结构：
- `Ability` 最终必须回到原版 `Pawn_AbilityTracker`
- 装备态武器不能把 `CompTick()` 当成可靠宿主同步入口
- 已据此在主模组补入表达系统内部唯一正式宿主同步口：
- `ExpressionSurfaceAccess.ResolveHostSync()`
- 新增 `ExpressionHostSyncSurface`
- 新增 `IExpressionHostSynchronizer`
- 新增 `IExpressionAbilityHostSynchronizer`
- 新增 `IExpressionHediffHostSynchronizer`
- 当前 Trigger 只在自己的生命周期 / 指令入口调用这个正式同步口
- 不再直接 new 各类宿主同步实现
- 当前最小宿主回接已推进到：
- `Ability` 可同步到原版 `Pawn_AbilityTracker`
- `Hediff` 可同步到原版 `Hediff` 宿主的最小存在状态
- `Notify_Unequipped` 现在也会通过正式同步口回收已不成立的宿主结果

### 当前结论
- 这次改动把“结果到原版宿主”的入口正式收口到表达系统 `Access/Surface` 层
- Trigger 现在知道的只有“去调用表达系统正式宿主同步口”，不知道宿主细节
- `Task 16` 已不再是“只有四类样本输入”，而是已经进入非 `Verb` 结果的第一版宿主回接阶段
- 当前仍未闭合的主要缺口是：
- `Passive` 的专属消费宿主
- `Ability` 的专属按钮桥
- `Hediff` 的所有权标记、清理协议和复杂 `applyMode` 语义

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Access/Contracts/IExpressionHostSync.cs`
- `Source/BDP/Core/Expressions/Access/Surfaces/ExpressionSurfaceAccess.cs`
- `Source/BDP/Core/Expressions/Access/Surfaces/ExpressionFormalSurfaces.cs`
- `Source/BDP/Core/Expressions/Projection/IExpressionHostSynchronizer.cs`
- `Source/BDP/Core/Expressions/Projection/IExpressionAbilityHostSynchronizer.cs`
- `Source/BDP/Core/Expressions/Projection/IExpressionHediffHostSynchronizer.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultExpressionHostSynchronizer.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultExpressionAbilityHostSynchronizer.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultExpressionHediffHostSynchronizer.cs`
- `Source/BDP/Core/Trigger/State/CompTriggerBody.cs`
- `Source/BDP/Core/Trigger/State/CompTriggerBody.Lifecycle.cs`
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`
- `docs/plans/2026-03-25-表达系统执行计划-第一版.md`

### 本次工作
- 用户同意对芯片正式契约进行更激进的整体重定，不再局限于旧四列表结构
- 已新增正式决策文档：
- `docs/01-决策记录/2026-03-26-芯片表达契约重构裁定-第一版.md`
- 当前正式裁定为：
- 芯片表达未来主结构改为“统一条目 + 形态操作”
- 表达系统目录必须新增 `Contract` 层
- 旧 `VerbSources / AbilitySources / HediffSources / PassiveSources` 不再作为未来标准继续维护
- 形态处理从“命中后追加”改为 `Append / Replace / Disable`
- 轻量从属关系进入结构预留，但第一版只作解释语义和未来分组依据
- 已同步改写：
- `芯片标准结构说明`
- `表达系统骨架设计`
- `表达系统执行计划`

### 当前结论
- 当前主模组接下来的正确方向不是继续围着旧四列表补功能
- 而是先把芯片正式输入重定为统一条目契约，再回收 `Pipeline / Projection`
- 后续真正开始代码切换时，应采取一次任务内切干净的策略，不接受长期双轨

### 本次涉及文件
- `docs/01-决策记录/2026-03-26-芯片表达契约重构裁定-第一版.md`
- `docs/需求说明/2026-03-24-芯片标准结构说明-第一版.md`
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`
- `docs/plans/2026-03-25-表达系统执行计划-第一版.md`

### 本次工作
- 已按“从头到尾一次切干净”的要求新增完整实施计划：
- `docs/plans/2026-03-26-芯片表达契约重构实施计划-第一版.md`
- 这份计划覆盖：
- 文档定稿
- `Contract` 层新增
- `Config` 改写为统一 `Entries`
- 形态切换改为 `Operations`
- 旧四列表退出主路径
- `Pipeline` 改为只消费契约
- `Projection` 与宿主同步回接
- DevHarness 示例 Def 迁移
- 诊断、文档、日志、最终验证收口

### 当前结论
- 现在已经不是“继续讨论方向”的阶段
- 后续若开始执行，应直接按这份完整实施计划推进，不再反复回到旧四列表思路

### 本次涉及文件
- `docs/plans/2026-03-26-芯片表达契约重构实施计划-第一版.md`

### 本次工作
- 用户纠正了一条重要边界：
- “不能造假”针对的是表达结果真相，不能因此误删用户已经明确批准的视觉层 / 信息层宿主回退策略
- 已据此重新收口当前推进方向：
- 保留正式批准的视觉回退与信息回退
- 继续清理会伪造表达事实的假默认、假结果
- 不再把“整个表达系统完全没有回退”误写成新原则

### 当前结论
- 当前边界重新明确为：
- 结果层必须诚实
- 视觉层 / 信息层允许存在已获批准且明确声明的宿主回退
- 后续推进时，必须持续区分“表达真相”和“宿主适配退路”

### 本次涉及文件
- 对话裁定，无单独代码文件

### 本次工作
- 已继续推进手动入口投影层，但没有发明按钮规则
- 当前改动重点是把“手动入口结果形状”和“模组内正式访问入口”先钉死
- 已新增：
- `ManualEntryProjectionGroup`
- `ManualEntryProjectionItem`
- 并把 `ManualEntryProjection` 从“只有组 id”升级为“正式入口组集合”
- 同时已扩展表达系统内部正式读取口 `IExpressionReader`，统一提供：
- `GetSnapshot`
- `GetManualProjection`
- `GetVisualProjection`
- `GetInfoProjection`
- 这一步的目标是把模组内其它系统对表达系统的访问路径先收口到 `Access/Surface`
- 而不是让它们后续各自 new 投影器或跨层取数

### 当前结论
- 手动入口层当前已具备正式数据形状，但仍未正式定义“哪些结果应生成哪些按钮”
- 因此当前状态属于：
- 入口层骨架更完整了
- 读取入口更正式了
- 但手动入口业务规则与 Trigger 按钮宿主回接仍未完成

### 本次涉及文件
- `Source/BDP/Core/Expressions/Model/ManualEntryProjection.cs`
- `Source/BDP/Core/Expressions/Model/ManualEntryProjectionGroup.cs`
- `Source/BDP/Core/Expressions/Model/ManualEntryProjectionItem.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultManualEntryProjector.cs`
- `Source/BDP/Core/Expressions/Access/Contracts/IExpressionReader.cs`
- `Source/BDP/Core/Expressions/Access/Surfaces/ExpressionFormalSurfaces.cs`
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`
- `docs/plans/2026-03-25-表达系统执行计划-第一版.md`

### 本次工作
- 继续推进视觉层与信息层的正式结果形状，但没有越权去发明视觉宿主规则或信息宿主规则
- 视觉层方面：
- `VisualExpressionProjection` 已补上
- 常驻显示结果标识集合
- 执行跟随结果标识
- 枪口 / 发射基准跟随结果标识
- 当前仍保持：没有正式答案的字段就留空，不伪造
- 同时继续保留用户已批准的视觉宿主回退字段
- 信息层方面：
- `ExpressionInfoProjection` 已从“文本行列表”推进为“文本 + 结构化条目”双轨结果
- 新增 `ExpressionInfoProjectionEntry`
- 说明层现在可以正式表达：
- 每条结果的标识、显示名、大类、来源关系、高层关系、角色键、形态键、武器模式、可用性、可投影性
- 以及当前默认主远程 / 默认主近战 / 当前执行表达的结果标识

### 当前结论
- 表达系统当前的三类投影层状态更清楚了：
- 手动入口层：已有正式形状，无正式按钮规则
- 视觉层：已有正式读取位与正式宿主回退，无正式视觉宿主接线
- 信息层：已有正式结构化说明结果与正式宿主回退，无正式信息宿主接线
- 这意味着主模组内部“表达结果是什么”与“宿主拿它怎么显示/怎么兼容”正在被逐步切开

### 本次涉及文件
- `Source/BDP/Core/Expressions/Model/VisualExpressionProjection.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultVisualProjectionBuilder.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionInfoProjection.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionInfoProjectionEntry.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultExpressionInfoProjector.cs`
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`
- `docs/plans/2026-03-25-表达系统执行计划-第一版.md`

### 本次工作
- 中途验证过一个边界尝试：
- 我曾尝试把表达系统读取口塞进 `TriggerExternalGizmoContext`
- 但该上下文是对外公开的 formal API，而 `IExpressionReader` 当前仍是主模组内 formal surface
- 这会导致主模组内入口被误借公共 Trigger API 泄漏到外部
- 因此这条接线已被主动回退

### 当前结论
- 当前已进一步确认：
- 主模组内 formal surface 和真正对模组外公开的 formal API 仍需继续分开
- `IExpressionReader` 现在可以服务主模组内其它系统
- 但还不能顺手借 Trigger 公共扩展上下文把它公开出去

### 本次涉及文件
- `Source/BDP/Core/Trigger/External/TriggerExternalGizmoContext.cs`
- `Source/BDP/Core/Trigger/External/TriggerEquippedGizmoService.cs`

### 本次工作
- 已顺手收一处内部实现问题：
- `ExpressionReaderSurface` 之前在读取 `Snapshot / Manual / Visual / Info` 时会重复跑多次主链
- 现在已改为内部统一先构建一份“已过默认主表达选择”的正式总表
- 再让说明、视觉、手动入口三类投影都从这同一份总表继续读取
- 这一步不改变正式边界，只减少重复计算并强化“多投影共读同一真相”

### 当前结论
- 表达系统当前在 Access/Surface 内部已经更接近真正的“一个正式真相，多层投影读取”
- 这也进一步压实了最初那条原则：
- 不允许按钮层、视觉层、说明层各自重新猜一套系统真相

### 本次涉及文件
- `Source/BDP/Core/Expressions/Access/Surfaces/ExpressionFormalSurfaces.cs`

### 本次工作
- 继续按表达系统执行计划推进 `Task 13`
- 当前没有去发明“哪些结果应该生成按钮”的新规则
- 只先把 Trigger 按钮宿主正式接回到表达系统的手动入口读取链
- 已新增：
- `Source/BDP/Core/Expressions/Projection/ExpressionManualGizmoBridge.cs`
- 这条内部桥当前做的事只有一件：
- 让 Trigger 装备态按钮宿主先从 `IExpressionReader.GetManualProjection` 读取正式手动入口投影
- 然后在正式按钮映射规则尚未落地前，诚实返回空集合
- 同时 `TriggerEquippedGizmoService` 已改为先走这条内部桥，再继续走原有 external gizmo registry

### 当前结论
- `Task 13` 当前已推进到“按钮宿主读取路径已正式回接”
- 但还没有推进到“按钮项如何生成具体 Gizmo”
- 这意味着 Trigger 侧已经不需要再直接从零散状态拼内部按钮真相
- 后面只需在这条正式读取链上继续补按钮映射规则，而不是再开第二条链

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Projection/ExpressionManualGizmoBridge.cs`
- `Source/BDP/Core/Trigger/External/TriggerEquippedGizmoService.cs`
- `docs/plans/2026-03-25-表达系统执行计划-第一版.md`

### 本次工作
- 继续按表达系统执行计划推进 `Task 12`
- 已新增：
- `Source/BDP/Core/Expressions/Projection/ExpressionVerbBridge.cs`
- 两条自动攻击 patch 现在不再是裸空实现
- 而是正式改为：
- 先从表达系统 `Snapshot` 读取默认主远程 / 默认主近战
- 然后尝试通过 `ExpressionVerbBridge` 回接自动远程与自动近战入口
- 但当前这条桥仍然诚实返回空
- 因为现在的正式结果总表虽然已经能回答：
- 默认主远程是谁
- 默认主近战是谁
- 却还不能正式回答：
- 这一条正式结果对应哪个真实 `Verb` 宿主实例
- 因此当前没有把自动攻击“先假做出来”，而是把缺口正式收口到了 `ExpressionVerbBridge`

### 当前结论
- `Task 12` 当前已从“裸空 patch”推进到“正式桥已存在，但因缺少宿主实例映射而诚实不可达”
- 这意味着下一步真正要补的，不再是 patch 本身
- 而是“正式结果 -> 真实 Verb 宿主实例”的正式映射层

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Projection/ExpressionVerbBridge.cs`
- `Source/BDP/Patches/Patch_Pawn_TryGetAttackVerb.cs`
- `Source/BDP/Patches/Patch_Pawn_MeleeVerbs_TryMeleeAttack.cs`
- `docs/plans/2026-03-25-表达系统执行计划-第一版.md`

### 本次工作
- 已按《芯片标准结构说明》回审表达系统当前实现，确认此前把 `Verb` 来源收缩成 `VerbPropsKey` 属于偏差
- 已把配置层与正式主链纠正为直接承载 `verbProps`，并从表达系统源码中移除 `VerbPropsKey`
- 同时补齐 `AbilityDefName / HediffDefName / ApplyModeKey / PassiveKey / ExposedData` 在声明链与结果链中的最小传递位
- `ExpressionInfoProjection` 的结构化结果也已改为围绕正式来源定义输出，不再传播 `VerbPropsKey`
- `BorderDefenseProtocol.DevHarness` 的测试芯片 Def 已从旧 `BdpExpressionConfig` 结构改回 `ChipExpressionConfig + VerbSources + verbProps`

### 本次工作
- 继续顺着 `Task 12` 往真正缺口推进，没有在 patch 层假装恢复自动攻击
- 当前已确认：
- 配置层里原本应有 `verbProps`
- 但这条身份线索之前没有被表达系统主链保留到正式结果层
- 已据此补齐：
- `ExpressionSourceDeclaration.VerbProps`
- `ExpressionSourceMaterial.VerbProps`
- `FormalExpressionResult.VerbProps`
- 并把它从 `VerbExpressionSourceConfig` 一路贯通到说明投影层
- 这一步的意义不是直接恢复自动攻击
- 而是正式建立“这条 Verb 表达未来该如何找到宿主”的身份抓手

### 当前结论
- 当前表达系统已经能正式保留：
- 哪条结果是默认主远程 / 默认主近战
- 这条结果关联的 `verbProps` 是什么
- 因此真正剩下的缺口被进一步收窄为：
- 如何把 `verbProps` 映射到当前 Pawn 身上的真实 `Verb` 宿主实例
- 而不再是“主链里连这条身份线索都没有”

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Model/ExpressionSourceDeclaration.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionSourceMaterial.cs`
- `Source/BDP/Core/Expressions/Model/FormalExpressionResult.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionInfoProjectionEntry.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceDeclarationProvider.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceCollector.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultSingleSideExpressionBuilder.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultExpressionInfoProjector.cs`
- `docs/plans/2026-03-25-表达系统执行计划-第一版.md`

### 本次工作
- 用户确认：可以借鉴旧 BDP 攻击系统“代理入口”与“运行时宿主实例”分离的思路
- 但必须遵循新 BDP 的架构原则，不能把旧版 `CompTriggerBody` 一把抓的真相模式搬回来
- 已据此先停下实现，重新做了一轮全局架构评估
- 当前结论已正式钉死：
- “结果到宿主实例”的映射器应落在 `Projection` 层
- 不应放回 `Trigger`
- 不应放进 patch
- 不应塞进 `FormalExpressionResult`
- 第一版推荐拆法为：
- `IExpressionVerbHostResolver + ExpressionVerbBridge`
- `IManualEntryGizmoResolver + ExpressionManualGizmoBridge`
- 其中 resolver 只做解析，bridge 只做接线
- 并已把这条决策和实施顺序写回骨架设计与执行计划

### 当前结论
- 当前 `Task 12~13` 的真正缺口已被正式收口为：
- `FormalExpressionResult / verbProps -> 真实 Verb 宿主实例`
- `ManualEntryProjection -> 真实 Gizmo 宿主实例`
- 这两个缺口都属于“投影到宿主实例”的问题
- 不再属于表达真相本身

### 本次涉及文件
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`
- `docs/plans/2026-03-25-表达系统执行计划-第一版.md`

### 本次工作
- 用户提醒补看《芯片标准结构说明》，已按 UTF-8 重新纳入当前实现依据
- 重新对照需求文档后确认：
- 芯片对表达系统的定位应是“上游声明源”
- 而不是运行时 owner，也不是直接产出最终结果的对象
- 这说明前面新建 `Core/Expressions/Config` 的方向是对的，但当时的配置粒度偏薄
- 已据此继续把 `Config` 层补到更接近需求文档最小结构的样子：
- 新增 `ExpressionSourceConfigBase`
- 新增 `ExpressionSourceConditionConfig`
- 新增 `ExpressionSourceTrionConfig`
- 四类来源配置现在都继承公共基类，开始统一承载：
- `id`
- `label`
- `role`
- `tags`
- `conditions`
- `trion`
- 同时各自补上最小专属字段位：
- `Verb` 增加 `verbProps`
- `Ability` 增加 `AbilityDefName`
- `Hediff` 增加 `HediffDefName` 与 `ApplyModeKey`
- `Passive` 增加 `PassiveKey` 与 `ExposedData`
- 这些字段当前仍然只是正式声明结构，不代表下游投影已实现
- 随后继续把一部分稳定公共信息往内部链传递：
- `ExpressionSourceDeclaration` 新增 `RoleKey / Tags / Trion`
- `ExpressionSourceMaterial` 新增 `RoleKey / Tags / Trion`
- `DefaultExpressionSourceDeclarationProvider` 开始把这些公共区块从芯片配置翻译到内部声明
- `DefaultExpressionSourceCollector` 再把这些信息继续带入来源材料
- 当前刻意没有提前把 `conditions` 接进运行时判定链
- 原因是条件解释层尚未正式建立，先传 `role / tags / trion` 更稳，不会把未定规则混进主链

### 当前结论
- 表达系统现在已经不仅有“来源声明存在”这件事，而且开始能保留来源的公共结构信息
- 当前 `Config -> Declaration -> Material` 这条链的语义比前一轮更接近需求文档
- 同时仍然守住了边界：
- 配置层是声明
- 内部链是翻译与整理
- 没有把芯片配置直接当最终结果或 owner 使用

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Config/ExpressionSourceConfigBase.cs`
- `Source/BDP/Core/Expressions/Config/ExpressionSourceConditionConfig.cs`
- `Source/BDP/Core/Expressions/Config/ExpressionSourceTrionConfig.cs`
- `Source/BDP/Core/Expressions/Config/VerbExpressionSourceConfig.cs`
- `Source/BDP/Core/Expressions/Config/AbilityExpressionSourceConfig.cs`
- `Source/BDP/Core/Expressions/Config/HediffExpressionSourceConfig.cs`
- `Source/BDP/Core/Expressions/Config/PassiveExpressionSourceConfig.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionSourceDeclaration.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionSourceMaterial.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceDeclarationProvider.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceCollector.cs`

### 本次工作
- 继续按表达系统执行计划推进，没有跳去补旧功能
- 新增 `IExpressionSnapshotPipeline` 与 `DefaultExpressionSnapshotPipeline`
- 这一步的意义不是新增对外接口，而是把主模组内部已经存在的散件正式串成一条总表生成链：
- 来源收集
- 来源过滤
- 单侧整理
- `Special` 武器拦截
- 双侧高层重算
- 总表装配
- 当前组合层仍然保持诚实空壳，没有在未定规则词典前硬造双武器或组合技
- 随后继续对齐《芯片标准结构说明》，把稳定公共声明信息继续往内部链传递：
- `FormalExpressionResult` 新增 `RoleKey / Tags / Trion`
- `DefaultSingleSideExpressionBuilder` 开始把这些公共声明信息带入正式结果
- 这样后续投影层和诊断层不需要反查芯片配置才能知道结果的基础身份
- 接着把说明投影从纯空实现推进到“最小诚实输出”：
- `DefaultExpressionInfoProjector` 现在会读取正式总表
- 输出当前正式结果的最小文本说明
- 如有 `Special` 武器拦截，也会明确写出
- 这一步仍然只读正式结果，不回看内部解析过程，不越界读取隐藏实现

### 当前结论
- 表达系统现在已经有一条正式内部主链，而不是一组松散模块
- 配置层的公共声明信息已经能一路流到正式结果层
- 说明投影虽然还很简陋，但已经不是假空壳，主模组内部至少能读到“当前正式成立了什么”
- 当前仍然保留一个清楚的诚实边界：
- 组合层规则词典未建立
- 条件解释层未建立
- 所以这两部分还没有被偷做进主链

### 本次验证
- 已多次执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Pipeline/IExpressionSnapshotPipeline.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSnapshotPipeline.cs`
- `Source/BDP/Core/Expressions/Model/FormalExpressionResult.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultSingleSideExpressionBuilder.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultExpressionInfoProjector.cs`

### 本次工作
- 继续沿主模组内部骨架推进，优先补了条件层边界，而不是提前硬做组合技规则
- 原因很直接：
- 当前来源材料此前默认 `IsEnabled = true`
- 这会把“带条件但尚未解释”的来源错误地伪装成正式成立
- 已据此新增条件评估骨架：
- `ExpressionConditionEvaluation`
- `IExpressionConditionEvaluator`
- `DefaultExpressionConditionEvaluator`
- 当前默认条件评估器只诚实处理两种情况：
- 没有条件的来源，视为可成立
- 有条件但尚未接入正式解释层的来源，视为不可正式成立
- 同时把条件结构继续沿主链下传：
- `ExpressionSourceDeclaration` 新增 `Conditions`
- `ExpressionSourceMaterial` 新增 `ConditionEvaluation`
- `DefaultExpressionSourceDeclarationProvider` 现在会把配置层条件带进内部声明
- `DefaultExpressionSourceCollector` 现在会在收集来源材料时评估条件
- 并把 `IsEnabled` 改成依据条件评估结果，而不是默认真
- 这一步的意义不是实现了完整条件系统
- 而是先把“没有被正式解释的条件，不能被偷偷当作已满足”这条边界钉死

### 当前结论
- 表达系统的来源层现在比前一轮更诚实
- 无条件来源可以继续进入后续链路
- 有条件但未解释的来源会被明确挡在正式成立之外
- 这样后面接 CombatBody / Trion / 形态条件解释器时，不需要先返工纠正“默认全成立”的脏语义

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Model/ExpressionConditionEvaluation.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionSourceDeclaration.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionSourceMaterial.cs`
- `Source/BDP/Core/Expressions/Pipeline/IExpressionConditionEvaluator.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionConditionEvaluator.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceDeclarationProvider.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceCollector.cs`

### 本次工作
- 继续按计划推进条件层，但没有提前硬定具体条件词典
- 已确认：
- CombatBody 已有正式只读面
- Trion 已有正式只读面
- 形态条件仍缺正式读取口，当前不应偷摸 Trigger 私有实现
- 因此本轮重点不是“实现条件规则”，而是把条件解释层拆成可插拔的正式内部结构
- 新增：
- `IExpressionConditionInterpreter`
- `CombatBodyExpressionConditionInterpreter`
- `TrionExpressionConditionInterpreter`
- `ModeExpressionConditionInterpreter`
- `DefaultExpressionConditionEvaluator` 现在不再自己一把抓所有条件
- 而是负责：
- 遍历条件集合
- 把单条条件分发给对应分支解释器
- 汇总每条条件的评估结果
- 当前三类解释器的处理原则一致：
- 已识别到自己负责的条件分支
- 但具体词典尚未正式接入时，不伪装成已成立
- 这一步的价值是先把条件解释层按 owner 分支切干净：
- CombatBody 条件未来只从 CombatBody 正式读取面解释
- Trion 条件未来只从 Trion 正式读取面解释
- 形态条件未来只能等 Trigger 正式形态读取口落地后再接
- 不会把这些逻辑继续堆回一个总评估器里

### 当前结论
- 表达系统条件层现在已经具备“总汇总器 + 分支解释器”的正式骨架
- 当前还没有具体条件词典，这一点是故意保留的诚实空白
- 但后续接条件规则时，已经不需要再拆一次大结构

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Pipeline/IExpressionConditionInterpreter.cs`
- `Source/BDP/Core/Expressions/Pipeline/CombatBodyExpressionConditionInterpreter.cs`
- `Source/BDP/Core/Expressions/Pipeline/TrionExpressionConditionInterpreter.cs`
- `Source/BDP/Core/Expressions/Pipeline/ModeExpressionConditionInterpreter.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionConditionEvaluator.cs`

### 本次工作
- 继续按计划回到组合层，但仍然坚持“先补身份，再补规则”
- 当前组合层真正缺的不是空列表本身
- 而是高层结果的正式身份还不够清楚，容易以后又被压回一个模糊的 `Composite`
- 已据此新增：
- `CompositeExpressionKind`
- `CompositeExpressionReference`
- 其中：
- `CompositeExpressionKind` 用来区分双武器、组合技、非攻击联动三类高层结果
- `CompositeExpressionReference` 用来记录高层结果引用了哪些下层结果
- 不让组合层将来只能靠显示名或特判猜自己是什么
- 同时补了几处主链承接：
- `FormalExpressionResult` 新增 `CompositeKind`
- 单侧结果默认显式写成 `CompositeKind.None`
- `CompositeExpressionSet` 新增 `References`
- `DefaultCompositeExpressionResolver` 在当前仍返回空高层结果的前提下，也开始返回空引用列表
- 这说明现在组合层已经有了“结果身份 + 引用关系”的正式容器
- 即使规则词典还没接入，也不会继续长成一团没有身份的高层结果集合
- 另外把说明投影顺手补齐：
- `DefaultExpressionInfoProjector` 现在会把 `composite` 字段也输出出来
- 这样主模组内部调试时，能直接看出一条结果是不是高层合成结果，以及属于哪一类

### 当前结论
- 组合层现在虽然仍然没有正式规则词典
- 但“高层结果是什么”这件事已经有了稳定身份模型
- 这会让后续补双武器或组合技规则时，不必再返工重做高层结果模型

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Model/CompositeExpressionKind.cs`
- `Source/BDP/Core/Expressions/Model/CompositeExpressionReference.cs`
- `Source/BDP/Core/Expressions/Model/FormalExpressionResult.cs`
- `Source/BDP/Core/Expressions/Model/CompositeExpressionSet.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultSingleSideExpressionBuilder.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultCompositeExpressionResolver.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultExpressionInfoProjector.cs`

### 本次工作
- 继续按计划推进，没有转向外部公开面
- 补了一个主模组内部专用的表达系统运行时入口：
- `ExpressionRuntimeService`
- 这个入口的定位很明确：
- 只给主模组内部统一接入当前表达总表与说明投影
- 不构成对外 formal surface
- 不允许外部依赖它形成稳定契约
- 它内部只通过已有正式表面接上游系统：
- 通过 `TriggerSurfaceAccess` 读取 Trigger 正式激活面
- 条件解释分支中通过 CombatBody / Trion 正式读取面获取事实
- 不直接摸私有实现，不跳过边界
- 当前它能提供两件内部能力：
- `BuildSnapshot(Pawn)`
- `BuildInfoProjection(Pawn)`
- 这意味着表达系统现在不再只是“目录里有一堆模块”
- 而是已经有一条主模组内部真实可调用的收口入口
- 同时仍然保持内部运算链与未来对外表面分离：
- 这是内部 runtime service
- 不是外部 formal surface

### 当前结论
- 表达系统骨架现在已经具备：
- 上游声明配置层
- 来源收集与条件层
- 单侧整理
- Special 拦截
- 高层结果身份模型
- 总表装配
- 默认主结果选择
- 说明投影
- 主模组内部统一入口
- 到这里为止，主模组内部的“表达系统骨架”已经不再只是概念图，而是有一条真实内部主链

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Runtime/ExpressionRuntimeService.cs`

### 本次工作
- 用户指出要及时清理已经不需要的层，已回查当前 `Access` 与原 `Runtime` 的关系
- 结论很明确：
- 在追加 `Access/Surface` 层之后，`ExpressionRuntimeService` 已经只剩“转手再调一遍”的重复职责
- 它不再承担独立架构价值
- 已据此完成清理：
- 删除 `Source/BDP/Core/Expressions/Runtime/ExpressionRuntimeService.cs`
- 把原先 Runtime 里负责的总表拼装与说明投影调用逻辑，直接收回到 `ExpressionReaderSurface`
- 现在表达系统主模组内的正式访问路径已经更清楚：
- 外部系统先走 `ExpressionSurfaceAccess`
- 再拿 `IExpressionReader`
- 最终由 `ExpressionReaderSurface` 统一收口到内部主链
- 当前没有发现其它同等级的重复壳层
- 至少在“入口层”这个问题上，已经从：
- `Access -> Runtime -> Pipeline`
- 收敛成：
- `Access -> Pipeline`

### 当前结论
- `Runtime` 这层当前确实已经不需要，删除是正确的
- 表达系统现在的正式入口结构比前一轮更干净，没有再多留一层中转壳
- 需要强调的是：
- 这仍然不是程序集级权限封死
- 但至少从架构分层上，正式入口已经不再和内部中转层并存冲突

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过
- 已检索 `ExpressionRuntimeService`
- 结果：无剩余引用

### 本次涉及文件
- `Source/BDP/Core/Expressions/Access/Surfaces/ExpressionFormalSurfaces.cs`
- `Source/BDP/Core/Expressions/Runtime/ExpressionRuntimeService.cs`

### 本次工作
- 已回到表达系统骨架主任务，继续补剩余缺口
- 当前最关键的新发现是：
- 需求文档明确要求表达系统能读“芯片当前形态”
- 但主模组里还没有正式形态系统
- 所以这一步不能去硬造形态实现，也不能继续靠口头约定
- 已据此先补了 Trigger 的最小正式读取位：
- `ITriggerLoadoutReader.GetChipModeKey(Thing chip)`
- `TriggerLoadoutReaderSurface.GetChipModeKey(Thing chip)`
- `CompTriggerBody.GetChipModeKey(Thing chip)`
- 当前 `CompTriggerBody` 的实现明确返回 `null`
- 这是故意的诚实空值，表达的是：
- 正式读取位已经存在
- 但主模组当前还没有正式形态真值
- 同时把表达来源声明提供器的签名也扩成：
- `GetDeclarations(Thing chip, ITriggerLoadoutReader triggerLoadoutReader)`
- 这样后续做形态覆盖时，可以从正式读取口拿当前形态键
- 不需要再回头去偷摸 Trigger 私有状态
- 当前实现里虽然已经把形态读取位接进提供器签名
- 但并没有伪造任何形态覆盖逻辑

### 当前结论
- 形态系统虽然还没实现
- 但“表达系统如何合法读取形态事实”这条正式边界已经立起来了
- 这能避免后续形态功能落地时，再返工改访问面

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Trigger/Access/Contracts/ITriggerLoadoutReader.cs`
- `Source/BDP/Core/Trigger/Access/Surfaces/TriggerFormalSurfaces.cs`
- `Source/BDP/Core/Trigger/State/CompTriggerBody.Reads.cs`
- `Source/BDP/Core/Expressions/Pipeline/IExpressionSourceDeclarationProvider.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceDeclarationProvider.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceCollector.cs`

### 本次工作
- 继续按计划推进表达系统本体，重点补“当前形态键如何沿声明链传递”的结构位
- 当前并没有去实现真实形态覆盖逻辑
- 只是在配置层和结果链上把未来需要的锚点先钉死
- 已新增：
- `ChipExpressionModeConfig`
- 并让 `ChipExpressionConfig` 正式承认 `Modes` 区块
- 这对应需求文档里芯片总结构中的 `modes`
- 当前第一版仍然只立结构位，不提前写死覆盖算法
- 同时把 `ModeKey` 逐层带入：
- `ExpressionSourceDeclaration.ModeKey`
- `ExpressionSourceMaterial.ModeKey`
- `FormalExpressionResult.ModeKey`
- 目前基础层来源统一写 `ModeKey = null`
- 语义是：
- 它来自基础层声明
- 还不是某个具体形态覆盖块
- 另外把说明投影也补了最小可读输出：
- `DefaultExpressionInfoProjector` 现在会额外输出 `modeKey`
- 这样主模组内部调试时可以直接看出一条结果是否已经带有形态键

### 当前结论
- 表达系统现在已经不仅有“形态读取位”
- 还具备了“形态键沿声明链和结果链传递”的正式结构
- 这意味着后续形态覆盖系统落地时，不需要再返工补模型字段
- 只需要把真实覆盖规则和合并策略接入即可

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Config/ChipExpressionModeConfig.cs`
- `Source/BDP/Core/Expressions/Config/ChipExpressionConfig.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionSourceDeclaration.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionSourceMaterial.cs`
- `Source/BDP/Core/Expressions/Model/FormalExpressionResult.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceDeclarationProvider.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceCollector.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultSingleSideExpressionBuilder.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultExpressionInfoProjector.cs`

### 本次工作
- 继续沿“形态键承接结构”向前推进，但仍然没有越界去发明完整形态覆盖规则
- 当前新增的最小落地是：
- `DefaultExpressionSourceDeclarationProvider` 现在不只会追加基础层来源声明
- 还会在当前形态键命中某个 `ChipExpressionModeConfig` 时，追加该形态块自己的来源声明
- 这一步的规则非常克制：
- 只做“精确命中当前形态块，则把该块来源加入声明链”
- 不做删除基础层来源
- 不做覆盖优先级
- 不做冲突解决
- 不做多层继承
- 这样做的目的不是宣布形态系统完成
- 而是把形态块从“只存在于配置结构里”推进到“能够正式进入声明链”
- 同时继续保留诚实边界：
- 真实形态合并算法仍然不存在
- 当前只是基础层声明 + 当前命中形态块声明并列进入后续链路

### 当前结论
- 形态相关骨架现在已经推进到第三步：
- Trigger 有正式形态读取位
- 配置层有 `Modes` 结构
- 当前命中的形态块来源能进入声明链
- 这说明表达系统骨架本身已经不再缺“形态接入通道”
- 后续真正要补的已经更集中在：
- 形态合并规则词典
- 高层组合规则词典

### 任务进度
- 如果只看“表达系统骨架是否正式搭起来”，当前大致可认为已完成约 `70% - 80%`
- 已经完成的主要是：
- 目录与分层
- Config / Access / Model / Pipeline / Projection 骨架
- 正式入口
- 来源声明链
- 条件层骨架
- Special 拦截骨架
- 高层结果身份模型
- 说明投影最小读面
- 仍然明显未完成的主要是：
- 形态真实合并规则
- CombatBody / Trion / 形态条件词典
- 双武器 / 组合技 / 非攻击联动规则词典
- 更完整的手动入口 / 视觉 / 兼容投影

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceDeclarationProvider.cs`


### 本次工作
- 继续按表达系统执行计划推进，没有转向外部读取面
- 先补出了表达系统内部缺失的“芯片表达声明配置层”，新增了：
- `ChipExpressionConfig`
- `VerbExpressionSourceConfig`
- `AbilityExpressionSourceConfig`
- `HediffExpressionSourceConfig`
- `PassiveExpressionSourceConfig`
- `VerbExpressionModeConfig`
- 这层配置被明确放在 `Core/Expressions/Config`，用于承载芯片 Def 对表达来源的正式上游声明
- 同时保留 `Model` 只承载运行时结果对象，不把声明配置重新塞回结果模型
- 已把 `DefaultExpressionSourceDeclarationProvider` 从“永远返回空”推进到“能读取芯片 Def 上的表达声明配置，并翻译成内部 `ExpressionSourceDeclaration`”
- 中途编译抓出一个真实边界问题：
- 公开配置类不能直接引用内部运行时枚举
- 已据此把配置层武器模式枚举与运行时武器模式枚举拆开，避免配置契约直接泄漏内部结果模型
- 随后继续把 `DefaultExpressionSourceCollector` 从空壳推进到最小可工作的诚实收集器：
- 只读取 Trigger 正式激活槽位
- 只读取这些槽位里芯片的正式表达声明
- 跳过双手绑定镜像副本，避免镜像槽位被当成独立表达来源重复收集
- 把声明翻译成 `ExpressionSourceMaterial`
- 仅构建最小语义上下文，不提前做结果裁定

### 当前结论
- 表达系统主链现在已经不再停留在纯空壳阶段，而是打通了：
- 芯片声明配置
- 来源声明读取
- 正式激活槽位收集
- 来源材料表生成
- 当前仍然保持架构边界干净：
- Config 是上游声明契约层
- Model 是运行时结果层
- Pipeline 只负责内部翻译与计算
- 尚未重新把高层合成、投影、外部表面混回收集链

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Config/ChipExpressionConfig.cs`
- `Source/BDP/Core/Expressions/Config/VerbExpressionSourceConfig.cs`
- `Source/BDP/Core/Expressions/Config/AbilityExpressionSourceConfig.cs`
- `Source/BDP/Core/Expressions/Config/HediffExpressionSourceConfig.cs`
- `Source/BDP/Core/Expressions/Config/PassiveExpressionSourceConfig.cs`
- `Source/BDP/Core/Expressions/Config/VerbExpressionModeConfig.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceDeclarationProvider.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceCollector.cs`
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`
- `docs/plans/2026-03-25-表达系统执行计划-第一版.md`

### 本次工作
- 已基于表达系统骨架设计文档，继续生成完整执行计划文档
- 计划文档不是只写第一批实现，而是把：
- 目录落点
- 结果对象
- 计算接口
- 计算实现
- 默认投影三线
- 手动入口
- 视觉
- 说明
- 四大类表达补齐
- 文档回收停点
  全部写成了明确任务序列
- 这样后续实现时，不需要再靠记忆回想“后面还要做什么”

### 当前结论
- 当前关于表达系统，已经同时有：
- 骨架设计文档
- 完整执行计划文档
- 后续如果要进入实现，已经不再缺少路线层文档

### 本次涉及文件
- `docs/plans/2026-03-25-表达系统执行计划-第一版.md`

### 本次工作
- 用户提醒要先想清楚哪些属于内部，哪些才应该对外开放
- 已据此先把刚落下来的表达系统模型对象全部收口为 `internal`
- 同时在骨架设计文档里补上目录层级可见性裁定：
- `Core/Expressions/Model`
- `Core/Expressions/Pipeline`
- `Core/Expressions/Projection`
  当前都属于主模组内部层
- 后续如果需要正式对外读取面，应单独建立 formal surface，不允许直接把内部模型和内部运算模块公开出去
- 已重新编译主模组，构建通过

### 当前结论
- 表达系统当前新落下来的骨架仍处于“内部运算层”阶段，没有把未定型的内部结构误暴露成正式外部边界

### 本次涉及文件
- `Source/BDP/Core/Expressions/Model/ExpressionResultKind.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionOriginKind.cs`
- `Source/BDP/Core/Expressions/Model/FormalExpressionResult.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionSourceMaterial.cs`
- `Source/BDP/Core/Expressions/Model/SingleSideExpressionSet.cs`
- `Source/BDP/Core/Expressions/Model/CompositeExpressionSet.cs`
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`

### 本次工作
- 按小块推进方式，继续为表达系统补下列内部骨架：
- 内部运算接口第一批：
  - `IExpressionSourceCollector`
  - `IExpressionSourceFilter`
  - `ISingleSideExpressionBuilder`
  - `ISpecialWeaponOverrideResolver`
  - `ICompositeExpressionResolver`
  - `IExpressionSnapshotAssembler`
- 内部结果对象补齐：
  - `ExpressionSnapshot`
  - `SpecialWeaponOverrideResolution`
- 投影接口与最小投影结果对象第一批：
  - `IPrimaryExpressionSelector`
  - `IManualEntryProjector`
  - `IVisualProjectionBuilder`
  - `IExpressionInfoProjector`
  - `ManualEntryProjection`
  - `VisualExpressionProjection`
  - `ExpressionInfoProjection`
- 以上新增内容全部保持 `internal`，并且每一小块完成后都重新编译验证

### 当前结论
- 表达系统当前已经拥有可继续落实现的内部骨架，但仍然没有误开放成正式外部边界
- 当前适合继续向前推进最小实现，不需要回头再补更多抽象壳层

### 本次涉及文件
- `Source/BDP/Core/Expressions/Pipeline/IExpressionSourceCollector.cs`
- `Source/BDP/Core/Expressions/Pipeline/IExpressionSourceFilter.cs`
- `Source/BDP/Core/Expressions/Pipeline/ISingleSideExpressionBuilder.cs`
- `Source/BDP/Core/Expressions/Pipeline/ISpecialWeaponOverrideResolver.cs`
- `Source/BDP/Core/Expressions/Pipeline/ICompositeExpressionResolver.cs`
- `Source/BDP/Core/Expressions/Pipeline/IExpressionSnapshotAssembler.cs`
- `Source/BDP/Core/Expressions/Projection/IPrimaryExpressionSelector.cs`
- `Source/BDP/Core/Expressions/Projection/IManualEntryProjector.cs`
- `Source/BDP/Core/Expressions/Projection/IVisualProjectionBuilder.cs`
- `Source/BDP/Core/Expressions/Projection/IExpressionInfoProjector.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionSnapshot.cs`
- `Source/BDP/Core/Expressions/Model/SpecialWeaponOverrideResolution.cs`
- `Source/BDP/Core/Expressions/Model/ManualEntryProjection.cs`
- `Source/BDP/Core/Expressions/Model/VisualExpressionProjection.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionInfoProjection.cs`

### 本次工作
- 用户明确要求：表达系统设计文档不能只写“先做的部分”，必须把完整后续路线一并写清，否则实现时容易中途迷路
- 已据此把表达系统骨架文档继续扩成“完整落地路线”版本
- 新增内容包括：
- 最终完整落地目标
- 完整阶段拆分
- 每阶段目标与完成标准
- 固定停点
- 长期禁忌
- 未来扩展预留边界
- 当前这份文档已经不只是骨架说明，而是表达系统从内部结果链到多条投影层的正式路线锚点

### 当前结论
- 表达系统设计现在已经同时覆盖：
- 当前骨架
- 模块映射
- 完整落地路线
- 后续再推进时，不应再靠对话记忆回想“后面还要补什么”

### 本次涉及文件
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`

### 本次工作
- 已继续把表达系统骨架文档向“可落实现”的方向推进
- 新增并钉死了：
- 哪些部分先作为结果对象
- 哪些部分先作为计算接口
- 各模块的输入输出关系
- 当前明确不允许再长出的壳层
- 当前最需要重点防膨胀的几个模块
- 这一步的目标是让后续进入实现前，表达系统不再依赖口头记忆，而是已经有一份接近“对象到接口映射”的正式锚点

### 当前结论
- 表达系统的骨架设计已经从“层级概念”推进到“对象/模块/接口职责映射”层
- 继续往下时，最自然的下一步已不是再谈抽象方向，而是开始收第一版实现顺序与最小落地切片

### 本次涉及文件
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`

### 本次工作
- 用户进一步指出：对表达系统来说，装载事实与禁用事实不必直接作为输入，因为“正式激活”本身已经意味着已装载且未禁用
- 已据此收紧骨架文档中的边界描述：
- Trigger 向表达系统提供的上游事实，改为以“当前正式激活结果”为主
- “已装载 / 未禁用”不再被表达系统重复当作独立输入条件
- 同时保持：芯片侧别、芯片形态、表达系统自身仍需判断的业务成立条件，继续留在来源材料层

### 当前结论
- 表达系统上游输入边界比前一版更干净
- Trigger 先消化装载与禁用，表达系统只接已经成立的激活结果，这样更符合 owner 分责

### 本次涉及文件
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`

### 本次工作
- 已按《芯片标准结构说明》继续回审并纠正表达系统偏差，不再沿用 `VerbPropsKey`
- `Verb` 来源现已改为正式承载 `verbProps`，并沿 `VerbExpressionSourceConfig -> ExpressionSourceDeclaration -> ExpressionSourceMaterial -> FormalExpressionResult` 全链传递
- 同时把芯片文档要求但之前未贯通的最小字段补进正式链路：
- `AbilityDefName`
- `HediffDefName`
- `ApplyModeKey`
- `PassiveKey`
- `ExposedData`
- `ExpressionInfoProjection` 现在改为输出 `verbClass / abilityDef / hediffDef / applyMode / passiveKey` 这些正式来源信息，不再传播 `VerbPropsKey`
- `BorderDefenseProtocol.DevHarness` 的测试芯片 Def 已从旧 `BdpExpressionConfig` 改回 `ChipExpressionConfig + VerbSources + VerbProps`

### 当前结论
- 表达系统现在与《芯片标准结构说明》在“来源最小结构”这一层已重新对齐
- 当前剩余缺口不再是“芯片来源结构没接上”，而是这些正式来源如何投影到宿主实例与视觉/信息读取面

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过
- 已执行 `dotnet msbuild BDP.DevHarness.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Config/VerbExpressionSourceConfig.cs`
- `Source/BDP/Core/Expressions/Config/PassiveExpressionSourceConfig.cs`
- `Source/BDP/Core/Expressions/Config/PassiveExpressionExposedDatumConfig.cs`
- `Source/BDP/Core/Expressions/Model/PassiveExpressionExposedDatum.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionSourceDeclaration.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionSourceMaterial.cs`
- `Source/BDP/Core/Expressions/Model/FormalExpressionResult.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionInfoProjectionEntry.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceDeclarationProvider.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultExpressionSourceCollector.cs`
- `Source/BDP/Core/Expressions/Pipeline/DefaultSingleSideExpressionBuilder.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultExpressionInfoProjector.cs`
- `BorderDefenseProtocol.DevHarness/1.6/Defs/Trigger/ThingDefs_BDP_TestChips.xml`
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`
- `docs/plans/2026-03-25-表达系统执行计划-第一版.md`
- `docs/需求说明/2026-03-24-当前实现对照与需求缺口说明-第一版.md`

### 本次工作
- 已把 `Task 12` 从“桥接已接线但诚实返回空”推进到“能解析真实 Verb 宿主实例”
- 新增：
- `IExpressionVerbHostResolver`
- `DefaultExpressionVerbHostResolver`
- 当前默认 resolver 直接基于正式结果里的 `verbProps` 创建并缓存真实 `Verb`，宿主绑定到当前主装备，不回退到 Trigger 私有缓存
- `ExpressionVerbBridge` 已改为真正调用 resolver，因此自动远程 / 自动近战入口现在不再是空桥

### 当前结论
- `Task 12` 当前最小闭环已成立：
- 表达系统能选出默认主远程 / 默认主近战
- 并能把这条正式结果翻译成原版可消费的真实 `Verb`

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过
- 已执行 `dotnet msbuild BDP.DevHarness.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Projection/IExpressionVerbHostResolver.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultExpressionVerbHostResolver.cs`
- `Source/BDP/Core/Expressions/Projection/ExpressionVerbBridge.cs`

### 本次工作
- 已把 `Task 13` 从“只有正式形状、没有真实入口”推进到“最小手动入口闭环”
- `DefaultManualEntryProjector` 现已按当前最小规则，为每条可投影 `Verb` 正式结果生成一个手动入口组和一个主入口项
- 新增：
- `IManualEntryGizmoResolver`
- `DefaultManualEntryGizmoResolver`
- 当前默认 resolver 按“每组一个主入口按钮”把正式手动入口翻译成最小 `Command_VerbTarget`
- `ExpressionManualGizmoBridge` 现已真正把正式手动入口投影接到 Trigger 按钮宿主

### 当前结论
- `Task 13` 当前最小闭环已成立，但仍是克制实现：
- 还没有副入口
- 还没有复杂分组
- 还没有组合技 / 双武器专属按钮策略
- 但已经不再是空投影或空按钮桥

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过
- 已执行 `dotnet msbuild BDP.DevHarness.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Projection/DefaultManualEntryProjector.cs`
- `Source/BDP/Core/Expressions/Projection/IManualEntryGizmoResolver.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultManualEntryGizmoResolver.cs`
- `Source/BDP/Core/Expressions/Projection/ExpressionManualGizmoBridge.cs`

### 本次工作
- 已继续推进 `Task 14` 的最小视觉投影闭环
- `DefaultVisualProjectionBuilder` 现已不再只返回全空壳：
- 常驻显示结果会收集当前可投影的 `Verb` 结果
- 执行跟随读取当前执行表达
- 枪口跟随优先跟随当前执行表达，否则回退到默认主远程
- 视觉关系类型会优先根据当前已成立的高层结果识别 `Combo / DualWeapon`，否则在存在常驻结果时识别为 `SingleSide`
- 当前仍明确保持：
- `UseDualSideVisualMask = false`
- 原因不是忘了做，而是当前正式结果里还没有双手绑定视觉伪装所需的正式依据，不能伪造

### 当前结论
- `Task 14` 的最小可读视觉结果已成立
- 但双手锁定视觉双侧伪装仍是后续缺口，需要先把对应正式依据带进表达结果链

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过
- 已执行 `dotnet msbuild BDP.DevHarness.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Projection/DefaultVisualProjectionBuilder.cs`

### 本次工作
- 用户确认：`Task 14` 当前若缺口不影响大局，可先冻结并转向 `Task 15`
- 已据此把 `Task 14` 当前已知缺口明确收口为后续补项：
- 双手锁定视觉双侧伪装仍未正式落地
- 当前原因不是实现偷懒，而是正式结果链里尚未保留这条视觉语义所需的正式依据
- 在这条依据进入表达结果链之前，视觉层继续保持诚实输出，不伪造 `UseDualSideVisualMask`

### 当前结论
- `Task 14` 当前状态可视为：
- 最小视觉读取结果已成立
- 双手锁定视觉双侧伪装作为后续正式缺口保留
- 不阻塞继续推进 `Task 15`

### 本次工作
- 已继续推进 `Task 15`，把说明投影从“结果列表”补到“正式可消费的摘要 + 标记”层
- `ExpressionInfoProjection` 新增：
- `场景默认主攻击决策结果（不再保留固定字段）`
- `HasSpecialWeaponOverride`
- `ExpressionInfoProjectionEntry` 新增：
- `IsPrimaryRanged`
- `IsPrimaryMelee`
- `是否命中场景默认主攻击决策（不再保留固定标记）`
- `IsCurrentExecuting`
- `DefaultExpressionInfoProjector` 现已额外输出：
- 特殊侧武器拦截开启/关闭
- 场景默认主攻击决策
- 当前执行表达
- 结果总表摘要行（total / verb / ability / hediff / passive / composite）
- 这样说明投影现在已不只适合纯文本调试，也能更稳定地给 UI / 调试面 / 后续正式读取面直接消费

### 当前结论
- `Task 15` 当前最小正式闭环已成立：
- 能说明全部正式结果
- 能说明 Main / Sub / Special / Composite 关系
- 能说明默认主远程 / 默认主近战 / 场景默认主攻击决策
- 能说明当前执行表达

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过
- 已执行 `dotnet msbuild BDP.DevHarness.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `Source/BDP/Core/Expressions/Model/ExpressionInfoProjection.cs`
- `Source/BDP/Core/Expressions/Model/ExpressionInfoProjectionEntry.cs`
- `Source/BDP/Core/Expressions/Projection/DefaultExpressionInfoProjector.cs`

### 本次工作
- 已继续推进 `Task 16`，但这轮不做四类宿主全量回接，而是先把四大类都送进正式主链并给出真实输入样本
- 当前判断是：
- `Verb` 已具备最完整链路
- `Ability / Hediff / Passive` 在代码主链上其实已经能从芯片声明一路进入正式结果与说明投影
- 当前更缺的是“真实样本输入”，而不是再继续空转加结构
- 已据此在 `DevHarness` 补入：
- `BDP_TestChipAbility`
- `BDP_TestChipHediff`
- `BDP_TestChipPassive`
- 三者分别使用：
- `AbilitySources`
- `HediffSources`
- `PassiveSources`
- 从而让四个顶层表达大类现在都已经有真实样本能进入表达系统主链

### 当前结论
- `Task 16` 当前这一轮的最小目标已成立：
- 四大类顶层表达现在都已被正式承认并具备样本输入
- `Ability / Hediff / Passive` 已能进入：
- 芯片声明层
- 来源材料层
- 正式结果层
- 说明投影层
- 当前仍未继续做的是：
- `Ability` 宿主实例与按钮桥
- `Hediff` 正式施加宿主
- `Passive` 对外消费系统
- 这些属于下一阶段宿主回接问题，不再是“是否进入表达系统主链”的问题

### 本次验证
- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过
- 已执行 `dotnet msbuild BDP.DevHarness.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

### 本次涉及文件
- `BorderDefenseProtocol.DevHarness/1.6/Defs/Trigger/ThingDefs_BDP_TestChips.xml`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/TriggerDevHarnessDefs.cs`

### 本次工作
- 用户补充确认：
- 默认主远程与默认主近战是两条并列默认读取线，不是互斥二选一
- 适合拆且能拆的表达系统模块应直接拆开，不为“以后可能方便”保留大块混合模块
- 已将这两条裁定补入表达系统骨架设计文档
- 同时把内部运算模块与投影模块的强制拆分清单写入文档，明确不再允许回退成“大而全表达运行时”或“投影服务一把抓”结构

### 当前结论
- 表达系统后续实现时，默认主攻击层必须天然支持“默认主远程 + 默认主近战”并列存在
- 表达系统内部应继续走“小模块串联”方向，而不是回到一个运行时对象包办收集、计算、投影的旧路

### 本次涉及文件
- `docs/plans/2026-03-25-表达系统骨架设计-第一版.md`
