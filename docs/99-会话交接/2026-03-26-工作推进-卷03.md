# 2026-03-26 工作推进 卷03

## 本轮任务

- `Task 7：接管自动近战入口`

## 本次工作

- 已按更新后计划执行 `Task 7`
- `Patch_Pawn_MeleeVerbs_TryMeleeAttack` 已不再直接向表达层索要默认主近战 verb
- 当前改为：
- patch 只向 `DefaultMeleeAttackExecutor.TryGetAutoAttackVerb()` 读取正式近战入口
- 由近战执行器先确认当前默认主近战结果是否合法
- 再把真实 verb 交回原版自动近战链
- `ExpressionVerbBridge` 已进一步收紧：
- 自动近战过渡读取口已删除
- 当前只保留 `TryResolveFormalVerb()` 作为执行器内部辅助桥
- `DefaultMeleeAttackExecutor` 当前已经同时承担两种职责：
- 正式近战执行时，把计划/结果送进原版近战 verb 启动链
- 自动近战读取时，作为自动近战入口的正式读取/分发口

## 当前结论

- 自动近战入口现在已经不再由 patch 直接临时决定执行对象
- 自动近战的正式起手点已经统一收回到近战执行器
- 近战落地链当前保持不变：
- `BdpVerb_MeleeAttackDamage`
- 原版近战伤害链
- 这一刀完成后，主模组现在可以更清楚回答：
- 自动近战谁起手：`DefaultMeleeAttackExecutor`
- 自动近战谁真正落地伤害：`BdpVerb_MeleeAttackDamage` 与原版近战伤害链

## 本次验证

- 已执行 `dotnet msbuild BDP.csproj -p:Configuration=Debug -t:Build -v:minimal`
- 结果：通过

## 本次涉及文件

- `Source/BDP/Patches/Patch_Pawn_MeleeVerbs_TryMeleeAttack.cs`
- `Source/BDP/Core/AttackExecution/DefaultMeleeAttackExecutor.cs`
- `Source/BDP/Core/Expressions/Projection/ExpressionVerbBridge.cs`
