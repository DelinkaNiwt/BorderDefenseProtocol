# 2026-04-24 工作推进 卷01

## 本轮完成

- 新增 `BDP（边境防御协议）` 标准芯片 XML 字段说明文档：
  - `docs/需求说明/2026-04-24-BDP标准芯片XML字段全表.md`
- 文档范围只覆盖：
  - 标准芯片 `ThingDef（物品定义）` 内的 `BDP（边境防御协议）` 主扩展；
  - 原版 / 引擎块只保留最外层入口，不展开内部字段；
  - `BDP（边境防御协议）` 直辖字段全部展开并加中文注释。
- 已按当前源码口径整理出：
  - 最低必填块：`Profile / Loadout / Expression / Trion`
  - 标准芯片主扩展下可统一枚举字段 / 节点位总数：`77`
- 已把下列子结构写进文档并逐项注释：
  - `ChipDefinitionConfig（芯片主扩展）`
  - `Profile（画像块）`
  - `Loadout（装载块）`
  - `Trion（芯片资源块）`
  - `Extensions（扩展块）`
  - `Expression（表达块）`
  - `Entries/li（基础表达条目）`
  - `Conditions/li（条件子项）`
  - `Modes/li（形态块）`
  - `Operations/li（形态操作子项）`
  - `Execution / OriginSpread（执行 / 散布块）`
  - `ExposedData / RangedModules（被动暴露数据 / 远程模块挂载）`

## 关键结论

- 标准芯片 XML 的真正业务核心，集中在：
  - `<modExtensions>`
  - `<li Class="BDP.Core.Chips.ChipDefinitionConfig">`
- `VerbProps（动作属性）`、`Tool（工具）`、`Maneuver（动作机动定义）` 属于原版对象，文档只保留入口，不展开内部字段。
- `<RangedModules><li><config>` 虽然入口属于 `BDP（边境防御协议）`，但内部结构由具体模块配置类决定，不存在统一固定字段表，所以文档只把它标记为“开放子树入口”。

## 自检结果

- 已做字段标签覆盖检查，结果：
  - `DOC_TAG_CHECK PASS`
