# 2026-04-23 工作推进 卷01

## 本轮完成

- 已修复毒蛇连续攻击对“最终实体目标”的停火优先级：
  - 若存在 `SemanticTarget（语义目标）` 且它是实体，则优先用它做存活校验。
  - 实体目标死亡、倒地、销毁或已脱离地图时，持续攻击 job 立即结束。
  - 只有不存在实体语义目标时，才回退到 `TargetA（任务目标）`。
- 已把语义目标读取入口补到正式远程宿主：
  - `BdpVerb_Shoot` 现在会优先从冻结攻击上下文读取 `ConfirmedTargetSnapshot.SemanticTarget`。
  - 若上下文里暂时没有，再回退读取当前待消费发射计划里的语义目标。
- 已新增专项烟雾测试：
  - `Source/BDP.Tests/RangedSemanticTargetPrioritySmokeTests.ps1`

## 验证

- `Source/BDP.Tests/RangedSemanticTargetPrioritySmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/RangedSessionLossDiagnosticsSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/RangedContinuationSessionTargetSmokeTests.ps1`
  - PASS
- `Source/BDP/BDP.csproj`
  - `dotnet msbuild` PASS
