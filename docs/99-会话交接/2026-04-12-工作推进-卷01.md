# 2026-04-12 工作推进 卷01

## 本卷范围

- 主线：毒蛇样本正式设计收口
- 约束：文档写入主模组目录；不改主模组代码；不改主模组配置

## 本轮完成

- 已再次确认主线结论：
  - 主模组当前架构对毒蛇盲验样本**够用**
- 已确认毒蛇落地边界：
  - 只允许后续写在 `DevHarness（测试模组）`
  - 不允许为毒蛇回改主模组正式代码
- 已把毒蛇正式设计文档写入：
  - `docs/plans/2026-04-12-毒蛇锚点路径制导模块设计-第一版.md`

## 本轮设计裁定

- 模块名定为：
  - `AnchorPathGuidanceModule（锚点路径制导模块）`
- 芯片名定为：
  - `BDP_TestChipViper（测试毒蛇芯片定义名）`
- 结束规则定为：
  - 主动确认结束优先
  - 收满固定点数次之
- 状态分层定为：
  - `AnchorPathNode（锚点路径节点）` 只保存冻结原文
  - `AnchorPathCastCursor（锚点路径单枪游标）` 只保存单枪运行态
- 防串脏规则定为：
  - 不允许整次攻击会话共用可变游标
  - 每个 `Cast（单枪次）` 必须重建本枪游标
  - `Flight（飞行阶段）` 只消费，不回写原文

## 这样定的原因

- 旧问题已经明确指向“同一攻击会话内跨枪状态污染”。
- 主模组现有 `AttackContext（攻击上下文）`、`AttackContextSnapshot（攻击上下文快照）`、`Targeting（瞄准阶段）` 持续交互能力都已足够。
- 现在继续碰主模组，只会把业务模块问题错误上升成架构问题。

## 当前未做

- 还没有开始写毒蛇实现代码。
- 还没有开始写毒蛇测试模组 Def（定义）与模块类。
- 还没有进游戏做毒蛇专用实测。

## 追加记录 01

- 已按反馈重写毒蛇设计文档。
- 已删除对话总结式表述。
- 已改为正式设计文档结构：
  - 文档范围
  - 设计目标
  - 非目标
  - 数据模型
  - 阶段设计
  - 状态归属
  - 性能约束
  - 诊断日志
  - 验收标准

## 下一执行入口

- 后续如果进入编码阶段，应直接以本文档为唯一设计基线，在 `DevHarness（测试模组）` 内开工。

## 追加记录 02

### 本轮范围

- 修补主模组 `Targeting（瞄准阶段）` 输入事实缺口

### 本轮完成

- 已新增中性输入枚举：
  - `TargetingInputButton（目标输入按钮）`
  - `TargetingInputModifiers（目标输入修饰键）`
- 已新增中性运行时输入桥：
  - `TargetingInputRuntimeFacts（目标输入运行时事实）`
  - `TargetingInputRuntimeScope（目标输入运行时作用域）`
- 已让 `TargetingInputFrame（目标输入帧）` 正式携带：
  - `PressedButton（按下按钮）`
  - `Modifiers（修饰键）`
- 已新增原版薄桥补丁：
  - `Patch_Targeter_OrderPawnForceTarget_TargetingInput`
- 已让 `AttackExecutionTargetingSource（攻击执行瞄准来源）` 在构建输入帧时读取运行时输入作用域

### 关键裁定

- 这次补的是“输入事实层”
- 不是“毒蛇锚点业务层”
- 主模组只知道：
  - 哪个按钮
  - 哪些修饰键
- 主模组不知道：
  - 锚点
  - 路径
  - 终点

### 验证

- `Source/BDP.Tests/TargetingInputModifierBridgeSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/RangedTargetingProtocolSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/RangedTargetingSessionContinuitySmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/RangedTargetingDriverBoundarySmokeTests.ps1`
  - PASS
- `Source/BDP/BDP.csproj`
  - PASS

## 追加记录 03

### 本轮范围

- 基于新的中性输入事实桥，重写毒蛇设计文档

### 本轮完成

- 已重写毒蛇交互模型：
  - `Shift（换挡键） + 左键` 设锚点
  - 普通左键选最终目标
  - 右键 / `Esc（退出键）` 取消
- 已把最终目标正式扩成：
  - `Pawn（人物）`
  - `Thing（物体）`
  - `Cell（地面格）`
- 已删除旧版“先走采集流程再结束”的错误交互前提
- 已把毒蛇设计文档改成以“目标为主线、锚点为插入式修饰”的正式结构

## 追加记录 04

### 本轮范围

- 修补主架构 `Targeting（瞄准阶段）` 的段合法性查询缺口

### 本轮完成

- 已新增中性查询对象：
  - `TargetingSegmentLegalityRequest（段合法性请求）`
  - `TargetingSegmentLegalityResult（段合法性结果）`
- 已新增中性查询服务：
  - `ITargetingSegmentLegalityService（段合法性服务）`
  - `DefaultTargetingSegmentLegalityService（默认段合法性服务）`
- 已让 `TargetingRecord（目标记录）` 暴露：
  - `SegmentLegality（段合法性查询面）`

### 关键裁定

- 这次补的是：
  - “任意起点 -> 候选目标”的中性合法性查询口
- 不是：
  - 路径业务实现
  - 锚点规则实现
- 默认实现只复用：
  - `TargetingParameters（目标参数）`
  - `Verb.CanHitTargetFrom（从指定起点命中目标）`

### 验证

- `Source/BDP.Tests/TargetingSegmentLegalitySmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/RangedTargetingProtocolSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/RangedTargetingSessionContinuitySmokeTests.ps1`
  - PASS
- `Source/BDP/BDP.csproj`
  - PASS

## 追加记录 05

### 本轮范围

- 把毒蛇设计文档接到新的段合法性查询口

### 本轮完成

- 已把毒蛇文档正式声明依赖：
  - `ITargetingSegmentLegalityService（Targeting 段合法性服务）`
- 已把锚点段与终点段的处理改成：
  - 先查合法性
  - 非法即 `Reject（拒绝）`
- 已补责任边界章节：
  - 毒蛇模块负责什么
  - 主模组负责什么
  - 原版 / 现有 BDP 负责什么
- 已把“非法段视觉反馈”和“发射前复核失败”写入验收与日志要求

## 追加记录 06

### 本轮范围

- 继续收口毒蛇终点语义与动态失效规则

### 本轮完成

- 已给毒蛇终点补正式语义：
  - 地面终点 = 固定地面格
  - 对象终点 = 保持对象语义
- 已给路径原文与单枪游标补：
  - `TerminalCellSnapshot（终点位置快照）`
- 已写死对象终点失效处理：
  - 当前枪次中止
  - 不回落成地面终点
- 已把相关日志与验收项补进文档
