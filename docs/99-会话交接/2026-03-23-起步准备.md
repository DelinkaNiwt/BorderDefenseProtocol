# 2026-03-23 起步准备

## 当前已完成

- 新建 `BorderDefenseProtocol` 目录
- 建立最小模组骨架
- 建立最小文档目录
- 写入起步约束
- 写入中文文件与终端规则
- 写入第一条决策记录
- 初始化新项目自己的 git 仓库
- 设定 git 中文路径与输出编码配置
- 放开旧版仓库 safe.directory，后续可读取历史
- 写出第一版“旧 BDP 领域地图”研究记录
- 写出第一版“旧 BDP 与 RimWorld 原版宿主映射”研究记录
- 写出第一版“三核心边界草案”

## 当前已确认

- 二次重构只基于旧 BDP
- 中文优先
- 务实有效优先，不搞过重工程形式
- 起步阶段先做准备和研究，不急着开始重构实现
- 中文文件当前采用 UTF-8
- 手工编辑当前统一走 `bdp_ascii` 映射路径

## 当前风险

- 终端默认编码不是 UTF-8，直接读中文文件仍可能乱码
- `apply_patch` 当前不可用，暂时只能用明确 UTF-8 写入方式落文件
- 旧版历史文档很多，不能直接当新项目规范
- 三核心边界草案还是第一版，还没经过更细的旧代码对照

## 下一步

- 继续细读旧版里 Trion / 战斗体 / 触发体 的互相调用点
- 把“谁在直接改谁的真值”列成问题清单
- 在此基础上提出第一版正式核心设计草案
