# 2026-04-18 工作推进 卷01

## 本卷范围

- 主线：执行“毒蛇通用骨架轻量上提”实施计划
- 约束：
  - 不用 `worktree（独立工作树）`
  - 直接在当前项目改
  - 逐成员补 `XML（结构化）` 注释

## 本轮完成

- 已新增口径文档：
  - `docs/01-决策记录/2026-04-18-远程目标语义与合法性边界口径-第一版.md`
- 已收口目标与合法性注释边界：
  - `ConfirmRecord（确认阶段记录）`
  - `ConfirmedTargetSnapshot（已确认目标冻结快照）`
  - `ProjectileInitPlan（投射物初始化计划）`
  - `ArrivalContribution（到达阶段贡献）`
  - `TargetingRecord（瞄准阶段记录）`
  - `TargetingSegmentLegalityRequest（段合法性请求）`
  - `DefaultTargetingSegmentLegalityService（默认段合法性服务）`
  - `AttackExecutionTargetingSource（攻击执行瞄准来源）`

## 当前裁定

- 主模组这次只上提轻量骨架：
  - 统一目标口径
  - 统一冻结口径
  - 统一语义目标默认解析口径
  - 统一单段合法性边界口径
- 不上提毒蛇专属业务：
  - 路径
  - 锚点
  - 业务专属最终确认规则

## 追加记录 01

- 已按 `TDD（测试先行开发）` 新增失败测试：
  - `Source/BDP.Tests/AttackExecutionSemanticTargetResolverSmokeTests.ps1`
- 已新增共享解析器：
  - `Source/BDP/Core/AttackExecution/AttackExecutionSemanticTargetResolver.cs`
- 已把重复语义目标解析从以下位置收拢到共享入口：
  - `AttackExecutionService.Stages（攻击执行阶段服务）`
  - `RangedAttackProtocolService（远程攻击协议服务）`

## 追加记录 02

- 已补强边界烟测：
  - `Source/BDP.Tests/TargetingSegmentLegalitySmokeTests.ps1`
- 已把以下口径锁死为“只管当前候选点或单段，不管最终确认”：
  - `TargetingRecord（瞄准阶段记录）`
  - `AttackExecutionTargetingSource（攻击执行瞄准来源）`
  - `DefaultTargetingSegmentLegalityService（默认段合法性服务）`

## 验证

- `Source/BDP.Tests/DualRangedConfirmedTargetSnapshotSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/TargetingSegmentLegalitySmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/AttackExecutionSemanticTargetResolverSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/DualRangedNecessaryLosSemanticsSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/RangedProtocolBoundarySmokeTests.ps1`
  - PASS
- `Source/BDP/BDP.csproj`
  - PASS

## 追加记录 03

### 本轮范围

- 为“毒蛇右键取消越权”和“步骤同步灰区”整理正式实施计划

### 本轮完成

- 已新增计划文件：
  - `docs/plans/2026-04-18-毒蛇右键取消与步骤同步边界收口实施计划-第一版.md`
- 已明确这次修复目标：
  - 不改玩家可感知手感
  - 只收回职责边界
  - 不扩主模组协议

### 下一执行入口

- 后续若开始落地，应直接按该计划做：
  - 先补 2 条失败烟测
  - 再做最小实现
  - 最后跑总回归与双工程编译

## 追加记录 04

### 本轮范围

- 正式实施“毒蛇右键取消越权”和“步骤同步灰区”收口

### 本轮完成

- 已新增烟测：
  - `Source/BDP.Tests/PathLatchRightClickCancelBoundarySmokeTests.ps1`
  - `Source/BDP.Tests/PathLatchStepIndexOwnershipSmokeTests.ps1`
- 已把毒蛇右键取消从 `Preview（预览阶段）` 收回到 `Targeting（目标选择阶段）` 正式输入链
- 已让毒蛇模块停止直写 `InteractionSession.StepIndex（交互会话步骤号）`
- 已保留：
  - 右键整次退出
  - `Shift + 左键` 追加锚点
  - 普通左键确认最终目标

### 验证

- `Source/BDP.Tests/PathLatchRightClickCancelBoundarySmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/PathLatchStepIndexOwnershipSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/TargetingInputModifierBridgeSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/RangedTargetingProtocolSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/RangedTargetingSessionContinuitySmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/DualRangedConfirmedTargetSnapshotSmokeTests.ps1`
  - PASS
- `Source/BDP/BDP.csproj`
  - PASS
- `Source/BDP.DevHarness/BDP.DevHarness.csproj`
  - PASS
