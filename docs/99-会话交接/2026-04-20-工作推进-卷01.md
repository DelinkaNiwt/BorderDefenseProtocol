# 2026-04-20 工作推进 卷01

## 本轮完成

- 已给 `BDP_TestChipPathLatchVolley`（测试毒蛇齐射路径芯片）追加爆炸模块挂载。
- 挂载模块：
  - `BDP_TestRangedPathLatchModule`
  - `BDP_TestRangedExplosiveModule`
- 爆炸范围按“直径 3 格”写为：
  - `ExplosionRadius = 1.5`
- 其它战斗参数保持原样：
  - 仍复用 `BDP_TestBulletSemantic`
  - 仍为 `Simultaneous（齐射）`
  - 仍为 `burstShotCount = 5`
  - 仍保留路线引导模块。

## 验证

- `Source/BDP.Tests/DevHarnessExplosiveRangedModuleSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/DevHarnessPathLatchVolleyChipSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/DevHarnessDynamicChipMenuSmokeTests.ps1`
  - PASS

---

## 本轮完成

- 已在 DevHarness（开发测试模组）实现爆炸远程模块：
  - `Source/BDP.DevHarness/RangedModules/Samples/ExplosiveModule.cs`
- 模块职责已落地：
  - `Preview（预览阶段）` 显示灰白色爆炸覆盖格。
  - `Impact（终结结算阶段）` 提交 `AreaEffectPlan（范围效果计划）`。
  - 真实爆炸继续复用主模组统一弹体宿主与原版 `GenExplosion.DoExplosion（原版爆炸执行接口）`。
- 已新增模块 Def：
  - `BDP_TestRangedExplosiveModule`
- 已新增测试芯片：
  - `BDP_TestChipExplosiveRanged`
- 已按最新约束落地：
  - 不新建投射物。
  - 继续复用 `BDP_TestBulletSemantic`
  - 预览颜色固定灰白色，不进入 XML（配置）。

## 验证

- `Source/BDP.Tests/DevHarnessExplosiveRangedModuleSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/RangedModuleSampleModulesSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/DevHarnessDynamicChipMenuSmokeTests.ps1`
  - PASS
- `Source/BDP.DevHarness/BDP.DevHarness.csproj`
  - `dotnet msbuild` PASS

---

## 本轮完成

- 已完成 DevHarness（开发测试模组）爆炸远程模块方案确认。
- 已按最新约束完成方案修订：
  - 不新建投射物。
  - 继续复用现有远程类测试芯片共享投射物。
  - 预览颜色固定灰白色，不进入 XML（配置）。
- 已写入设计文档：
  - `docs/plans/2026-04-20-DevHarness爆炸远程模块设计-第一版.md`
- 已写入实施计划：
  - `docs/plans/2026-04-20-DevHarness爆炸远程模块实施计划-第一版.md`
- 方案结论：
  - 新增独立爆炸模块。
  - 复用 BDP 的 `Preview（预览）` 与 `Impact（终结结算）` 阶段。
  - 复用原版 `GenExplosion.DoExplosion（原版爆炸执行接口）`。
  - 默认抑制基线单体命中，避免子弹直伤与爆炸伤害叠加。

## 后续建议

- 下一步按实施计划先写烟雾测试，再新增 `ExplosiveModule（爆炸模块）` 源码与 XML 定义。

---

## 本轮完成

- 已在 DevHarness（测试后置模组）新增测试芯片：
  - `BDP_TestChipPathLatchVolley`
- 新芯片以 `BDP_TestChipPathLatch` 为基底，只调整：
  - 远程节奏改为 `Simultaneous（齐射）`
  - `SpreadRadius（发射点散布半径）` 使用齐射芯片同款 `0.3`
  - `burstShotCount（发射数量）` 改为 `5`
- 毒蛇路径模块、非直线视线要求、射程、热身、冷却、声音、Trion 消耗等参数保持毒蛇基线不变。

## 验证

- `Source/BDP.Tests/DevHarnessPathLatchVolleyChipSmokeTests.ps1`
  - PASS
- `Source/BDP.Tests/DevHarnessDynamicChipMenuSmokeTests.ps1`
  - PASS
- `ThingDefs_TestChips_Combat.xml`
  - XML 解析 PASS
- `Source/BDP.DevHarness/BDP.DevHarness.csproj`
  - PASS
