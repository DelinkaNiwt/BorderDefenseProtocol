# 2026-03-23 工作推进-卷01

## 记录 03

### 本次做了什么

- 增加“正文只呈现最终结果”的工作原则
- 调整工作日志规则，固定为每 10 次记录合并到同一卷
- 新增第一阶段对象到接口映射表

### 当前结论

- 第一阶段的核心对象、接口承接关系、越界边界已经有了第一版落点
- 后面真正开写时，可以据此检查 owner 是否混乱、协调者是否变厚、投影器是否越界

### 本次涉及文件

- `docs/00-项目宪章/项目起步约束.md`
- `docs/plans/2026-03-23-第一阶段对象到接口映射表-第一版.md`

## 记录 04

### 本次做了什么

- 新增旧功能纳入时机清单
- 新增起步顺序与停点文档
- 把主线推进顺序固定成“资源 -> 战斗体 -> 装载 -> 表达 -> 投影 -> 收尾”

### 当前结论

- 现在不仅有范围和映射，也有了明确路线和暂停检查点
- 后面该深入的地方主要是资源语义、战斗体相位、入口桥接、收尾完整性
- UI、复杂组合、表现优化等内容明确不在当前起步主线里

### 本次涉及文件

- `docs/plans/2026-03-23-旧功能纳入时机清单-第一版.md`
- `docs/plans/2026-03-23-起步顺序与停点-第一版.md`

## 记录 05

### 本次做了什么

- 对照当前新项目 `Source` 骨架和旧 `CompTrion` 代码
- 新增第一批实现任务清单，专门覆盖资源闭环

### 当前结论

- 现在已经可以按文档直接开始第一批资源系统实现
- 第一批文件落点、任务顺序、停点和回查旧代码点都已明确

### 本次涉及文件

- `docs/plans/2026-03-23-第一批实现任务清单-资源闭环-第一版.md`

## 记录 06

### 本次做了什么

- 新增“二次重构总纲”文件
- 把根本任务、根本目标、最终检验方式、最终追求、硬约束、阶段路线集中整理到一个入口

### 当前结论

- 以后继续推进前，优先回看总纲即可快速校准方向
- 分散文档仍保留细节，总纲负责统一入口

### 本次涉及文件

- `docs/00-项目宪章/二次重构总纲-第一版.md`

## 记录 07

### 本次做了什么

- 实现了资源闭环第一版代码
- 实现了战斗体基础相位第一版代码
- 实现了触发体装载与局部激活第一版代码
- 增加了资源系统最小 XML 基底
- 两次编译验证均通过

### 当前结论

- `CompTrion` 已成为第一版资源真值 owner
- 战斗体当前被压成“相位真值 + 协调者 + 宿主桥”三层
- `CompTriggerBody` 当前只承担槽位、装载、激活、关闭，不再吞表达和 UI
- 触发体切换状态机仍未落实，当前只是最小激活骨架

### 本次涉及文件

- `Source/BDP/Core/Trion/ITrionState.cs`
- `Source/BDP/Core/Trion/ITrionEvents.cs`
- `Source/BDP/Core/Trion/CompProperties_Trion.cs`
- `Source/BDP/Core/Trion/CompTrion.cs`
- `1.6/Defs/Core/ThingDefs_BDP_TrionBases.xml`
- `Source/BDP/Core/CombatBody/CombatBodyPhase.cs`
- `Source/BDP/Core/CombatBody/ICombatBodyPhaseState.cs`
- `Source/BDP/Core/CombatBody/ICombatBodyCoordinator.cs`
- `Source/BDP/Core/CombatBody/ICombatBodyHost.cs`
- `Source/BDP/Core/CombatBody/CombatBodyState.cs`
- `Source/BDP/Core/CombatBody/PawnCombatBodyHost.cs`
- `Source/BDP/Core/CombatBody/CombatBodyCoordinator.cs`
- `Source/BDP/Core/Trigger/TriggerSide.cs`
- `Source/BDP/Core/Trigger/ITriggerSlotState.cs`
- `Source/BDP/Core/Trigger/ITriggerLoadoutState.cs`
- `Source/BDP/Core/Trigger/ITriggerActivationCoordinator.cs`
- `Source/BDP/Core/Trigger/TriggerSlotState.cs`
- `Source/BDP/Core/Trigger/TriggerActivationCoordinator.cs`
- `Source/BDP/Core/Trigger/CompProperties_TriggerBody.cs`
- `Source/BDP/Core/Trigger/CompTriggerBody.cs`

## 记录 16

### 本次做了什么
- 把主模组里残留的测试 Def 与测试常量删除，测试内容继续向独立后置模组迁出
- 新建 `BorderDefenseProtocol.DevHarness` 的最小后置测试模组骨架、测试 Def 与独立工程文件
- 回头修正方向：取消“测试模组用 Harmony 直接挂主模组按钮”的做法
- 在主模组新增正式扩展口：
  - `ITriggerExternalGizmoProvider`
  - `TriggerExternalGizmoContext`
  - `TriggerExternalGizmoRegistry`
- 让测试后置模组改为注册自己的按钮提供器，再通过主模组公开接口执行装载、激活、关闭、卸载
- 继续修正 `CombatBodyState`，让冷却结束在查询入口主动结算，不再要求外部先跑一遍时间过程

### 当前结论

- 测试模组“物理隔离”这件事已经不再只是把文件挪出去，而是开始按正式扩展协议接入
- 这更符合二次重构目标：主模组负责骨架和标准，测试模组本身也应该是一个普通扩展案例
- `CombatBody` 至少第一层的“再次激活不能依赖时间流动先推进”已经开始落地

### 本次涉及文件

- `Source/BDP/Core/Trigger/CompTriggerBody.cs`
- `Source/BDP/Core/Trigger/ITriggerExternalGizmoProvider.cs`
- `Source/BDP/Core/Trigger/TriggerExternalGizmoContext.cs`
- `Source/BDP/Core/Trigger/TriggerExternalGizmoRegistry.cs`
- `Source/BDP/Core/CombatBody/CombatBodyState.cs`
- `Source/BDP/Core/CombatBody/ICombatBodyPhaseState.cs`
- `Source/BDP/Core/CombatBody/CombatBodyCoordinator.cs`
- `docs/01-决策记录/2026-03-24-测试后置模组正式扩展入口-第一版.md`
- `BorderDefenseProtocol.DevHarness/`

## 记录 17

### 本次做了什么
- 主模组编译通过，确认新的触发体外部按钮扩展口已进入 `BDP.dll`
- 发现测试模组第一次并行编译失败只是因为它先拿到了旧版 `BDP.dll`
- 在主模组编译完成后重新单独编译测试模组，已编译通过
- 复查确认测试模组源码里不再保留针对 `CompTriggerBody.CompGetEquippedGizmosExtra()` 的 Harmony 挂接

### 当前结论

- 现在的测试模组接入方式已经从“补丁挂按钮”改成“正式扩展口注册按钮提供器”
- 主模组与测试模组至少在编译层面已经完成这次结构切换
- 下一步重点应转回游戏内验证，确认按钮显示、测试芯片装载、暂停下激活与远近战表达表现都仍然成立

### 本次涉及文件

- `Source/BDP/Core/Trigger/CompTriggerBody.cs`
- `Source/BDP/Core/Trigger/TriggerExternalGizmoRegistry.cs`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/DevHarnessBootstrap.cs`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/DevHarnessTriggerGizmoProvider.cs`

## 记录 18

### 本次做了什么
- 根据游戏红字回查，确认测试后置模组启动时报错不是扩展口本身有问题，而是静态启动阶段触发了诊断工具
- 锁定报错链：
  - `DevHarnessBootstrap`
  - `TriggerExternalGizmoRegistry.Register`
  - `BdpDiagnostics.Once`
- 把 `BdpDiagnostics` 改成“早期安全”写法：
  - `Prefs.DevMode` 读取失败时自动静默
  - 读取 `TickManager` 失败时返回 `-1`
  - 诊断输出本身不允许反过来导致模组启动崩溃

### 当前结论

- 之前的问题本质上仍然是同一类错误：在游戏状态未稳定时过早读取依赖对象
- 这次不是简单删日志，而是把统一诊断工具本身修成了更稳妥的底层能力
- 后续即使再有启动阶段调用诊断，也不应再直接把模组拉崩

### 本次涉及文件

- `Source/BDP/Support/Diagnostics/BdpDiagnostics.cs`

## 记录 19

### 本次做了什么
- 继续落实“事件通信不能只停在接口摆设”这个方向
- 为 CombatBody 新增正式阶段事件口：
  - `ICombatBodyEvents`
  - `CombatBodyPhaseChangedArgs`
- 让 `ICombatBodyCoordinator` 直接继承 `ICombatBodyEvents`
- 重写 `CombatBodyCoordinator`，在以下变化后主动广播 `PhaseChanged`
  - 激活成功
  - 手动关闭成功
  - 崩解触发成功
- 同步新增决策记录，明确 CombatBody 事件口当前只表达阶段变化，不承载复杂业务

### 当前结论

- Trigger 侧已经有最小事件口，CombatBody 侧现在也开始具备正式的变化广播能力
- 这还不是“大一统事件总线”，但已经不再是只有查询、没有变化表达
- 目前保持了克制：事件负责广播，真值仍由状态对象持有，避免再把所有业务强塞进一个漂亮空壳里

### 本次涉及文件

- `Source/BDP/Core/CombatBody/ICombatBodyEvents.cs`
- `Source/BDP/Core/CombatBody/CombatBodyPhaseChangedArgs.cs`
- `Source/BDP/Core/CombatBody/ICombatBodyCoordinator.cs`
- `Source/BDP/Core/CombatBody/CombatBodyCoordinator.cs`
- `docs/01-决策记录/2026-03-24-CombatBody阶段事件口-第一版.md`

## 记录 20

### 本次做了什么

- 在主模组新增 `TriggerResolvedExpressionCache`
- 让 `CompTriggerBody` 作为 Trigger owner 正式持有这层缓存桥
- 让 `BdpProjectionRuntime` 统一改为读取同一份表达快照
- 用 Trigger 事件驱动表达缓存失效，不再让事件口只停留在名义层
- 继续补强核心代码与测试模组的注释覆盖
- 更新修正任务清单与新的决策记录
- 主模组、测试模组重新编译验证通过

### 当前结论

- 当前“事件通信已落实”的真实落点已经存在：Trigger 状态变化会正式影响表达运行时读取结果
- 这条链已经进入按钮、自动远程、自动近战共用的主查询入口
- `CombatBody` 当前仍然只是预备骨架，不能误判成已经接上主模组主线

### 本次涉及文件

- `Source/BDP/Core/Runtime/TriggerResolvedExpressionCache.cs`
- `Source/BDP/Core/Trigger/CompTriggerBody.cs`
- `Source/BDP/Core/Runtime/BdpProjectionRuntime.cs`
- `Source/BDP/Core/Trigger/TriggerSlotState.cs`
- `Source/BDP/Core/Trigger/ITriggerEvents.cs`
- `Source/BDP/Core/Trigger/TriggerSlotStateChangedArgs.cs`
- `Source/BDP/Core/Trion/ITrionState.cs`
- `Source/BDP/Core/Trion/ITrionEvents.cs`
- `Source/BDP/Core/Expressions/BdpExpressionConfig.cs`
- `Source/BDP/Core/CombatBody/PawnCombatBodyHost.cs`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/DevHarnessBootstrap.cs`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/DevHarnessTriggerGizmoProvider.cs`
- `BorderDefenseProtocol.DevHarness/1.6/Defs/Trigger/ThingDefs_BDP_TestTriggerBody.xml`
- `BorderDefenseProtocol.DevHarness/1.6/Defs/Trigger/ThingDefs_BDP_TestChips.xml`
- `docs/01-决策记录/2026-03-24-Trigger事件驱动表达缓存-第一版.md`
- `docs/plans/2026-03-23-第一阶段修正任务清单-立即结算与事件落实-第一版.md`

## 记录 21

### 本次做了什么

- 继续按“普通玩家和初级模组开发者也能看懂”的标准补强代码注释
- 扩大覆盖到主模组核心接口、枚举、状态对象、运行时、投影器、补丁入口、诊断工具
- 扩大覆盖到测试后置模组的常量文件与 XML 配置
- 重新编译主模组与测试模组，确认纯注释改动没有引入语法问题

### 当前结论

- 当前主链核心代码已经不再只靠类名猜意思，打开关键文件基本都能直接看出：
  - 它是什么
  - 它负责什么
  - 它不负责什么
  - 它为什么放在这里
- 注释补强的重点仍然放在真正参与主链的代码，而不是为了覆盖率去给无关产物凑字数

### 本次涉及文件

- `Source/BDP/BDPMod.cs`
- `Source/BDP/Support/Diagnostics/BdpDiagnostics.cs`
- `Source/BDP/Core/Cleanup/ICollapseCleanupCoordinator.cs`
- `Source/BDP/Core/Cleanup/CombatBodyCleanupCoordinator.cs`
- `Source/BDP/Core/CombatBody/CombatBodyPhase.cs`
- `Source/BDP/Core/CombatBody/ICombatBodyCoordinator.cs`
- `Source/BDP/Core/CombatBody/ICombatBodyHost.cs`
- `Source/BDP/Core/CombatBody/CombatBodyCoordinator.cs`
- `Source/BDP/Core/CombatBody/CombatBodyState.cs`
- `Source/BDP/Core/Expressions/BasicExpressionResolver.cs`
- `Source/BDP/Core/Expressions/ExpressionSource.cs`
- `Source/BDP/Core/Expressions/ResolvedExpressions.cs`
- `Source/BDP/Core/Expressions/TriggerExpressionCollector.cs`
- `Source/BDP/Core/Expressions/VerbExpressionProjectionRegistry.cs`
- `Source/BDP/Core/Expressions/IExpressionCollector.cs`
- `Source/BDP/Core/Expressions/IExpressionProjectionRegistry.cs`
- `Source/BDP/Core/Expressions/IExpressionResolver.cs`
- `Source/BDP/Core/Expressions/IExpressionSource.cs`
- `Source/BDP/Core/Expressions/IResolvedExpressions.cs`
- `Source/BDP/Core/Projections/AutoMeleeProjection.cs`
- `Source/BDP/Core/Projections/AutoRangedProjection.cs`
- `Source/BDP/Core/Projections/ManualCommandProjection.cs`
- `Source/BDP/Core/Projections/SurfaceProjection.cs`
- `Source/BDP/Core/Projections/IAutoMeleeProjection.cs`
- `Source/BDP/Core/Projections/IAutoRangedProjection.cs`
- `Source/BDP/Core/Projections/IManualCommandProjection.cs`
- `Source/BDP/Core/Projections/ISurfaceProjection.cs`
- `Source/BDP/Core/Projections/IVisualProjection.cs`
- `Source/BDP/Core/Runtime/TriggerResolvedExpressionCache.cs`
- `Source/BDP/Core/Trigger/CompProperties_TriggerBody.cs`
- `Source/BDP/Core/Trigger/CompTriggerBody.cs`
- `Source/BDP/Core/Trigger/TriggerActivationCoordinator.cs`
- `Source/BDP/Core/Trigger/ITriggerActivationCoordinator.cs`
- `Source/BDP/Core/Trigger/ITriggerExternalGizmoProvider.cs`
- `Source/BDP/Core/Trigger/ITriggerLoadoutState.cs`
- `Source/BDP/Core/Trigger/ITriggerSlotState.cs`
- `Source/BDP/Core/Trigger/SwitchContext.cs`
- `Source/BDP/Core/Trigger/SwitchPhase.cs`
- `Source/BDP/Core/Trigger/TriggerExternalGizmoContext.cs`
- `Source/BDP/Core/Trigger/TriggerSide.cs`
- `Source/BDP/Core/Trion/CompProperties_Trion.cs`
- `Source/BDP/Core/Trion/CompTrion.cs`
- `Source/BDP/Patches/Patch_Pawn_MeleeVerbs_TryMeleeAttack.cs`
- `Source/BDP/Patches/Patch_Pawn_TryGetAttackVerb.cs`
- `1.6/Defs/Core/ThingDefs_BDP_TrionBases.xml`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/TriggerDevHarnessDefs.cs`
- `BorderDefenseProtocol.DevHarness/1.6/Defs/Trigger/ThingDefs_BDP_TestTriggerBody.xml`
- `BorderDefenseProtocol.DevHarness/1.6/Defs/Trigger/ThingDefs_BDP_TestChips.xml`

## 记录 22

### 本次做了什么

- 全量回查当前主模组里所有 `tick / 时间点 / 查询热路径 / 事件装配点`
- 把 `CompTriggerBody` 的切换表现从“tick 清理 + 查询顺手清理”改成“只读判断当前是否仍在表现期”
- 去掉 `CompTriggerBody.CompTick()`
- 去掉 `ITriggerLoadoutState.ResolveSwitching()`
- 清理主模组和测试模组按钮 / 表达 / 自动攻击热路径上的高频诊断日志
- 新增一条正式决策记录，明确本轮后哪些时间依赖已经清掉，哪些属于合法保留
- 重新编译主模组与测试模组，确认通过

### 当前结论

- Trigger 主链现在不再通过 tick 或查询补结算来维持逻辑成立
- 当前仍保留的时间依赖，主要只剩合法时间过程：
  - 切换表现是否仍在持续
  - Trion 持续消耗与自然恢复
  - CombatBody 预备骨架里的剩余时间字段
- 后续如果仍看到循环日志，优先怀疑外部测试环境或新增日志点，而不是当前这条 Trigger 主链

### 本次涉及文件

- `Source/BDP/Core/Trigger/ITriggerLoadoutState.cs`
- `Source/BDP/Core/Trigger/CompTriggerBody.cs`
- `Source/BDP/Core/Expressions/TriggerExpressionCollector.cs`
- `Source/BDP/Core/Runtime/BdpProjectionRuntime.cs`
- `Source/BDP/Core/Runtime/TriggerResolvedExpressionCache.cs`
- `Source/BDP/Core/Projections/ManualCommandProjection.cs`
- `Source/BDP/Patches/Patch_Pawn_TryGetAttackVerb.cs`
- `Source/BDP/Patches/Patch_Pawn_MeleeVerbs_TryMeleeAttack.cs`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/DevHarnessTriggerGizmoProvider.cs`
- `docs/01-决策记录/2026-03-24-时间依赖清理范围-第一版.md`

## 记录 23

### 本次做了什么

- 把新的“跨链路保留来源语义”需求正式收束成统一机制
- 固定当前正式名称为“语义上下文”
- 写入一条短决策记录，明确它是什么、它不是什么、第一批接入范围是什么
- 写出第一版接入计划，只选两条首批链路：
  - 攻击行为链
  - 崩裂原因链

### 当前结论

- 当前需求的本质已经明确：结果在跨链路传递时，不能丢掉来源语义
- 语义上下文不会被做成新的总线或总协调器，而是轻量语义载荷
- 下一步应按计划先接最小范围，而不是一口气扩散到整个模组

### 本次涉及文件

- `docs/01-决策记录/2026-03-24-语义上下文机制-第一版.md`
- `docs/plans/2026-03-24-语义上下文接入计划-第一版.md`

## 记录 24

### 本次做了什么

- 新增语义上下文核心类型：
  - `SemanticSourceKind`
  - `ISemanticContext`
  - `SemanticContext`
  - `SemanticDamageRecord`
  - `ISemanticDamageSource`
- 让表达来源 `IExpressionSource / ExpressionSource` 能正式携带语义上下文
- 在 `TriggerExpressionCollector` 里为当前攻击表达来源生成第一版攻击语义
- 让 `CombatBodyPhaseChangedArgs` 能携带语义上下文
- 让 `CombatBodyCoordinator.TriggerCollapse(reason)` 在广播阶段变化时附带第一版崩裂语义
- 重新编译主模组与测试模组，确认通过

### 当前结论

- “语义上下文”已经不再只是文档概念，而是进入了当前主模组代码
- 第一批真实落点已存在于两条链：
  - 攻击表达来源链
  - 崩裂原因链
- 伤害落地侧目前仍然只是先立桥位，没有假装已经完整接回原版伤害系统

### 本次涉及文件

- `Source/BDP/Core/Semantics/SemanticSourceKind.cs`
- `Source/BDP/Core/Semantics/ISemanticContext.cs`
- `Source/BDP/Core/Semantics/SemanticContext.cs`
- `Source/BDP/Core/Semantics/SemanticDamageRecord.cs`
- `Source/BDP/Core/Semantics/ISemanticDamageSource.cs`
- `Source/BDP/Core/Expressions/IExpressionSource.cs`
- `Source/BDP/Core/Expressions/ExpressionSource.cs`
- `Source/BDP/Core/Expressions/TriggerExpressionCollector.cs`
- `Source/BDP/Core/CombatBody/CombatBodyPhaseChangedArgs.cs`
- `Source/BDP/Core/CombatBody/CombatBodyCoordinator.cs`

## 记录 16

### 本次做了什么
- 把“暂停下必须立即结算”和“只有时间表现允许依赖时间推进”正式写成决策记录
- 新增本轮修正任务清单，把 Trigger、事件通信、表达读取、测试修复、CombatBody 回查串成明确执行顺序
- 重写 `CompTriggerBody`，把激活逻辑改成请求后立即提交，不再把逻辑成立绑到时间流动
- 为 Trigger 增加最小事件口，先广播装载变化、激活已提交、关闭已提交
- 给动态生成的 Verb 补上真实的 `verbTracker/directOwner` 宿主接线，准备处理当前自动攻击空引用
- 重新编译验证通过

### 当前结论

- 当前第一批修正已经从“讨论原则”进入“代码已落地”的阶段
- Trigger 现在不再是时间状态机优先，而是逻辑结果优先
- 事件通信目前只是最小落地，离完整事件总线还很远，但已经不再是完全停留在名义层
- 下一步需要玩家重新进游戏验证：暂停状态下点击 `测:开主0 / 测:开副0` 后，`测试远程 / 测试近战` 是否会立即出现，以及自动入口红字是否消失

### 本次涉及文件

- `docs/01-决策记录/2026-03-23-暂停结算与时间过程分层原则-第一版.md`
- `docs/plans/2026-03-23-第一阶段修正任务清单-立即结算与事件落实-第一版.md`
- `docs/00-项目宪章/项目起步约束.md`
- `Source/BDP/Core/Trigger/ITriggerEvents.cs`
- `Source/BDP/Core/Trigger/TriggerSlotStateChangedArgs.cs`
- `Source/BDP/Core/Trigger/CompTriggerBody.cs`
- `Source/BDP/Core/Runtime/BdpProjectionRuntime.cs`
- `Source/BDP/Core/Expressions/VerbExpressionProjectionRegistry.cs`

## 记录 08

### 本次做了什么

- 实现了表达系统第一版代码
- 实现了入口与展示系统的最小投影代码
- 实现了最小收尾协调代码
- 持续编译验证通过

### 当前结论

- 第一阶段六块系统现在都已有第一版代码骨架
- 表达层当前只做“收集来源 -> 解析默认主近战/主远程 -> 投到 Verb”
- 入口层当前只做“手动按钮、自动远程、自动近战、默认表面”四条口的最小投影
- 收尾层当前只做“释放资源、关闭槽位、恢复宿主”的最小事务链
- 当前最大缺口已不再是骨架缺失，而是具体业务规则和真实接线尚未补齐

### 本次涉及文件

- `Source/BDP/Core/Expressions/ExpressionHostKind.cs`
- `Source/BDP/Core/Expressions/BdpExpressionConfig.cs`
- `Source/BDP/Core/Expressions/IExpressionSource.cs`
- `Source/BDP/Core/Expressions/ExpressionSource.cs`
- `Source/BDP/Core/Expressions/IExpressionCollector.cs`
- `Source/BDP/Core/Expressions/IResolvedExpressions.cs`
- `Source/BDP/Core/Expressions/ResolvedExpressions.cs`
- `Source/BDP/Core/Expressions/IExpressionResolver.cs`
- `Source/BDP/Core/Expressions/TriggerExpressionCollector.cs`
- `Source/BDP/Core/Expressions/BasicExpressionResolver.cs`
- `Source/BDP/Core/Expressions/IExpressionProjectionRegistry.cs`
- `Source/BDP/Core/Expressions/VerbExpressionProjectionRegistry.cs`
- `Source/BDP/Core/Projections/IManualCommandProjection.cs`
- `Source/BDP/Core/Projections/IAutoRangedProjection.cs`
- `Source/BDP/Core/Projections/IAutoMeleeProjection.cs`
- `Source/BDP/Core/Projections/ISurfaceProjection.cs`
- `Source/BDP/Core/Projections/IVisualProjection.cs`
- `Source/BDP/Core/Projections/ManualCommandProjection.cs`
- `Source/BDP/Core/Projections/AutoRangedProjection.cs`
- `Source/BDP/Core/Projections/AutoMeleeProjection.cs`
- `Source/BDP/Core/Projections/SurfaceProjection.cs`
- `Source/BDP/Core/Cleanup/ICollapseCleanupCoordinator.cs`
- `Source/BDP/Core/Cleanup/CombatBodyCleanupCoordinator.cs`

## 记录 09

### 本次做了什么

- 给触发体补上了最小切换状态机
- 把 `CompTriggerBody -> 表达收集 -> 解析 -> 投影` 收成统一查询链
- 把手动入口挂到当前引擎真实的 `CompGetGizmosExtra()` 上
- 初始化 Harmony，并接入自动远程与自动近战两条原版入口桥

### 当前结论

- 触发体现在不再是“瞬间切换”，已经具备最小前后摇状态
- 当前查询链已经不只是内部骨架，而是开始真正回到 RimWorld 原版入口
- 旧代码中的历史接口名不能直接照搬，必须以当前引擎真实可用接口为准
- 当前下一步重点已从“继续补框架”转向“补真实 Def 配置和按旧代码事实填业务规则”

### 本次涉及文件

- `Source/BDP/Core/Trigger/SwitchPhase.cs`
- `Source/BDP/Core/Trigger/SwitchContext.cs`
- `Source/BDP/Core/Trigger/CompTriggerBody.cs`
- `Source/BDP/Core/Runtime/BdpProjectionRuntime.cs`
- `Source/BDP/BDPMod.cs`
- `Source/BDP/Patches/Patch_Pawn_TryGetAttackVerb.cs`
- `Source/BDP/Patches/Patch_Pawn_MeleeVerbs_TryMeleeAttack.cs`

## 记录 10

### 本次做了什么

- 增加了第一轮游戏内冒烟测试内容 Def
- 给触发体增加了开发模式下的测试装载/激活按钮
- 修正了当前引擎真实可用的 Gizmo 挂点
- 把最小手动入口和自动攻击入口真正接到现有查询链上
- 编译通过，推进到第一次可测试节点

### 当前结论

- 现在已经可以在游戏中拿到测试触发体，并通过测试按钮装入测试芯片
- 可以开始验证槽位切换、按钮生成、自动远程、自动近战这几条主链是否开始成立
- 这一轮测试仍然是“骨架冒烟测试”，不是旧 BDP 全表现复刻测试

### 本次涉及文件

- `Source/BDP/Core/Trigger/TriggerBodyTestDefs.cs`
- `Source/BDP/Core/Trigger/CompTriggerBody.cs`
- `1.6/Defs/Trigger/ThingDefs_BDP_TestTriggerBody.xml`
- `1.6/Defs/Trigger/ThingDefs_BDP_TestChips.xml`

## 记录 11

### 本次做了什么

- 系统排查了“装备后无按钮”的根因
- 确认问题不在按钮内容本身，而在按钮入口宿主基类与挂点用错
- 把 `CompTriggerBody` 从 `ThingComp` 改回 `CompEquippable`
- 把按钮入口改到 `CompGetEquippedGizmosExtra()`
- 增加统一诊断工具并在关键链路打点

### 当前结论

- 当前根因是装备场景的按钮入口不走 `ThingComp` 这条链，而走 `CompEquippable`
- 诊断日志现在已经覆盖模组初始化、装备按钮入口、表达收集、按钮生成、自动攻击入口桥
- 下一次进游戏测试时，能更快判断是“入口没进来”还是“进来了但后面链路断了”

### 本次涉及文件

- `Source/BDP/BDPMod.cs`
- `Source/BDP/Core/Trigger/CompTriggerBody.cs`
- `Source/BDP/Core/Runtime/BdpProjectionRuntime.cs`
- `Source/BDP/Core/Expressions/TriggerExpressionCollector.cs`
- `Source/BDP/Core/Projections/ManualCommandProjection.cs`
- `Source/BDP/Patches/Patch_Pawn_TryGetAttackVerb.cs`
- `Source/BDP/Patches/Patch_Pawn_MeleeVerbs_TryMeleeAttack.cs`
- `Source/BDP/Support/Diagnostics/BdpDiagnostics.cs`

## 记录 12

### 本次做了什么
- 沿报错链确认模组加载空指针发生在模组构造阶段的诊断调用
- 移除 `BDPMod` 构造阶段的诊断输出，只保留 Harmony 初始化
- 重新核对 RimWorld `Prefs.DevMode` 的源码行为，确认诊断工具不需要额外包一层错误判断
- 重新编译验证通过

### 当前结论

- 当前这次红字的直接根因不是业务链，而是“过早打日志”
- 诊断工具本身的 `Once/Throttled` 仍会影响日志频率，但这次加载崩溃的根因不在节流
- 下一次进游戏应先验证模组能稳定加载，再继续看装备后按钮链是否已经进入

### 本次涉及文件

- `Source/BDP/BDPMod.cs`
- `Source/BDP/Support/Diagnostics/BdpDiagnostics.cs`

## 记录 13

### 本次做了什么
- 结合玩家反馈，确认当前仅能看到“表达来源收集完成，数量=0”“表达运行时开始解析，来源数量=0”两条循环日志
- 回查按钮链、自动攻击链、槽位激活链，确认现有两条日志更像来自自动攻击入口，不足以证明按钮入口已经被调用
- 重写测试触发体与测试芯片 Def 文件，重新用直接文件加载方式确认 XML 可正常读取
- 在按钮入口边界补充更明确的诊断日志，用于区分“按钮入口”与“自动攻击入口”
- 重新编译验证通过

### 当前结论

- 之前看到的两条日志不能再被当成“按钮链已进入”的证据，它们更可能只是自动攻击补丁在循环触发
- 下一轮测试的关键不再是只看“来源数量=0”，而是先看是否出现“按钮入口：进入 CompTriggerBody.CompGetEquippedGizmosExtra()”
- 如果仍然没有任何“按钮入口”日志，就说明问题不在表达层，而在 RimWorld 调用装备按钮入口的前面一段

### 本次涉及文件

- `1.6/Defs/Trigger/ThingDefs_BDP_TestTriggerBody.xml`
- `1.6/Defs/Trigger/ThingDefs_BDP_TestChips.xml`
- `Source/BDP/Core/Trigger/CompTriggerBody.cs`

## 记录 14

### 本次做了什么
- 接受“当前加载的就是现模组”这一前提后，重新回到代码事实排查按钮入口未触发的更前层根因
- 核对 RimWorld `BaseWeapon` 的继承结果，确认其原本就自带一份 `CompEquippable`
- 确认测试触发体 Def 之前又额外追加了一份 `CompTriggerBody : CompEquippable`，形成“一件武器两个 equippable comp”
- 将测试触发体 Def 的 `comps` 改为覆盖式，只保留 `Forbiddable + TriggerBody + Styleable`
- 重新编译验证通过

### 当前结论

- “没有自定义按钮”与“出现 Verb_CompEquippable_BDP_TestTriggerBody 重复注册报错”现在可以由同一个根因解释
- 根因不是表达层，而是测试触发体 Def 同时挂了两条 equippable 链
- 下一轮测试应优先确认重复注册红字是否消失，以及开发模式调试按钮是否出现

### 本次涉及文件

- `1.6/Defs/Trigger/ThingDefs_BDP_TestTriggerBody.xml`
- `Source/BDP/Core/Trigger/CompProperties_TriggerBody.cs`

## 记录 15

### 本次做了什么
- 根据游戏内截图确认当前能看到调试按钮、能装载测试芯片、也能发出激活请求，但仍看不到“测试远程/测试近战”
- 回查触发体切换实现与 RimWorld 装备 tick 机制，确认当前“激活完成”逻辑挂在 `CompTick()` 上
- 核对原版 `Pawn_EquipmentTracker.EquipmentTrackerTick()`，确认殖民者装备状态下不会替自定义触发体 comp 推进整套 `CompTick()`，只会推进 verb tracker
- 将切换状态推进补到查询入口上，使按钮查询与攻击查询时都能顺手结算冷却完成的切换
- 增加“槽位激活完成”诊断日志
- 重新编译验证通过

### 当前结论

- 当前问题的根因不是“按钮生成错”，也不是“芯片没装进去”，而是“请求激活后没有自然推进到完成激活”
- 这属于典型的 RimWorld 引擎接线问题：装备在 Pawn 身上时，不能想当然依赖 `CompTick()` 完成局部状态机
- 下一轮测试应重点看：点击 `测:开主0 / 测:开副0` 后，稍等片刻是否出现“槽位激活完成”日志，以及 `测试远程 / 测试近战` 按钮是否出现

### 本次涉及文件

- `Source/BDP/Core/Trigger/CompTriggerBody.cs`

## ��¼ 25

### ��������ʲô?
- ����ͨоƬ��������ĵ�һĬ����ʾ���̶�ΪоƬ������������ȡ `verb.label`
- ������С������ؽӿ� `IBdpSemanticCarrier`
- ������С������˹��� `BdpDamageSemanticBridge`
- ���� BDP �Զ��� Verb ������
  - `BdpVerbBase`
  - `BdpVerb_Shoot`
  - `BdpVerb_MeleeAttackDamage`
- ��д `VerbExpressionProjectionRegistry`���õ�ǰ��Զ��/����ս���������䵽 BDP �Զ��� Verb �ϣ����ڴ�����ֱ�ӹ��빥������
- ��ģ�����±���ͨ��

### ��ǰ����

- ��һ����С�����Ѿ��ӡ�ֻ�б�����Դ�������塱�ƽ��������ɳ�������ս/��Զ�� Verb �����Լ�Ҳ�ܴ����塱
- ���ڹ��������Ѿ���ֻ�ǿ����ֶΣ����ǿ�ʼ������������ RimWorld ������������������
- ��ǰ��û�н��� Projectile��Explosion���˿�д�����Σ���˻��������ƹ������

### �����漰�ļ�

- `Source/BDP/Core/Expressions/TriggerExpressionCollector.cs`
- `Source/BDP/Core/Expressions/VerbExpressionProjectionRegistry.cs`
- `Source/BDP/Core/Semantics/IBdpSemanticCarrier.cs`
- `Source/BDP/Core/Semantics/BdpDamageSemanticBridge.cs`
- `Source/BDP/Core/Verbs/BdpVerbBase.cs`
- `Source/BDP/Core/Verbs/BdpVerb_Shoot.cs`
- `Source/BDP/Core/Verbs/BdpVerb_MeleeAttackDamage.cs`

## 记录 26

### 本次做了什么
- 重新编译主模组与测试模组，确认攻击语义链第二批代码真实可编译
- 修正 `BdpBullet` 的原版依赖命名空间问题，使主模组重新编译通过
- 补上爆炸表达的正式语义链：
  - 新增 `BdpExplosiveProjectile`
  - 在爆炸生成边界把攻击语义带入 `Explosion`
  - 在爆炸真正对目标 `TakeDamage` 前把语义压回当前伤害作用域
- 给测试后置模组新增爆炸测试芯片与爆炸测试投射物，并补对应开发按钮
- 主模组、测试模组再次编译通过

### 当前结论

- 近战直伤、远程直伤、远程爆炸三条攻击语义链现在都已有正式代码接点
- 当前阶段已经到可进入游戏验证的节点，重点不再是“能不能编译”，而是“伤口括号来源名是否按攻击行为名显示”
- 爆炸链无法只靠瞬时线程作用域完成，因为 `Explosion` 会跨 Tick 结算；当前实现已把跨 Tick 语义承载收束到爆炸实例这一处

### 本次涉及文件

- `Source/BDP/Core/Semantics/BdpDamageSemanticBridge.cs`
- `Source/BDP/Core/Projectiles/BdpBullet.cs`
- `Source/BDP/Core/Projectiles/BdpExplosiveProjectile.cs`
- `Source/BDP/Patches/Patch_Explosion_StartExplosion_BdpSemantics.cs`
- `Source/BDP/Patches/Patch_DamageWorker_ExplosionDamageThing_BdpSemantics.cs`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/TriggerDevHarnessDefs.cs`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/DevHarnessTriggerGizmoProvider.cs`
- `BorderDefenseProtocol.DevHarness/1.6/Defs/Trigger/ThingDefs_BDP_TestChips.xml`
- `docs/plans/2026-03-24-攻击语义伤口来源桥接设计-第一版.md`
- `docs/plans/2026-03-24-语义上下文接入计划-第一版.md`

## 记录 27

### 本次做了什么
- 根据游戏内红字回查原版 Def，确认爆炸测试芯片使用了错误的贴图路径与不存在的爆炸声音 Def
- 将爆炸测试投射物贴图改为 `Things/Projectile/Grenade`
- 将爆炸测试芯片物品贴图改为 `Things/Item/Equipment/WeaponRanged/Grenades`
- 移除无效的 `Explosion_Small` 绑定，改为回退原版默认爆炸声
- 继续补充主模组与测试模组当前主线文件的注释覆盖，重点解释宿主职责、接线边界、语义进入伤害链的时机
- 主模组、测试模组重新编译通过

### 当前结论

- 这轮红字根因是测试资源名写错，不是攻击语义架构链路本身出错
- 当前可重新进入游戏验证爆炸测试，不应再出现贴图缺失和 `Explosion_Small` 未解析红字
- 当前主线链路相关文件的注释覆盖已经继续加密一批，但后续仍要持续向更多非主线文件扩展

### 本次涉及文件

- `BorderDefenseProtocol.DevHarness/1.6/Defs/Trigger/ThingDefs_BDP_TestChips.xml`
- `Source/BDP/Core/Projectiles/BdpExplosiveProjectile.cs`
- `Source/BDP/Core/Projectiles/BdpBullet.cs`
- `Source/BDP/Core/Verbs/BdpVerb_Shoot.cs`
- `Source/BDP/Core/Verbs/BdpVerb_MeleeAttackDamage.cs`
- `Source/BDP/Core/Semantics/BdpDamageSemanticBridge.cs`
- `Source/BDP/Core/Semantics/SemanticRuntimeScope.cs`
- `Source/BDP/Patches/Patch_Explosion_StartExplosion_BdpSemantics.cs`
- `Source/BDP/Patches/Patch_DamageWorker_ExplosionDamageThing_BdpSemantics.cs`
- `Source/BDP/Patches/Patch_DamageWorker_AddInjury_SourceLabel.cs`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/TriggerDevHarnessDefs.cs`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/DevHarnessTriggerGizmoProvider.cs`

## 记录 28

### 本次做了什么
- 针对“远程同次测试里有的伤口显示芯片名，有的仍显示触发体名”回查原版伤害与伤口显示链
- 确认 `DamageWorker_AddInjury` 主链本身大体会经过当前补丁，不像是单纯漏补入口
- 进一步确认原版 `HediffSet.AddDirect(...)` 在伤口合并成功时只累加严重度，不会刷新旧伤口的来源字段
- 新增 `Patch_Hediff_Injury_TryMergeWith_BdpSemantics`，只在当前存在 BDP 攻击语义且伤口合并成功时，刷新合并后旧伤口的来源字段
- 将伤口来源写入逻辑收敛到 `BdpDamageSemanticBridge.TryApplyInjurySource(...)`，避免多处散写
- 主模组、测试模组重新编译通过

### 当前结论

- 这轮问题的高概率根因不是伤害没进入语义链，而是旧伤口与新伤口合并后保留了旧来源字段
- 当前修正是一个很窄的“合并后刷新来源”接线，不改伤害数值、不改多段伤口判定
- 下一轮游戏验证应重点看：对同一目标连续远程命中时，是否还会出现一部分伤口来源回退成触发体名称

### 本次涉及文件

- `Source/BDP/Core/Semantics/BdpDamageSemanticBridge.cs`
- `Source/BDP/Patches/Patch_DamageWorker_AddInjury_SourceLabel.cs`
- `Source/BDP/Patches/Patch_Hediff_Injury_TryMergeWith_BdpSemantics.cs`

## 记录 29

### 本次做了什么
- 重新用 RimSearcher 读原版 `Projectile.Impact(...)` 与 `Bullet.Impact(...)`
- 确认 `BdpBullet.Impact(...)` 里原先调用的 `base.Impact(...)` 实际会落到原版 `Bullet.Impact(...)`
- 确认当前 `BdpBullet` 代码因此先执行了一遍原版完整子弹伤害，再执行一遍自定义复制逻辑，形成“一枪双重伤害”
- 将 `BdpBullet` 改为只执行 `Projectile` 层基础落点收尾，再执行一次带语义的子弹伤害逻辑
- 主模组、测试模组重新编译通过

### 当前结论

- “一枪 4 伤口且来源名一半触发体、一半芯片”的更直接根因是 `BdpBullet` 重复结算了整套子弹伤害
- 之前对“伤口合并”的判断不是主因，只是次级风险点
- 当前下一轮测试应优先验证：远程测试链一枪是否仍会生成成对重复伤口，以及是否还会混出触发体来源名

### 本次涉及文件

- `Source/BDP/Core/Projectiles/BdpBullet.cs`
- `docs/99-会话交接/2026-03-23-工作推进-卷01.md`

## 记录 30

### 本次做了什么?
- 重新排查当前主模组已实现系统的存读档恢复支持
- 确认 `CompTrion`、`CombatBodyState`、`SwitchContext` 这几类数值/阶段状态已有正式序列化
- 锁定当前最大缺口在 `CompTriggerBody`：旧实现只在槽位里保存芯片引用，没有把芯片放进 RimWorld 正式 `ThingOwner` 容器
- 重写 `CompTriggerBody`，补上：
  - `IThingHolder`
  - `ThingOwner<Thing> chipContainer`
  - 装载/卸载时正式入容器/出容器
  - `PostExposeData()` 中对芯片容器的正式序列化
  - 读档后的最小一致性修复
- 主模组与测试模组重新编译通过
- 新增一份简短的存读档烟雾检查清单，供后续进游戏核对

### 当前结论

- 当前第一高风险点“芯片只是字段引用，不是正式被持有物”已经修正
- 修正后，芯片本身及其 comp 状态才有机会随触发体一起被正式存档和读回
- 这轮修的是宿主规则，不是补一个读档后临时重建按钮的表面症状
- 修复前做出的旧存档如果当时就没把芯片正式存进去，不能指望自动补救回来

### 本次涉及文件

- `Source/BDP/Core/Trigger/CompTriggerBody.cs`
- `docs/plans/2026-03-24-存读档烟雾检查清单-第一版.md`
- `docs/99-会话交接/2026-03-23-工作推进-卷01.md`
## 记录 31

### 本次做了什么?
- 按已确认的短注释风格，补强第一阶段核心小文件与测试模组注释
- 重写并清理以下文件的注释：
  - `Source/BDP/Core/Trigger/TriggerSlotState.cs`
  - `Source/BDP/Core/Trion/CompTrion.cs`
  - `Source/BDP/Core/CombatBody/CombatBodyState.cs`
  - `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/TriggerDevHarnessDefs.cs`
  - `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/DevHarnessTriggerGizmoProvider.cs`
- 主模组与测试模组重新编译通过

### 当前结论

- 当前注释风格已开始从“能看出意思”收敛到“短、直、覆盖关键点”
- 第一阶段最常被打开的几个小文件，普通玩家和初级 mod 开发者现在更容易直接看懂职责
- 下一批如继续补注释，应优先挑大但关键的宿主文件，而不是平均撒到所有文件

### 本次涉及文件

- `Source/BDP/Core/Trigger/TriggerSlotState.cs`
- `Source/BDP/Core/Trion/CompTrion.cs`
- `Source/BDP/Core/CombatBody/CombatBodyState.cs`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/TriggerDevHarnessDefs.cs`
- `BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/DevHarnessTriggerGizmoProvider.cs`
### 本次工作
- 恢复 BorderDefenseProtocol.DevHarness/1.6/Defs/Trigger/ThingDefs_BDP_TestChips.xml
- 按逐条短注释风格重写测试触发体 XML 与 Trigger 核心接口注释
- 补强 DevHarnessBootstrap、DevHarnessTriggerGizmoProvider、TriggerExternalGizmoRegistry 等高频入口注释
- 主模组与测试模组重新编译通过

### 本次工作
- 继续按逐成员短注释风格补强 CompTriggerBody、TriggerActivationCoordinator、BdpProjectionRuntime、TriggerResolvedExpressionCache 
- 不改行为，只收紧第一阶段主链文件的可读性
- 主模组重新编译通过


### 本次工作
- 继续把第一阶段表达层、投影层、语义层、投射物与 Verb 文件按逐成员短注释风格铺完
- 补齐 IVisualProjection 等剩余接口文件注释
- 主模组与测试模组重新编译通过
