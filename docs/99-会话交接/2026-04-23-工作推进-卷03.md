# 2026-04-23 工作推进 卷03

## 本轮完成

- 已把远程发射点散布从“按枪序号固定分布”改成“每发子弹在区间内独立随机取样”。
- 已把作者侧配置从旧的 `SpreadRadius` 改成新的 `OriginSpread`：
  - `LateralMin`
  - `LateralMax`
  - `ForwardMin`
  - `ForwardMax`
- 已清掉源码主链路里对旧固定散布字段的正向使用：
  - `OriginSpreadSequenceIndex`
  - `OriginSpreadSequenceCount`
  - `OriginSpreadRadius`
  - `ResolveCenteredSequenceRatio`
  - `ResolveDeclaredOriginSpreadOffset`
- 已确认真实发射时在 `BdpVerb_Shoot` 内通过 `Rand.Range(...)` 做随机抽样，而不是按序号映射固定点位。
- 已补掉一个遗漏调用点：`AttackExecutionService.Stages.cs` 的兜底 `BuildSingleEmit(...)` 还在按旧签名传参，已改为显式传“无随机区间”。
- 已把 DevHarness 残留的旧配置也一起迁移：
  - `1.6/Defs/Things/Items/Chips/Test/ThingDefs_TestChips_Combat.xml`
  - `1.6/Defs/Pawn/Combos/Test/ComboDefs_TestCombos.xml`
- 已把烟测扩大到扫描整个 DevHarness `1.6/Defs`，防止再混入 `<SpreadRadius>` 旧写法。

## 验证结果

- `Source/BDP.Tests/RangedRandomOriginSpreadSmokeTests.ps1` PASS
- `Source/BDP.Tests/DevHarnessPathLatchVolleyChipSmokeTests.ps1` PASS
- `Source/BDP.Tests/RangedProtocolBoundarySmokeTests.ps1` PASS
- `Source/BDP.Tests/TriggerVisualMarkerOverlaySmokeTests.ps1` PASS
- `dotnet msbuild .\Source\BDP\BDP.csproj` PASS
- `dotnet msbuild .\Source\BDP.DevHarness\BDP.DevHarness.csproj` PASS

## 当前结论

- 现在的发射点散布设计已经不是“固定散布伪随机观感”，而是真随机区间散布。
- 随机性发生在“每发子弹真实发射前”的发射点偏移抽样，不再绑定齐射序号。
- 后续如果要调手感，直接改 `OriginSpread` 四个边界即可。

## 本轮补充

- 已按最新口径把 DevHarness 测试配置里的 `ForwardMin` 改为 `0`。
- 含义是取消朝小人自身方向的后退偏移，只在左右和朝目标前方范围内随机。
