# BDP 主模组审计问题修订实施计划

> **For Codex/Claude（执行者）:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task.

**Goal:** 按审计复审后的结论，用最小必要改动修正 BDP 主模组的真实边界问题，并保留已经确认正确的业务行为。

**Architecture:** 不重写 BDP 主链，不新建大框架。默认只处理已经确认会影响行为边界或关键维护判断的五类事项：自动远程原版手动门、触发体卸下清理烟测、临时作用域清理、远程模块运行时校验、Trion 读档账本注释修正；触发体卸下后芯片不恢复激活是业务正确状态，不做反向修复。其它低优先维护项只列为“需单独确认”，不得顺手执行。

**Tech Stack:** RimWorld 1.6、C# 7.3、.NET Framework 4.8、Harmony（补丁框架）、PowerShell smoke tests（烟测）、XML（可扩展标记语言）Def。

---

## Scope Freeze（作用域冻结）

本计划只处理主模组：

```text
模组工程/BorderDefenseProtocol
```

不做：

- 不看旧模组。
- 不改测试模组业务实现。
- 不引入新架构层。
- 不做旧版兼容保留。
- 不改变“触发体卸下即关闭并注销芯片，重装备后无激活芯片”的业务规则。
- 不改变“所有人形 Pawn 暂时都接入 BDP”的现状。
- 不新增设置项、白名单、策略层、管理器或通用框架。
- 不借机整理无关注释、无关命名、无关文件格式。
- 不借机扩展诊断日志；除非当前任务本身要求诊断边界。
- 不修改伴生测试模组业务 Def 或业务代码。

## Stop Rule（强制暂停规则）

执行时只允许做本计划“默认执行范围”列出的改动。出现以下任一情况，必须立即停止，先向用户说明原因、影响范围和最小替代方案，得到明确同意后才能继续：

- 需要修改本计划未列出的主模组源码文件。
- 需要修改 DevHarness、Legacy、文档之外的业务资源或 XML。
- 需要新增公共 API（公开接口）、新配置项、新设置项、新状态字段。
- 需要改变触发体卸下/重装备业务语义。
- 需要改变所有人形 Pawn 接入 BDP 的现状。
- 需要为了测试而改变正式代码结构。
- 想顺手修低优先问题、注释、命名、格式、日志、性能或兼容项。

默认允许改动的代码文件只有：

```text
模组工程/BorderDefenseProtocol/Source/BDP/Patches/Patch_Pawn_TryGetAttackVerb.cs
模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/AttackExecutionSurfaceAccess.cs
模组工程/BorderDefenseProtocol/Source/BDP/Core/Semantics/SemanticRuntimeScope.cs
模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Interaction/TargetingInputRuntimeScope.cs
模组工程/BorderDefenseProtocol/Source/BDP/Patches/Patch_Targeter_OrderPawnForceTarget_TargetingInput.cs
模组工程/BorderDefenseProtocol/Source/BDP/Patches/Patch_DamageWorker_ExplosionDamageThing_BdpSemantics.cs
模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedModules/Config/BdpRangedAttackModuleDef.cs
模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedModules/Runtime/RangedAttackModuleResolver.cs
模组工程/BorderDefenseProtocol/Source/BDP/Core/CombatBodySession/CombatBodySessionService.cs
```

默认允许新增的测试文件只有：

```text
模组工程/BorderDefenseProtocol/Source/BDP.Tests/TriggerDetachTeardownBoundarySmokeTests.ps1
模组工程/BorderDefenseProtocol/Source/BDP.Tests/RuntimeScopeCleanupBoundarySmokeTests.ps1
模组工程/BorderDefenseProtocol/Source/BDP.Tests/RangedModuleRuntimeBoundarySmokeTests.ps1
```

允许修改的既有测试文件只有：

```text
模组工程/BorderDefenseProtocol/Source/BDP.Tests/AutoAttackSeparationSmokeTests.ps1
```

## 当前问题处理状态

已完成：

- `About/About.xml` 已更新为正确现状。
- `CompProperties_CombatBodyHost.activationCost` 已删除。
- `CompProperties_TriggerBody.switchCooldownTicks` 已删除。
- `TriggerSwitchTransitionService` 中容易误判的同名局部参数已改为 `transitionTicks`。

默认执行：

1. 自动远程入口补回原版 `onlyManualCast`（仅手动施放）边界。
2. 新增触发体卸下清理契约烟测，只锁定“卸下清理干净、重装备不恢复激活芯片”的业务规则，不改触发体逻辑。
3. 临时作用域改成异常路径也能清理。
4. 远程模块 `runtimeClass` 加最小 Def 校验和运行时异常隔离。
5. 修正 Trion（触力）读档账本关键注释，不改读档行为。
6. 全量验证。

需单独确认后才允许执行：

1. 战斗体镜像服装反射移除品质时补边界诊断。
2. 补跨层 DTO（数据传输对象）/记录模型注释。

---

### Task 0: Preflight Baseline（执行前基线确认）

**Files:**

- Read: `模组工程/BorderDefenseProtocol/About/About.xml`
- Read: `模组工程/BorderDefenseProtocol/Source/BDP/Core/CombatBody/Bridge/CompProperties_CombatBodyHost.cs`
- Read: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Trigger/State/CompProperties_TriggerBody.cs`
- Read: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Trigger/Switching/Flow/TriggerSwitchTransitionService.cs`

**Step 1: Confirm completed cleanup has no residue**

Run:

```powershell
rg -n "activationCost|switchCooldownTicks|当前处于起步准备阶段" "模组工程\BorderDefenseProtocol" -g "*.cs" -g "*.xml"
```

Expected:

```text
No output.
```

**Step 2: Confirm XML still parses**

Run:

```powershell
$files = rg --files "模组工程\BorderDefenseProtocol" -g "*.xml"
$errors = @()
foreach ($file in $files) {
    try { [xml](Get-Content -LiteralPath $file -Raw) | Out-Null }
    catch { $errors += "$file :: $($_.Exception.Message)" }
}
if ($errors.Count -eq 0) { "XML_PARSE_OK files=$($files.Count)" } else { $errors }
```

Expected:

```text
XML_PARSE_OK files=7
```

---

### Task 1: Auto Ranged Manual Gate（自动远程手动边界）

**Problem:** BDP 自动远程当前只检查 `verb.Available()`，没有补回原版 `Pawn.TryGetAttackVerb` 对 `onlyManualCast`、`Wait_Combat`、`allowManualCastWeapons` 的限制。

**Rule:** 不在补丁里再次调用 `pawn.TryGetAttackVerb(...)`，避免递归。只复用原版关键判断。

**Files:**

- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Patches/Patch_Pawn_TryGetAttackVerb.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/AttackExecutionSurfaceAccess.cs`
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/AutoAttackSeparationSmokeTests.ps1`

**Step 1: Add smoke test checks first**

In `AutoAttackSeparationSmokeTests.ps1`, add assertions:

```powershell
Assert-True (
    $attackPatchText -match 'bool allowManualCastWeapons'
) 'Patch_Pawn_TryGetAttackVerb must receive original allowManualCastWeapons from Pawn.TryGetAttackVerb.'

Assert-True (
    $attackPatchText -match 'TryGetAutoRangedVerb\(__instance,\s*allowManualCastWeapons,'
) 'Patch_Pawn_TryGetAttackVerb must pass allowManualCastWeapons into BDP auto-ranged selection.'

Assert-True (
    $attackSurfaceText -match 'PassesVanillaManualAutoGate'
) 'AttackExecutionSurfaceAccess must apply the vanilla manual/auto gate before returning a BDP auto-ranged verb.'

Assert-True (
    ($attackSurfaceText -match 'onlyManualCast') -and
    ($attackSurfaceText -match 'JobDefOf\.Wait_Combat') -and
    ($attackSurfaceText -match 'allowManualCastWeapons')
) 'BDP auto-ranged selection must preserve the vanilla onlyManualCast / Wait_Combat / allowManualCastWeapons boundary.'

Assert-True (
    $attackSurfaceText -notmatch 'pawn\.TryGetAttackVerb\s*\('
) 'AttackExecutionSurfaceAccess must not call Pawn.TryGetAttackVerb from inside the TryGetAttackVerb patch path.'
```

**Step 2: Run the smoke test and confirm it fails**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\AutoAttackSeparationSmokeTests.ps1"
```

Expected:

```text
FAIL: missing allowManualCastWeapons / PassesVanillaManualAutoGate checks.
```

**Step 3: Pass original allowManualCastWeapons through the patch**

Change `Patch_Pawn_TryGetAttackVerb.Postfix` from:

```csharp
public static void Postfix(ref Verb __result, Pawn __instance)
```

to:

```csharp
public static void Postfix(ref Verb __result, Pawn __instance, bool allowManualCastWeapons)
```

Change call from:

```csharp
if (!AttackExecutionSurfaceAccess.TryGetAutoRangedVerb(__instance, out Verb rangedVerb))
```

to:

```csharp
if (!AttackExecutionSurfaceAccess.TryGetAutoRangedVerb(__instance, allowManualCastWeapons, out Verb rangedVerb))
```

**Step 4: Add the minimal vanilla gate helper**

In `AttackExecutionSurfaceAccess.cs`, add:

```csharp
using Verse.AI;
```

Change method signature:

```csharp
public static bool TryGetAutoRangedVerb(Pawn pawn, bool allowManualCastWeapons, out Verb verb)
```

After `TryResolveAutoPrimaryVerb(...)` returns `rangedVerb`, before staging module session, add:

```csharp
if (!PassesVanillaManualAutoGate(pawn, rangedVerb, allowManualCastWeapons))
{
    return false;
}
```

Add private helper near `TryResolveAutoPrimaryVerb`:

```csharp
/// <summary>
/// 复用原版 TryGetAttackVerb 对 onlyManualCast 的自动/手动边界。
/// 这里只检查 BDP 自动远程是否可被原版自动入口选中，不重新调用 Pawn.TryGetAttackVerb，避免补丁递归。
/// </summary>
private static bool PassesVanillaManualAutoGate(Pawn pawn, Verb verb, bool allowManualCastWeapons)
{
    if (verb?.verbProps == null)
    {
        return false;
    }

    if (!verb.verbProps.onlyManualCast)
    {
        return true;
    }

    if (allowManualCastWeapons)
    {
        return true;
    }

    Job currentJob = pawn?.CurJob;
    return currentJob != null && currentJob.def != JobDefOf.Wait_Combat;
}
```

**Step 5: Run the smoke test again**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\AutoAttackSeparationSmokeTests.ps1"
```

Expected:

```text
AutoAttackSeparation PASS
```

---

### Task 2: Trigger Detach Cleanup Contract（触发体卸下清理约束）

**Problem:** 原审计中的“重装备不恢复激活芯片”不是问题。真实需要锁定的是：触发体卸下时必须清干净，重装备无激活芯片才正确。

**Execution Gate:** 本任务已获用户确认加入默认执行，但只允许新增烟测，不允许修改触发体正式逻辑。若烟测发现现有清理链缺口，必须先停止并向用户说明，不得顺手修。

**Files:**

- Create: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/TriggerDetachTeardownBoundarySmokeTests.ps1`
- Read/Verify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Trigger/State/CompTriggerBody.cs`
- Read/Verify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Trigger/Runtime/TriggerDetachTeardownTransaction.cs`

**Step 1: Add smoke test**

Create `TriggerDetachTeardownBoundarySmokeTests.ps1`:

```powershell
$ErrorActionPreference = "Stop"

function Assert-True {
    param([bool]$Condition, [string]$Message)
    if (-not $Condition) { throw $Message }
}

$sourceRoot = Split-Path -Parent $PSScriptRoot
$repoRoot = Split-Path -Parent $sourceRoot

$compPath = Join-Path $repoRoot 'Source\BDP\Core\Trigger\State\CompTriggerBody.cs'
$detachPath = Join-Path $repoRoot 'Source\BDP\Core\Trigger\Runtime\TriggerDetachTeardownTransaction.cs'

$compText = Get-Content -LiteralPath $compPath -Raw -Encoding utf8
$detachText = Get-Content -LiteralPath $detachPath -Raw -Encoding utf8

Assert-True (
    $compText -match 'public override void Notify_Unequipped\(Pawn pawn\)'
) 'CompTriggerBody must own trigger-body unequip teardown.'

Assert-True (
    $compText -match 'RequestRelease\(\)'
) 'Unequipping a trigger body while combat body is active must request release first.'

Assert-True (
    $compText -match 'SyncReservedTrion\(pawn,\s*0f\)'
) 'Unequipping a trigger body must clear reserved Trion.'

Assert-True (
    $compText -match 'ForceTeardownOnDetach\(pawn\)'
) 'Unequipping a trigger body must run detach teardown.'

Assert-True (
    $detachText -match 'ClearSwitchContext'
) 'Detach teardown must clear switch contexts.'

Assert-True (
    $detachText -match 'UnregisterSlotDrain'
) 'Detach teardown must unregister active slot drains.'

Assert-True (
    $detachText -match 'slot\.SetActive\(false\)'
) 'Detach teardown must deactivate slots instead of restoring them on re-equip.'

Assert-True (
    $detachText -match 'UnregisterCombatBodyMaintenanceDrain'
) 'Detach teardown must unregister combat body maintenance drain.'

Assert-True (
    $detachText -match 'ClearPublishedProjection'
) 'Detach teardown must clear published projection.'

Write-Output 'TriggerDetachTeardownBoundary PASS'
```

**Step 2: Run the smoke test**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\TriggerDetachTeardownBoundarySmokeTests.ps1"
```

Expected:

```text
TriggerDetachTeardownBoundary PASS
```

**Step 3: If this fails**

Do not add re-equip restoration. Fix the detach cleanup chain only.

---

### Task 3: Runtime Scope Final Cleanup（临时作用域最终清理）

**Problem:** 临时输入/语义作用域用 Prefix 压栈、Postfix 弹栈。异常路径下 Postfix 不够稳。

**Files:**

- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Semantics/SemanticRuntimeScope.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Interaction/TargetingInputRuntimeScope.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Patches/Patch_Targeter_OrderPawnForceTarget_TargetingInput.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Patches/Patch_DamageWorker_ExplosionDamageThing_BdpSemantics.cs`
- Create: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/RuntimeScopeCleanupBoundarySmokeTests.ps1`

**Step 1: Add smoke test first**

Create `RuntimeScopeCleanupBoundarySmokeTests.ps1` that asserts:

```powershell
$ErrorActionPreference = "Stop"

function Assert-True {
    param([bool]$Condition, [string]$Message)
    if (-not $Condition) { throw $Message }
}

$sourceRoot = Split-Path -Parent $PSScriptRoot
$repoRoot = Split-Path -Parent $sourceRoot

$semanticPath = Join-Path $repoRoot 'Source\BDP\Core\Semantics\SemanticRuntimeScope.cs'
$targetingPath = Join-Path $repoRoot 'Source\BDP\Core\AttackExecution\TargetingProtocol\Interaction\TargetingInputRuntimeScope.cs'
$targetPatchPath = Join-Path $repoRoot 'Source\BDP\Patches\Patch_Targeter_OrderPawnForceTarget_TargetingInput.cs'
$damagePatchPath = Join-Path $repoRoot 'Source\BDP\Patches\Patch_DamageWorker_ExplosionDamageThing_BdpSemantics.cs'

$semanticText = Get-Content -LiteralPath $semanticPath -Raw -Encoding utf8
$targetingText = Get-Content -LiteralPath $targetingPath -Raw -Encoding utf8
$targetPatchText = Get-Content -LiteralPath $targetPatchPath -Raw -Encoding utf8
$damagePatchText = Get-Content -LiteralPath $damagePatchPath -Raw -Encoding utf8

Assert-True (($semanticText -match 'disposed') -and ($semanticText -match 'scopeId')) 'SemanticRuntimeScope PopScope must be idempotent and token-bound.'
Assert-True (($targetingText -match 'disposed') -and ($targetingText -match 'scopeId')) 'TargetingInputRuntimeScope PopScope must be idempotent and token-bound.'

Assert-True ($targetPatchText -match 'HarmonyFinalizer') 'Targeting input patch must clean temporary scope in a Harmony finalizer.'
Assert-True ($damagePatchText -match 'HarmonyFinalizer') 'Explosion damage semantic patch must clean temporary scope in a Harmony finalizer.'

Write-Output 'RuntimeScopeCleanupBoundary PASS'
```

**Step 2: Run it and confirm failure**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\RuntimeScopeCleanupBoundarySmokeTests.ps1"
```

Expected:

```text
FAIL: missing token/idempotent/finalizer boundary.
```

**Step 3: Make PopScope idempotent and token-bound**

In both scope classes:

- Change stack item from raw context/facts to a small private frame:

```csharp
private sealed class ScopeFrame
{
    public long ScopeId;
    public ISemanticContext Value;
}
```

For targeting input, `Value` is `TargetingInputRuntimeFacts`.

- Add:

```csharp
[ThreadStatic]
private static long nextScopeId;
```

- On `Push`, increment id, push frame, return `new PopScope(scopeId)`.
- On `Current`, read `stack.Peek().Value`.
- In `PopScope.Dispose`, return immediately if already disposed.
- Only pop if `stack.Peek().ScopeId == scopeId`.
- If mismatch, log once through `BdpDiagnostics.Once(...)` and do not pop an unrelated frame.

**Step 4: Replace Postfix cleanup with Finalizer cleanup**

In both Harmony patch files:

```csharp
[HarmonyFinalizer]
public static Exception Finalizer(IDisposable __state, Exception __exception)
{
    __state?.Dispose();
    return __exception;
}
```

Add:

```csharp
using System;
```

Keep Prefix unchanged.

Remove the old Postfix cleanup method.

**Step 5: Run smoke test**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\RuntimeScopeCleanupBoundarySmokeTests.ps1"
```

Expected:

```text
RuntimeScopeCleanupBoundary PASS
```

---

### Task 4: Ranged Module Runtime Validation（远程模块运行时校验）

**Problem:** `BdpRangedAttackModuleDef.runtimeClass` 配错时，可能在攻击时才异常。

**Files:**

- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedModules/Config/BdpRangedAttackModuleDef.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedModules/Runtime/RangedAttackModuleResolver.cs`
- Create: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/RangedModuleRuntimeBoundarySmokeTests.ps1`

**Step 1: Add smoke test**

The test should assert:

- `BdpRangedAttackModuleDef` overrides `ConfigErrors()`.
- It checks `runtimeClass == null`.
- It checks `IRangedAttackModuleRuntime`.
- It checks parameterless constructor.
- `RangedAttackModuleResolver` wraps create/initialize in `try/catch`.
- Resolver logs once and continues instead of throwing.

**Step 2: Add Def-level validation**

In `BdpRangedAttackModuleDef.cs`:

```csharp
using System;
using System.Collections.Generic;
using Verse;
```

Implement:

```csharp
/// <summary>
/// 校验远程模块运行时入口，避免错误 Def 到攻击热路径才爆出。
/// </summary>
public override IEnumerable<string> ConfigErrors()
{
    foreach (string error in base.ConfigErrors())
    {
        yield return error;
    }

    if (runtimeClass == null)
    {
        yield return defName + " 缺少 runtimeClass。";
        yield break;
    }

    if (!typeof(IRangedAttackModuleRuntime).IsAssignableFrom(runtimeClass))
    {
        yield return defName + " 的 runtimeClass 必须实现 IRangedAttackModuleRuntime。";
    }

    if (runtimeClass.GetConstructor(Type.EmptyTypes) == null)
    {
        yield return defName + " 的 runtimeClass 必须提供公开无参构造。";
    }
}
```

**Step 3: Add runtime isolation**

In `RangedAttackModuleResolver.ResolveSlots`, wrap:

```csharp
IRangedAttackModuleRuntime runtime = Activator.CreateInstance(...) as IRangedAttackModuleRuntime;
runtime.Initialize(...);
```

with:

```csharp
try
{
    // create and initialize
}
catch (Exception ex)
{
    BdpDiagnostics.Once(
        "ranged_module.runtime_failed." + mount.moduleDef.defName,
        "远程模块运行时创建或初始化失败，已跳过。module=" + mount.moduleDef.defName + ", runtimeClass=" + mount.moduleDef.runtimeClass + "\n" + ex);
    continue;
}
```

Add:

```csharp
using System;
using BDP.Support.Diagnostics;
```

**Step 4: Run smoke test**

Expected:

```text
RangedModuleRuntimeBoundary PASS
```

---

### Deferred Task 5: CombatBody Mirror Apparel Quality Boundary（战斗体镜像服装品质边界，需单独确认）

**Problem:** 当前用反射移除 `CompQuality`，行为可接受，但字段缺失时没有诊断。

**Decision:** 这是低优先维护项，不进入默认执行范围。若用户后续明确批准，只给当前反射边界补一次性诊断；不重做服装系统，不新建大抽象。

**Execution Gate:** 未得到明确批准前，不得修改 `PawnCombatBodyBridge.cs`，不得新增本任务测试文件。

**Files:**

- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/CombatBody/Bridge/PawnCombatBodyBridge.cs`
- Create: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyMirrorQualityBoundarySmokeTests.ps1`

**Step 1: Add smoke test**

Assert:

- `RemoveQualityComp` still exists and remains private.
- It contains `BindingFlags.NonPublic`.
- It logs through `BdpDiagnostics.Once` if reflection fields are unavailable.
- No new public service/class is introduced for this low-level cleanup.

**Step 2: Add diagnostic only**

In `RemoveQualityComp`, after resolving fields:

```csharp
if (compsField == null || compsByTypeField == null)
{
    BdpDiagnostics.Once(
        "combatbody.mirror_apparel.quality_reflection_unavailable",
        "战斗体镜像服装无法访问 RimWorld 内部品质 Comp 缓存字段，已降级为只清空公开 compQuality 引用。");
    copy.compQuality = null;
    return;
}
```

Add `using BDP.Support.Diagnostics;` if missing.

**Step 3: Run smoke test**

Expected:

```text
CombatBodyMirrorQualityBoundary PASS
```

---

### Deferred Task 6: Cross-Layer DTO Comments（跨层数据注释，需单独确认）

**Problem:** 少量跨层数据模型缺逐成员注释，容易把“内部记录”“公开快照”“临时诊断”混用。

**Decision:** 这是低优先维护项，不进入默认执行范围。若用户后续明确批准，不全仓补注释，只补指定跨层 DTO。

**Execution Gate:** 未得到明确批准前，不得修改这些 DTO 文件，不得顺手整理注释。

**Files:**

- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedProtocol/Model/FireEmitRecord.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedProtocol/Model/PrepareRecord.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedProtocol/Model/RangedAttackProtocolResult.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedProtocol/Model/RangedAttackTrionGateResult.cs`
- Modify if still missing direct member comments: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Trigger/Visual/Diagnostics/TriggerVisualEmissionDiagnosticsAccess.cs`
- Test: no behavior test; compile is enough.

**Step 1: Add concise member comments**

Each public/internal field or property gets one short XML comment:

```csharp
/// <summary>
/// 当前发射条目对应的目标语义；只服务本次远程协议结果，不作为长期真值保存。
/// </summary>
public LocalTargetInfo SemanticTarget { get; set; }
```

Keep comments short.

**Step 2: Do not rename fields**

No behavior changes. No serialization key changes.

---

### Task 7: Trion Restore Comment Fix（Trion 读档注释修正）

**Problem:** 注释容易让人误以为持续消耗账本完整序列化恢复；实际是数值存档，持续消耗登记表读档后重建。

**Execution Gate:** 本任务已获用户确认加入默认执行，但只允许修正误导性注释，不允许改变 Trion 存读档行为、字段、注册流程或恢复顺序。

**Files:**

- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/CombatBodySession/CombatBodySessionService.cs`
- Read: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Trion/CompTrion.cs`
- Read: `模组工程/BorderDefenseProtocol/Source/BDP/Core/CombatBodySession/CombatBodySessionTrionBinding.cs`

**Step 1: Update misleading comment**

Replace:

```csharp
/// Trion 账本本身由 owner 序列化恢复，这里只补回事件句柄。
```

with:

```csharp
/// Trion 标量由 CompTrion 序列化恢复；持续消耗登记表是运行时账本，读档后由各 owner 重新注册。
/// 这里负责补回战斗体维护消耗与事件句柄。
```

**Step 2: No behavior change**

Do not change Trion save/load behavior in this task.

---

### Task 8: Final Verification（最终验证）

**Files:**

- Verify all touched files.

**Step 1: Run targeted smoke tests**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\AutoAttackSeparationSmokeTests.ps1"
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\TriggerDetachTeardownBoundarySmokeTests.ps1"
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\RuntimeScopeCleanupBoundarySmokeTests.ps1"
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\RangedModuleRuntimeBoundarySmokeTests.ps1"
```

Expected:

```text
All PASS.
```

Only if the remaining deferred tasks were explicitly approved and executed, also run their corresponding smoke tests.

**Step 2: Run existing nearby smoke tests**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\AutoRangedEntryTokenGuardSmokeTests.ps1"
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\AutoRangedEntryStagingGuardSmokeTests.ps1"
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\CombatBodyTriggerTrionIntegrationSmokeTests.ps1"
powershell -ExecutionPolicy Bypass -File "模组工程\BorderDefenseProtocol\Source\BDP.Tests\FormalHostVerbSmokeTests.ps1"
```

Expected:

```text
All PASS.
```

**Step 3: XML parse verification**

Run:

```powershell
$files = rg --files "模组工程\BorderDefenseProtocol" -g "*.xml"
$errors = @()
foreach ($file in $files) {
    try { [xml](Get-Content -LiteralPath $file -Raw) | Out-Null }
    catch { $errors += "$file :: $($_.Exception.Message)" }
}
if ($errors.Count -eq 0) { "XML_PARSE_OK files=$($files.Count)" } else { $errors }
```

Expected:

```text
XML_PARSE_OK files=7
```

**Step 4: Build without touching mod Assemblies output**

Run:

```powershell
$project = (Resolve-Path -LiteralPath "模组工程\BorderDefenseProtocol\Source\BDP\BDP.csproj").Path
$out = Join-Path $env:TEMP "bdp-plan-build-out"
$obj = Join-Path $env:TEMP "bdp-plan-build-obj"
New-Item -ItemType Directory -Force -Path $out, $obj | Out-Null
dotnet build $project -c Release "/p:OutputPath=$out\" "/p:BaseIntermediateOutputPath=$obj\" "/p:IntermediateOutputPath=$obj\Release\"
```

Expected:

```text
已成功生成。
0 个警告
0 个错误
```

**Step 5: Search residue**

Run:

```powershell
rg -n "activationCost|switchCooldownTicks|当前处于起步准备阶段|pawn\.TryGetAttackVerb\s*\(" "模组工程\BorderDefenseProtocol\Source\BDP" -g "*.cs"
```

Expected:

- No `activationCost` / `switchCooldownTicks` / stale About text.
- No `pawn.TryGetAttackVerb(...)` inside BDP auto-ranged bridge path.
- References to `TryGetAttackVerb` patch attribute are okay.

---

## Commit Plan（提交计划）

No new branch. No worktree.

Suggested commits after verification:

1. `fix: preserve vanilla manual gate for BDP auto ranged`
2. `test: lock trigger detach teardown boundary`
3. `fix: harden temporary runtime scope cleanup`
4. `fix: validate ranged module runtime classes`
5. `chore: clarify trion restore bookkeeping comment`

Do not commit if any targeted smoke test or build fails.
