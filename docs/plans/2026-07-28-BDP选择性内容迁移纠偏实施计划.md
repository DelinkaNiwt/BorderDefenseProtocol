# BDP 选择性内容迁移纠偏 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 恢复 `BorderDefenseProtocol.DevHarness` 为完整候选内容库，并把主模组收紧到已确认的事项 01 至 03、正式 Trion（触粒子）腺体基因图标和原主模组既有内容。

**Architecture:** 主模组继续作为一个 RimWorld（边缘世界）模组包，内部只发布单向依赖的 `BDP.Core.dll` 与最小 `BDP.Content.dll`。未确认业务恢复到独立候选模组，今后按事项逐个“移动并删除原件”，避免重复 DefName（定义名称）、补丁和资源真值。

**Tech Stack:** RimWorld 1.6（边缘世界1.6）、C# 7.3（C#语言7.3）、XML（可扩展标记语言）、Harmony（补丁框架）、PowerShell（命令行脚本）、Git（版本控制）、System.Drawing（图像检查库）。

---

## 执行约束

- 不建立 branch（分支）或 worktree（工作树），直接在当前工程执行。
- 不整体回滚事项 01 提交；只从 `9ec835ca^` 精确恢复候选模组树和受迁移影响的测试。
- 不做旧存档、旧 DefName 或旧命名空间兼容。
- 不提前整理事项 04 及之后的业务内容。
- 主模组的 `BDP.Core.dll` 不得依赖任何本模组其他 DLL（动态链接库）。
- 文件删除前必须验证目标绝对路径位于主模组目录内。
- 先写失败的边界测试，再实施最小纠偏。

### Task 1: 建立选择性迁移边界测试

**Files:**

- Create: `Source/BDP.Tests/SelectiveContentMigrationBoundarySmokeTests.ps1`
- Modify: `Source/BDP.Tests/UnifiedModAssemblyLayoutSmokeTests.ps1`
- Modify: `Source/BDP.Tests/UnifiedModTestPathSmokeTests.ps1`
- Modify: `Source/BDP.Tests/ContentBootstrapSmokeTests.ps1`

**Step 1: 写候选库与主模组白名单断言**

新测试必须检查：

- 候选模组的 `About/About.xml`、`LoadFolders.xml`、工程、代表性 Def、源码、资源、测试和文档存在。
- 主模组不存在 `1.6/ContentRoot`、`1.6/ContentVersion`、`docs/DevHarness`、`Source/BDP.Content.Tests`。
- 主模组 `Source/BDP.Content` 排除构建产物后只含 `BDP.Content.csproj` 与 `ContentBootstrap.cs`。
- `ContentBootstrap` 保留 `Harmony("niwt.bdp.content").PatchAll()`，不注册触发体按钮或 Trion 面板，不引用 `BDP.DevHarness`。
- 主模组 `LoadFolders.xml` 只加载 `/` 和 `1.6`。
- 新基因图标为 64×64 透明 PNG，并具有透明角像素。

**Step 2: 运行并确认测试先失败**

Run:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\SelectiveContentMigrationBoundarySmokeTests.ps1'
```

Expected: FAIL，至少报告候选模组 `About.xml` 尚未恢复或主模组 `ContentRoot` 尚未移除。

**Step 3: 提交测试红灯**

只提交新增和修订的边界测试，提交信息：

```text
test: 锁定选择性内容迁移边界
```

### Task 2: 恢复候选模组完整内容

**Files:**

- Restore tree: `../BorderDefenseProtocol.DevHarness`
- Restore 52 modified files: `Source/BDP.Tests/*.ps1`（以事项 01 的精确修改清单为准）

**Step 1: 校验历史基准**

Run:

```powershell
git cat-file -e 9ec835ca^
git diff-tree -r --name-only --diff-filter=M 9ec835ca^ 9ec835ca -- '模组工程/BorderDefenseProtocol/Source/BDP.Tests'
```

Expected: 历史对象存在，测试修改清单正好 52 项。

**Step 2: 恢复候选模组**

Run:

```powershell
git restore --source=9ec835ca^ -- '模组工程/BorderDefenseProtocol.DevHarness'
```

Expected: 候选模组恢复原 `About`、`LoadFolders.xml`、`1.6`、`Source`、`Textures` 和 `Docs`。

**Step 3: 恢复迁移前测试路径**

逐项对 52 个明确文件执行：

```powershell
git restore --source=9ec835ca^ -- '<relative-test-path>'
```

Expected: 候选业务测试重新指向 `BorderDefenseProtocol.DevHarness`，不再假定未确认内容已在主模组。

### Task 3: 清除主模组未确认内容

**Files:**

- Delete: `1.6/ContentRoot`
- Delete: `1.6/ContentVersion`
- Delete: `docs/DevHarness`
- Delete: `Source/BDP.Content.Tests`
- Delete: `Source/BDP.Content/*` except `BDP.Content.csproj` and `ContentBootstrap.cs`
- Modify: `LoadFolders.xml`
- Modify: `About/About.xml`

**Step 1: 验证删除目标**

对每个目标使用 `Resolve-Path`，确认其绝对路径以：

```text
C:\NiwtDatas\Projects\RimworldModStudio\模组工程\BorderDefenseProtocol\
```

开头；若有任一目标越界则立即停止。

**Step 2: 删除四个已确认目录**

使用 PowerShell 原生 `Remove-Item -LiteralPath -Recurse -Force` 删除上述明确目录。

**Step 3: 收紧 Content 源码**

删除 `Source/BDP.Content` 中除两个白名单文件外的源码和构建产物，使内容程序集暂时不包含事项 04 之后的业务。

**Step 4: 收紧加载目录和模组说明**

`LoadFolders.xml` 只保留：

```xml
<li>/</li>
<li>1.6</li>
```

`About.xml` 说明一个正式模组包包含 Core（核心）与 Content（内容）两个程序集，并且内容按确认事项逐步迁入。

### Task 4: 正式化空业务 Content 启动入口

**Files:**

- Modify: `Source/BDP.Content/ContentBootstrap.cs`
- Keep: `Source/BDP.Content/BDP.Content.csproj`

**Step 1: 删除未确认业务接线**

移除：

```csharp
using BDP.Core.Trion.External;
using BDP.Core.Trigger;
using BDP.DevHarness;
TriggerExternalGizmoRegistry.Register(...);
TrionGizmoExtensionRegistry.RegisterPanel(...);
```

**Step 2: 保留正式空入口**

静态构造器只执行：

```csharp
new Harmony("niwt.bdp.content").PatchAll();
```

该程序集当前无补丁，因此不会改变游戏行为，只保留以后接入已确认内容的入口。

### Task 5: 安装并验证正式基因图标

**Files:**

- Add: `1.6/Textures/UI/Icons/Genes/Gene_TrionGland.png`
- Keep: `1.6/Defs/Genes/Trion/GeneDefs_TrionGland.xml`

**Step 1: 检查路径真值**

确认 GeneDef（基因定义）的 `iconPath` 为：

```text
UI/Icons/Genes/Gene_TrionGland
```

**Step 2: 检查图像**

使用 `System.Drawing.Bitmap` 验证：

- 宽高均为 64。
- 四角至少一个像素 Alpha（透明度）为 0，且四角均不出现不透明底色。
- 文件位于主模组标准 `1.6/Textures` 路径。

不新增基因物品贴图，物品表现继续使用 RimWorld 原版基因贴图。

### Task 6: 构建与依赖验证

**Files:**

- Build: `Source/BDP/BDP.csproj`
- Build: `Source/BDP.Content/BDP.Content.csproj`
- Build: `../BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/BDP.DevHarness.csproj`

**Step 1: 构建主模组两个程序集**

Run:

```powershell
dotnet msbuild '.\模组工程\BorderDefenseProtocol\Source\BDP.Content\BDP.Content.csproj' /t:Rebuild /p:Configuration=Release /v:minimal
```

Expected: Core 依赖门禁 PASS，`BDP.Core.dll` 与 `BDP.Content.dll` 构建成功。

**Step 2: 构建候选模组**

Run:

```powershell
dotnet msbuild '.\模组工程\BorderDefenseProtocol.DevHarness\Source\BDP.DevHarness\BDP.DevHarness.csproj' /t:Rebuild /p:Configuration=Release /v:minimal
```

Expected: `BDP.DevHarness.dll` 构建成功；若只因程序集改名或工程路径变化失败，仅修复机械引用，不调整候选业务。

**Step 3: 验证发布目录**

主模组 `1.6/Assemblies` 只允许：

```text
BDP.Core.dll
BDP.Core.pdb
BDP.Content.dll
BDP.Content.pdb
```

候选模组的 `1.6/Assemblies` 保留自己的 `BDP.DevHarness.dll` 与调试符号。

### Task 7: 全量静态验收

**Files:**

- Test: `Source/BDP.Tests/*.ps1`
- Test: `../BorderDefenseProtocol.DevHarness/Source/BDP.Tests/*.ps1`
- Parse: both mod roots `*.xml`

**Step 1: 运行主模组测试**

逐个运行 `Source/BDP.Tests` 下 PowerShell 测试，记录通过、失败和脚本异常总数。

Expected: 新增选择性迁移边界测试通过；既有已知失败需与基线逐项比对，不隐藏新回归。

**Step 2: 运行候选模组测试**

逐个运行候选测试目录下 PowerShell 测试，记录结果并区分历史失败与本次新增失败。

**Step 3: 解析 XML**

递归读取两个模组活动目录中的 XML 文件并转换为 `[xml]`。

Expected: 无解析错误。

**Step 4: 再次运行边界与依赖门禁**

Expected: 选择性迁移边界、双程序集布局、Content 启动器和 Core 单向依赖检查全部通过。

### Task 8: 同步总计划、日志与提交

**Files:**

- Modify: `docs/plans/2026-07-28-BDP统一模组双程序集迁移总计划.md`
- Modify: `C:/NiwtDatas/Projects/RimworldModStudio/日志/Agent工作日志/Agent日志12.md`

**Step 1: 更新动态检查点**

写明：

- 事项 01：`已完成（已纠偏）`。
- 事项 03：`已完成（已纠偏）`。
- 事项 03A：`已完成`。
- 当前检查点进入事项 04，但事项 04 仍为 `未介绍`，不得自行实施。
- 记录实际测试、构建结果和提交号。

**Step 2: 写工作日志**

按时间倒序在日志顶部增加一条，简要记录恢复候选库、清理主模组、正式图标、测试和提交。

**Step 3: 提交实现**

仅暂存两个 BDP 模组、本次计划与工作日志的相关文件，不包含工作区其他既有改动。

提交信息：

```text
refactor: 收紧主模组至已确认迁移内容
```

**Step 4: 最终复核**

Run:

```powershell
git status --short -- '模组工程/BorderDefenseProtocol' '模组工程/BorderDefenseProtocol.DevHarness' '日志/Agent工作日志'
```

Expected: 本次范围无遗漏的未提交变更；工作区其他既有改动保持原样。
