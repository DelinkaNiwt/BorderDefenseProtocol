# BDP 事项 06 按钮状态补正实施计划

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development and superpowers:verification-before-completion while executing this plan.

**Goal:** 将玩家调试按钮改为上帝模式限定，并将开启、解除战斗体合并成一个按相位变化的正式按钮。

**Architecture:** 调试入口和画点运行链统一读取 RimWorld 原版 `DebugSettings.godMode`。战斗体按钮继续只调用既有正式读取面和命令面，不新增执行路线。

**Tech Stack:** RimWorld 1.6、C# 7.3、PowerShell 冒烟测试、MSBuild。

---

### 任务 1：建立失败契约

**文件：**

- 新建：`Source/BDP.Tests/Item06ButtonStateCorrectionSmokeTests.ps1`
- 修改：`Source/BDP.Tests/Item06CombatBodyGizmoAndDiagnosticsCleanupSmokeTests.ps1`
- 修改：`Source/BDP.Tests/TriggerVisualMarkerOverlaySmokeTests.ps1`

步骤：

1. 检查画点按钮、地图绘制和发射点记录只使用 `DebugSettings.godMode`。
2. 检查四个 Trion 数值调节按钮只使用 `DebugSettings.godMode`。
3. 检查战斗体提供器只构建一个 `Command_Action`。
4. 检查未开启和已开启相位均可操作，崩解和冷却相位才禁用。
5. 运行专项测试，确认旧实现按预期失败。
6. 提交失败契约。

### 任务 2：最小修改运行代码

**文件：**

- 修改：`Source/BDP/Core/Genes/TrionGeneGizmoBridge.cs`
- 修改：`Source/BDP/Core/CombatBody/External/CombatBodyTriggerGizmoProvider.cs`
- 修改：`Source/BDP/Core/Trigger/Visual/Diagnostics/TriggerVisualEmissionDiagnosticsAccess.cs`
- 修改：`Source/BDP.Content/Trigger/Diagnostics/TriggerVisualMarkerGizmoProvider.cs`
- 修改：`Source/BDP.Content/Trigger/Diagnostics/MapComponent_TriggerVisualMarkerOverlay.cs`

步骤：

1. 将五个玩家调试按钮的显示门槛改为 `DebugSettings.godMode`。
2. 将画点地图绘制和发射点记录门槛改为 `DebugSettings.godMode`。
3. 用一个动作按钮按 `CombatBodyPhase` 选择文字和正式命令。
4. 仅在 `Collapsing` 和 `Cooldown` 时禁用按钮。
5. 运行专项测试并提交实现。

### 任务 3：完整验证与记录

**文件：**

- 修改：`docs/plans/2026-07-28-BDP统一模组双程序集迁移总计划.md`
- 修改：`C:/NiwtDatas/Projects/RimworldModStudio/日志/Agent工作日志/Agent日志13.md`

步骤：

1. 构建 Core、Content 和候选程序集。
2. 运行 Core 单向依赖检查。
3. 运行事项 06 补正专项测试。
4. 解析两个模组全部 XML。
5. 运行全量 PowerShell 冒烟测试并对照 12 项既有失败基线。
6. 更新动态计划和工作日志，提交最终记录。

