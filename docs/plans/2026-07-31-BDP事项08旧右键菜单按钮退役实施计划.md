# BDP 事项08旧右键菜单按钮退役 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 删除候选模组中已经没有调用者的旧右键菜单按钮类。

**Architecture:** 先建立“候选运行源码不得存在旧类型”的退役边界检查，再物理删除单个C#文件；不修改正式Content按钮实现。

**Tech Stack:** C# 7.3、PowerShell冒烟测试、MSBuild。

---

### Task 1：建立退役失败契约

**Files:**

- Create: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessLegacyRightClickCommandRetirementSmokeTests.ps1`

1. 断言候选运行源码不再出现`Command_ActionWithRightClickMenu`。
2. 断言Content正式形态按钮`Command_ChipMode`继续存在并覆盖右键菜单入口。
3. 删除前运行，确认因旧类仍存在而失败。
4. 提交失败契约。

### Task 2：删除旧类并验证

**Files:**

- Delete: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Command_ActionWithRightClickMenu.cs`
- Modify: `模组工程/BorderDefenseProtocol/docs/plans/2026-07-28-BDP统一模组双程序集迁移总计划.md`
- Modify: `日志/Agent工作日志/Agent日志16.md`

1. 物理删除旧C#文件。
2. 运行退役测试。
3. 构建DevHarness和Content，确认零编译错误。
4. 搜索现行源码确认旧类型引用为零。
5. 更新总计划和工作日志并提交。

