# Trion 检测仪时长与培育仓外观 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将固定 Trion 检测仪基准时间改为 1 游戏小时，并完整采用原版培育仓外观和默认横向 2×1 占地。

**Architecture:** 保留现有检测工作流，只修改建筑常量和 ThingDef（物体定义）展示参数。使用现有专项冒烟测试锁定时间、贴图、尺寸与默认朝向。

**Tech Stack:** RimWorld 1.6、C# 7.3、XML Def、PowerShell、MSBuild。

---

### Task 1: 锁定并修改时间与外观

**Files:**

- Modify: `Source/BDP.Tests/TrionDetectorProgressLifecycleSmokeTests.ps1`
- Modify: `Source/BDP.Tests/TrionAssessmentDeviceSmokeTests.ps1`
- Modify: `Source/BDP/Core/Trion/Assessment/Building_TrionDetector.cs`
- Modify: `1.6/Defs/Buildings/Trion/ThingDefs_TrionDetector.xml`

**Step 1: Write the failing test**

将断言改为：

- `WorkRequired = 2500f`
- 检查信息包含“1游戏小时”
- 贴图为 `Things/Building/Misc/GrowthVat/GrowthVat`
- `size`、`drawSize` 均为 `(1,2)`
- `defaultPlacingRot` 为 `East`

**Step 2: Run test to verify it fails**

Run:

```powershell
& Source/BDP.Tests/TrionDetectorProgressLifecycleSmokeTests.ps1
& Source/BDP.Tests/TrionAssessmentDeviceSmokeTests.ps1
```

Expected: FAIL，旧代码仍为 7,500 tick 和高科技研究台 2×2 外观。

**Step 3: Write minimal implementation**

只修改上述常量、检查文本和 XML 展示参数。

**Step 4: Run tests and build**

Run:

```powershell
& Source/BDP.Tests/TrionDetectorProgressLifecycleSmokeTests.ps1
& Source/BDP.Tests/TrionAssessmentDeviceSmokeTests.ps1
dotnet msbuild Source/BDP/BDP.csproj /t:Rebuild /p:Configuration=Release /v:minimal
```

Expected: 两项测试通过，编译退出码为 0。

**Step 5: Commit**

```powershell
git add <本任务精确文件>
git commit -m "tune: 调整Trion检测仪时长与外观"
```
