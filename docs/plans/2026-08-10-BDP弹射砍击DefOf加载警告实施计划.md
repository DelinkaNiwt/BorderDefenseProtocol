# BDP 弹射砍击 DefOf 加载警告 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 消除弹射砍击在 Def 加载阶段过早访问 `DamageDefOf.Cut` 的警告，同时保持 XML 配置和运行期兜底行为不变。

**Architecture:** 数据字段默认保持 `null`，由 XML 正常注入；实际伤害执行点才访问已初始化的 `DamageDefOf.Cut`。不增加生命周期方法，也不触及芯片制造和存档兼容代码。

**Tech Stack:** RimWorld 1.6 C#、XML、PowerShell 冒烟测试、MSBuild。

---

### Task 1: 用失败测试锁定 DefOf 初始化边界

**Files:**
- Create: `Source/BDP.Tests/BounceSlashDefOfInitializationSmokeTests.ps1`

**Step 1:** 断言 `CompProperties_BounceSlash.damageDef` 与 `PawnFlyer_BounceSlash.damageDef` 的字段声明不含 `DamageDefOf` 初值。

**Step 2:** 断言 AbilityDef XML 显式配置 `Cut`，并且运行期伤害调用保留 `damageDef ?? DamageDefOf.Cut`。

**Step 3:** 运行专项测试，预期因当前字段初值而 FAIL。

### Task 2: 移除过早 DefOf 初值

**Files:**
- Modify: `Source/BDP.Content/BounceSlash/CompAbilityEffect_BounceSlash.cs`
- Modify: `Source/BDP.Content/BounceSlash/PawnFlyer_BounceSlash.cs`

**Step 1:** 将两个字段声明改为 `public DamageDef damageDef;`。

**Step 2:** 不改变 XML 与实际伤害点的运行期空值兜底。

**Step 3:** 运行专项测试，预期 PASS。

### Task 3: 验证、日志与提交

**Files:**
- Modify: `C:/NiwtDatas/Projects/RimworldModStudio/日志/Agent工作日志/Agent日志39.md`

**Step 1:** 重建 `Source/BDP.Content/BDP.Content.csproj` Release。

**Step 2:** 运行弹射砍击相关测试、XML 解析和 `git diff --check`。

**Step 3:** 记录仅修复 DefOf 初始化警告，明确未触及另外两类问题。

**Step 4:** 提交为 `fix: defer bounce slash damage DefOf access`。
