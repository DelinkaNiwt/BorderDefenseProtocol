# BDP 统一模组双程序集迁移进度归档

本文件保存从动态总计划移出的较早进度记录。总计划只保留最近二十条，归档内容不得视为失效。

### 2026-07-29：事项 05 完成

- `TriggerLoadoutPanelExtension` 和 `TriggerLoadoutPanelProvider` 进入 `BDP.Content.Trigger.UI`，由 Content 正式启动入口注册；候选重复源码和注册删除。
- 当前候选触发体显式挂载许可扩展；面板不再根据槽位数量猜测，未许可触发体即使有槽位也不显示。
- 面板继续只调用 Core 正式读取面、交互面和命令面；Core 未新增类别或界面业务，零本模组程序集依赖门禁通过。
- 主、副槽位视觉、启用/关闭/切换和进度条行为保留；玩家文字清除开发术语，显示编号从1开始，底层索引不变。
- 三个程序集构建成功；46 个 XML 全部可解析；事项 05 九项专项检查通过；全量 224 项为 212 通过、12 项既有基线失败、0 项新增失败。
- 失败契约提交为 `5d43c742`，运行实现提交为 `29db6c20`；下一检查点转到事项 06。

### 2026-07-29：事项 05 整体设计确认

- 当前活动右侧芯片面板只保留战斗中的启用、关闭和切换，不承担装入、卸下或编辑职责。
- 每个具体触发体通过 `TriggerLoadoutPanelExtension` 主动许可玩家面板；Content 拥有许可扩展与面板，Core 保持中性。
- 面板视觉结构不重做；玩家文字去除开发者语义，显示编号从1开始，底层索引保持不变。
- 只迁移当前活动面板；旧式独立窗口、诊断窗口和摘要控件继续留在候选库。
- 用户授权设计与逐文件实施计划写完后直接开始修改，不需要再次等待确认。
- 逐文件实施计划已完成，拆分为失败契约、运行迁移、玩家文字、构建回归和完成记录五个任务。

## 决定记录归档

### 2026-07-30

- 用户确认容量潜质继续使用模糊档位；检测后的Trion释放力和芯片释放力要求都显示精确整数，不使用小数，也不为释放力另建模糊档位系统。具体整数上下限与生成分布仍待确认。

- 用户确认现有一次Trion潜质检测同时揭示相互独立的容量潜质与释放力，不新增第二套检测；并补正现有行为：植入有效腺体后“Trion容量潜质”仍在角色面板永久保留，不再因精确容量可读而隐藏。释放力的检测显示精度仍待确认。
- 用户确认事项07B采用“永久先天底数＋原版属性最终值”分层：每个角色独立生成并保存先天释放力，腺体只负责实际启用；未来后天或临时修正只改变`BDP_TrionIntensity`最终属性，不覆盖先天底数。具体数值范围与分布仍待后续确认。
- 用户确认事项07B采用与潜在容量数值独立的先天门槛；现有“Trion潜质”修正为“Trion容量潜质”，新角色属性为`TrionIntensity（Trion释放力）`，芯片字段为`TrionIntensityRequirement（释放力要求）`，删除旧`PowerRequirement`且不兼容；暂不建立容量与释放力的概率关联。
- 用户确认事项07A完整设计：形态仅存在于正式启用期间并保存在根槽，启用从默认形态开始、关闭清除；切换即时且不收费，共用表达保持宿主状态，旧攻击暂定立即中断；Core负责真值、命令、存读档和原子回滚，Content按现有玩家芯片控制许可注册通用Gizmo，左键循环、右键精确选择；候选库增加一枚最小校验芯片，不迁入“光魂”。
- 用户再次明确最终交付目标：`BorderDefenseProtocol.DevHarness（候选测试模组）`只在迁移期间充当候选库；所有内容逐项迁入主模组或明确舍弃后，事项20必须物理退役整个测试模组，最终只保留主模组以及严格单向依赖的`BDP.Core.dll / BDP.Content.dll`。
- 用户确认启用互斥改为受控 `ActivationExclusionGroups / ChipExclusionGroupDef`；冲突时同侧让位芯片与全部跨侧冲突芯片同时正常关闭，目标等待全部解除后再正常开启。同侧或互相冲突的后请求覆盖前请求，跨侧非冲突请求并行；等待/开启中的同一目标不可主动取消，Content 面板使用亮琥珀色等待描边。
- 用户确认事项 07 形态配置采用“统一表达目录＋形态引用条目标识”：删除 `Loadout.InitialModeKey`，新增 `Expression.DefaultModeKey`；单形态直接启用全部条目，多形态通过 `ActiveEntryIds`完整列出本形态条目；旧追加、替换、禁用操作结构不兼容删除。本项只支持默认形态解释，当前形态状态、切换和存读档留到事项 07A。

### 2026-07-29

- 用户推翻此前“芯片本体承担持续费用”的决定：删除 `ActiveDrainPerSecond`整条链，改由最终有效表达的 `SustainCostBySourceCount`统一收费；档位从 1 连续书写，超出最高档沿用末档，Combo 必须显式配置。候选状态芯片确认采用 1 个有效来源 `2/秒`、2 个及以上总计 `5/秒`。
- 用户确认事项 07 的扩展采用静态强类型方案：Core 只提供 `ChipExtensionConfig`父类型、集合和重复类型校验；Content 按实际业务定义具体扩展；删除旧三段文本及没有消费者的表达运行快照复制链，不建立注册中心或自动执行框架。
- 用户确认立即实施事项 07 芯片分类正式化：Core 只建立 `ChipCategoryDef` 和统一引用/校验机制，不预设具体业务分类；DevHarness 以候选库专属“未审定芯片”分类承接现有正常芯片，故意非法样本继续缺失分类；删除旧 `PrimaryCategory` 和画像 `Tags`，不兼容。
- 用户确认 Combo 条目没有显式 `<Trion>`或`<TrionResolve>`时费用归零，不再自动继承芯片A第一条表达费用；同时按推荐完整删除 Combo 顶层无消费者的容量、激活费用及其配置/契约/求值管道。
- 用户确认芯片每次激活的一次性费用属于正确需求；现有表达结果的 `UseCost` 等具体扣费保持不动。删除没有运行消费者、也无正式 XML 使用的表达条目 `ActivationCost` 及其传递/组合继承空管道；组合定义顶层 Trion 参数另行审查。
- 用户确认事项07的`CapacityCost（容量占用）`完全保持现况：开启战斗体时锁定所有已装芯片的合计容量，关闭单枚芯片不释放，解除战斗体后统一释放；现有字段名和运行机制均不改。
- 用户确认持续 Trion 消耗只允许三类正式来源：战斗体固定维持、芯片激活固定维持、战斗体伤口泄漏；表达条目级 `SustainCost` 与芯片级持续消耗语义重复，删除其整条实现链，芯片字段规范为 `ActiveDrainPerSecond`。
- 用户确认事项 07 保留芯片本体启用、停用延迟；`WarmupTicks / WinddownTicks`正式改名为 `ActivationDelayTicks / DeactivationDelayTicks（启用/停用延迟）`，默认60/30 tick，且不得与攻击前摇或形态切换时长混用。
- 用户确认事项 07 保留一枚芯片成对占用主副同编号槽位的底层能力，但去掉“双持”业务语义；正式改为必填 `SlotOccupancy（槽位占用方式）`，只设 `Single（单槽）`和`PairedHands（成对主副槽）`，以`Unspecified（未指定）`识别漏填；修正成对激活费重复结算，具体双持攻击仍留给表达与上层应用。用户授权实施计划写完后直接执行。
- 用户确认事项 07 的芯片槽位区域保持简单二选一：旧 `SidePolicy`不兼容改为必填 `SlotRegion（槽位区域）`，只允许 `MainSub（主副槽区）`与`Special（特殊槽区）`；底层 `Unspecified（未指定）`只用于把漏填识别为定义错误，不建立开放式槽位组 Def。
- 用户确认事项 07 保留“可共同搭载但不能同时启用”的芯片互斥机制；自由文本 `ExclusionTags（排斥标签）`改为受控的 `ChipExclusionGroupDef（芯片互斥组定义）`，现有临时 `DualWieldTest`不转正。
- 用户确认事项 07 的功率需求是真实公共参数：使用者另有“可输出功率”属性，芯片分别以自身需求作单芯片门槛；功率不足时即使 Trion 足够且芯片已搭载、激活，仍不能真正使用其效果；不建立所有芯片共同瓜分的总功率额度，并新增事项07B完成正式接线。
- 用户用“光魂”提出真实多形态需求：默认大盾形态只有护盾格挡，巨刃形态只有近战攻击，两者共享快速冲刺 Ability（技能）；确认保留默认形态需求并改为 `Expression.DefaultModeKey`，新增事项07A完成通用形态状态、切换、存读档和表达刷新，具体“光魂”不提前迁入。
- 用户确认事项 07 保留统一的 `Extensions（扩展）`分级以整理芯片 XML；内部改为继承 Core 中性共同父类型的强类型扩展，具体字段由 Content 业务扩展定义；删除旧的块名、目标系统和任意文本载荷。
- 用户确认事项 07 的芯片分类必须存在且统一受控；采用 RimWorld 式 `ChipCategoryDef（芯片分类定义）`，Core 只提供中性分类制度，具体分类以后结合真实芯片逐项确定；现有临时分类和自由文本标签不转正。
- 用户确认事项 06 按钮状态补正：画点诊断及 Trion `+50/-50/MAX/归零` 四个调试按钮只在上帝模式显示；开启、解除战斗体合并为一个按钮，未开启和已开启时都亮且可点击，只有崩解或冷却阶段变灰。
- 用户确认事项 06 最终只保留“画点诊断”，“姿态诊断”也删除；删除按钮不得只是隐藏，其专属窗口、旧控件和已经失去调用者的 Core 诊断传递链必须一并清理，但画点仍依赖的共用视觉测量能力不得误删。
- 用户确认事项 05：只正式化战斗中的芯片启用、关闭和切换；具体触发体通过 Content 许可扩展主动声明是否显示玩家面板；玩家文字清理开发者语义，槽位编号从1开始；Core 不加入类别业务，旧式窗口和诊断内容不迁入；设计与计划写完后可直接实施。
- 用户在事项 04 完成后的独立支线确认：候选测试模组所有红色拖尾改为亮薄荷绿长拖尾，并删除不再使用的炽红预设；画点诊断等其它红色表现不属于拖尾，不作修改。


### 2026-07-28

- 用户确认采用符合 RimWorld（边缘世界）习惯的“`1.6`主版本根目录 + `1.6/Content`补充加载层”：程序集继续统一放在 `1.6/Assemblies`，Core/待复查主配置保留 `1.6/Defs`，已确认 Content 配置和专属资源进入 `1.6/Content/Defs`、`1.6/Content/Textures`；当前不建立 `1.6/Core`，避免提前把现有全部主配置定性为 Core。
- 用户确认重构默认不兼容旧存档、旧 DefName（定义名称）和旧命名空间。
- 用户要求触发器、触发体的完整归类与命名延后处理，但必须明确记录且不可遗漏；现安排为事项 08A，在全部底层整理结束后、任何上层应用审查前执行。
- 用户要求把“开启战斗体”和“解除战斗体”升格为 Core 正式按钮；角色手持任意触发体且拥有 Trion 腺体基因时显示，事项 06 先建立正式入口，再删除原开发按钮。
- 用户当时确认内容侧诊断按钮只保留“画点诊断”和“姿态诊断”；事项 06 最终确认时又明确删除“姿态诊断”，现以只保留画点的最终决定为准。
- 用户补正两个既有 Harmony（运行时补丁）的归属：二者都是护盾芯片具体业务实现的一部分，不是通用护盾底层；统一留到事项 14“护盾应用”逐项处理。
- 用户确认继续推进事项 03，并要求持续同步总计划各事项状态。
- 用户确认主模组使用新生成的简单正立方体平面几何 Trion 腺体基因图标；基因物品继续沿用游戏原版贴图。
- 用户确认测试候选模组完整恢复自己的 `About.xml` 和 `LoadFolders.xml`；实际游戏是否加载由用户自行控制。
- 用户纠正事项 01 的整批物理迁移方式：主模组只允许放入已确认事项，未确认内容恢复到测试模组候选库，以后确认一个迁入一个。
- 用户确认光束拖尾的历史余痕必须参与存读档：保存时尚未淡完的线段应保存剩余状态，读档后从原进度继续淡出；不得把它改成读档即消失的纯瞬时效果。
- 用户确认删除拖尾扩展的 `enabled（启用）` 字段；正式启用条件统一为“来源定义挂载 `BeamTrailExtension（光束拖尾扩展）` 且填写有效预设”，不需要拖尾时不挂载扩展。
- 用户确认正式拖尾定义扩展命名为 `BDP.Content.Projectiles.BeamTrail.BeamTrailExtension`，去除 `Chip（芯片）` 和 `DevHarness（开发测试框架）` 语义，使任意投射物来源定义都能复用。
- 用户确认光束拖尾只保留“先定义拖尾预设，再由来源定义引用预设”这一条正式配置路线；无人使用的旧投射物直接配置入口不迁移、不兼容并删除。
- 用户确认 `BeamTrailExtension（光束拖尾扩展）` 引用预设的正式字段名为 `preset（预设）`，替代重复表达类型语义的 `beamTrailPreset`。
- 用户确认事项 04 的准确边界：亮薄荷绿长短预设以首版色值 `#DAFDE5` 进入主模组，事项 04 本身不决定芯片映射；原炽红预设与两处引用当时暂留候选库，后续再由独立支线退役。
- 用户确认修正光束拖尾的镜头范围判断：按整条线段覆盖范围与镜头范围是否相交决定绘制，不再只检查线段中点，避免长光束穿过画面却被漏画。
- 用户确认事项 04 完整设计，并授权在逐文件实施计划写完后直接落实代码，不需要再次停下等待实施确认。

## 进度记录归档

### 2026-07-29：事项 07 激活费用归属修正完成

- 保留芯片本体 `ActivationCost（激活费用）`及 `TriggerTrionBindingService` 中每次激活的一次性扣费，不修改候选芯片数值。
- 删除表达条目 `ActivationCost`、内部/公开快照传递、组合表达 `ActivationCostResolve` 及相关求值复制；`UseCost（使用费用）`和`MinimumRequired（最低要求）`原运行路径不变。
- 组合定义顶层 `ComboTrionConfig.ActivationCost / CapacityCost（组合激活费用/容量占用）`不属于本轮范围，继续保留并等待单独审查。
- 标准芯片 XML 字段全表由77项修正为76项；42个版本 XML 全部可解析，三个程序集 Release 构建成功，Core 单向依赖门禁通过。
- 全量测试共226项，214项通过、12项失败；失败名单与既有基线完全一致，新增失败为0。
- 计划提交 `f1864a6d`，失败契约提交 `e82022a5`，运行与发布产物提交 `9cae569a`。

### 2026-07-29：候选红色拖尾退役

- `BDP_TestChipRangedVolley` 与 `BDP_TestChipPathLatchVolley` 从炽红拖尾改为亮薄荷绿长拖尾；其它六个芯片的预设保持不变。
- 候选本地 `BeamTrailPresetDefs.xml` 已删除；主模组仍只定义亮薄荷绿长、短两个正式预设。
- 候选拖尾引用最终为红色 0、长版 3、短版 5；候选程序集构建成功，46 个 XML 全部可解析，事项 04 八项专项检查全部通过。
- 全量 223 项回归为 211 项通过、12 项既有基线失败；失败名单与支线实施前一致，新增失败为 0。
- 该支线不属于事项 05，完成后仍按原检查点介绍事项 05。

### 2026-07-29：事项 04 完成

- 正式公共系统进入 `BDP.Content.Projectiles.BeamTrail`，旧 `BeamTrailConfig`直接配置入口、`Chip`专属命名、`enabled`开关和 `beamTrailPreset`字段已移除。
- 主模组 `1.6/Content`补充层包含亮薄荷绿长、短两个正式预设和通用贴图；候选模组只保留炽红预设，八处芯片引用保持红色 2、长版 1、短版 5，没有重新选择业务效果。
- 历史余痕继续参与存读档，活动头段不重复保存；投射物终止时清理活动头段；长线段改按整段覆盖范围与镜头相交判断。
- Core、Content、候选三个程序集 Release 构建成功，Core 门禁通过；47 个 XML 全部解析成功；事项 04 八项专项测试全部通过。
- 全量测试共 223 项，211 项通过、12 项失败；失败名单与迁移前基线完全一致，没有新增失败。
- 主要实施提交为 `d4b23fd1`、`0bf19263`、`aa63d468`、`1d0fb677`、`c3a76caa`、`d3a6efbe`；检查点转到尚未介绍的事项 05。

### 2026-07-29：事项 04 Content 加载层实施计划补充

- 已在事项 04 实施计划中新增 Task 3A，明确先改四项测试，再移动预设、贴图并加入第三个加载根目录。
- 门禁算法和 Core 工程构建接线保持不变；通过物理目录归属使既有 `CoreDefsRoot`只覆盖 `1.6/Defs`。
- 计划更新提交后按用户原授权直接实施。

### 2026-07-29：事项 04 整体设计确认

- 用户确认事项 04 完整设计，并授权逐文件实施计划完成后直接开始落实代码。
- 独立设计文档为 `docs/plans/2026-07-29-BDP光束拖尾公共系统正式化设计.md`。
- 当前只落盘设计和更新检查点；运行代码将在实施计划完成后按测试先行方式修改。

### 2026-07-29：事项 04 实施计划完成

- 已建立 `docs/plans/2026-07-29-BDP光束拖尾公共系统正式化实施计划.md`，把失败契约、六个源码、正式预设、候选机械对接、构建回归和完成记录拆成五个任务。
- 计划明确 Core（核心）运行代码不改、候选 C#（C#语言）不新增 Content（内容）工程引用、具体芯片选择不变。
- 按用户授权，计划提交后直接使用测试先行方式实施，不再暂停等待第二次实施确认。

### 2026-07-29：事项 04 视野判断确认

- 用户确认把现有“只看线段中点”的绘制判断修正为“整条线段覆盖范围与镜头范围相交”；这样长光束只要有一部分进入画面就会绘制。
- 该修正只影响是否漏画，不改变拖尾颜色、寿命、飞行路径、余痕存档或芯片预设选择。
- 事项 04 的逐项取舍已经确认完毕；下一步提交完整设计给用户整体确认，确认前不迁移运行文件。

### 2026-07-29：事项 04 迁移范围纠正

- 用户指出事项 04 只负责将选定预设并入主模组，没有授权当前决定普通齐射或路线引导齐射芯片使用哪个预设。
- 撤销“本事项确认两处红色预设引用如何映射”的安排；候选芯片配置不属于事项 04 的修改范围。
- 用户确认红色预设与现有两处芯片引用暂留候选库，等对应应用事项再与引用一并处理。

### 2026-07-29：事项 04 首版颜色与名称确认

- 参考图中的偏绿亮色首版定名为“亮薄荷绿（Bright Mint）”，采用亮块中心色 `#DAFDE5`，RimWorld（边缘世界）颜色配置为 `(0.855, 0.992, 0.898, 1.0)`。
- 正式预设命名为 `BDP_TrailPreset_BrightMintLong`（亮薄荷绿长拖尾）与 `BDP_TrailPreset_BrightMintShort`（亮薄荷绿短拖尾）。
- 颜色仍标记为“待游戏实测校色”；用户观察实际发光效果后可以继续调整，不影响当前系统和迁移设计。
- 用户随后澄清事项 04 不处理芯片选用预设；两处原红色预设引用的映射问题已从本事项撤回。

### 2026-07-28：事项 04 已介绍，等待确认

- 已按投射物发射、飞行采样、终止、地图绘制和读档时序检查候选光束拖尾实现；本轮没有修改运行代码。
- Core（核心程序集）已经具备中性的投射物视觉附加宿主，只广播位置与生命周期事实，不知道光束拖尾名称；事项 04 原则上不需要改 Core。
- 候选库现有七个拖尾源码文件：其中运行时线段、附加件、地图管理、外观快照、预设类型和定义扩展可整理为 Content（内容程序集）公共视觉系统；未被 Def 使用的旧直接配置入口属于清理候选。
- 初查时三个具体拖尾预设和八处芯片引用均位于候选库；用户随后要求将其中两个改为同色长短版并随公共系统进入主模组，但明确不在事项 04 决定任何芯片的预设选择。
- 用户已确认保留历史光线的存读档：保存时尚未淡完的线段必须保存寿命进度，读档后继续淡出；仍跟随活体投射物的临时头段不单独保存，由投射物恢复后重新建立。
- 用户已确认只保留“预设定义 + 来源引用”配置路线；未被 Def 使用的 `BeamTrailConfig`（旧投射物直接配置入口）不迁移并删除，不做兼容。
- 用户已确认正式扩展采用 `BDP.Content.Projectiles.BeamTrail.BeamTrailExtension`，不再以 `Chip（芯片）`命名；三个具体预设和八处候选芯片只在必要范围内同步类型引用。
- 用户已确认删除扩展中的 `enabled（启用）`字段；扩展存在且预设有效即启用，未挂载扩展即不产生拖尾。
- 用户已确认正式预设引用字段采用 `preset（预设）`，候选配置实施时从 `beamTrailPreset`机械改名，不改变其 DefName（定义名称）或具体数值。
- 用户参考图 `C:\NiwtDatas\Projects\RimworldModStudio\日志\QQ20260728-234712.png` 的亮色中心约为 `#DAFDE5`，包含外缘柔光的平均色约为 `#D4F8E0`，视觉上接近浅亮的薄荷荧光绿；正式名称和代表色值仍待确认。
- 原炽红预设当前由 `BDP_TestChipRangedVolley` 与 `BDP_TestChipPathLatchVolley`引用；事项 04 不处理这两处引用，建议预设本身暂留候选库以保持可加载。

### 2026-07-28：事项 03A 纠偏设计确认

- 暂停事项 04，插入事项 03A“撤回未确认内容并恢复测试候选库”。
- 测试模组恢复事项 01 前的候选源码、Def（定义）、资源、测试、文档和加载元数据。
- 主模组只保留原有内容、双程序集外壳、Core 依赖门禁、最小 Content 启动入口和新 Trion 腺体图标。
- 事项 03 将撤下尚未确认的触发体按钮、Trion 面板和护盾补丁接线；以后确认一个事项，只移动该事项及必要依赖。
- 纠偏设计见 `docs/plans/2026-07-28-BDP选择性内容迁移纠偏设计.md`。

### 2026-07-28：事项 03 初次完成（后经事项 03A 纠偏）

- `DevHarnessBootstrap.cs` 已正式改为 `ContentBootstrap.cs`，类和命名空间改为 `BDP.Content.ContentBootstrap`，Harmony（补丁框架）标识改为 `niwt.bdp.content`。
- 当时启动入口做三条接线：加载内容程序集补丁、注册触发体外部按钮提供器、注册 Trion 面板提供器；事项 03A 已撤下后两条和护盾业务，现只保留空业务补丁扫描入口。
- 新增事项 03 专项测试，并同步两项依赖旧启动文件名的既有测试；测试先因正式入口缺失而失败，最小实现后专项测试全部通过。
- Release（发布）双程序集重新构建成功，Core（核心程序集）零本模组依赖门禁通过。
- 全量 222 项 PowerShell（命令行脚本）冒烟测试中 210 项通过；12 项失败与事项 01、02 记录的既有基线完全一致，没有新增失败。
- 初次实现、测试与发布产物提交为 `2c5c519f`；最终状态以事项 03A 完成记录为准。

### 2026-07-28：补正业务归属、正式按钮与触发体命名时机

- 纠正护盾补丁归属：现有伤害拦截与护盾绘制补丁均为护盾芯片业务实现，启动接线只负责加载，正式审查留到事项 14。
- 事项 06 调整为“先在 Core 正式化战斗体开启/解除按钮，再清理内容侧按钮”；内容侧最终只保留画点和姿态两项开发诊断。
- 新增事项 08A，锁定触发器与触发体完整分类、命名和 Def 规范的执行窗口：底层阶段收尾、上层应用开始之前。
- 本轮只更新计划与日志，不修改运行代码。

### 2026-07-28：事项 02 完成

- 新增独立检查器 `Source/Build/Verify-BdpCoreIsolation.ps1`，自动读取 `Source` 下全部 C# 工程的 `AssemblyName`（程序集名）、`RootNamespace`（根命名空间）和实际命名空间，不把禁止目标写死为当前的 `BDP.Content`。
- 三层门禁分别检查 Core（核心）的本模组工程引用、编译 DLL 的 `AssemblyRef`（程序集引用表）以及核心 C#/XML（源码/配置）中的其他本模组专属名称。
- DLL 以字节载入反射只读环境，不锁定发布文件；RimWorld（边缘世界）、Unity（游戏引擎）、Harmony（补丁框架）和 .NET（运行环境）等外部程序集不在禁止名单中。
- 核心工程完成 Build（构建）后自动执行同一检查器；真实 Release（发布）重新构建输出门禁 PASS，随后才继续生成 `BDP.Content.dll`，最终 0 警告、0 错误。
- 专项测试验证真实 Core 通过，并验证模拟工程反向引用、真实 DLL 反向引用和明确内容命名三类违规都会失败且指出位置。
- 全量冒烟测试由 219 项增加为 221 项，209 项通过；失败仍为事项 01 已记录的同一组 12 项，没有新增回归。
- 技术设计提交为 `8e2e6997`，实施计划提交为 `d96ca6af`，检查器提交为 `bc735393`，构建接线与发布产物提交为 `806abe5b`。
- 当前检查点转到事项 03，等待用户确认。

### 2026-07-28：事项 01 实施前基线检查

- 用户已确认事项 01。
- 实施前检查发现两个模组范围内有 61 个已跟踪未提交改动和 10 个未跟踪文件，且多项与本次迁移路径直接重叠。
- 用户授权在不改变项目内容的前提下提交现有状态。
- 已用独立提交 `24fb1058` 原样保存迁移前基线；提交后两个模组范围工作区干净。
- 事项 01 进入实施阶段。

### 2026-07-28：事项 01 完成

- 主模组当时成为唯一可被 RimWorld（边缘世界）识别的 BDP 模组；原伴生模组的 `About.xml` 和 `LoadFolders.xml` 已移除，后续由事项 03A 纠偏恢复候选模组。
- 核心工程输出 `BDP.Core.dll`，内容工程输出 `BDP.Content.dll`；内容工程单向引用核心工程，两者统一发布到主模组 `1.6/Assemblies`。
- 原伴生模组源码、Def（定义）、贴图、音效、测试和文档曾机械迁入主模组；后续由事项 03A 按确认一个迁移一个的原则纠偏。
- 本项未重命名 `DevHarness`、`Test`、`Sample` 等业务语义，未调整具体芯片和上层应用参数。
- 新增统一模组结构检查和测试脚本路径检查；两项专用检查均通过。
- Release（发布）重新构建为 0 警告、0 错误；42 个 XML（可扩展标记语言）全部可解析。
- 219 个现有冒烟测试中 207 个通过，剩余 12 个均来自迁移前基线，本项未越界修复。
- 迁移前基线提交为 `24fb1058`，事项 01 结构实现提交为 `9ec835ca`；当时检查点转到事项 02。

## 2026-07-28：计划建立

- 新增统一模组双程序集设计文档。
- 新增本迁移总计划。
- 尚未修改运行代码。
- 事项00已完成；当时检查点切换到事项01，等待用户确认。

### 2026-07-28：事项 03A 完成

- `BorderDefenseProtocol.DevHarness` 已恢复为完整候选内容库，保留自己的 `About.xml`、`LoadFolders.xml`、源码、Def（定义）、资源、测试、文档和 `BDP.DevHarness.dll`。
- 主模组已移除 `ContentRoot`、`ContentVersion`、`docs/DevHarness`、候选业务测试和 Content 具体业务源码；`BDP.Content` 只保留工程文件与正式空业务启动入口。
- 主模组新增独立的 64×64 透明 Trion（触粒子）腺体基因图标；正式 GeneDef（基因定义）继续引用 `UI/Icons/Genes/Gene_TrionGland`，不再借用测试模组资源。
- 主模组与候选模组 Release（发布）构建均成功；Core（核心程序集）依赖门禁通过，Core 源码和 Def 中没有 Content 或 DevHarness 反向引用。
- 46 个 XML（可扩展标记语言）全部可解析；主模组发布目录只含 Core 与 Content，候选目录只含 DevHarness。
- 主模组 222 项测试中 210 项通过，12 项失败与既有基线完全一致；候选模组护盾专项测试 1 项通过。
- 纠偏设计提交 `128335dc`，实施计划提交 `64c03d37`，边界测试提交 `6f9d5508`，候选库恢复与主模组收紧提交 `54a1d41b`，构建引用修正提交 `6724cbbb`。
- 当前检查点进入事项 04，但事项 04 仍为 `未介绍`，不得自动实施。

### 2026-07-29：事项 04 完整构建发现 XML 归属阻塞

- 失败契约提交 `d4b23fd1`，正式拖尾源码提交 `0bf19263`，亮薄荷绿预设与资源提交 `aa63d468`；专项测试已经转绿。
- 首次完整 Core（核心）构建稳定复现门禁失败：工程层和 DLL（动态链接库）层没有反向依赖，只有明确名称层把 `1.6/Defs` 中合法的 Content（内容）预设误判为“核心 XML 写出 Content 类型”。
- 根因是事项 02 构建接线把整个主模组 Def（定义）目录当成 Core Def 根目录；在没有 Content Def 时成立，现在首个正式 Content Def 进入后失效。
- 推荐给同一主模组增加独立 Content 配置加载根目录，让 XML 归属物理可辨；这不会形成第二个模组。因为该处理超出事项 04 原逐文件计划，已暂停并等待用户确认。

### 2026-07-29：事项 04 Content 加载目录命名确认

- 用户接受“主版本根目录 + Content 补充层”命名：`LoadFolders.xml`依次加载 `/`、`1.6`、`1.6/Content`。
- `1.6/Assemblies`继续统一放两个 DLL（动态链接库）；`1.6/Defs`保留 Core/尚待事项 09 复查归属的主配置；事项 04 正式预设和专属贴图进入 `1.6/Content/Defs`与`1.6/Content/Textures`。
- 当前不建立 `1.6/Core`，因为那会引发大范围搬迁，并在业务归属复查前把现有全部主配置提前定性为 Core。
- 下一步先更新实施计划和失败测试，再实施目录迁移并重新验证 Core 门禁。

### 2026-07-29：事项 06 按钮状态补正

- 根因确认：`Prefs.DevMode`只表示开发者模式，不能代表顶部工具栏的上帝模式；现改为 RimWorld 原版`DebugSettings.godMode`。
- “画点诊断”和 Trion `+50/-50/MAX/归零`共五个玩家调试按钮只在上帝模式显示；画点地图绘制和发射点记录也同步受上帝模式约束。
- Core原两个战斗体按钮合并为一个动作按钮：未开启时显示“开启战斗体”，已开启时显示“解除战斗体”，两者均亮且可点击；崩解和冷却阶段显示对应文字并禁用。
- 三个程序集构建成功，Core零本模组程序集依赖门禁通过；46个XML全部可解析；补正专项检查通过。
- 全量测试共224项，212项通过、12项既有基线失败，新增失败为0；设计计划提交`89b194ce`，失败契约提交`5496deb2`，运行修正提交`611d208f`。

### 2026-07-29：事项 06 完成

- Core正式战斗体提供器增加Trion腺体基因资格判断；该项初次完成时曾同时显示开启、解除两个按钮，后续已按“事项06按钮状态补正”合并为一个。
- 唯一保留的“画点诊断”迁入`BDP.Content.Trigger.Diagnostics`，由Content正式注册；初次完成时使用开发者模式门槛，后续已补正为上帝模式门槛。
- 候选六按钮提供器、Trigger装载诊断窗口、战斗体诊断窗口、姿态诊断窗口、旧式Trigger状态控件、旧式槽位窗口和摘要控件已物理删除。
- 无调用者的`ITriggerIntegrityDiagnostics`、诊断表面、宿主字段、获取入口、按钮上下文传递和两个专用读取方法已删除；画点仍使用的姿态快照、访问面和发射点记录保留。
- 三个程序集构建成功；Core零本模组程序集依赖门禁通过；46个XML全部可解析；事项06七项专项检查通过。
- 全量测试共223项，211项通过、12项既有基线失败，新增失败为0；设计提交`4a52f769`，失败契约提交`574a6366`，运行实现提交`b4eff3dc`，迁移边界测试同步提交`835033c8`。
