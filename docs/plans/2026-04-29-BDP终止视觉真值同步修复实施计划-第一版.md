# BDP Terminal Visual Truth Sync Fix Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 修复投射物在本 tick 内已经真实命中/终止后，终止白线末段仍使用理论段终点或 `sampleStart` 的问题，让视觉末段与逻辑终止点使用同一份冻结真值。

**Architecture:** 修复落点放在 `BdpProjectile` 宿主层，而不是追踪模块或 `BeamTrail` 业务层。宿主层新增一份中性的“终止视觉真值点”冻结状态；谁先知道本 tick 的真实终止点，谁就在清理前写入它；`TickInterval` 收尾发布最后一个视觉 sample 与 terminate 事件时统一消费这份冻结真值。

**Tech Stack:** C#, RimWorld projectile host, PowerShell smoke tests

---

### Task 1: 锁定终止视觉契约

**Files:**
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/RangedTerminalVisualSampleTruthSmokeTests.ps1`
- Reference: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Projectiles/BdpProjectile.cs`

**Step 1: 写失败烟测**

- 断言宿主存在独立的终止视觉真值冻结字段或等价实现。
- 断言 `ResolveVisualSampleEnd(...)` 的 `Destroyed` 分支不再返回 `currentFlightPathSnapshot.End` 或 `sampleStart` 作为主路径。
- 断言 `TickInterval(...)` 在 `Destroyed` 时会把最后一个 sample 与 terminate 都绑定到冻结真值。

**Step 2: 运行烟测确认失败**

Run:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\RangedTerminalVisualSampleTruthSmokeTests.ps1'
```

Expected: FAIL，提示当前宿主仍用理论终点 / sampleStart 处理终止视觉样本。

### Task 2: 宿主层冻结终止视觉真值

**Files:**
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Projectiles/BdpProjectile.cs`

**Step 1: 增加冻结状态**

- 新增一份只服务视觉层的终止真值字段。
- 它不带追踪业务语义，不暴露给附件层以外的模块。

**Step 2: 在真实终止路径冻结真值**

- 在真实命中 / 阻挡命中 / 其它销毁保底路径里，在 `Destroy()` 和清理锚点之前写入终止视觉真值。
- 保证撞墙命中可以冻结到阻挡真点，而不是段理论终点。

**Step 3: 统一消费冻结真值**

- `ResolveVisualSampleEnd(...)` 在终止场景优先返回冻结真值。
- `NotifyVisualTerminate(...)` 也消费同一份冻结真值。
- 完成后清理冻结状态，避免跨 tick 脏读。

### Task 3: 让测试转绿并补基本构建验证

**Files:**
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/RangedTerminalVisualSampleTruthSmokeTests.ps1`
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/ProjectileVisualAttachmentHostSmokeTests.ps1`
- Build: `模组工程/BorderDefenseProtocol/Source/BDP/BDP.csproj`

**Step 1: 跑红灯烟测**

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\RangedTerminalVisualSampleTruthSmokeTests.ps1'
```

Expected: PASS

**Step 2: 跑相邻视觉边界烟测**

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\ProjectileVisualAttachmentHostSmokeTests.ps1'
```

Expected: PASS

**Step 3: 跑主模组构建**

```powershell
dotnet msbuild '.\模组工程\BorderDefenseProtocol\Source\BDP\BDP.csproj'
```

Expected: Build succeeded / Exit code 0

**Step 4: 提交**

```bash
git add -- '模组工程/BorderDefenseProtocol/Source/BDP/Core/Projectiles/BdpProjectile.cs' '模组工程/BorderDefenseProtocol/Source/BDP.Tests/RangedTerminalVisualSampleTruthSmokeTests.ps1' '模组工程/BorderDefenseProtocol/docs/plans/2026-04-29-BDP终止视觉真值同步修复实施计划-第一版.md'
git commit -m "fix: sync terminal visual sample with projectile truth"
```
