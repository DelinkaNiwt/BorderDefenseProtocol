# BDP 正式芯片共用贴图迁移 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 让主模组独立加载时，三枚已正式迁移芯片可解析其共用贴图。

**Architecture:** 保持三枚芯片现有的 `texPath` 不变。把 DevHarness（测试模组）中已确认的原始 PNG（便携式网络图形）复制到主模组 `LoadFolders.xml` 已加载的 `1.6/Content/Textures` 对应相对路径；用主模组资源回归脚本锁定存在性和字节一致性。

**Tech Stack:** RimWorld 1.6 Def（定义）XML（可扩展标记语言）、PowerShell（脚本语言）烟雾测试、Git（版本控制）。

---

### Task 1: 主模组贴图资源回归检查

**Files:**
- Create: `Source/BDP.Tests/FormalTriggerChipTextureSmokeTests.ps1`
- Test: `Source/BDP.Tests/FormalTriggerChipTextureSmokeTests.ps1`

**Step 1: 写入失败检查**

断言主模组下的 `1.6/Content/Textures/Things/Trigger/Chip/BDP_TriggerChip.png` 存在，且 SHA256（安全散列算法）等于已确认源图 `46D747674E99BF4ED5BFD409162FF442B9E0C99802EA560876E86840689B544A`。

**Step 2: 运行检查，确认失败**

Run: `powershell -ExecutionPolicy Bypass -File Source/BDP.Tests/FormalTriggerChipTextureSmokeTests.ps1`

Expected: 因主模组目标 PNG 缺失而失败。

### Task 2: 迁移原始贴图

**Files:**
- Create: `1.6/Content/Textures/Things/Trigger/Chip/BDP_TriggerChip.png`

**Step 1: 最小实现**

将 `BorderDefenseProtocol.DevHarness/1.6/Textures/Things/Trigger/Chip/BDP_TriggerChip.png` 原样复制到主模组目标路径；不修改 Def、不删除测试模组源文件。

**Step 2: 运行检查，确认通过**

Run: `powershell -ExecutionPolicy Bypass -File Source/BDP.Tests/FormalTriggerChipTextureSmokeTests.ps1`

Expected: 输出 `FormalTriggerChipTexture PASS`。

### Task 3: 完整验证和提交

**Files:**
- Modify: `日志/Agent工作日志/Agent日志34.md`

**Step 1: 验证引用和资源**

运行正式贴图检查，核对三枚正式芯片均引用该相对路径，并比较主模组与 DevHarness 源 PNG 的 SHA256。

**Step 2: 提交**

```powershell
git add 1.6/Content/Textures/Things/Trigger/Chip/BDP_TriggerChip.png Source/BDP.Tests/FormalTriggerChipTextureSmokeTests.ps1 docs/plans/2026-08-02-BDP正式芯片共用贴图迁移实施计划.md
git commit -m "fix: 迁移正式芯片共用贴图"
```
