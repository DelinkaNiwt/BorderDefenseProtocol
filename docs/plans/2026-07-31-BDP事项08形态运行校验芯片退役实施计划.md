# BDP 事项08形态运行校验芯片退役 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 删除已完成事项07A验收使命的纯校验芯片，同时保留正式形态底层与通用自动检查。

**Architecture:** 新增退役边界测试后，删除校验XML及其专用正向测试；同步其它候选总表检查，不把校验字段移植给真实业务芯片。

**Tech Stack:** RimWorld 1.6 XML、PowerShell冒烟测试、MSBuild。

---

### Task 1：建立退役失败契约

**Files:**

- Create: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessChipModeProbeRetirementSmokeTests.ps1`

1. 断言候选活动Defs不含校验芯片文件与定义名。
2. 断言Core形态运行测试和Content按钮测试继续存在。
3. 删除前运行并确认失败。
4. 提交失败契约。

### Task 2：删除校验物并同步总表检查

**Files:**

- Delete: `模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Things/Items/Chips/Test/ThingDefs_ChipModeRuntimeProbe.xml`
- Delete: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessChipModeRuntimeProbeSmokeTests.ps1`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessChipActivationRequirementAuthoringSmokeTests.ps1`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessChipTrionConfigSmokeTests.ps1`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessChipSlotOccupancyAuthoringSmokeTests.ps1`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessTriggerChipTextureSmokeTests.ps1`

1. 删除校验XML和专用测试。
2. 从候选文件名单中移除校验文件。
3. 合法芯片总数17改为16，单槽芯片16改为15。
4. 运行退役检查和四项相邻总表检查。
5. 提交删除与测试同步。

### Task 3：完整验证和记录

**Files:**

- Modify: `模组工程/BorderDefenseProtocol/docs/plans/2026-07-28-BDP统一模组双程序集迁移总计划.md`
- Modify: `日志/Agent工作日志/Agent日志16.md`

1. 解析全部当前XML。
2. 构建三个程序集。
3. 运行全量测试并与12项既有失败基线比较。
4. 更新事项08进度和下一对象。
5. 提交记录与产物。

## 完成记录

- 退役边界已按先红后绿完成。
- 校验XML和专属正向测试已删除，四项候选总表已同步为16枚合法业务芯片。
- 三个程序集Release构建均为0警告0错误，Core隔离门禁通过。
- 当前43份XML全部可解析，活动目录中的旧定义引用为0。
- 全量272项为260项通过，失败仍为同一12项既有基线，新增失败0。
