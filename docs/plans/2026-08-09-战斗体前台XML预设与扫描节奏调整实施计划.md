# 战斗体前台 XML 预设与扫描节奏调整实施计划

> **For Codex：REQUIRED SUB-SKILL（必需子技能）：使用 `superpowers:executing-plans` 按任务逐项执行本计划。**

**Goal（目标）：** 将战斗体前台模式改为 XML 配置，临时启用原版装备预设，并把扫描调整为 16 tick 与更贴近头顶的起点。

**Architecture（架构）：** Core 的 `CombatBodyHostConfigDef` 只提供中性模式与预设名；桥接层从全局 Def 读取选择。Content XML 提供临时具体预设。镜像原身实现保持不变，扫描 Mote 只调整两个常量和上边界公式。

**Tech Stack（技术栈）：** C# 7.3、RimWorld 1.6 Def、PowerShell 烟雾测试、`dotnet msbuild`。

---

### Task 1：把前台模式归位到 XML 配置

**Files（文件）：**

- Modify（修改）：`Source/BDP/Core/CombatBody/Bridge/CombatBodyHostConfigDef.cs`
- Modify（修改）：`Source/BDP/Core/CombatBody/Bridge/CompProperties_CombatBodyHost.cs`
- Modify（修改）：`Source/BDP/Core/CombatBody/Bridge/PawnCombatBodyBridge.cs`
- Modify（修改）：`Source/BDP/Core/CombatBody/Bridge/CompCombatBodyHost.cs`
- Modify（修改）：`1.6/Defs/CombatBodyDef/Config.xml`
- Create（新建）：`Source/BDP.Tests/CombatBodyFrontModeXmlConfigSmokeTests.ps1`

**Steps（步骤）：**

1. 写失败测试：断言全局配置 Def 拥有两个字段，CompProperties 不再拥有它们，桥接层读取解析器，XML 选择 `Preset`。
2. 运行测试并确认因字段仍在 CompProperties 而失败。
3. 实现最小配置迁移；删除桥接构造函数中不再使用的 Props 依赖。
4. 运行新增测试与 `CombatBodyFrontReplacementSmokeTests.ps1`。
5. 提交：`feat: 将战斗体前台模式归位到XML配置`。

### Task 2：增加临时观察预设

**Files（文件）：**

- Create（新建）：`1.6/Content/Defs/CombatBody/TemporaryFrontPreset.xml`
- Create（新建）：`Source/BDP.Tests/CombatBodyTemporaryFrontPresetSmokeTests.ps1`

**Steps（步骤）：**

1. 写失败测试：断言临时预设存在、DefName 与全局配置一致，并包含四件指定原版衣物。
2. 运行测试确认失败。
3. 新建仅含 `CombatBodyFrontPresetDef` 的临时 Content XML；逐条目中文注释。
4. 运行测试并做 XML 语法解析。
5. 提交：`feat: 增加战斗体临时服装观察预设`。

### Task 3：加快扫描并收紧头顶边界

**Files（文件）：**

- Modify（修改）：`Source/BDP.Content/CombatBody/Transform/Mote_CombatBodyScan.cs`
- Modify（修改）：`Source/BDP.Tests/CombatBodyTransformScanContentSmokeTests.ps1`

**Steps（步骤）：**

1. 先把测试期望改为 `DurationTicks = 16`、`HeadUpperBoundFactor = 0.38f`，并断言上边界公式使用该系数。
2. 运行测试，确认因当前仍为 24 tick 且使用 0.5 而失败。
3. 增加常量并修改唯一上边界公式；下边界和镜像逻辑不动。
4. 运行全部 `CombatBodyTransform*` 测试。
5. 提交：`fix: 加快战斗体扫描并收紧头顶边界`。

### Task 4：完整验证与日志

**Files（文件）：**

- Modify（修改）：`../../日志/Agent工作日志/Agent日志38.md`

**Steps（步骤）：**

1. 运行前台模式、镜像恢复、扫描变换和 Content 启动入口测试。
2. Release 重建 Core 与 Content，确认 Core 依赖隔离通过。
3. 更新倒序工作日志。
4. 提交最终 DLL/PDB 与日志：`chore: 交接预设战斗体扫描实测版本`。
5. 确认 `main` 工作区干净。
