# 毒蛇通用骨架轻量上提 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal（目标）:** 以最小改动把“目标语义口径”和“合法性边界口径”正式收口，并把重复的语义目标解析逻辑收拢成一处中性骨架。

**Architecture（架构）:** 不新增毒蛇、锚点、路径、目标角色枚举等业务概念，不做“大一统目标系统”。这次只做三件事：第一，补一份正式口径文档；第二，补一个很小的 `SemanticTargetResolver（语义目标解析器）`；第三，把现有成员注释和边界说明补齐。毒蛇业务仍全部留在测试模组。

**Tech Stack（技术栈）:** C# / RimWorld Def / BDP Core / PowerShell 烟雾测试 / Markdown 文档 / UTF-8

---

### Task 1: 收口目标语义与合法性边界口径

**Files:**
- Create: `模组工程/BorderDefenseProtocol/docs/01-决策记录/2026-04-18-远程目标语义与合法性边界口径-第一版.md`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Model/ConfirmRecord.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Model/ConfirmedTargetSnapshot.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedProtocol/Model/ProjectileInitPlan.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Projectiles/RangedFlightProtocol/Arrival/ArrivalContribution.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Model/TargetingRecord.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Targeting/TargetingSegmentLegalityRequest.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Targeting/DefaultTargetingSegmentLegalityService.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/AttackExecutionTargetingSource.cs`

**Step 1: 写口径文档骨架**

在 `2026-04-18-远程目标语义与合法性边界口径-第一版.md` 先写出以下固定章节：

```md
# 远程目标语义与合法性边界口径

## 1. 目标字段词汇表
## 2. 各阶段字段流转
## 3. 候选合法性 / 段合法性 / 最终确认合法性
## 4. 主模组负责什么
## 5. 业务模块负责什么
## 6. 明确不做什么
```

**Step 2: 把实际口径填实**

文档里必须明确写出并逐项解释：

```text
Target
SemanticTarget
NavigationTarget
LaunchTarget
AimTarget
CurrentTarget
NextDestination
NextTarget
```

以及三条边界：

```text
当前候选点合法性 = 光标提示 / 禁止图标
段合法性 = 某一段能否成立
最终确认合法性 = 本次攻击能否正式落地
```

**Step 3: 补成员注释，不改行为**

在上述 C# 文件中，把成员注释改成统一口径，至少保证：

- `ConfirmRecord.Target` 说明为“确认阶段导航目标”
- `ConfirmRecord.SemanticTarget` 说明为“确认阶段语义目标”
- `ConfirmedTargetSnapshot.NavigationTarget` 说明为“冻结后的导航目标”
- `ProjectileInitPlan.LaunchTarget / AimTarget / CurrentTarget` 说明三者各自职责
- `TargetingRecord.HasCurrentTargetLegalityOverride` 说明只服务当前候选点
- `TargetingSegmentLegalityRequest` 说明只描述中性段事实

**Step 4: 验证文档与注释关键字**

Run:

```powershell
Select-String -Path '.\模组工程\BorderDefenseProtocol\docs\01-决策记录\2026-04-18-远程目标语义与合法性边界口径-第一版.md' -Pattern 'SemanticTarget|NavigationTarget|LaunchTarget|AimTarget|CurrentTarget|段合法性|最终确认合法性'
```

Expected:

```text
能命中上述关键字，且不是空文档
```

**Step 5: 运行现有边界测试**

Run:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DualRangedConfirmedTargetSnapshotSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\TargetingSegmentLegalitySmokeTests.ps1'
```

Expected:

```text
DualRangedConfirmedTargetSnapshotSmokeTests PASS
TargetingSegmentLegalitySmokeTests PASS
```

**Step 6: 检查点提交（仅在用户明确同意时）**

```bash
git add 模组工程/BorderDefenseProtocol/docs/01-决策记录/2026-04-18-远程目标语义与合法性边界口径-第一版.md 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Model/ConfirmRecord.cs 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Model/ConfirmedTargetSnapshot.cs 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedProtocol/Model/ProjectileInitPlan.cs 模组工程/BorderDefenseProtocol/Source/BDP/Core/Projectiles/RangedFlightProtocol/Arrival/ArrivalContribution.cs 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Model/TargetingRecord.cs 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Targeting/TargetingSegmentLegalityRequest.cs 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Targeting/DefaultTargetingSegmentLegalityService.cs 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/AttackExecutionTargetingSource.cs
git commit -m "docs: unify ranged target semantics and legality vocabulary"
```

### Task 2: 提取语义目标解析器

**Files:**
- Create: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/AttackExecutionSemanticTargetResolver.cs`
- Create: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/AttackExecutionSemanticTargetResolverSmokeTests.ps1`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/AttackExecutionService.Stages.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedProtocol/RangedAttackProtocolService.cs`

**Step 1: 先写失败烟雾测试**

在 `AttackExecutionSemanticTargetResolverSmokeTests.ps1` 先断言：

```powershell
Assert-True (Test-Path -LiteralPath $resolverPath) 'AttackExecutionSemanticTargetResolver.cs must exist.'
Assert-True ($resolverText -match 'ConfirmedTargetSnapshot') 'Resolver must read ConfirmedTargetSnapshot.'
Assert-True ($resolverText -match 'SemanticTarget') 'Resolver must prefer SemanticTarget.'
Assert-True ($stagesText -match 'AttackExecutionSemanticTargetResolver') 'AttackExecutionService.Stages must use shared resolver.'
Assert-True ($protocolText -match 'AttackExecutionSemanticTargetResolver') 'RangedAttackProtocolService must use shared resolver.'
Assert-True ($stagesText -notmatch 'private static LocalTargetInfo ResolveSemanticTarget') 'Stages must not keep a local semantic resolver.'
Assert-True ($protocolText -notmatch 'private static LocalTargetInfo ResolveSemanticTarget') 'Protocol service must not keep a local semantic resolver.'
```

**Step 2: 运行测试，确认先失败**

Run:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\AttackExecutionSemanticTargetResolverSmokeTests.ps1'
```

Expected:

```text
FAIL，提示 resolver 文件不存在，或服务仍保留本地 ResolveSemanticTarget
```

**Step 3: 写最小实现**

新增解析器，保持逻辑只做这一条：

```csharp
internal static class AttackExecutionSemanticTargetResolver
{
    public static LocalTargetInfo Resolve(AttackExecutionPreparedContext request)
    {
        ConfirmedTargetSnapshot confirmedTarget = request?.AttackContextSnapshot?.GetNode(AttackContextKeys.ConfirmedTarget) as ConfirmedTargetSnapshot;
        if (confirmedTarget != null && confirmedTarget.SemanticTarget.IsValid)
        {
            return confirmedTarget.SemanticTarget;
        }

        return request != null ? request.Target : LocalTargetInfo.Invalid;
    }
}
```

然后把：

- `AttackExecutionService.Stages.cs`
- `RangedAttackProtocolService.cs`

里各自的本地 `ResolveSemanticTarget(...)` 删除，统一改为调用新解析器。

**Step 4: 运行新增测试**

Run:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\AttackExecutionSemanticTargetResolverSmokeTests.ps1'
```

Expected:

```text
AttackExecutionSemanticTargetResolverSmokeTests PASS
```

**Step 5: 回归现有语义测试**

Run:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DualRangedConfirmedTargetSnapshotSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DualRangedNecessaryLosSemanticsSmokeTests.ps1'
```

Expected:

```text
DualRangedConfirmedTargetSnapshotSmokeTests PASS
DualRangedNecessaryLosSemanticsSmokeTests PASS
```

**Step 6: 检查点提交（仅在用户明确同意时）**

```bash
git add 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/AttackExecutionSemanticTargetResolver.cs 模组工程/BorderDefenseProtocol/Source/BDP.Tests/AttackExecutionSemanticTargetResolverSmokeTests.ps1 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/AttackExecutionService.Stages.cs 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedProtocol/RangedAttackProtocolService.cs
git commit -m "refactor: centralize semantic target resolution"
```

### Task 3: 把“候选合法性”和“正式确认合法性”边界写死

**Files:**
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/AttackExecutionTargetingSource.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Model/TargetingRecord.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Targeting/TargetingSegmentLegalityRequest.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Targeting/DefaultTargetingSegmentLegalityService.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/TargetingSegmentLegalitySmokeTests.ps1`

**Step 1: 先给测试补失败断言**

在 `TargetingSegmentLegalitySmokeTests.ps1` 追加一组边界断言：

```powershell
Assert-True ($targetingRecordText -match 'HasCurrentTargetLegalityOverride') 'TargetingRecord must keep current-candidate legality override.'
Assert-True ($targetingSourceText -match 'TryEvaluateCurrentTargetLegality') 'AttackExecutionTargetingSource must keep current-candidate legality probe.'
Assert-True ($defaultServiceText -notmatch 'Confirm|FinalConfirm|AttackAllowed') 'Default segment legality service must not take over final confirm responsibility.'
```

**Step 2: 运行测试，确认先失败**

Run:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\TargetingSegmentLegalitySmokeTests.ps1'
```

Expected:

```text
若边界描述不够清楚或断言未满足，则先 FAIL
```

**Step 3: 做最小实现**

只补注释与少量测试文本，不改运行行为：

- `TryEvaluateCurrentTargetLegality(...)` 注释写明它是“当前候选点探针”
- `TargetingSegmentLegalityRequest` 注释写明它不代表最终确认
- `DefaultTargetingSegmentLegalityService` 注释写明它只回答“这一段现在能不能成立”

**Step 4: 重新运行测试**

Run:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\TargetingSegmentLegalitySmokeTests.ps1'
```

Expected:

```text
TargetingSegmentLegalitySmokeTests PASS
```

**Step 5: 做一次总回归**

Run:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DualRangedConfirmedTargetSnapshotSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\TargetingSegmentLegalitySmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\RangedProtocolBoundarySmokeTests.ps1'
```

Expected:

```text
全部 PASS，且没有新增 `TargetRole（目标角色）`、`PathSystem（路径系统）` 之类大抽象
```

**Step 6: 检查点提交（仅在用户明确同意时）**

```bash
git add 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/AttackExecutionTargetingSource.cs 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Model/TargetingRecord.cs 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Targeting/TargetingSegmentLegalityRequest.cs 模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/TargetingProtocol/Targeting/DefaultTargetingSegmentLegalityService.cs 模组工程/BorderDefenseProtocol/Source/BDP.Tests/TargetingSegmentLegalitySmokeTests.ps1
git commit -m "docs: lock current-target and segment-legality boundaries"
```

## 不包含在本计划中的内容

- 不抽通用“路径系统”
- 不抽 `TargetRole（目标角色）` 大枚举
- 不把 `PathLatchState / PathLatchSegmentResolver` 上提到主模组
- 不处理 `DevHarness -> v2` 的包拆分与菜单发现逻辑

## 完成标准

- 主模组多目标语义口径有一份集中说明
- 语义目标默认解析逻辑只保留一份中性实现
- 候选合法性 / 段合法性 / 最终确认合法性的边界写清楚
- 不引入新的毒蛇业务字段
- 不扩大主模组对测试模组的耦合
