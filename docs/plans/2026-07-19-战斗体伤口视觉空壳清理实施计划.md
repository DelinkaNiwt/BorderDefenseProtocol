# 战斗体伤口视觉空壳清理 Implementation Plan（实施计划）

> **For Claude（执行者要求）:** REQUIRED SUB-SKILL（必需子技能）: Use superpowers:executing-plans to implement this plan task-by-task（逐项执行本计划）。项目规则禁止工作树与子代理，因此直接在当前工程串行执行。

**Goal（目标）:** 删除未接入运行时的通用伤口视觉草案及其失效静态测试，同时保持现有喷溅与伤口 Trion 链不变。

**Architecture（实现方式）:** 这是纯删除任务。项目文件使用 C# 通配符收集源码，不需要修改项目清单；以运行链引用搜索、伤口回归测试和主模组编译证明现有功能未受影响。

**Tech Stack（技术环境）:** C# 7.3（C# 语言 7.3）、PowerShell（微软命令脚本）、RimWorld 1.6（边缘世界 1.6）、Harmony（运行时补丁库）。

---

### Task 1: 建立删除前基线

**Files（文件）:**
- Test（测试）: `Source/BDP.Tests/CombatBodyWoundSprayRuntimeSmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/CombatBodyWoundSprayLifecycleSmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/CombatBodyWoundOverlaySuppressSmokeTests.ps1`

**Step 1:** 执行三项现有测试，确认喷溅运行时、生命周期和原版覆盖贴图压制当前均通过。

**Step 2:** 全仓搜索四个草案类型名，确认除四个定义文件和专用静态测试外没有业务引用。

### Task 2: 删除空壳

**Files（文件）:**
- Delete（删除）: `Source/BDP/Core/CombatBody/Wounds/Visuals/ICombatBodyWoundVisualProvider.cs`
- Delete（删除）: `Source/BDP/Core/CombatBody/Wounds/Visuals/CombatBodyWoundVisualContext.cs`
- Delete（删除）: `Source/BDP/Core/CombatBody/Wounds/Visuals/CombatBodyWoundVisualDecision.cs`
- Delete（删除）: `Source/BDP/Core/CombatBody/Wounds/Visuals/CombatBodyWoundVisualMode.cs`
- Delete（删除）: `Source/BDP.Tests/CombatBodyWoundVisualInterfaceSmokeTests.ps1`

**Step 1:** 删除五个已批准文件。

**Step 2:** 再次搜索四个类型名，预期无结果。

### Task 3: 验证与提交

**Files（文件）:**
- Verify（验证）: `Source/BDP/BDP.csproj`
- Verify（验证）: `Source/BDP.Tests/CombatBodyWound*SmokeTests.ps1`

**Step 1:** 编译主模组，预期成功且不新增警告。

**Step 2:** 执行剩余全部战斗体伤口静态测试，预期全部通过。

**Step 3:** 只暂存本计划、设计修订和五个删除文件，检查暂存清单后提交。
