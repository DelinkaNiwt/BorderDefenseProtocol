# BDP 制造重构交互、命名与本地化回归修复 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 恢复芯片制造台原版交互格、统一成品命名，并消除开发语言文件对主命令语言文件的遮蔽。

**Architecture:** XML 只补原版必需的交互格声明；完整芯片名称由现有组合解析入口统一生成；开发语言资源通过唯一相对文件名保持可插拔。测试分别锁定原版工作台契约、名称格式和跨加载目录语言文件唯一性。

**Tech Stack:** RimWorld 1.6 XML、C# 7.3、Verse 翻译系统、PowerShell 冒烟测试、MSBuild。

---

### Task 1: 建立三个回归的失败测试

**Files:**
- Modify: `Source/BDP.Tests/ChipFabricatorVanillaArchitectureSmokeTests.ps1`
- Modify: `Source/BDP.Tests/ChipCombinationResolverSmokeTests.ps1`
- Create: `Source/BDP.Tests/LanguageLoadFolderCollisionSmokeTests.ps1`

**Step 1:** 断言制造台 Def 同时声明 `hasInteractionCell=true` 和现有偏移。

**Step 2:** 断言组合解析器使用语言键生成“触发器芯片：动作/动作[枪型]”，且不保留动作分隔空格或旧圆括号格式。

**Step 3:** 按 `LoadFolders.xml` 的实际加载目录扫描语言文件，断言同一模组内不存在重复相对路径。

**Step 4:** 分别运行三项测试，确认因现有三个根因而失败。

### Task 2: 实施最小根因修复

**Files:**
- Modify: `1.6/Content/Defs/ThingDef/Buildings/ChipFabricator.xml`
- Modify: `Source/BDP.Content/Assembly/ChipManufacturing/Resolution/ChipCombinationResolver.cs`
- Modify: `Languages/ChineseSimplified (简体中文)/Keyed/ChipManufacturing.xml`
- Rename: `1.6/Development/Languages/ChineseSimplified (简体中文)/Keyed/Commands.xml` → `DevelopmentDiagnostics.xml`

**Step 1:** 补充原版交互格开关。

**Step 2:** 以动作标签、可空枪型标签和两个语言键构造完整动态名称。

**Step 3:** 重命名开发语言文件，内容不变。

**Step 4:** 重跑三项测试，确认全部通过。

### Task 3: 回归验证、日志与提交

**Files:**
- Modify: `C:/NiwtDatas/Projects/RimworldModStudio/日志/Agent工作日志/Agent日志39.md`

**Step 1:** 重建 Content Release 和 Development Release，确认 Core 隔离通过。

**Step 2:** 运行芯片制造 20 项回归、中文本地化边界和开发程序集相关测试。

**Step 3:** 解析全部受影响 XML，运行 `git diff --check`。

**Step 4:** 写工作日志并提交 `fix: restore chip naming and command localization`。
