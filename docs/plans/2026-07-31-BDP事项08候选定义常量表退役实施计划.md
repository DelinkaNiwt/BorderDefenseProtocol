# BDP 事项08候选定义常量表退役 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 删除旧固定候选菜单遗留的无调用定义名称常量表。

**Architecture:** 用退役测试确认候选运行源码不再包含该类型，同时确认正式装配界面仍按真实芯片实例工作；随后物理删除单个C#文件。

**Tech Stack:** C# 7.3、PowerShell冒烟测试、MSBuild。

---

### Task 1：建立退役失败契约

**Files:**

- Create: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessDefNameConstantsRetirementSmokeTests.ps1`

1. 断言候选运行源码不含`TriggerDevHarnessDefs`。
2. 断言正式装配窗口从仓库取得真实芯片并把`Thing`交给装配事务。
3. 删除前运行并确认失败。
4. 提交失败契约。

### Task 2：删除常量表并验证

**Files:**

- Delete: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/TriggerDevHarnessDefs.cs`
- Modify: `模组工程/BorderDefenseProtocol/docs/plans/2026-07-28-BDP统一模组双程序集迁移总计划.md`
- Modify: `日志/Agent工作日志/Agent日志16.md`

1. 物理删除常量表。
2. 运行退役测试并构建DevHarness。
3. 运行全量测试，与12项既有失败基线比对。
4. 更新总计划和工作日志并提交。

