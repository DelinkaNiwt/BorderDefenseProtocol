# BDP 光束拖尾公共系统正式化实施计划

> **For Claude（给执行者）：** REQUIRED SUB-SKILL（必须使用的流程）: Use `superpowers:executing-plans（执行计划）` to implement this plan task-by-task.

**Goal（目标）：** 将候选模组的光束拖尾公共系统选择性转正到 `BDP.Content.dll`（内容程序集），加入两个亮薄荷绿正式预设，保留候选红色预设和既有业务选择，并修正长线段穿过镜头时的漏画。

**Architecture（架构）：** `BDP.Core.dll`（核心程序集）保持中性且不改动；`BDP.Content.dll` 单向使用 Core 已有投射物视觉附加接口，实现拖尾定义、运行时附加件、地图绘制和历史余痕存读档。主模组用 `1.6`主版本根目录承载 Core/待复查主内容，用 `1.6/Content`补充加载根目录承载已确认 Content 配置与专属资源；候选模组只保留红色预设及具体应用。

**Tech Stack（技术环境）：** RimWorld 1.6（边缘世界1.6）、C# 7.3（C#语言7.3）、XML（可扩展标记语言）、PowerShell（命令行脚本）、MSBuild（微软构建工具）、Git（版本控制）。

---

## 执行约束

- 完整设计见 `模组工程/BorderDefenseProtocol/docs/plans/2026-07-29-BDP光束拖尾公共系统正式化设计.md`。
- 项目规则禁止创建 `worktree（工作树）`和新分支；直接在当前工程实施。
- 只处理事项 04，不修改 Core 运行代码、具体芯片选择、上层应用参数或事项 05 之后的内容。
- 所有 C# 成员保留或补齐中文注释，XML 每个正式条目保留中文逐项注释，统一 UTF-8（编码）。
- 每个任务先运行指定失败测试，再写最小实现；计划外改动立即暂停。

## 执行结果

事项 04 已于 2026-07-29 完成：

- 失败契约提交：`d4b23fd1`。
- 正式拖尾源码提交：`0bf19263`。
- 亮薄荷绿预设与通用资源提交：`aa63d468`。
- Content 补充加载层设计与计划提交：`2d8785b1`、`071e2a78`。
- Core/Content XML 物理分层提交：`1d0fb677`。
- 测试路径修正提交：`c3a76caa`。
- Release（发布）程序集产物提交：`d3a6efbe`。

最终验证：

- Core、Content、候选三个程序集 Release 构建成功，Core 门禁通过。
- 两个模组共 47 个 XML（配置文件）全部解析成功。
- 事项 04 八项专项测试全部通过。
- 全量 223 项冒烟测试中 211 项通过、12 项失败；失败集合与迁移前基线完全一致，没有新增失败。
- 主模组发布目录只有 `BDP.Core.dll`、`BDP.Content.dll`及各自 PDB（调试符号）；候选发布目录只有 `BDP.DevHarness.dll`及其 PDB。

## 执行中发现并已确认的架构补充

2026-07-29 首次完整构建时，Core（核心）门禁把主模组整个 `1.6/Defs` 都当成 Core 自己的 XML（配置文件）扫描，因此把本事项新增的合法 `BDP.Content.Projectiles.BeamTrail` 类型误报为 Core 反向依赖。

已确认的事实：

- `BDP.Core.dll` 的工程引用和 DLL（动态链接库）引用没有指向 Content（内容程序集）。
- 误报只发生在“明确名称层”的 XML 扫描。
- 事项 02 原设计要求扫描“核心 XML”，但现有构建接线把整个主模组 Def（定义）目录传成了“核心 Def 根目录”；在主模组尚无 Content Def 时两者恰好相同，首个 Content Def 进入后该假设失效。

用户已确认采用同一主模组的独立 Content 补充加载根目录：

```text
/
1.6
1.6/Content
```

两个 DLL 继续统一位于 `1.6/Assemblies`；Core/待复查主配置保留在 `1.6/Defs`；本事项正式预设和专属贴图分别进入 `1.6/Content/Defs`、`1.6/Content/Textures`。不修改门禁算法，不按业务路径写死排除项，也不删除 XML 检查层。

### Task 1：建立事项 04 的失败契约

**Files（文件）：**

- Create（新建）: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/BeamTrailContentFormalizationSmokeTests.ps1`
- Modify（修改）: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailVisualSmokeTests.ps1`
- Modify（修改）: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailLoadThreadSafetySmokeTests.ps1`
- Modify（修改）: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/BeamTrailLiveHeadTerminationSmokeTests.ps1`
- Modify（修改）: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailPresetInjectionSmokeTests.ps1`
- Modify（修改）: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/SelectiveContentMigrationBoundarySmokeTests.ps1`

**Step 1：编写正式化边界测试**

新测试统一解析主模组与候选模组路径，并明确断言：

```powershell
$mainBeamTrailRoot = Join-Path $mainModRoot 'Source\BDP.Content\Projectiles\BeamTrail'
$candidateBeamTrailRoot = Join-Path $candidateModRoot 'Source\BDP.DevHarness\Projectiles\BeamTrail'
$mainPresetPath = Join-Path $mainModRoot '1.6\Defs\Projectiles\BeamTrail\BeamTrailPresetDefs.xml'
$candidatePresetPath = Join-Path $candidateModRoot '1.6\Defs\Things\Projectiles\Test\BeamTrailPresetDefs.xml'
$mainTexturePath = Join-Path $mainModRoot 'Textures\Things\Projectile\BDP_BeamTrail.png'
$candidateTexturePath = Join-Path $candidateModRoot 'Textures\Things\Projectile\BDP_BeamTrail.png'
```

测试必须验证：

- 主模组存在六个正式源码文件，候选拖尾源码目录不存在。
- 正式命名空间为 `BDP.Content.Projectiles.BeamTrail`。
- 正式扩展为 `BeamTrailExtension`，只含 `BeamTrailPresetDef preset`。
- 正式源码不存在 `ChipBeamTrailExtension`、`BeamTrailConfig`、`beamTrailPreset`、`enabled` 和 `BDP.DevHarness`。
- `ContentBootstrap（内容启动入口）`不注册拖尾业务。
- 主模组存在通用贴图，候选贴图不存在。
- 主模组预设只有亮薄荷绿长、短两项，颜色均为 `(0.855, 0.992, 0.898, 1.0)`。
- 候选预设只保留 `BDP_TrailPreset_HotRed`，参数不变，类型改用 Content 正式类型。
- 候选芯片八处扩展只机械改为正式类和 `<preset>`字段；红色仍为两处，长版仍为一处，短版仍为五处。
- 视野判断包含起点/终点最小最大边界、线宽扩展和镜头矩形相交，不再使用中点唯一判断。
- 历史段继续通过 `Scribe_Collections.Look` 保存，活动头段字典不保存。

**Step 2：把三项既有运行测试改查主模组正式目录**

将三个脚本的拖尾根目录统一改为：

```powershell
$beamTrailRoot = Join-Path $repoRoot 'Source\BDP.Content\Projectiles\BeamTrail'
```

同时把 `DevHarnessBeamTrailVisualSmokeTests.ps1` 的旧 `BeamTrailConfig`正向断言改为反向断言：

```powershell
Assert-True (-not (Test-Path -LiteralPath $configPath)) '正式系统不得保留 BeamTrailConfig.cs。'
Assert-True ($appearanceText -notmatch 'CreateFrom\s*\(\s*BeamTrailConfig') '外观快照不得保留旧直接配置入口。'
```

**Step 3：把预设注入测试改为正式系统与候选应用双边界**

正式源码从主模组读取；正式长短预设从主模组读取；候选红色预设和芯片 XML 继续从候选模组读取。长短对应断言使用：

```powershell
<preset>BDP_TrailPreset_BrightMintLong</preset>
<preset>BDP_TrailPreset_BrightMintShort</preset>
```

红色断言保持：

```powershell
<preset>BDP_TrailPreset_HotRed</preset>
```

**Step 4：更新选择性迁移边界测试的已确认范围**

- 从候选必需路径中移除候选拖尾贴图。
- 允许 `Source\BDP.Content\Projectiles\BeamTrail` 六个正式源码文件。
- 继续禁止 `Source\BDP.Content.Tests`、`docs\DevHarness` 和其他未确认候选目录。

**Step 5：运行测试，确认它们因正式实现尚不存在而失败**

Run（运行）:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\BeamTrailContentFormalizationSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailVisualSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailPresetInjectionSmokeTests.ps1'
```

Expected（预期）: FAIL（失败），明确指出主模组正式拖尾源码、预设或贴图尚不存在；失败不能来自脚本语法错误。

**Step 6：提交失败契约**

```powershell
git add -- '模组工程/BorderDefenseProtocol/Source/BDP.Tests/BeamTrailContentFormalizationSmokeTests.ps1' '模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailVisualSmokeTests.ps1' '模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailLoadThreadSafetySmokeTests.ps1' '模组工程/BorderDefenseProtocol/Source/BDP.Tests/BeamTrailLiveHeadTerminationSmokeTests.ps1' '模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailPresetInjectionSmokeTests.ps1' '模组工程/BorderDefenseProtocol/Source/BDP.Tests/SelectiveContentMigrationBoundarySmokeTests.ps1'
git commit -m 'test: 锁定事项04拖尾正式化边界'
```

### Task 2：迁移并规范正式拖尾源码

**Files（文件）：**

- Move（移动）: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Projectiles/BeamTrail/BeamTrailAppearanceSnapshot.cs` → `模组工程/BorderDefenseProtocol/Source/BDP.Content/Projectiles/BeamTrail/BeamTrailAppearanceSnapshot.cs`
- Move（移动）: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Projectiles/BeamTrail/BeamTrailAttachment.cs` → `模组工程/BorderDefenseProtocol/Source/BDP.Content/Projectiles/BeamTrail/BeamTrailAttachment.cs`
- Move（移动）: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Projectiles/BeamTrail/BeamTrailMapComponent.cs` → `模组工程/BorderDefenseProtocol/Source/BDP.Content/Projectiles/BeamTrail/BeamTrailMapComponent.cs`
- Move（移动）: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Projectiles/BeamTrail/BeamTrailPresetDef.cs` → `模组工程/BorderDefenseProtocol/Source/BDP.Content/Projectiles/BeamTrail/BeamTrailPresetDef.cs`
- Move（移动）: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Projectiles/BeamTrail/BeamTrailSegment.cs` → `模组工程/BorderDefenseProtocol/Source/BDP.Content/Projectiles/BeamTrail/BeamTrailSegment.cs`
- Move/Rename（移动并改名）: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Projectiles/BeamTrail/ChipBeamTrailExtension.cs` → `模组工程/BorderDefenseProtocol/Source/BDP.Content/Projectiles/BeamTrail/BeamTrailExtension.cs`
- Delete（删除）: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Projectiles/BeamTrail/BeamTrailConfig.cs`

**Step 1：移动六个有效文件并统一命名空间**

所有正式源码使用：

```csharp
namespace BDP.Content.Projectiles.BeamTrail
```

保留既有中文逐成员注释；把“测试、候选、芯片专属”表述改为中性的投射物来源描述。

**Step 2：移除外观快照的旧直接配置重载**

`BeamTrailAppearanceSnapshot`只保留：

```csharp
public static BeamTrailAppearanceSnapshot CreateFrom(BeamTrailPresetDef preset)
```

继续对贴图路径、宽度、寿命、透明度、淡出比例和指数做现有安全收束。

**Step 3：实现正式扩展**

`BeamTrailExtension.cs`的完整核心结构：

```csharp
using BDP.Core.Projectiles.Visual;
using Verse;

namespace BDP.Content.Projectiles.BeamTrail
{
    /// <summary>
    /// 光束拖尾定义扩展。
    /// 任意投射物来源 Def 都可以通过它引用一个正式拖尾预设。
    /// </summary>
    public sealed class BeamTrailExtension : DefModExtension, IProjectileVisualAttachmentProvider
    {
        /// <summary>当前来源使用的拖尾预设。</summary>
        public BeamTrailPresetDef preset;

        /// <summary>
        /// 为本次投射物创建独立的拖尾视觉附加件。
        /// </summary>
        /// <returns>预设有效时返回附加件；否则返回空。</returns>
        public IProjectileVisualAttachment CreateAttachment()
        {
            if (preset == null)
            {
                Log.WarningOnce("[BDP.BeamTrail] 拖尾扩展缺少有效 preset（预设），本次投射物不会生成拖尾。", GetHashCode());
                return null;
            }

            return new BeamTrailAttachment(BeamTrailAppearanceSnapshot.CreateFrom(preset));
        }
    }
}
```

**Step 4：修正整段视野判断**

用起点、终点和线宽形成覆盖矩形，替换中点判断：

```csharp
private static bool IsPotentiallyVisible(BeamTrailSegment segment, CellRect visibleRect)
{
    float halfWidth = Mathf.Max(0.01f, segment.TrailWidth) * 0.5f;
    float minX = Mathf.Min(segment.Start.x, segment.End.x) - halfWidth;
    float maxX = Mathf.Max(segment.Start.x, segment.End.x) + halfWidth;
    float minZ = Mathf.Min(segment.Start.z, segment.End.z) - halfWidth;
    float maxZ = Mathf.Max(segment.Start.z, segment.End.z) + halfWidth;

    return maxX >= visibleRect.minX
        && minX <= visibleRect.maxX + 1f
        && maxZ >= visibleRect.minZ
        && minZ <= visibleRect.maxZ + 1f;
}
```

保留历史段、活动头段、材质惰性解析、单路径一次警告和地图缓存的现有时序。

**Step 5：确认候选旧源码已清空**

候选 `BeamTrail`目录应不存在或为空；不得遗留第二份同名类型。`BDP.DevHarness.csproj`不新增对 `BDP.Content`工程的编译引用，因为候选 C# 已不再直接使用正式拖尾类型。

**Step 6：运行源码专项测试和双程序集构建**

Run（运行）:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailVisualSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailLoadThreadSafetySmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\BeamTrailLiveHeadTerminationSmokeTests.ps1'
msbuild '.\模组工程\BorderDefenseProtocol\Source\BDP.Content\BDP.Content.csproj' /t:Rebuild /p:Configuration=Release
msbuild '.\模组工程\BorderDefenseProtocol.DevHarness\Source\BDP.DevHarness\BDP.DevHarness.csproj' /t:Rebuild /p:Configuration=Release
```

Expected（预期）: 三项专项测试 PASS（通过）；两个工程 0 Error（错误）；Core 单向依赖门禁 PASS。

**Step 7：提交正式源码**

```powershell
git add -- '模组工程/BorderDefenseProtocol/Source/BDP.Content/Projectiles/BeamTrail' '模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Projectiles/BeamTrail'
git commit -m 'feat: 正式化内容侧光束拖尾系统'
```

### Task 3：迁移贴图与长短预设并机械对接候选配置

**Files（文件）：**

- Move（移动）: `模组工程/BorderDefenseProtocol.DevHarness/Textures/Things/Projectile/BDP_BeamTrail.png` → `模组工程/BorderDefenseProtocol/Textures/Things/Projectile/BDP_BeamTrail.png`
- Create（新建）: `模组工程/BorderDefenseProtocol/1.6/Defs/Projectiles/BeamTrail/BeamTrailPresetDefs.xml`
- Modify（修改）: `模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Things/Projectiles/Test/BeamTrailPresetDefs.xml`
- Modify（修改）: `模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Things/Items/Chips/Test/ThingDefs_TestChips_Combat.xml`

**Step 1：移动通用贴图**

保留原 32×32 白色半透明贴图内容和正式路径：

```text
Things/Projectile/BDP_BeamTrail
```

候选模组不再保留副本。

**Step 2：创建主模组正式预设 XML**

使用 `BDP.Content.Projectiles.BeamTrail.BeamTrailPresetDef`类型。

长版完整参数：

```xml
<defName>BDP_TrailPreset_BrightMintLong</defName>
<label>亮薄荷绿长拖尾</label>
<trailTexPath>Things/Projectile/BDP_BeamTrail</trailTexPath>
<trailColor>(0.855, 0.992, 0.898, 1.0)</trailColor>
<trailWidth>0.1105</trailWidth>
<segmentLifetimeTicks>30</segmentLifetimeTicks>
<startOpacity>1.0</startOpacity>
<fadeRatio>0.8</fadeRatio>
<fadeExponent>10.0</fadeExponent>
<altitudeOffset>0</altitudeOffset>
<startForwardOffset>0</startForwardOffset>
<debugLogging>false</debugLogging>
```

短版完整参数：

```xml
<defName>BDP_TrailPreset_BrightMintShort</defName>
<label>亮薄荷绿短拖尾</label>
<trailTexPath>Things/Projectile/BDP_BeamTrail</trailTexPath>
<trailColor>(0.855, 0.992, 0.898, 1.0)</trailColor>
<trailWidth>0.09</trailWidth>
<segmentLifetimeTicks>6</segmentLifetimeTicks>
<startOpacity>0.85</startOpacity>
<fadeRatio>0.55</fadeRatio>
<fadeExponent>2.5</fadeExponent>
<altitudeOffset>0</altitudeOffset>
<startForwardOffset>0</startForwardOffset>
<debugLogging>false</debugLogging>
```

**Step 3：把候选预设文件收紧为红色一项**

候选 XML 只保留：

```xml
<BDP.Content.Projectiles.BeamTrail.BeamTrailPresetDef>
  <defName>BDP_TrailPreset_HotRed</defName>
  ...
</BDP.Content.Projectiles.BeamTrail.BeamTrailPresetDef>
```

红色颜色、宽度、寿命、淡出和其他参数全部保持原值。

**Step 4：机械更新八处候选扩展**

统一替换：

```xml
<li Class="BDP.Content.Projectiles.BeamTrail.BeamTrailExtension">
  <preset>...</preset>
</li>
```

映射只按同一预设转正改名：

```text
BDP_TrailPreset_ColdBlue → BDP_TrailPreset_BrightMintLong
BDP_TrailPreset_TrackingShortBlue → BDP_TrailPreset_BrightMintShort
BDP_TrailPreset_HotRed → 保持不变
```

删除八处 `<enabled>true</enabled>`；不修改对应 ThingDef（物品定义）中的任何其他字段。

**Step 5：运行正式化和配置专项测试**

Run（运行）:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\BeamTrailContentFormalizationSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailPresetInjectionSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\SelectiveContentMigrationBoundarySmokeTests.ps1'
```

Expected（预期）: 全部 PASS；红色引用计数为 2，长版为 1，短版为 5。

**Step 6：提交资源和 XML**

```powershell
git add -- '模组工程/BorderDefenseProtocol/Textures/Things/Projectile/BDP_BeamTrail.png' '模组工程/BorderDefenseProtocol/1.6/Defs/Projectiles/BeamTrail/BeamTrailPresetDefs.xml' '模组工程/BorderDefenseProtocol.DevHarness/Textures/Things/Projectile/BDP_BeamTrail.png' '模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Things/Projectiles/Test/BeamTrailPresetDefs.xml' '模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Things/Items/Chips/Test/ThingDefs_TestChips_Combat.xml'
git commit -m 'feat: 加入亮薄荷绿拖尾正式预设'
```

### Task 3A：建立 Content 补充加载根目录

**Files（文件）：**

- Modify（修改）: `模组工程/BorderDefenseProtocol/LoadFolders.xml`
- Move（移动）: `模组工程/BorderDefenseProtocol/1.6/Defs/Projectiles/BeamTrail/BeamTrailPresetDefs.xml` → `模组工程/BorderDefenseProtocol/1.6/Content/Defs/Projectiles/BeamTrail/BeamTrailPresetDefs.xml`
- Move（移动）: `模组工程/BorderDefenseProtocol/Textures/Things/Projectile/BDP_BeamTrail.png` → `模组工程/BorderDefenseProtocol/1.6/Content/Textures/Things/Projectile/BDP_BeamTrail.png`
- Modify（修改）: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/BeamTrailContentFormalizationSmokeTests.ps1`
- Modify（修改）: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailVisualSmokeTests.ps1`
- Modify（修改）: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailPresetInjectionSmokeTests.ps1`
- Modify（修改）: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/SelectiveContentMigrationBoundarySmokeTests.ps1`
- Modify（修改）: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/UnifiedModAssemblyLayoutSmokeTests.ps1`

**Step 1：先把测试改为已确认的加载目录结构**

正式预设和贴图路径改为：

```powershell
$mainPresetPath = Join-Path $mainModRoot '1.6\Content\Defs\Projectiles\BeamTrail\BeamTrailPresetDefs.xml'
$mainTexturePath = Join-Path $mainModRoot '1.6\Content\Textures\Things\Projectile\BDP_BeamTrail.png'
```

两个加载目录测试统一要求：

```powershell
$loadedFolders.Count -eq 3
$loadedFolders[0] -eq "/"
$loadedFolders[1] -eq "1.6"
$loadedFolders[2] -eq "1.6/Content"
```

同时断言不存在旧 `1.6\Defs\Projectiles\BeamTrail`和根级 `Textures\Things\Projectile\BDP_BeamTrail.png`，避免同一资源加载两次。

**Step 2：运行测试确认正确失败**

Run（运行）:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\BeamTrailContentFormalizationSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailVisualSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailPresetInjectionSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\SelectiveContentMigrationBoundarySmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\UnifiedModAssemblyLayoutSmokeTests.ps1'
```

Expected（预期）: FAIL（失败），原因分别是 Content 补充层中的预设、贴图或第三个加载目录尚不存在；不得因脚本语法错误失败。

**Step 3：实施最小目录迁移**

`LoadFolders.xml`完整结果：

```xml
<loadFolders>
  <v1.6>
    <li>/</li>
    <li>1.6</li>
    <li>1.6/Content</li>
  </v1.6>
</loadFolders>
```

只移动本事项两个已确认资源，不搬迁其他既有 Def（定义）或贴图。Core 工程仍把 `1.6/Defs`作为核心 Def 根目录，不修改门禁脚本和构建接线。

**Step 4：运行专项测试与 Core 门禁**

Run（运行）:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\BeamTrailContentFormalizationSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailVisualSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailPresetInjectionSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\SelectiveContentMigrationBoundarySmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\UnifiedModAssemblyLayoutSmokeTests.ps1'
dotnet msbuild '.\模组工程\BorderDefenseProtocol\Source\BDP\BDP.csproj' /t:Rebuild /p:Configuration=Release /nologo /v:minimal
```

Expected（预期）: 四项测试 PASS；Core 构建输出“未依赖任何其他本模组程序集”并以 0 Error（错误）结束。

**Step 5：提交加载目录分层**

```powershell
git add -- '模组工程/BorderDefenseProtocol/LoadFolders.xml' '模组工程/BorderDefenseProtocol/1.6/Content/Defs/Projectiles/BeamTrail/BeamTrailPresetDefs.xml' '模组工程/BorderDefenseProtocol/1.6/Content/Textures/Things/Projectile/BDP_BeamTrail.png' '模组工程/BorderDefenseProtocol/1.6/Defs/Projectiles/BeamTrail/BeamTrailPresetDefs.xml' '模组工程/BorderDefenseProtocol/Textures/Things/Projectile/BDP_BeamTrail.png' '模组工程/BorderDefenseProtocol/Source/BDP.Tests/BeamTrailContentFormalizationSmokeTests.ps1' '模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailVisualSmokeTests.ps1' '模组工程/BorderDefenseProtocol/Source/BDP.Tests/SelectiveContentMigrationBoundarySmokeTests.ps1' '模组工程/BorderDefenseProtocol/Source/BDP.Tests/UnifiedModAssemblyLayoutSmokeTests.ps1'
git commit -m 'fix: 分离Core与Content配置加载根目录'
```

### Task 4：完成构建、XML 与回归验证

**Files（文件）：**

- Verify（验证）: `模组工程/BorderDefenseProtocol/Source/BDP.Content/BDP.Content.csproj`
- Verify（验证）: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/BDP.DevHarness.csproj`
- Verify（验证）: 两个模组范围内全部 XML
- Verify（验证）: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/*.ps1`

**Step 1：检查旧语义和迁移边界**

Run（运行）:

```powershell
rg -n 'BDP\.DevHarness\.Projectiles\.BeamTrail|ChipBeamTrailExtension|BeamTrailConfig|beamTrailPreset|<enabled>true</enabled>|BDP_TrailPreset_ColdBlue|BDP_TrailPreset_TrackingShortBlue' '.\模组工程\BorderDefenseProtocol\Source\BDP.Content' '.\模组工程\BorderDefenseProtocol\1.6\Content\Defs\Projectiles\BeamTrail' '.\模组工程\BorderDefenseProtocol.DevHarness\1.6\Defs\Things\Projectiles\Test\BeamTrailPresetDefs.xml' '.\模组工程\BorderDefenseProtocol.DevHarness\1.6\Defs\Things\Items\Chips\Test\ThingDefs_TestChips_Combat.xml'
```

Expected（预期）: 无匹配；候选其他尚未确认目录不在本次清理范围。

**Step 2：重新构建主模组两个程序集和候选程序集**

Run（运行）:

```powershell
msbuild '.\模组工程\BorderDefenseProtocol\Source\BDP\BDP.csproj' /t:Rebuild /p:Configuration=Release
msbuild '.\模组工程\BorderDefenseProtocol\Source\BDP.Content\BDP.Content.csproj' /t:Rebuild /p:Configuration=Release
msbuild '.\模组工程\BorderDefenseProtocol.DevHarness\Source\BDP.DevHarness\BDP.DevHarness.csproj' /t:Rebuild /p:Configuration=Release
```

Expected（预期）: 三个工程 0 Error；Core 构建末尾输出依赖门禁 PASS；主模组发布目录只有 `BDP.Core.dll` 和 `BDP.Content.dll`，候选发布目录只有 `BDP.DevHarness.dll`。

**Step 3：解析两个模组全部 XML**

使用 PowerShell 逐文件执行 `[xml](Get-Content -Raw)`，汇总总数和失败文件。

Expected（预期）: 0 个 XML 解析失败。

**Step 4：运行事项 04 专项测试**

Run（运行）:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\BeamTrailContentFormalizationSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailVisualSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailLoadThreadSafetySmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\BeamTrailLiveHeadTerminationSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailPresetInjectionSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\ProjectileVisualAttachmentHostSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\CoreModAssemblyIsolationVerifierSmokeTests.ps1'
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\SelectiveContentMigrationBoundarySmokeTests.ps1'
```

Expected（预期）: 八项全部 PASS。

**Step 5：运行全量冒烟测试并比对基线**

按文件名顺序执行 `Source/BDP.Tests/*.ps1`，分别记录 PASS 与 FAIL。预期总数从 222 增加到 223；允许的失败只能是已经记录的十二项既有基线：

```text
ComboSourceResolutionBoundarySmokeTests.ps1
DevHarnessDualWeaponVisualConfigSmokeTests.ps1
DevHarnessExplosiveRangedModuleSmokeTests.ps1
DevHarnessPathLatchVolleyChipSmokeTests.ps1
DevHarnessTrackingRangedModuleSmokeTests.ps1
DevHarnessViperVolleyVisualDefaultsSmokeTests.ps1
DualRangedHostOwnershipSmokeTests.ps1
ManualEntryIconPresentationSmokeTests.ps1
MeleeNormalRhythmContractSmokeTests.ps1
RangedModulePrivateContextFreezeSmokeTests.ps1
TrionGeneGizmoSmokeTests.ps1
VisualPoseResolverBoundarySmokeTests.ps1
```

若旧测试因事项 04 的已确认正式路径或名称而失败，应只更新该测试的路径/名称预期；不得顺手改业务代码。

### Task 5：同步长期计划、工作日志并提交完成状态

**Files（文件）：**

- Modify（修改）: `模组工程/BorderDefenseProtocol/docs/plans/2026-07-28-BDP统一模组双程序集迁移总计划.md`
- Modify（修改）: `日志/Agent工作日志/Agent日志12.md`

**Step 1：更新总计划**

- 事项 04 状态改为“已完成”。
- 写入源码、预设、候选保留边界、构建、XML、专项和全量测试结果。
- 当前检查点转到事项 05“正式化 Trigger（触发器）装载界面”，状态保持“未介绍”。
- 明确下一步是先向用户介绍事项 05，不能自动实施。
- 决定记录仍最多保留最近二十条，进度记录仍按倒序。

**Step 2：写工作日志**

在 `Agent日志12.md`顶部记录：

- 本次迁移了什么。
- 没有迁移什么。
- 实际验证数字。
- 各实施提交号。
- 下一事项只进入介绍阶段。

**Step 3：检查差异并提交**

Run（运行）:

```powershell
git diff --check
git status --short -- '模组工程/BorderDefenseProtocol' '模组工程/BorderDefenseProtocol.DevHarness' '日志/Agent工作日志/Agent日志12.md'
```

只提交事项 04 的计划和日志文件，不吸收其他目录的无关改动。

```powershell
git add -- '模组工程/BorderDefenseProtocol/docs/plans/2026-07-28-BDP统一模组双程序集迁移总计划.md' '日志/Agent工作日志/Agent日志12.md'
git commit -m 'docs: 完成事项04光束拖尾正式化'
```

**Step 4：最终确认**

重新运行 `git show --stat --oneline HEAD` 和事项 04 专项测试，确认文档提交没有改变运行结果。

完成标准：

- 事项 04 的设计、计划、失败契约、源码、资源配置和完成记录均有独立可回退提交。
- 运行代码只新增到 `BDP.Content.dll`，Core 保持零本模组程序集依赖。
- 主模组获得两个亮薄荷绿预设；候选红色预设和两处业务引用保持不变。
- 正式预设和专属贴图位于 `1.6/Content`补充加载层；Core 门禁只扫描 `1.6/Defs`中的核心 XML。
- 候选模组无重复拖尾源码或贴图，仍能在主模组之后加载。
- 自动验证无新增失败；游戏内颜色观感留给用户实测后再校色。
