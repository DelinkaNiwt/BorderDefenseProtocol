# BDP 制造台上帝模式立刻完成 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为芯片制造台增加仅上帝模式可见、无材料与工时消耗地批量完成全部有效芯片账单的 Gizmo 调试按钮。

**Architecture:** 建筑只提供调试入口，批量生成、半成品清理和账单删除集中放入 Content 的调试完成服务。服务直接建立正式成品并复制账单组合记录，不伪造原版制作工作流。

**Tech Stack:** RimWorld 1.6 C#、`Building_WorkTable`、`BillStack`、`ThingMaker`、PowerShell 冒烟测试、MSBuild。

---

### Task 1: 用失败测试定义调试按钮与完成边界

**Files:**
- Create: `Source/BDP.Tests/ChipFabricatorDebugCompletionSmokeTests.ps1`

**Step 1:** 断言建筑只在 `DebugSettings.godMode` 下添加调试 Gizmo，并调用独立完成服务。

**Step 2:** 断言服务处理全部芯片账单和剩余数量、直接初始化成品组合、在交互格落地、删除完成账单，并跳过非 Valid（有效）记录。

**Step 3:** 断言服务不查询动态材料成本、不创建制作 Job、不授予经验，并清理已绑定半成品。

**Step 4:** 运行测试，预期因服务和入口尚不存在而 FAIL。

### Task 2: 实现批量完成服务与上帝模式入口

**Files:**
- Create: `Source/BDP.Content/Assembly/ChipManufacturing/Debug/ChipFabricatorDebugCompletionService.cs`
- Create: `Source/BDP.Content/Assembly/ChipManufacturing/Debug/ChipFabricatorDebugCompletionReport.cs`
- Modify: `Source/BDP.Content/Assembly/Building/Building_ChipFabricator.cs`
- Modify: `Languages/ChineseSimplified (简体中文)/Keyed/ChipManufacturing.xml`

**Step 1:** 服务快照账单栈中的芯片账单，对有效有限账单按 `repeatCount` 直接制造成品。

**Step 2:** 成品使用配方产物 Def，通过 `CompManufacturedChip.InitializeFromBill` 写入组合记录，并用 `GenPlace.TryPlaceThing` 落在交互格附近。

**Step 3:** 清理绑定半成品前终止当前相关 Job，退回半成品材料；完成后调用原版 `BillStack.Delete`。

**Step 4:** 建筑仅在上帝模式追加调试命令，并用语言包显示汇总消息。

**Step 5:** 运行专项测试，预期 PASS。

### Task 3: 回归验证、日志与提交

**Files:**
- Modify: `C:/NiwtDatas/Projects/RimworldModStudio/日志/Agent工作日志/Agent日志39.md`

**Step 1:** 重建 Content Release，并运行芯片制造计划测试与新增专项测试。

**Step 2:** 解析语言 XML，运行 Core 隔离和 `git diff --check`。

**Step 3:** 记录实现与游戏内待测项。

**Step 4:** 提交为 `feat: add god-mode chip queue completion`。
