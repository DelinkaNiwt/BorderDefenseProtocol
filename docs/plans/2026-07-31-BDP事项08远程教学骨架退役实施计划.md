# BDP 事项08远程教学骨架退役 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 删除无正式业务价值的远程攻击教学运行内容，同时保留Core远程模块和Combo挂载的通用能力。

**Architecture:** 用退役边界测试锁定活动运行内容不再出现教学实体；物理删除C#模块，并从混合XML中只移除教学Def与专用Combo区块。原有测试去掉教学存在性要求，但保留通用底层和其它真实业务候选检查。

**Tech Stack:** RimWorld 1.6 XML、C# 7.3、PowerShell冒烟测试、MSBuild。

---

### Task 1：建立退役失败契约

**Files:**

- Create: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessRangedTeachingSkeletonRetirementSmokeTests.ps1`

1. 检查候选活动Defs、候选运行源码、现行自动测试和两份现行编写说明。
2. 断言不再出现教学模块、配置类和专用Combo标识。
3. 运行并确认当前因实体仍存在而失败。
4. 独立提交失败契约。

### Task 2：删除教学运行实体

**Files:**

- Delete: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/RangedModules/Samples/RangedTeachingSkeletonModule.cs`
- Modify: `模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Pawn/Expressions/Test/RangedAttackModuleDefs_Test.xml`
- Modify: `模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Pawn/Combos/Test/ComboDefs_TestCombos.xml`

1. 删除C#文件。
2. 只删除教学模块Def区块。
3. 只删除文件开头的教学Combo区块，保留其余Combo。

### Task 3：收紧测试与现行说明

**Files:**

- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/RangedModuleComboMountSmokeTests.ps1`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/RangedModuleSampleModulesSmokeTests.ps1`
- Modify: `模组工程/BorderDefenseProtocol/docs/plans/2026-04-09-BDP远程攻击业务模块编写标准.md`
- Modify: `模组工程/BorderDefenseProtocol/docs/需求说明/2026-04-10-BDP远程模块编写说明书-第一版.md`

1. Combo挂载测试保留Core字段和传递链检查，删除专用Combo存在性要求。
2. 样例模块测试删除教学实体存在性要求，保留主模组无业务样例、旧分裂样例已删除、统一投射物宿主等边界。
3. 两份现行说明把教学实体名改为明显的作者占位名，并移除失效文件路径。
4. 运行退役测试及两个相邻测试，预期全部通过。
5. 提交删除与同步清理。

### Task 4：完整验证和记录

**Files:**

- Modify: `模组工程/BorderDefenseProtocol/docs/plans/2026-07-28-BDP统一模组双程序集迁移总计划.md`
- Modify: `日志/Agent工作日志/Agent日志16.md`

1. 构建Core、Content和DevHarness。
2. 解析两个模组当前全部XML。
3. 运行全量自动检查并与12项既有失败基线比较。
4. 更新事项08当前状态和下一对象。
5. 提交程序集产物、总计划与工作日志。

## 执行补正

全量检查发现`ComboTrionOwnershipSmokeTests.ps1（Combo费用归属检查）`曾要求候选库必须保留至少一个显式条目费用示例，而该示例正是已删除的教学Combo。

Core仍保留并检查`entry.Trion（表达条目费用）`处理链，现有真实候选Combo使用`TrionResolve（费用求值规则）`。因此没有给其它Combo虚构费用，只删除“候选库必须养一个示例”的旧断言。补正后全量失败恢复为原有12项。
