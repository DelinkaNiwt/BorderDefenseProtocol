# 毒蛇原版精度驱动分段偏航与末段收束 Implementation Plan（实施计划）

> **For Claude（面向 Claude）:** REQUIRED SUB-SKILL（必需子技能）: Use `superpowers:executing-plans（执行计划）` to implement this plan task-by-task（逐任务实施本计划）.

**Goal（目标）:** 让毒蛇每发子弹冻结一次原版精度事实，中间续段按该事实产生安全且不断链的偏航，最终续段以独立小散布收束，同时保留原版命中资格。

**Architecture（架构）:** Core（核心层）只新增中性的 `ProjectileAccuracySnapshot（投射物精度快照）`，由 `BdpVerb_Shoot（BDP 射击动词）`在现有原版 `ShotReport（射击报告）`生成后写入每发计划，并由到达阶段只读暴露。Content（内容层）的 `RoutePathModule（路线引导模块）`消费这份快照，使用稳定随机、安全收缩、名义锚点回退和最终段小半径完成“乱而有序”；不逐段重跑原版命中链，不改写命中类别。

**Tech Stack（技术栈）:** C# 7.3（C# 语言 7.3）、.NET Framework 4.8（.NET 框架 4.8）、RimWorld 1.6（边缘世界 1.6）Verse/RimWorld API（游戏接口）、PowerShell（微软命令行脚本）专项检查、XML（可扩展标记语言）Def（定义）。

**Design（设计依据）:** `docs/plans/2026-08-09-毒蛇原版精度驱动分段偏航与末段收束设计.md`

**Workspace（工作区）:** 遵循项目 `AGENTS.md（代理说明文件）`，不创建 worktree（工作树），不新建分支，直接在当前工程按任务提交。

**Default Calibration（默认标定）:** `IntermediateSpreadRadius（中间段散布半径） = 1.25`、`FinalSpreadRadius（最终段散布半径） = 0.30`、`HighAccuracySpreadScale（高精度散布比例） = 0.25`、`SpreadSafetyShrinkSteps（安全收缩次数） = 4`、`ArrivalTolerance（到达容差） = 0.35`。实施过程中不得临场改值；只有完成游戏内验证并另行记录依据后才可调整。

---

## 执行总约束

- 先用 `superpowers:test-driven-development（测试驱动开发）`执行每个实现任务。
- 遇到测试失败或游戏行为异常，先用 `superpowers:systematic-debugging（系统化调试）`追根因。
- 完成前用 `superpowers:verification-before-completion（完成前验证）`执行本计划末尾全部验证。
- 不修改 `BorderDefenseProtocol.Legacy（旧版模组）`。
- 不把正式毒蛇业务反向写入 `BorderDefenseProtocol.DevHarness（伴生测试模组）`。
- Core 源码不得出现毒蛇、路线、锚点、收束等业务命名。
- 不顺手修改毒蛇伤害、射速、射程、Trion（三离子）消耗、自动绕障和左右分流。
- 新增 C# 成员逐成员写中文注释；新增 XML 条目逐条写中文注释；文本统一 UTF-8（统一码编码）。
- 每个任务完成后只提交本任务文件，不夹带计划外改动。

### Task 1: 建立中性投射物精度快照模型

**Files（文件）:**

- Create（新建）: `Source/BDP/Core/AttackExecution/RangedProtocol/Model/ProjectileAccuracySnapshot.cs`
- Create（新建）: `Source/BDP.Tests/ProjectileAccuracySnapshotSmokeTests.ps1`

**Step 1: 写失败的专项检查**

在 `ProjectileAccuracySnapshotSmokeTests.ps1` 使用现有 `Assert-True（断言）`与 `Read-Source（读取源码）`样式，先锁定：

```powershell
$snapshotPath = Join-Path $bdpCoreRoot 'AttackExecution\RangedProtocol\Model\ProjectileAccuracySnapshot.cs'
$snapshotText = if (Test-Path -LiteralPath $snapshotPath) {
    Read-Source $snapshotPath
} else {
    ''
}

Assert-True (Test-Path -LiteralPath $snapshotPath) `
    'ProjectileAccuracySnapshot.cs must exist.'

Assert-True (
    ($snapshotText -match 'public\s+sealed\s+class\s+ProjectileAccuracySnapshot\s*:\s*IExposable') -and
    ($snapshotText -match 'public\s+bool\s+IsAvailable') -and
    ($snapshotText -match 'public\s+float\s+StandardAimChance') -and
    ($snapshotText -match 'public\s+float\s+IgnoringPostureAimChance') -and
    ($snapshotText -match 'public\s+float\s+PassCoverChance') -and
    ($snapshotText -match 'public\s+float\s+ForcedMissRadius') -and
    ($snapshotText -match 'public\s+float\s+AccuracyFactor') -and
    ($snapshotText -match 'ProjectileAccuracySnapshot\s+CloneTyped') -and
    ($snapshotText -match 'void\s+ExposeData\s*\(')
) 'Accuracy snapshot must expose and persist neutral original-shot facts.'

Assert-True (
    $snapshotText -notmatch 'Viper|Route|Anchor|Convergence'
) 'Core accuracy snapshot must not contain Viper route business naming.'
```

脚本开头补一行中文编码注释，并以 UTF-8 BOM（带签名的统一码编码）保存，防止 Windows PowerShell（微软命令行）误判中文。

**Step 2: 运行检查并确认失败**

Run（运行）:

```powershell
& '.\Source\BDP.Tests\ProjectileAccuracySnapshotSmokeTests.ps1'
```

Expected（预期）: FAIL（失败），提示 `ProjectileAccuracySnapshot.cs must exist.`。

**Step 3: 实现最小中性模型**

新类保持纯数据，不引用 Content（内容层）：

```csharp
using UnityEngine;
using Verse;

namespace BDP.Core.AttackExecution.RangedProtocol.Model
{
    /// <summary>
    /// 单发投射物在正式开火时冻结的原版精度事实。
    /// 只承载原版射击报告的公开结果，不解释任何具体弹道业务。
    /// </summary>
    public sealed class ProjectileAccuracySnapshot : IExposable
    {
        /// <summary>当前快照是否来自一次有效的原版射击报告。</summary>
        public bool IsAvailable { get; set; }

        /// <summary>忽略目标体型与姿态前的标准目标瞄准概率。</summary>
        public float StandardAimChance { get; set; } = 0.5f;

        /// <summary>包含目标体型、但忽略姿态后的瞄准概率。</summary>
        public float IgnoringPostureAimChance { get; set; } = 0.5f;

        /// <summary>原版射击报告给出的掩体通过概率。</summary>
        public float PassCoverChance { get; set; } = 1f;

        /// <summary>当前发射计划冻结的强制失准半径。</summary>
        public float ForcedMissRadius { get; set; }

        /// <summary>当前发射计划在原版报告外追加的精度倍率。</summary>
        public float AccuracyFactor { get; set; } = 1f;

        /// <summary>深度复制当前快照。</summary>
        public ProjectileAccuracySnapshot CloneTyped()
        {
            return new ProjectileAccuracySnapshot
            {
                IsAvailable = IsAvailable,
                StandardAimChance = StandardAimChance,
                IgnoringPostureAimChance = IgnoringPostureAimChance,
                PassCoverChance = PassCoverChance,
                ForcedMissRadius = ForcedMissRadius,
                AccuracyFactor = AccuracyFactor
            };
        }

        /// <summary>存档读写当前精度事实。</summary>
        public void ExposeData()
        {
            bool isAvailable = IsAvailable;
            float standardAimChance = StandardAimChance;
            float ignoringPostureAimChance = IgnoringPostureAimChance;
            float passCoverChance = PassCoverChance;
            float forcedMissRadius = ForcedMissRadius;
            float accuracyFactor = AccuracyFactor;

            Scribe_Values.Look(ref isAvailable, "isAvailable", false);
            Scribe_Values.Look(ref standardAimChance, "standardAimChance", 0.5f);
            Scribe_Values.Look(ref ignoringPostureAimChance, "ignoringPostureAimChance", 0.5f);
            Scribe_Values.Look(ref passCoverChance, "passCoverChance", 1f);
            Scribe_Values.Look(ref forcedMissRadius, "forcedMissRadius", 0f);
            Scribe_Values.Look(ref accuracyFactor, "accuracyFactor", 1f);

            IsAvailable = isAvailable;
            StandardAimChance = Mathf.Clamp01(standardAimChance);
            IgnoringPostureAimChance = Mathf.Clamp01(ignoringPostureAimChance);
            PassCoverChance = Mathf.Clamp01(passCoverChance);
            ForcedMissRadius = Mathf.Max(0f, forcedMissRadius);
            AccuracyFactor = accuracyFactor > 0f ? accuracyFactor : 1f;
        }
    }
}
```

**Step 4: 运行专项检查与 Core 构建**

Run（运行）:

```powershell
& '.\Source\BDP.Tests\ProjectileAccuracySnapshotSmokeTests.ps1'
dotnet build '.\Source\BDP\BDP.csproj' -c Release
```

Expected（预期）: 检查输出 `ProjectileAccuracySnapshotSmokeTests PASS（通过）`；Core 构建 0 errors（零错误）。

**Step 5: 提交**

```powershell
git add -- 'Source/BDP/Core/AttackExecution/RangedProtocol/Model/ProjectileAccuracySnapshot.cs' 'Source/BDP.Tests/ProjectileAccuracySnapshotSmokeTests.ps1'
git commit -m 'feat: 增加中性投射物精度快照'
```

### Task 2: 让精度快照随每发计划存档并向到达阶段暴露

**Files（文件）:**

- Modify（修改）: `Source/BDP/Core/AttackExecution/RangedProtocol/Model/ProjectileInitPlan.cs`
- Modify（修改）: `Source/BDP/Core/Projectiles/RangedFlightProtocol/Arrival/ArrivalStageContext.cs`
- Modify（修改）: `Source/BDP.Tests/ProjectileAccuracySnapshotSmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/RangedModulePrivateContextSaveLoadSmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/RangedProjectilePlanDiagnosticsSmokeTests.ps1`

**Step 1: 扩展失败检查**

加入断言：

```powershell
Assert-True (
    ($planText -match 'public\s+ProjectileAccuracySnapshot\s+AccuracySnapshot') -and
    ($planText -match 'Scribe_Deep\.Look\(ref accuracySnapshot,\s*"accuracySnapshot"\)')
) 'ProjectileInitPlan must persist the per-projectile accuracy snapshot.'

Assert-True (
    $arrivalContextText -match 'public\s+ProjectileAccuracySnapshot\s+AccuracySnapshot\s*\{\s*get;\s*\}'
) 'ArrivalStageContext must expose the frozen accuracy snapshot read-only.'
```

**Step 2: 运行并确认失败**

```powershell
& '.\Source\BDP.Tests\ProjectileAccuracySnapshotSmokeTests.ps1'
```

Expected（预期）: FAIL（失败），提示计划尚未携带快照。

**Step 3: 给计划增加深存档字段**

在 `ProjectileInitPlan（投射物初始化计划）`加入逐成员中文注释：

```csharp
/// <summary>
/// 当前投射物在正式开火时冻结的原版精度事实。
/// 后半段只消费这份快照，不回头读取实时射手或天气。
/// </summary>
public ProjectileAccuracySnapshot AccuracySnapshot { get; set; }
```

在 `ExposeData（存档读写）`的局部变量、`Scribe_Deep.Look（深度存档）`和回写区分别加入：

```csharp
ProjectileAccuracySnapshot accuracySnapshot = AccuracySnapshot;
Scribe_Deep.Look(ref accuracySnapshot, "accuracySnapshot");
AccuracySnapshot = accuracySnapshot;
```

不为旧存档补造可用快照；`null`表示缺失事实，由 Content（内容层）回退。

**Step 4: 到达上下文只读暴露**

构造函数加入：

```csharp
AccuracySnapshot = initPlan != null ? initPlan.AccuracySnapshot : null;
```

公开成员加入：

```csharp
/// <summary>当前投射物在正式开火时冻结的原版精度事实。</summary>
public ProjectileAccuracySnapshot AccuracySnapshot { get; }
```

不得公开整个内部 `ProjectileInitPlan（投射物初始化计划）`。

**Step 5: 运行专项检查、存档回归和 Core 构建**

```powershell
& '.\Source\BDP.Tests\ProjectileAccuracySnapshotSmokeTests.ps1'
& '.\Source\BDP.Tests\RangedModulePrivateContextSaveLoadSmokeTests.ps1'
& '.\Source\BDP.Tests\RangedProjectilePlanDiagnosticsSmokeTests.ps1'
dotnet build '.\Source\BDP\BDP.csproj' -c Release
```

Expected（预期）: 全部 PASS（通过），Core 构建零错误。

**Step 6: 提交**

```powershell
git add -- 'Source/BDP/Core/AttackExecution/RangedProtocol/Model/ProjectileInitPlan.cs' 'Source/BDP/Core/Projectiles/RangedFlightProtocol/Arrival/ArrivalStageContext.cs' 'Source/BDP.Tests/ProjectileAccuracySnapshotSmokeTests.ps1'
git commit -m 'feat: 随投射物计划传递原版精度事实'
```

### Task 3: 在现有原版射击报告生成点冻结每发精度事实

**Files（文件）:**

- Modify（修改）: `Source/BDP/Core/Verbs/BdpVerb_Shoot.cs`
- Modify（修改）: `Source/BDP.Tests/ProjectileAccuracySnapshotSmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/RangedSemanticTargetPrioritySmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/RangedTargetSemanticsCarrySmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/RangedWarmupFireTimeRefreshSmokeTests.ps1`

**Step 1: 写失败检查锁定唯一写入点**

```powershell
Assert-True (
    ($verbText -match 'ShotReport\s+shotReport') -and
    ($verbText -match 'CaptureAccuracySnapshot\(plan,\s*shotReport,\s*accuracyFactor,\s*forcedMissRadius\)') -and
    ($verbText -match 'private\s+static\s+void\s+CaptureAccuracySnapshot')
) 'BdpVerb_Shoot must capture accuracy facts immediately after the original ShotReport.'

Assert-True (
    ([regex]::Matches($verbText, 'AccuracySnapshot\s*=')).Count -eq 1
) 'The formal ranged host must have one accuracy-snapshot assignment point.'
```

**Step 2: 运行并确认失败**

```powershell
& '.\Source\BDP.Tests\ProjectileAccuracySnapshotSmokeTests.ps1'
```

Expected（预期）: FAIL（失败），提示缺少正式写入点。

**Step 3: 在报告生成后、随机命中分支前写入**

保持当前精度临时注入与恢复顺序不变。在 `shotReport（射击报告）`生成并完成 `finally（最终恢复）`后、读取调整概率前调用：

```csharp
CaptureAccuracySnapshot(plan, shotReport, accuracyFactor, forcedMissRadius);
```

新增中性辅助成员：

```csharp
/// <summary>
/// 把本发原版射击报告的公开精度事实冻结到正式投射物计划。
/// 不保存随机命中分支，也不改变后续原版命中裁定。
/// </summary>
private static void CaptureAccuracySnapshot(
    ProjectileInitPlan plan,
    ShotReport shotReport,
    float accuracyFactor,
    float forcedMissRadius)
{
    if (plan == null) return;

    float safeAccuracyFactor = accuracyFactor > 0f ? accuracyFactor : 1f;
    plan.AccuracySnapshot = new ProjectileAccuracySnapshot
    {
        IsAvailable = true,
        StandardAimChance = Mathf.Clamp01(
            shotReport.AimOnTargetChance_StandardTarget * safeAccuracyFactor),
        IgnoringPostureAimChance = Mathf.Clamp01(
            shotReport.AimOnTargetChance_IgnoringPosture * safeAccuracyFactor),
        PassCoverChance = Mathf.Clamp01(shotReport.PassCoverChance),
        ForcedMissRadius = Mathf.Max(0f, forcedMissRadius),
        AccuracyFactor = safeAccuracyFactor
    };
}
```

不要移动、复制或改写以下原有分支：

- forced miss（强制失准）；
- wild miss（野射失手）；
- cover intercept（掩体拦截）；
- intended target（预定目标）。

毒蛇当前强制失准半径为 0；走强制失准提前返回的其它武器允许没有快照，后半段采用设计规定的 0.5 中性回退。

**Step 4: 运行 Core 专项检查和构建**

```powershell
& '.\Source\BDP.Tests\ProjectileAccuracySnapshotSmokeTests.ps1'
& '.\Source\BDP.Tests\RangedSemanticTargetPrioritySmokeTests.ps1'
& '.\Source\BDP.Tests\RangedTargetSemanticsCarrySmokeTests.ps1'
& '.\Source\BDP.Tests\RangedWarmupFireTimeRefreshSmokeTests.ps1'
dotnet build '.\Source\BDP\BDP.csproj' -c Release
```

Expected（预期）: 全部 PASS（通过）；没有新增编译警告或错误。

**Step 5: 提交**

```powershell
git add -- 'Source/BDP/Core/Verbs/BdpVerb_Shoot.cs' 'Source/BDP.Tests/ProjectileAccuracySnapshotSmokeTests.ps1'
git commit -m 'feat: 冻结每发原版射击精度事实'
```

### Task 4: 用原版精度驱动配置替换旧技能散布配置

**Files（文件）:**

- Modify（修改）: `Source/BDP.Content/RangedModules/RoutePath/RoutePathConfig.cs`
- Modify（修改）: `Source/BDP.Content/RangedModules/RoutePath/RoutePathState.cs`
- Modify（修改）: `Source/BDP.Content/RangedModules/RoutePath/RouteSegmentResolver.cs`
- Modify（修改）: `Source/BDP.Content/RangedModules/RoutePath/RoutePathModule.cs`
- Modify（修改）: `1.6/Content/Defs/RangedModuleDef/RoutePath.xml`
- Create（新建）: `Source/BDP.Tests/RoutePathOrderedSpreadSmokeTests.ps1`

**Step 1: 写失败检查锁定新配置与旧字段退役**

检查主模组正式 Content（内容层），不要再把 DevHarness（开发测试模组）的旧 `PathLatch（路径锁存样本）`当作实现真值：

```powershell
Assert-True (
    ($configText -match 'public\s+float\s+IntermediateSpreadRadius\s*=\s*1\.25f') -and
    ($configText -match 'public\s+float\s+FinalSpreadRadius\s*=\s*0\.30f') -and
    ($configText -match 'public\s+float\s+HighAccuracySpreadScale\s*=\s*0\.25f') -and
    ($configText -match 'public\s+int\s+SpreadSafetyShrinkSteps\s*=\s*4')
) 'RoutePathConfig must expose ordered-spread parameters.'

Assert-True (
    ($configText -notmatch 'LowSkillSpreadMultiplier|HighSkillSpreadMultiplier|AnchorSpreadRadius') -and
    ($moduleText -notmatch 'SkillDefOf\.Shooting|ResolveSkillSpreadMultiplier')
) 'RoutePath must not calculate Shooting skill a second time.'

Assert-True (
    ($xmlText -match '<IntermediateSpreadRadius>1\.25</IntermediateSpreadRadius>') -and
    ($xmlText -match '<FinalSpreadRadius>0\.30</FinalSpreadRadius>') -and
    ($xmlText -match '<HighAccuracySpreadScale>0\.25</HighAccuracySpreadScale>') -and
    ($xmlText -match '<SpreadSafetyShrinkSteps>4</SpreadSafetyShrinkSteps>')
) 'RoutePath Def must declare ordered-spread defaults.'
```

**Step 2: 运行并确认失败**

```powershell
& '.\Source\BDP.Tests\RoutePathOrderedSpreadSmokeTests.ps1'
```

Expected（预期）: FAIL（失败），提示仍是旧技能散布字段。

**Step 3: 修改配置并完整克隆新字段**

用设计中的四个字段替换旧三个字段；`CloneTyped（强类型复制）`逐字段复制：

```csharp
IntermediateSpreadRadius = IntermediateSpreadRadius,
FinalSpreadRadius = FinalSpreadRadius,
HighAccuracySpreadScale = HighAccuracySpreadScale,
SpreadSafetyShrinkSteps = SpreadSafetyShrinkSteps
```

保留 `ArrivalTolerance（到达容差） = 0.35f`，本任务不改其它自动路线配置。

**Step 4: 修改投射物路径上下文**

在 `RoutePathContext（路线飞行上下文）`保存：

```csharp
public float IntermediateSpreadRadius { get; set; }
public float FinalSpreadRadius { get; set; }
public float HighAccuracySpreadScale { get; set; } = 0.25f;
public int SpreadSafetyShrinkSteps { get; set; } = 4;
```

同步更新：

- `Reset（重置）`；
- `CloneTyped（强类型复制）`；
- `ExposeData（存档读写）`。

不读取旧存档字段，不保留旧属性别名。

**Step 5: 修改快照填充入口**

把 `PopulatePathSnapshot（填充路径快照）`参数收敛为新配置值：

```csharp
public static void PopulatePathSnapshot(
    RoutePathContext target,
    RouteConfirmedSnapshot snapshot,
    float arrivalTolerance,
    float intermediateSpreadRadius,
    float finalSpreadRadius,
    float highAccuracySpreadScale,
    int spreadSafetyShrinkSteps)
```

所有值在写入时做最小边界收束：半径不小于 0，比例限制 0～1，收缩次数限制 0～8。

**Step 6: 修改模块初始化调用和 XML**

`IProjectileInitStageModule.Contribute（投射物初始化阶段贡献）`只冻结配置，不读取 Pawn（小人）射击技能。删除：

- `ResolveEffectiveAnchorSpreadRadius（解析有效锚点散布）`；
- `ResolveSkillSpreadMultiplier（解析技能散布倍率）`；
- 两个高低技能倍率解析成员。

XML 按设计值替换旧三项，每条写中文注释。

**Step 7: 运行专项检查与 Content 构建**

```powershell
& '.\Source\BDP.Tests\RoutePathOrderedSpreadSmokeTests.ps1'
dotnet build '.\Source\BDP.Content\BDP.Content.csproj' -c Release
```

Expected（预期）: 专项检查 PASS（通过），Content 构建零错误。

**Step 8: 提交**

```powershell
git add -- 'Source/BDP.Content/RangedModules/RoutePath/RoutePathConfig.cs' 'Source/BDP.Content/RangedModules/RoutePath/RoutePathState.cs' 'Source/BDP.Content/RangedModules/RoutePath/RouteSegmentResolver.cs' 'Source/BDP.Content/RangedModules/RoutePath/RoutePathModule.cs' '1.6/Content/Defs/RangedModuleDef/RoutePath.xml' 'Source/BDP.Tests/RoutePathOrderedSpreadSmokeTests.ps1'
git commit -m 'refactor: 毒蛇散布改读原版精度配置'
```

### Task 5: 实现稳定的中间偏航与末段独立收束

**Files（文件）:**

- Modify（修改）: `Source/BDP.Content/RangedModules/RoutePath/RouteSegmentResolver.cs`
- Modify（修改）: `Source/BDP.Tests/RoutePathOrderedSpreadSmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/RangedFlightPathSnapshotHostSmokeTests.ps1`

**Step 1: 扩展失败检查**

锁定以下事实：

```powershell
Assert-True (
    ($resolverText -match 'ProjectileAccuracySnapshot') -and
    ($resolverText -match 'ResolveAccuracyQuality') -and
    ($resolverText -match 'IntermediateSpreadRadius') -and
    ($resolverText -match 'FinalSpreadRadius') -and
    ($resolverText -match 'HighAccuracySpreadScale') -and
    ($resolverText -match 'SpreadSafetyShrinkSteps') -and
    ($resolverText -match 'GenSight\.LineOfSight') -and
    ($resolverText -match 'CanBeSeenOverFast') -and
    ($resolverText -match 'offset\s*\*=\s*0\.5f')
) 'Route resolver must build stable accuracy-driven safe spread.'

Assert-True (
    ($resolverText -match 'SampleKind') -and
    ($resolverText -match 'Gen\.HashCombineInt') -and
    ($resolverText -match 'Rand\.PushState') -and
    ($resolverText -match 'Rand\.PopState')
) 'Intermediate and final samples must use distinct stable seeds.'
```

**Step 2: 运行并确认失败**

```powershell
& '.\Source\BDP.Tests\RoutePathOrderedSpreadSmokeTests.ps1'
```

Expected（预期）: FAIL（失败），提示尚未消费精度快照。

**Step 3: 实现精度质量与段半径**

```csharp
private static float ResolveAccuracyQuality(ProjectileAccuracySnapshot snapshot)
{
    return snapshot != null && snapshot.IsAvailable
        ? Mathf.Clamp01(snapshot.StandardAimChance)
        : 0.5f;
}

private static float ResolveSegmentSpreadRadius(
    RoutePathContext snapshot,
    ProjectileAccuracySnapshot accuracySnapshot,
    bool isFinal)
{
    if (snapshot == null) return 0f;
    float quality = ResolveAccuracyQuality(accuracySnapshot);
    float highAccuracyScale = Mathf.Clamp01(snapshot.HighAccuracySpreadScale);
    float spreadScale = Mathf.Lerp(1f, highAccuracyScale, quality);
    float baseRadius = isFinal
        ? snapshot.FinalSpreadRadius
        : snapshot.IntermediateSpreadRadius;
    return Mathf.Max(0f, baseRadius * spreadScale);
}
```

不要再次读取 Pawn（小人）、天气、武器或技能；这些事实已经包含在 `StandardAimChance（标准目标瞄准概率）`。

**Step 4: 实现中间/最终独立稳定采样**

增加私有枚举：

```csharp
private enum SampleKind
{
    Intermediate = 1,
    Final = 2
}
```

种子在现有攻击实例、结果、轮内发射序号、段序号后，再混入 `SampleKind（样本类型）`。删除旧的 0.45 格硬限制；新半径由 XML（可扩展标记语言）明确控制。

**Step 5: 实现偏移折半与双向 LOS 检查**

核心入口采用：

```csharp
private static bool TryResolveSafeSpreadDestination(
    Map map,
    Vector3 start,
    Vector3 nominalDestination,
    LocalTargetInfo followingNominalTarget,
    bool isFinal,
    Vector3 sampledOffset,
    int shrinkSteps,
    out Vector3 destination)
```

循环最多 `shrinkSteps + 1` 次：

```csharp
Vector3 offset = sampledOffset.Yto0();
for (int attempt = 0; attempt <= shrinkSteps; attempt++)
{
    Vector3 candidate = (nominalDestination + offset).Yto0();
    if (IsSafeSpreadDestination(
            map, start, candidate, followingNominalTarget, isFinal))
    {
        destination = candidate;
        return true;
    }

    offset *= 0.5f;
}

destination = nominalDestination.Yto0();
return IsSafeSpreadDestination(
    map, start, destination, followingNominalTarget, isFinal);
```

`IsSafeSpreadDestination（判断安全偏航目的地）`必须检查：地图、边界、`CanBeSeenOverFast（可被看过）`、实际起点到候选的 LOS（直线视野），以及非最终段候选到下一名义目标的 LOS。

**Step 6: 让续段快照入口接收地图和精度事实**

将 `BuildCurrentLegFlightPathSnapshot（构造当前段飞行快照）`扩展为接收：

- `Map（地图）`；
- `ProjectileAccuracySnapshot（投射物精度快照）`；
- 当前实际起点；
- 当前段后的下一名义目标。

首段仍不调用该入口。最终段以 `FrozenFinalDestination（冻结最终落点）`为名义中心。

**Step 7: 运行专项检查与构建**

```powershell
& '.\Source\BDP.Tests\RoutePathOrderedSpreadSmokeTests.ps1'
& '.\Source\BDP.Tests\RangedFlightPathSnapshotHostSmokeTests.ps1'
dotnet build '.\Source\BDP.Content\BDP.Content.csproj' -c Release
```

Expected（预期）: 全部 PASS（通过），Content 构建零错误。

**Step 8: 提交**

```powershell
git add -- 'Source/BDP.Content/RangedModules/RoutePath/RouteSegmentResolver.cs' 'Source/BDP.Tests/RoutePathOrderedSpreadSmokeTests.ps1'
git commit -m 'feat: 增加毒蛇安全偏航与末段收束'
```

### Task 6: 接入到达阶段并补齐失准后的路线回接

**Files（文件）:**

- Modify（修改）: `Source/BDP.Content/RangedModules/RoutePath/RouteSegmentResolver.cs`
- Modify（修改）: `Source/BDP.Content/RangedModules/RoutePath/RoutePathModule.cs`
- Modify（修改）: `Source/BDP.Tests/RoutePathOrderedSpreadSmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/ProjectileContinuationAdmissionBoundarySmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/ProjectileContinuationObjectiveBlockingSmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/RoundEmitSequenceLaneAlternationSmokeTests.ps1`

**Step 1: 写失败检查锁定“不推进的回接段”**

```powershell
Assert-True (
    ($resolverText -match 'TryResolveContinuation') -and
    ($resolverText -match 'out\s+bool\s+advanceLeg') -and
    ($resolverText -match 'ArrivalTolerance') -and
    ($resolverText -match 'CurrentLegIndex')
) 'Route resolver must distinguish normal advancement from recovery continuation.'

Assert-True (
    ($moduleText -match 'context\.AccuracySnapshot') -and
    ($moduleText -match 'if\s*\(advanceLeg\)') -and
    ($moduleText -match 'TryAdvanceLeg')
) 'Arrival must consume accuracy facts and advance only for a normal next leg.'
```

**Step 2: 运行并确认失败**

```powershell
& '.\Source\BDP.Tests\RoutePathOrderedSpreadSmokeTests.ps1'
```

Expected（预期）: FAIL（失败），提示尚无回接裁定。

**Step 3: 增加纯解析入口**

在 `RouteSegmentResolver（路线段解析器）`增加：

```csharp
public static bool TryResolveContinuation(
    RoutePathContext snapshot,
    Map map,
    Vector3 start,
    ProjectileAccuracySnapshot accuracySnapshot,
    string attackInstanceId,
    string resultId,
    int emitSequence,
    out bool advanceLeg,
    out LocalTargetInfo nextTarget,
    out ProjectileFlightPathSnapshot nextPath)
```

规则顺序固定：

1. 没有下一名义段：返回 false。
2. 尝试为下一名义段生成安全偏航；成功则 `advanceLeg = true`。
3. 安全偏航和零偏移下一段都失败，且实际位置距当前名义段目标大于 `ArrivalTolerance（到达容差）`：生成“实际位置 → 当前名义目标”的无散布回接段，`advanceLeg = false`。
4. 已经处于当前名义目标容差内：`advanceLeg = true`，提交无散布下一名义段，让统一投射物宿主对真实新增阻挡执行现有裁短和撞击；不得循环回接。

距离统一在 Y=0（水平面）比较：

```csharp
private static bool IsWithinArrivalTolerance(
    Vector3 current,
    Vector3 nominal,
    float tolerance)
{
    float safeTolerance = tolerance > 0f ? tolerance : 0.35f;
    return (current.Yto0() - nominal.Yto0()).sqrMagnitude
        <= safeTolerance * safeTolerance;
}
```

回接段不增加随机偏移、不改变当前段序号、不改写语义最终目标。

**Step 4: 到达模块改为消费一次解析结论**

替换当前“先解析下一段、立即推进、再构造快照”的顺序：

```csharp
if (!RouteSegmentResolver.TryResolveContinuation(
        state.PathSnapshot,
        context.Map,
        context.Projectile.ExactPosition,
        context.AccuracySnapshot,
        context.AttackInstanceId,
        context.ResultId,
        context.EmitIndex,
        out bool advanceLeg,
        out LocalTargetInfo nextTarget,
        out ProjectileFlightPathSnapshot nextSnapshot))
{
    return;
}

if (advanceLeg && !RouteSegmentResolver.TryAdvanceLeg(state.PathSnapshot))
{
    return;
}
```

然后继续写入现有：

- `OverrideContinueFlight = true（继续飞行）`；
- `NextDestination（下一目的地）`；
- `NextTarget（下一目标）`；
- `NextFlightPathSnapshot（下一飞行快照）`。

不得添加停止请求，不得改 `ProjectileHitFlags（投射物命中类别）`。

**Step 5: 运行续段、阻挡和轮内序号回归**

```powershell
& '.\Source\BDP.Tests\RoutePathOrderedSpreadSmokeTests.ps1'
& '.\Source\BDP.Tests\ProjectileContinuationAdmissionBoundarySmokeTests.ps1'
& '.\Source\BDP.Tests\ProjectileContinuationObjectiveBlockingSmokeTests.ps1'
& '.\Source\BDP.Tests\RoundEmitSequenceLaneAlternationSmokeTests.ps1'
dotnet build '.\Source\BDP.Content\BDP.Content.csproj' -c Release
```

Expected（预期）: 全部 PASS（通过）；真实阻挡扫描路径仍存在；轮内序号仍使用 `EmitSequence（轮内发射序号）`。

**Step 6: 提交**

```powershell
git add -- 'Source/BDP.Content/RangedModules/RoutePath/RouteSegmentResolver.cs' 'Source/BDP.Content/RangedModules/RoutePath/RoutePathModule.cs' 'Source/BDP.Tests/RoutePathOrderedSpreadSmokeTests.ps1'
git commit -m 'fix: 毒蛇失准后安全回接名义路线'
```

### Task 7: 补齐边界门禁与主模组回归

**Files（文件）:**

- Modify（修改）: `Source/BDP.Tests/ProjectileAccuracySnapshotSmokeTests.ps1`
- Modify（修改）: `Source/BDP.Tests/RoutePathOrderedSpreadSmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/RangedModuleUnifiedProjectileSmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/RangedModulePrivateContextProjectileCarrySmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/RangedTargetSemanticsCarrySmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/RangedContinuationCompleteSnapshotPrioritySmokeTests.ps1`
- Test（测试）: `Source/BDP.Tests/ProjectileCollisionBoundaryDiagnosticsSmokeTests.ps1`
- Test（测试）: `Source/Build/Verify-BdpCoreIsolation.ps1`

**Step 1: 增加负面边界断言**

专项检查必须额外禁止：

```powershell
Assert-True (
    ($contentText -notmatch 'ShotReport\.HitReportFor') -and
    ($routeModuleText -notmatch 'SkillDefOf\.Shooting') -and
    ($routeModuleText -notmatch 'ProjectileHitFlags')
) 'Content route business must consume frozen facts without rerunning or rewriting original hit logic.'

Assert-True (
    ($coreSnapshotText -notmatch 'BDP\.Content') -and
    ($coreSnapshotText -notmatch 'Viper|RoutePath|Anchor|Convergence')
) 'Core accuracy facts must stay assembly-neutral.'
```

**Step 2: 运行两个新专项检查**

```powershell
& '.\Source\BDP.Tests\ProjectileAccuracySnapshotSmokeTests.ps1'
& '.\Source\BDP.Tests\RoutePathOrderedSpreadSmokeTests.ps1'
```

Expected（预期）: PASS（通过）。

**Step 3: 运行重点回归**

```powershell
& '.\Source\BDP.Tests\RangedModuleUnifiedProjectileSmokeTests.ps1'
& '.\Source\BDP.Tests\RangedModulePrivateContextProjectileCarrySmokeTests.ps1'
& '.\Source\BDP.Tests\RangedTargetSemanticsCarrySmokeTests.ps1'
& '.\Source\BDP.Tests\RangedContinuationCompleteSnapshotPrioritySmokeTests.ps1'
& '.\Source\BDP.Tests\ProjectileCollisionBoundaryDiagnosticsSmokeTests.ps1'
& '.\Source\BDP.Tests\ProjectileContinuationObjectiveBlockingSmokeTests.ps1'
& '.\Source\BDP.Tests\RoundEmitSequenceLaneAlternationSmokeTests.ps1'
```

Expected（预期）: 全部 PASS（通过）。

**Step 4: 构建 Core 与 Content**

```powershell
dotnet build '.\Source\BDP\BDP.csproj' -c Release
dotnet build '.\Source\BDP.Content\BDP.Content.csproj' -c Release
```

Expected（预期）: 两个工程零错误；Core 构建后的隔离门禁自动执行并通过。

**Step 5: 运行 XML 解析检查**

```powershell
$files = Get-ChildItem -LiteralPath '.\1.6' -Recurse -Filter '*.xml' -File
foreach ($file in $files) {
    [xml](Get-Content -LiteralPath $file.FullName -Raw -Encoding utf8) | Out-Null
}
'XML parse PASS（XML 解析通过）'
```

**Step 6: 运行全量 PowerShell 专项检查**

```powershell
$failed = New-Object System.Collections.Generic.List[string]
Get-ChildItem -LiteralPath '.\Source\BDP.Tests' -Filter '*.ps1' -File |
    Sort-Object Name |
    ForEach-Object {
        try {
            & $_.FullName
        }
        catch {
            $failed.Add($_.Name)
            Write-Error $_
        }
    }

if ($failed.Count -gt 0) {
    throw ('Failed smoke tests（失败专项检查）: ' + ($failed -join ', '))
}
```

Expected（预期）: 无新增失败。若存在实施前已经稳定失败的旧 DevHarness（开发测试模组）样本检查，先用 `git show（查看提交）`和实施前基线证明，再单独报告；不得为通过本事项而顺手修改测试模组业务。

**Step 7: 提交测试收口**

只有本任务确实修改了专项检查时提交：

```powershell
git add -- 'Source/BDP.Tests/ProjectileAccuracySnapshotSmokeTests.ps1' 'Source/BDP.Tests/RoutePathOrderedSpreadSmokeTests.ps1'
git commit -m 'test: 锁定毒蛇原版精度收束边界'
```

### Task 8: 游戏内实测、临时诊断与最终收口

**Files（文件）:**

- Modify if needed（仅必要时修改）: `Source/BDP.Content/RangedModules/RoutePath/RoutePathModule.cs`
- Modify if needed（仅必要时修改）: `Source/BDP.Content/RangedModules/RoutePath/RouteSegmentResolver.cs`
- Modify if needed（仅必要时修改）: `1.6/Content/Defs/RangedModuleDef/RoutePath.xml`
- Create（新建）: `docs/04-架构评估/2026-08-09/2026-08-09-毒蛇原版精度驱动分段收束完成报告.md`
- Modify（修改）: `C:/NiwtDatas/Projects/RimworldModStudio/日志/Agent工作日志/当前未满20条的最新日志文件.md`

**Step 1: 建立游戏内对照场景**

最少准备：

- 射击技能 0、10、20 的三名 Pawn（小人）。
- 晴天和一种精度倍率明显较低的恶劣天气。
- 单锚点、多锚点、自动单侧、自动双侧四种路线。
- 靠近墙角的锚点。
- 最终目标为 Pawn（小人）和地面格两类。

**Step 2: 执行设计文档 13.3 的 13 项实测**

重点记录：

- 中间偏航是否随原版精度事实变化。
- 最终段是否明显收束但仍可失手。
- 首段野射无法连下一段时是否先回接名义锚点。
- 已到名义锚点后新增墙是否正常拦截且不无限回接。
- 自动双侧四发是否继续左/右/左/右。
- 存读档后同一发同一段偏移是否一致。

**Step 3: 仅在无法从画面判断时增加临时诊断**

诊断前缀固定：

```text
[BDP.Content.RoutePath.OrderedSpread]
```

每次只记录段边界事件，不逐 Tick（游戏刻）输出：

```text
attack / result / emitSequence / leg
accuracyQuality
nominalDestination / sampledDestination / finalDestination
shrinkAttempts
advanceLeg / recovery
```

复现和修正完成后删除临时诊断及其开关；最终代码不保留常驻业务日志。

**Step 4: 只调 XML 数值完成首轮平衡**

若机制正确但表现过弱或过强，优先只调整：

- `IntermediateSpreadRadius（中间段散布半径）`；
- `FinalSpreadRadius（最终段散布半径）`；
- `HighAccuracySpreadScale（高精度散布比例）`。

不得用额外技能倍率、天气倍率或段序号补丁覆盖原版精度快照。

**Step 5: 再跑 Task 7 全部自动验证**

Expected（预期）: 两个工程构建通过、XML 解析通过、专项检查无新增失败、Core 隔离门禁通过。

**Step 6: 写完成报告与工作日志**

完成报告记录：

- 最终实现与设计是否一致。
- 三个最终 XML 标定值。
- 自动验证结果。
- 13 项游戏内实测结果。
- 是否使用过临时诊断以及是否已删除。
- 已知但不属于本事项的支线问题。

工作日志按时间倒序写入最新且未满 20 条的日志文件；若已满 20 条则新建下一个编号文件。

**Step 7: 最终提交**

```powershell
git add -- 'Source/BDP.Content/RangedModules/RoutePath/RoutePathModule.cs' 'Source/BDP.Content/RangedModules/RoutePath/RouteSegmentResolver.cs' '1.6/Content/Defs/RangedModuleDef/RoutePath.xml' 'docs/04-架构评估/2026-08-09/2026-08-09-毒蛇原版精度驱动分段收束完成报告.md' '../../日志/Agent工作日志'
git commit -m 'feat: 完成毒蛇原版精度驱动分段收束'
```

提交前用 `git diff --cached --name-only（查看暂存文件）`确认没有计划外文件；未产生变化的路径不要强行加入。

## 最终完成门槛

- 两个新专项检查通过。
- Core 与 Content Release（发布）构建通过。
- Core 零本模组程序集依赖门禁通过。
- XML 全量解析通过。
- 重点回归与全量 PowerShell 专项检查无新增失败。
- 设计文档 13.3 的 13 项游戏内实测全部完成。
- 首段保持现有原版发射链；续段不重跑 `ShotReport（射击报告）`。
- 中间偏航不会因随机结果主动断链；真实新增阻挡仍可拦截。
- 最终段收束但不覆盖原版命中资格。
- 不存在遗留临时诊断、试验性代码或旧技能双重计算。
- 完成报告、工作日志和 Git（版本控制）提交齐全。
