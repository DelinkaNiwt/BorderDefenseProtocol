# 紧急脱离信标锚点 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 新增“紧急脱离信标”建筑，让战斗体紧急脱离优先传送到通电信标附近，并保留旧 BDP 的安全回退策略。

**Architecture:** 主模组新增一个轻量建筑服务点，建筑只暴露“是否可作为锚点”和未来 Trion（触力）接口。紧急脱离落点仍由 `CombatBodyEmergencyEscapeRouter` 统一解析，执行、芯片消费和崩解收尾不迁移到建筑上。

**Tech Stack:** RimWorld 1.6、Verse、RimWorld、C# 7.3、XML Def、PowerShell smoke tests。

**Project Constraint:** 直接在当前工程执行，不创建 worktree（工作树），不新建 git 分支；主模组只做中性基础设施；C# 至少逐成员注释，XML 至少逐条目注释。

---

## Task 1: 写路由与建筑烟测

**Files:**
- Create: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1`
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1`

**Step 1: 创建失败烟测**

新增测试文件，读取以下目标：

```powershell
$repoRoot = Split-Path -Parent (Split-Path -Parent $PSScriptRoot)
$routerPath = Join-Path $repoRoot 'Source\BDP\Core\CombatBody\Escape\CombatBodyEmergencyEscapeRouter.cs'
$buildingPath = Join-Path $repoRoot 'Source\BDP\Core\CombatBody\Escape\Building_EmergencyEscapeBeacon.cs'
$placeWorkerPath = Join-Path $repoRoot 'Source\BDP\Core\Buildings\PlaceWorker_OnlyOnePerMap.cs'
$trionSurfacePath = Join-Path $repoRoot 'Source\BDP\Core\Trion\TrionSurfaceAccess.cs'
$xmlPath = Join-Path $repoRoot '1.6\Defs\Buildings\CombatBody\ThingDefs_EmergencyEscapeBeacon.xml'
```

核心断言：

```powershell
Assert-True (Test-Path -LiteralPath $buildingPath) 'Building_EmergencyEscapeBeacon must exist.'
Assert-True (Test-Path -LiteralPath $placeWorkerPath) 'PlaceWorker_OnlyOnePerMap must exist.'
Assert-True (Test-Path -LiteralPath $xmlPath) 'Emergency escape beacon XML must exist.'

Assert-True (
    ($xmlText -match '<defName>BDP_EmergencyEscapeBeacon</defName>') -and
    ($xmlText -match '<thingClass>BDP\.Core\.CombatBody\.Building_EmergencyEscapeBeacon</thingClass>') -and
    ($xmlText -match '<texPath>Things/Building/Misc/DropBeacon</texPath>') -and
    ($xmlText -match 'CompProperties_Power') -and
    ($xmlText -match 'CompPowerTrader') -and
    ($xmlText -match 'CompProperties_Flickable') -and
    ($xmlText -match 'BDP\.Core\.Trion\.CompProperties_Trion') -and
    ($xmlText -match 'BDP\.Core\.Buildings\.PlaceWorker_OnlyOnePerMap')
) 'Emergency escape beacon XML must define powered orbital-beacon-textured Trion-ready building.'

Assert-True (
    ($buildingText -match 'class\s+Building_EmergencyEscapeBeacon\s*:\s*Building') -and
    ($buildingText -match 'public\s+bool\s+IsActiveAnchor') -and
    ($buildingText -match 'CompPowerTrader') -and
    ($buildingText -match 'TrionReader') -and
    ($buildingText -match 'TrionCommands')
) 'Emergency escape beacon building must expose active anchor and Trion surfaces.'

Assert-True (
    ($routerText -match 'TryFindBeaconDestination') -and
    ($routerText -match 'TryFindColonistAreaDestination') -and
    ($routerText -match 'TryFindLocalSafeDestination') -and
    ($routerText -match 'TryFindMapSafeDestination') -and
    ($routerText -match 'AllBuildingsColonistOfClass<Building_EmergencyEscapeBeacon>')
) 'Emergency escape router must preserve beacon/colonist/local/map fallback order.'

Assert-True (
    ($trionSurfaceText -match 'ResolveReader\(ThingWithComps thing\)') -and
    ($trionSurfaceText -match 'ResolveCommands\(ThingWithComps thing\)') -and
    ($trionSurfaceText -match 'ResolveEvents\(ThingWithComps thing\)')
) 'TrionSurfaceAccess must expose non-Pawn ThingWithComps surfaces for future building integration.'

Assert-True (
    ($routerText -notmatch 'ChipSurfaceAccess|SupportsEmergencyEscape|TryGetPassive|Resolve\(pawn\)')
) 'Router must not re-own emergency escape availability or chip parsing.'
```

**Step 2: 运行测试确认失败**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1"
```

Expected: FAIL，因为建筑、XML、路由扩展还不存在。

**Step 3: Commit**

```bash
git add "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1"
git commit -m "test: cover emergency escape beacon routing"
```

提交前只加入本测试文件，不混入无关脏改动。

---

## Task 2: 新增 Trion 通用 ThingWithComps 读取口

**Files:**
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Trion/TrionSurfaceAccess.cs`
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1`

**Step 1: 扩展 TrionSurfaceAccess**

在保留现有 `Pawn` 重载的前提下，新增 `ThingWithComps` 重载：

```csharp
/// <summary>
/// 读取任意 ThingWithComps 宿主上的 Trion 只读口。
/// 该入口服务建筑等非 Pawn 宿主，不承载任何战斗体业务。
/// </summary>
public static ITrionReader ResolveReader(ThingWithComps thing)
{
    CompTrion comp = ResolveComp(thing);
    return comp != null ? comp.Service : null;
}
```

同样新增：

```csharp
public static ITrionCommands ResolveCommands(ThingWithComps thing)
public static ITrionEvents ResolveEvents(ThingWithComps thing)
```

把私有帮助方法拆成：

```csharp
private static CompTrion ResolveComp(Pawn pawn)
{
    return pawn?.GetComp<CompTrion>();
}

private static CompTrion ResolveComp(ThingWithComps thing)
{
    return thing?.GetComp<CompTrion>();
}
```

**Step 2: 运行烟测确认仍失败在后续项**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1"
```

Expected: FAIL，但 Trion surface 相关断言应通过。

**Step 3: Commit**

```bash
git add "模组工程/BorderDefenseProtocol/Source/BDP/Core/Trion/TrionSurfaceAccess.cs" `
        "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1"
git commit -m "feat: expose Trion surfaces for thing comps"
```

---

## Task 3: 新增建筑类与单地图限制 PlaceWorker

**Files:**
- Create: `模组工程/BorderDefenseProtocol/Source/BDP/Core/CombatBody/Escape/Building_EmergencyEscapeBeacon.cs`
- Create: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Buildings/PlaceWorker_OnlyOnePerMap.cs`
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1`

**Step 1: 新增 Building_EmergencyEscapeBeacon**

Create:

```csharp
using BDP.Core.Trion;
using RimWorld;
using Verse;

namespace BDP.Core.CombatBody
{
    /// <summary>
    /// 紧急脱离信标建筑。
    /// 它只提供可用锚点和未来 Trion 接口，不持有紧急脱离流程真值。
    /// </summary>
    public sealed class Building_EmergencyEscapeBeacon : Building
    {
        /// <summary>
        /// 供电组件缓存。
        /// </summary>
        private CompPowerTrader cachedPowerComp;

        /// <summary>
        /// 当前建筑是否可作为紧急脱离锚点。
        /// </summary>
        public bool IsActiveAnchor
        {
            get
            {
                return Spawned
                    && !Destroyed
                    && Faction == Faction.OfPlayer
                    && IsPowered;
            }
        }

        /// <summary>
        /// 未来建筑 Trion 只读口。
        /// 当前版本只预留接口，不参与路由判断。
        /// </summary>
        public ITrionReader TrionReader
        {
            get { return TrionSurfaceAccess.ResolveReader((ThingWithComps)this); }
        }

        /// <summary>
        /// 未来建筑 Trion 命令口。
        /// 当前版本只预留接口，不参与路由判断。
        /// </summary>
        public ITrionCommands TrionCommands
        {
            get { return TrionSurfaceAccess.ResolveCommands((ThingWithComps)this); }
        }

        /// <summary>
        /// 当前供电是否满足启用要求。
        /// 没有供电组件时视为可用，便于 Def 调整时保持原版兼容。
        /// </summary>
        private bool IsPowered
        {
            get
            {
                CompPowerTrader power = PowerComp;
                return power == null || power.PowerOn;
            }
        }

        /// <summary>
        /// 读取供电组件。
        /// </summary>
        private CompPowerTrader PowerComp
        {
            get { return cachedPowerComp ?? (cachedPowerComp = this.TryGetComp<CompPowerTrader>()); }
        }
    }
}
```

**Step 2: 新增 PlaceWorker_OnlyOnePerMap**

Create:

```csharp
using Verse;

namespace BDP.Core.Buildings
{
    /// <summary>
    /// 限制同一地图内同 Def 建筑只能存在一座。
    /// 这是通用放置辅助，不持有具体业务流程。
    /// </summary>
    public sealed class PlaceWorker_OnlyOnePerMap : PlaceWorker
    {
        /// <summary>
        /// 判断当前位置是否允许放置。
        /// </summary>
        public override AcceptanceReport AllowsPlacing(
            BuildableDef checkingDef,
            IntVec3 loc,
            Rot4 rot,
            Map map,
            Thing thingToIgnore = null,
            Thing thing = null)
        {
            ThingDef thingDef = checkingDef as ThingDef;
            if (thingDef == null || map == null)
            {
                return AcceptanceReport.WasAccepted;
            }

            foreach (Building building in map.listerBuildings.AllBuildingsColonistOfDef(thingDef))
            {
                if (building != thingToIgnore)
                {
                    return new AcceptanceReport("地图内已存在一座" + checkingDef.label + "。");
                }
            }

            return AcceptanceReport.WasAccepted;
        }
    }
}
```

**Step 3: 运行烟测确认仍失败在 XML 或路由**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1"
```

Expected: FAIL，但建筑类和 PlaceWorker 断言应通过。

**Step 4: Commit**

```bash
git add "模组工程/BorderDefenseProtocol/Source/BDP/Core/CombatBody/Escape/Building_EmergencyEscapeBeacon.cs" `
        "模组工程/BorderDefenseProtocol/Source/BDP/Core/Buildings/PlaceWorker_OnlyOnePerMap.cs" `
        "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1"
git commit -m "feat: add emergency escape beacon building"
```

---

## Task 4: 新增建筑 XML

**Files:**
- Create: `模组工程/BorderDefenseProtocol/1.6/Defs/Buildings/CombatBody/ThingDefs_EmergencyEscapeBeacon.xml`
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1`

**Step 1: 创建 XML 文件**

Create:

```xml
<?xml version="1.0" encoding="utf-8"?>
<Defs>

  <!--
    CombatBody 紧急脱离信标建筑 Def。
    建筑只提供传送锚点服务，不持有紧急脱离流程真值。
  -->

  <!-- 紧急脱离信标。 -->
  <ThingDef ParentName="BuildingBase">
    <!-- Def 唯一名称。 -->
    <defName>BDP_EmergencyEscapeBeacon</defName>

    <!-- 游戏界面显示名称。 -->
    <label>紧急脱离信标</label>

    <!-- 玩家可见说明。 -->
    <description>战斗体紧急脱离的优先传送锚点。需要通电，同一地图只能建造一座。</description>

    <!-- 建筑分类。 -->
    <category>Building</category>

    <!-- 实例运行时类型。 -->
    <thingClass>BDP.Core.CombatBody.Building_EmergencyEscapeBeacon</thingClass>

    <!-- 允许玩家选中。 -->
    <selectable>true</selectable>

    <!-- 地图绘制类型。 -->
    <drawerType>MapMeshOnly</drawerType>

    <!-- 绘制高度层。 -->
    <altitudeLayer>Building</altitudeLayer>

    <!-- 可建造地形要求。 -->
    <terrainAffordanceNeeded>Light</terrainAffordanceNeeded>

    <!-- 受击默认音效。 -->
    <soundImpactDefault>BulletImpact_Metal</soundImpactDefault>

    <!-- 建筑修理效果。 -->
    <repairEffect>Repair</repairEffect>

    <!-- 被摧毁时留下资源。 -->
    <leaveResourcesWhenKilled>true</leaveResourcesWhenKilled>

    <!-- 被摧毁时留下瓦砾。 -->
    <filthLeaving>Filth_RubbleBuilding</filthLeaving>

    <!-- 基础属性。 -->
    <statBases>
      <!-- 售价折算。 -->
      <SellPriceFactor>0.70</SellPriceFactor>
      <!-- 最大耐久。 -->
      <MaxHitPoints>100</MaxHitPoints>
      <!-- 建造工时。 -->
      <WorkToBuild>2000</WorkToBuild>
      <!-- 质量。 -->
      <Mass>5</Mass>
      <!-- 可燃性。 -->
      <Flammability>0.5</Flammability>
    </statBases>

    <!-- 图像配置；复用原版轨道贸易信标贴图。 -->
    <graphicData>
      <!-- 世界与信息面板图标路径。 -->
      <texPath>Things/Building/Misc/DropBeacon</texPath>
      <!-- 单贴图建筑图形类。 -->
      <graphicClass>Graphic_Single</graphicClass>
      <!-- 绘制尺寸。 -->
      <drawSize>(1,1)</drawSize>
    </graphicData>

    <!-- 建筑占地。 -->
    <size>(1,1)</size>

    <!-- 是否可旋转。 -->
    <rotatable>false</rotatable>

    <!-- 通行性。 -->
    <passability>PassThroughOnly</passability>

    <!-- 填充比例。 -->
    <fillPercent>0.15</fillPercent>

    <!-- 路径代价。 -->
    <pathCost>14</pathCost>

    <!-- 禁止与区域重叠。 -->
    <canOverlapZones>false</canOverlapZones>

    <!-- 放置分类。 -->
    <designationCategory>Misc</designationCategory>

    <!-- 建造材料。 -->
    <costList>
      <!-- 钢铁。 -->
      <Steel>50</Steel>
      <!-- 工业零部件。 -->
      <ComponentIndustrial>2</ComponentIndustrial>
    </costList>

    <!-- 建造前置研究。 -->
    <researchPrerequisites>
      <!-- 电力研究。 -->
      <li>Electricity</li>
    </researchPrerequisites>

    <!-- ThingComp 列表。 -->
    <comps>
      <!-- 原版禁止组件。 -->
      <li Class="CompProperties_Forbiddable" />

      <!-- 原版用电组件。 -->
      <li Class="CompProperties_Power">
        <!-- 通电运行组件类型。 -->
        <compClass>CompPowerTrader</compClass>
        <!-- 基础耗电。 -->
        <basePowerConsumption>50</basePowerConsumption>
        <!-- 雨中短路。 -->
        <shortCircuitInRain>true</shortCircuitInRain>
      </li>

      <!-- 原版开关组件。 -->
      <li Class="CompProperties_Flickable" />

      <!-- BDP Trion 预留组件；当前不参与路由判断。 -->
      <li Class="BDP.Core.Trion.CompProperties_Trion">
        <!-- 非 Pawn 宿主基础最大容量；本轮只预留接口。 -->
        <baseMax>0</baseMax>
        <!-- 初始百分比。 -->
        <startPercent>1</startPercent>
        <!-- 每日恢复量；本轮不启用。 -->
        <recoveryPerDay>0</recoveryPerDay>
      </li>
    </comps>

    <!-- 放置辅助器。 -->
    <placeWorkers>
      <!-- 同一地图只允许一座该建筑。 -->
      <li>BDP.Core.Buildings.PlaceWorker_OnlyOnePerMap</li>
    </placeWorkers>
  </ThingDef>

</Defs>
```

**Step 2: 运行烟测确认仍失败在路由**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1"
```

Expected: FAIL，但 XML 断言应通过。

**Step 3: Commit**

```bash
git add "模组工程/BorderDefenseProtocol/1.6/Defs/Buildings/CombatBody/ThingDefs_EmergencyEscapeBeacon.xml" `
        "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1"
git commit -m "feat: define emergency escape beacon"
```

---

## Task 5: 扩展紧急脱离路由

**Files:**
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/CombatBody/Escape/CombatBodyEmergencyEscapeRouter.cs`
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1`

**Step 1: 改造 FindEscapeDestination**

将当前单段随机逻辑改成显式回退链：

```csharp
internal static IntVec3 FindEscapeDestination(Pawn pawn, Map map)
{
    if (pawn == null || map == null)
    {
        return IntVec3.Invalid;
    }

    IntVec3 destination;
    if (TryFindBeaconDestination(map, out destination))
    {
        return destination;
    }

    if (TryFindColonistAreaDestination(pawn, map, out destination))
    {
        return destination;
    }

    if (TryFindLocalSafeDestination(pawn, map, out destination))
    {
        return destination;
    }

    if (TryFindMapSafeDestination(map, out destination))
    {
        return destination;
    }

    return pawn.Position;
}
```

**Step 2: 新增信标落点查找**

```csharp
private static bool TryFindBeaconDestination(Map map, out IntVec3 destination)
{
    destination = IntVec3.Invalid;
    if (map == null)
    {
        return false;
    }

    foreach (Building_EmergencyEscapeBeacon beacon in map.listerBuildings.AllBuildingsColonistOfClass<Building_EmergencyEscapeBeacon>())
    {
        if (beacon == null || !beacon.IsActiveAnchor)
        {
            continue;
        }

        if (TryFindStandableNear(beacon.Position, map, 1, false, out destination))
        {
            return true;
        }
    }

    return false;
}
```

**Step 3: 新增殖民者建筑附近回退**

```csharp
private static bool TryFindColonistAreaDestination(Pawn pawn, Map map, out IntVec3 destination)
{
    destination = IntVec3.Invalid;
    if (pawn == null || map == null || map.listerBuildings.allBuildingsColonist.Count == 0)
    {
        return false;
    }

    Building nearestBuilding = null;
    float nearestDistance = float.MaxValue;
    foreach (Building building in map.listerBuildings.allBuildingsColonist)
    {
        if (building == null || building.Destroyed || !building.Spawned)
        {
            continue;
        }

        float distance = building.Position.DistanceToSquared(pawn.Position);
        if (distance < nearestDistance)
        {
            nearestDistance = distance;
            nearestBuilding = building;
        }
    }

    return nearestBuilding != null
        && TryFindStandableNear(nearestBuilding.Position, map, 10, false, out destination);
}
```

**Step 4: 保留小人附近回退**

```csharp
private static bool TryFindLocalSafeDestination(Pawn pawn, Map map, out IntVec3 destination)
{
    destination = IntVec3.Invalid;
    return pawn != null
        && TryFindStandableNear(pawn.Position, map, 12, false, out destination);
}
```

**Step 5: 新增地图中心安全格回退**

```csharp
private static bool TryFindMapSafeDestination(Map map, out IntVec3 destination)
{
    destination = IntVec3.Invalid;
    if (map == null)
    {
        return false;
    }

    return CellFinder.TryFindRandomCellNear(
        map.Center,
        map,
        map.Size.x / 2,
        cell => cell.IsValid
            && cell.InBounds(map)
            && cell.Standable(map)
            && !cell.Fogged(map)
            && !cell.Roofed(map),
        out destination);
}
```

**Step 6: 抽出站立格帮助方法**

```csharp
private static bool TryFindStandableNear(IntVec3 center, Map map, int radius, bool requireUnroofed, out IntVec3 destination)
{
    destination = IntVec3.Invalid;
    if (map == null || !center.IsValid)
    {
        return false;
    }

    return CellFinder.TryFindRandomCellNear(
        center,
        map,
        radius,
        cell => cell.IsValid
            && cell.InBounds(map)
            && cell.Standable(map)
            && !cell.Fogged(map)
            && (!requireUnroofed || !cell.Roofed(map)),
        out destination);
}
```

**Step 7: 运行烟测确认通过**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1"
```

Expected: PASS。

**Step 8: Commit**

```bash
git add "模组工程/BorderDefenseProtocol/Source/BDP/Core/CombatBody/Escape/CombatBodyEmergencyEscapeRouter.cs" `
        "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1"
git commit -m "feat: route emergency escape through beacon"
```

---

## Task 6: 构建与相关回归验证

**Files:**
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1`
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeExecutionSmokeTests.ps1`
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyCollapseEmergencySmokeTests.ps1`
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeResolverSmokeTests.ps1`

**Step 1: 跑新增和旧紧急脱离烟测**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1"
powershell -ExecutionPolicy Bypass -File "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeExecutionSmokeTests.ps1"
powershell -ExecutionPolicy Bypass -File "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyCollapseEmergencySmokeTests.ps1"
powershell -ExecutionPolicy Bypass -File "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeResolverSmokeTests.ps1"
```

Expected: 全部 PASS。

**Step 2: 主模组 Release 构建**

Run:

```powershell
dotnet build "模组工程/BorderDefenseProtocol/Source/BDP/BDP.csproj" -c Release
```

Expected: 0 errors。若出现已有 warning，只记录，不顺手修计划外问题。

**Step 3: XML 解析检查**

Run:

```powershell
[xml](Get-Content -LiteralPath "模组工程/BorderDefenseProtocol/1.6/Defs/Buildings/CombatBody/ThingDefs_EmergencyEscapeBeacon.xml" -Raw -Encoding utf8) | Out-Null
```

Expected: 无异常。

**Step 4: 最终提交**

```bash
git add "模组工程/BorderDefenseProtocol/Source/BDP" `
        "模组工程/BorderDefenseProtocol/1.6/Defs/Buildings/CombatBody/ThingDefs_EmergencyEscapeBeacon.xml" `
        "模组工程/BorderDefenseProtocol/Source/BDP.Tests/CombatBodyEmergencyEscapeBeaconRouteSmokeTests.ps1"
git commit -m "feat: add emergency escape beacon anchor"
```

提交前用 `git status --short` 确认没有把 Legacy、DevHarness 或无关日志混入。

---

## Task 7: 游戏内实测记录

**Files:**
- Modify: `日志/Agent工作日志/Agent日志08.md` 或新日志文件

**Step 1: 实测场景**

在游戏内验证：

```text
1. 无信标：紧急脱离仍回退到安全位置。
2. 有信标但未接电：不使用信标。
3. 有信标、开关开启且通电：优先传送到信标附近 1 格。
4. 信标周围被堵死：继续回退到殖民者建筑附近。
5. 尝试同地图建第二座：PlaceWorker 拒绝放置。
```

**Step 2: 诊断原则**

如游戏内才复现问题，优先加临时诊断日志：

```text
[BDP诊断] emergency_escape.beacon_route ...
```

诊断日志必须可搜索、可删除；确认问题后删除临时日志，只保留必要最终代码。

**Step 3: 记录结果**

按项目日志规则追加倒序工作日志，最多 20 条；超额另建新文件。
