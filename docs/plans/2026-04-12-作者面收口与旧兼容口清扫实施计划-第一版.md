# 作者面收口与旧兼容口清扫 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 删除表达配置层残留旧执行入口，并补齐作者面与样例面的收口护栏。

**Architecture:** 主模组作者输入面只承认 `Execution（统一执行配置）` 与当前正式公开阶段接口，不再让配置层直接写入旧 `ExecutionStyle（旧执行风格）`。`tools（多 Tool）` 继续作为正式近战声明面保留，但去掉“旧兼容写法”表述，避免把正常作者面误写成过渡口。

**Tech Stack:** C#、PowerShell smoke tests、RimWorld XML Def

---

### Task 1: 锁住旧执行入口失败测试

**Files:**
- Create: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/ExpressionAuthoringBoundarySmokeTests.ps1`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/RangedModuleSampleModulesSmokeTests.ps1`

**Step 1:** 写失败测试，锁 `ChipExpressionEntryConfig` / `ComboExpressionEntryConfig` / 解释器 / 校验器 不再保留 `ExecutionStyle（旧执行风格）` 作者入口。

**Step 2:** 运行新增 smoke test，确认先红灯。

**Step 3:** 补样例面断言，锁 `DevHarness（测试模组）` 样例继续只教 `AttackContextSnapshot（攻击上下文快照）` 与 `Execution（统一执行配置）`。

### Task 2: 清表达层旧兼容入口

**Files:**
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Expressions/Config/ChipExpressionEntryConfig.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Combos/Config/ComboExpressionEntryConfig.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Expressions/Contract/DefaultChipExpressionContractInterpreter.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/Chips/Validation/DefaultChipDefinitionValidator.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP/Core/AttackExecution/RangedAttackExecutionContext.cs`

**Step 1:** 删配置层 `ExecutionStyle（旧执行风格）` 字段与相关复制逻辑。

**Step 2:** 让解释器与校验器只从 `Execution（统一执行配置）` 生成正式执行风格，并更新注释，去掉“兼容入口”表述。

**Step 3:** 清理运行时上下文里“向后兼容读取”注释，改成真实职责描述。

### Task 3: 回归与记录

**Files:**
- Modify: `模组工程/BorderDefenseProtocol/docs/02-研究记录/2026-04-12-作者面收口与旧兼容口清扫记录.md`

**Step 1:** 运行定向 smoke tests 与 `BDP.csproj` 编译。

**Step 2:** 用短日志记下“删了什么、保留了什么、为什么保留”。
