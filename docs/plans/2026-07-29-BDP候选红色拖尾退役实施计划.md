# BDP 候选红色拖尾退役实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将候选测试模组中全部炽红拖尾引用替换为正式的亮薄荷绿长拖尾，并删除失去用途的炽红预设定义。

**Architecture:** 不改拖尾运行时代码，只调整候选业务配置。主模组继续作为正式预设提供方，候选测试模组只引用主模组的预设，不再重复保留红色预设。

**Tech Stack:** RimWorld XML（边缘世界配置文件）、PowerShell（自动检查脚本）、Git（版本控制）

---

## 完成状态

本计划已于 2026-07-29 完成：

- 候选炽红预设文件已删除，原两处引用已改为亮薄荷绿长拖尾。
- 候选拖尾引用为红色 0、亮薄荷绿长版 3、短版 5。
- 候选程序集构建成功，Core（核心）依赖门禁通过。
- 46 个 XML（配置文件）全部可解析，八项拖尾专项检查全部通过。
- 全量 223 项回归为 211 项通过、12 项既有基线失败，新增失败为 0。

### Task 1：先锁定新的配置契约

**Files:**
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailPresetInjectionSmokeTests.ps1`
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailPresetInjectionSmokeTests.ps1`

**Step 1: 修改测试断言**

要求测试检查：

- 候选预设文件已经不存在。
- 候选芯片配置不再包含 `BDP_TrailPreset_HotRed`。
- `BDP_TestChipRangedVolley` 和 `BDP_TestChipPathLatchVolley` 使用 `BDP_TrailPreset_BrightMintLong`。

**Step 2: 运行测试并确认失败**

```powershell
powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailPresetInjectionSmokeTests.ps1'
```

预期：测试因候选红色预设文件和红色引用仍存在而失败。

### Task 2：完成最小配置替换

**Files:**
- Modify: `模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Things/Items/Chips/Test/ThingDefs_TestChips_Combat.xml`
- Delete: `模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Things/Projectiles/Test/BeamTrailPresetDefs.xml`

**Step 1: 替换两处引用**

把两个 `<preset>BDP_TrailPreset_HotRed</preset>` 改成：

```xml
<preset>BDP_TrailPreset_BrightMintLong</preset>
```

**Step 2: 删除候选红色预设文件**

删除只包含 `BDP_TrailPreset_HotRed` 的候选 XML 文件。

**Step 3: 运行专项测试并确认通过**

重复 Task 1 的测试命令，预期输出：

```text
DevHarnessBeamTrailPresetInjectionSmokeTests PASS
```

### Task 3：完成构建、解析、记录和提交

**Files:**
- Modify: `模组工程/BorderDefenseProtocol/docs/plans/2026-07-28-BDP统一模组双程序集迁移总计划.md`
- Modify: `日志/Agent工作日志/Agent日志13.md`

**Step 1: 验证候选程序集构建**

```powershell
dotnet msbuild '.\模组工程\BorderDefenseProtocol.DevHarness\Source\BDP.DevHarness\BDP.DevHarness.csproj' /t:Build /p:Configuration=Release /nologo /v:minimal
```

预期：退出码为 0。

**Step 2: 验证 XML 解析和拖尾专项检查**

解析主模组与候选测试模组的全部 XML，并运行事项04的八项专项检查。预期：XML 解析失败数为 0，专项失败数为 0。

**Step 3: 同步计划与日志**

在长期总计划中记录本支线处理结果；在最新工作日志顶部写入本次变更、验证和提交。

**Step 4: 提交**

```powershell
git add -- <本计划列出的实际改动路径>
git commit -m 'refactor: 退役候选红色拖尾预设'
```
