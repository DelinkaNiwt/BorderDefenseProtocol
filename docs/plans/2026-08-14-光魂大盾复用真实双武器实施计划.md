# 光魂大盾复用真实双武器 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 撤回非武器手持视觉通用设施，以 Content（内容层）的 5 点钝伤盾牌武器表达复用既有双武器绘制系统。

**Architecture:** Core 恢复原有“真实武器芯片数量决定双武器关系”的单一真值。光魂盾姿态同时发布防御 Hediff 和无视觉近战武器结果；Hediff 继续承载盾面及回弹，并通过已有复合视觉预设能力在双持时外移。

**Tech Stack:** RimWorld 1.6、C#、XML、PowerShell 冒烟测试、Git 反向提交。

---

### Task 1: 建立失败边界测试

**Files:**
- Modify: `Source/BDP.Tests/LightSoulChipSmokeTests.ps1`
- Create: `Source/BDP.Tests/LightSoulRealWeaponBoundarySmokeTests.ps1`

**Step 1:** 增加断言：Core 不存在 `ParticipatesInHandheldVisualRelation` 和 `ActiveHandheldVisualSourceCount`。

**Step 2:** 增加断言：大盾形态公共激活唯一 5 点钝伤武器表达，两种姿态只切换各自 Hediff；武器表达无视觉，原 Hediff 视觉声明复合预设。

**Step 3:** 运行两个测试，确认因旧设施仍存在且盾击表达缺失而失败。

**Step 4:** 只提交测试。

### Task 2: 撤回错误设施链

**Files:**
- Revert commits: `1e526730`、`7378986a`、`f71992ec`、`78dcea06`、`803f6a7f`、`70c092dd`、`530187c9`

**Step 1:** 按逆序使用 `git revert --no-edit`，保留历史，不使用 reset（重置）。

**Step 2:** 运行边界测试，确认 Core 设施移除断言通过，盾击内容断言仍按预期失败。

**Step 3:** 编译 Core Release，确认恢复后的程序集可生成。

### Task 3: 增加真实盾牌武器表达

**Files:**
- Modify: `1.6/Content/Defs/ChipActionPresetDef/Presets.xml`
- Modify: `Languages/ChineseSimplified (简体中文)/Keyed/Gameplay.xml`
- Test: `Source/BDP.Tests/LightSoulChipSmokeTests.ps1`
- Test: `Source/BDP.Tests/LightSoulRealWeaponBoundarySmokeTests.ps1`

**Step 1:** 为大盾形态增加唯一一条 `PrimaryVerb`、`Melee`、`HitCount=1`、5 点 `Blunt` Tool 的公共真实武器条目。

**Step 2:** 大盾形态公共激活武器；两个姿态只切换各自 Hediff。武器条目不声明 Presentation，避免重复盾面。

**Step 3:** 为唯一公共盾击 Tool 新增 Keyed（键值语言包）文本。

**Step 4:** 运行测试，确认武器身份与伤害边界通过。

**Step 5:** 提交 Content 配置与测试。

### Task 4: 增加盾牌双持复合视觉

**Files:**
- Modify: `1.6/Content/Defs/ExpressionDef/Visual.xml`
- Modify: `1.6/Content/Defs/ChipActionPresetDef/Presets.xml`
- Test: `Source/BDP.Tests/LightSoulChipSmokeTests.ps1`
- Test: `Source/BDP.Tests/LightSoulRealWeaponBoundarySmokeTests.ps1`

**Step 1:** 让两个原 Hediff 视觉声明各自的 `CompositeVisualPresetDefName`。

**Step 2:** 增加灵活盾、举盾盾的双持复合预设，只复用原贴图并扩大手侧外移；举盾角度保持竖直。

**Step 3:** 运行测试，确认单持预设不变、双持预设存在且外移、举盾最终角度仍为竖直。

**Step 4:** 提交视觉配置与测试。

### Task 5: 回归、日志与交付

**Files:**
- Modify: `1.6/Assemblies/BDP.Core.dll`
- Modify: `1.6/Assemblies/BDP.Core.pdb`
- Modify: `日志/Agent工作日志/Agent日志46.md`

**Step 1:** 编译 Core Release，要求 0 警告、0 错误。

**Step 2:** 运行双武器关系、手部投影、单武器视觉、光魂配置和格挡反馈相关测试。

**Step 3:** 检查 XML 可解析、Core 无光魂业务词、工作日志不超过 20 条。

**Step 4:** 更新工作日志，提交程序集和日志。

**Step 5:** 在最终提交后重新编译并复验。
