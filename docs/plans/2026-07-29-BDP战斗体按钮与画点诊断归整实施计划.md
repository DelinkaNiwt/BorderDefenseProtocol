# BDP 战斗体按钮与画点诊断归整实施计划

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development and superpowers:verification-before-completion while executing this plan.

**Goal:** 完成事项 06，使 Core 提供受 Trion 腺体基因约束的正式战斗体按钮，Content 只保留画点诊断，并物理删除其余按钮的无用调用链。

**Architecture:** 正式玩家操作留在 Core；诊断显示留在单向依赖 Core 的 Content。删除只服务旧诊断窗口的 Core 表面和上下文传递，但保留画点仍在使用的视觉快照能力。

**Tech Stack:** RimWorld 1.6、C# 7.3、XML、PowerShell 冒烟测试、MSBuild。

---

### 任务 1：先固定事项 06 的新边界

**文件：**

- 新建：`Source/BDP.Tests/Item06CombatBodyGizmoAndDiagnosticsCleanupSmokeTests.ps1`
- 修改：`Source/BDP.Tests/CombatBodyTriggerTrionIntegrationSmokeTests.ps1`
- 修改：`Source/BDP.Tests/TriggerVisualMarkerOverlaySmokeTests.ps1`
- 修改：`Source/BDP.Tests/TriggerVisualPoseDiagnosticsSmokeTests.ps1`
- 修改：`Source/BDP.Tests/VisualAimMirrorVanillaParitySmokeTests.ps1`
- 修改：`Source/BDP.Tests/LegacyStyleTriggerGuiSmokeTests.ps1`
- 修改：`Source/BDP.Tests/TrionTriggerPanelReplacesLegacyGizmoSmokeTests.ps1`

步骤：

1. 写入新契约：正式按钮检查基因、两个按钮同时构建并按状态禁用。
2. 写入新契约：Content 注册且只提供画点诊断。
3. 写入新契约：候选旧按钮、窗口、控件全部不存在。
4. 写入新契约：Core 无用完整性诊断表面和姿态窗口传递链不存在，但画点需要的姿态快照仍存在。
5. 运行专项测试，确认它因旧实现仍在而按预期失败。
6. 提交失败契约。

### 任务 2：正式化 Core 战斗体按钮

**文件：**

- 修改：`Source/BDP/Core/CombatBody/External/CombatBodyTriggerGizmoProvider.cs`

步骤：

1. 使用 `TrionGlandEligibility.HasActiveTrionGland` 作为显式资格条件。
2. 始终构建“开启战斗体”和“解除战斗体”两个按钮。
3. 分别使用 `CanActivate` 和 `CanManualDeactivate` 设置禁用状态。
4. 保持调用正式 `ICombatBodyCommands`，不新增平行执行路线。
5. 运行正式按钮专项测试，确认通过。

### 任务 3：把画点诊断迁入 Content

**文件：**

- 新建：`Source/BDP.Content/Trigger/Diagnostics/TriggerVisualMarkerSettings.cs`
- 新建：`Source/BDP.Content/Trigger/Diagnostics/TriggerVisualMarkerGizmoProvider.cs`
- 新建：`Source/BDP.Content/Trigger/Diagnostics/MapComponent_TriggerVisualMarkerOverlay.cs`
- 新建：`Source/BDP.Content/Trigger/Diagnostics/TriggerVisualMarkerOverlayDrawer.cs`
- 修改：`Source/BDP.Content/ContentBootstrap.cs`
- 删除候选模组中对应的旧 `DevHarness` 文件。

步骤：

1. 迁移既有画点绘制逻辑，不改点位、颜色、尺寸和绘制时序。
2. 将命名空间改为 `BDP.Content.Trigger.Diagnostics`，删除测试和临时语义。
3. Content 启动入口注册画点按钮提供器。
4. 保留开发者模式限制和运行期总开关。
5. 运行画点专项测试，确认通过。

### 任务 4：物理删除旧按钮及无用调用链

**文件：**

- 删除候选模组六按钮提供器和三个诊断窗口。
- 删除候选模组两个旧式控件、一个旧式槽位窗口。
- 修改候选启动入口，移除事项 06 的按钮注册。
- 删除：`Source/BDP/Core/Trigger/Access/Contracts/ITriggerIntegrityDiagnostics.cs`
- 修改 Core Trigger 表面、宿主、按钮上下文和按钮构建服务。

步骤：

1. 删除所有不保留按钮的直接入口。
2. 删除无调用者的完整性诊断接口、表面、宿主字段、获取入口和专用读取方法。
3. 删除 `VisualPoseDiagnostics` 的按钮上下文属性与预抓取。
4. 保留画点直接使用的视觉姿态快照和访问面。
5. 全局搜索被删类型和玩家按钮文字，确认运行源码零残留。
6. 运行事项 06 全部专项测试。

### 任务 5：构建、全量验证和记录

**文件：**

- 修改：`docs/plans/2026-07-28-BDP统一模组双程序集迁移总计划.md`
- 修改：`C:/NiwtDatas/Projects/RimworldModStudio/日志/Agent工作日志/Agent日志13.md`

步骤：

1. 分别构建 Core、Content 和候选程序集。
2. 运行 Core 单向依赖检查。
3. 解析主模组与候选模组全部 XML。
4. 运行事项 06 专项测试。
5. 运行全量 PowerShell 冒烟测试，并与事项 05 的 12 项既有失败基线比较。
6. 更新总计划当前事项、决定记录和进度记录。
7. 写入最新工作日志。
8. 检查限定路径差异后提交代码、发布 DLL、计划和日志。

