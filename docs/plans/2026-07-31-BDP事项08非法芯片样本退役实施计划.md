# BDP 事项08非法芯片样本退役 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 彻底删除无实际玩法价值的故意非法芯片及其专用维护分支，同时保留全部正常芯片规范检查。

**Architecture:** 不新增测试夹具或兼容层。先用一项源码边界测试锁定“活动定义和候选源码中不存在非法芯片”，再删除候选定义与常量，并把原有检查收紧为只验证正常芯片。

**Tech Stack:** RimWorld 1.6 XML、C# 7.3、PowerShell 冒烟测试、MSBuild。

---

### Task 1：建立退役失败契约

**Files:**

- Create: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessInvalidChipRetirementSmokeTests.ps1`

**Step 1:** 新增测试，递归读取候选 `1.6/Defs` 与 `Source/BDP.DevHarness`，断言不含 `BDP_TestChipInvalidDefinition`。

**Step 2:** 单独运行测试。

Expected: FAIL，原因是当前 XML 与常量仍存在。

**Step 3:** 只提交失败契约。

### Task 2：物理删除非法样本

**Files:**

- Delete: `模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Things/Items/Chips/Test/ThingDefs_TestChips_Invalid.xml`
- Modify: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/TriggerDevHarnessDefs.cs`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/ChipCategoryDefinitionBoundarySmokeTests.ps1`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessChipSlotRegionAuthoringSmokeTests.ps1`
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessChipSlotOccupancyAuthoringSmokeTests.ps1`

**Step 1:** 删除非法 XML 和专用常量。

**Step 2:** 删除三个测试中的非法样本计数、跳过分支和必须存在断言。

**Step 3:** 保留合法芯片数量大于零以及所有正式字段断言。

**Step 4:** 运行新退役测试及三项相邻测试。

Expected: 全部 PASS。

**Step 5:** 提交运行内容与测试修正。

### Task 3：完整验证并更新长期记录

**Files:**

- Modify: `模组工程/BorderDefenseProtocol/docs/plans/2026-07-28-BDP统一模组双程序集迁移总计划.md`
- Modify: `日志/Agent工作日志/Agent日志16.md`

**Step 1:** 构建 Core、Content 与 DevHarness 三个程序集，确认零编译错误。

**Step 2:** 解析两个模组当前全部 XML，确认零解析错误。

**Step 3:** 搜索现行运行目录、候选源码和测试，确认非法定义名为零。

**Step 4:** 更新事项08进度，明确只完成第一个对象，下一对象仍待介绍确认。

**Step 5:** 提交验证记录。

## 执行补正

全量检查发现另外两项既有测试仍直接依赖已删除的非法文件或18枚旧总数：

- `DevHarnessChipActivationRequirementAuthoringSmokeTests.ps1`
- `DevHarnessTriggerChipTextureSmokeTests.ps1`

它们属于同一删除边界，已分别移除非法文件专用断言并将正常芯片总数同步为17。补正后全量失败恢复为原有12项，没有产生新失败。
