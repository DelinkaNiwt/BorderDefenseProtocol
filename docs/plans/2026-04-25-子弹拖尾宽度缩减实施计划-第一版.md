# BDP Beam Trail Width Reduction Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Reduce the current bullet beam-trail visual width to about 75% of its present thickness.

**Architecture:** Keep the existing BeamTrail rendering structure unchanged and only narrow the shared width constant from `0.13` to `0.0975`. Update the projectile XML config plus the C# fallback/default paths so runtime creation, null-config fallback, reset fallback, and save-load fallback all stay consistent.

**Tech Stack:** C#, RimWorld `Verse` / `UnityEngine`, DevHarness projectile defs, PowerShell smoke tests.

---

### Task 1: 写失败烟测

**Files:**
- Modify: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailVisualSmokeTests.ps1`

**Step 1: 给现有 BeamTrail 视觉烟测增加精确宽度断言**

- 要求以下位置都使用 `0.0975`：
  - `BeamTrailConfig.cs`
  - `BeamTrailAppearanceSnapshot.cs`
  - `BeamTrailSegment.cs`
  - `ThingDefs_TestProjectiles.xml`

**Step 2: 运行测试确认失败**

Run:

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailVisualSmokeTests.ps1'
```

Expected:

- FAIL，并直接指出旧宽度仍是 `0.13`。

### Task 2: 实现最小宽度调整

**Files:**
- Modify: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Projectiles/BeamTrail/BeamTrailConfig.cs`
- Modify: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Projectiles/BeamTrail/BeamTrailAppearanceSnapshot.cs`
- Modify: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/Projectiles/BeamTrail/BeamTrailSegment.cs`
- Modify: `模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Things/Projectiles/Test/ThingDefs_TestProjectiles.xml`

**Step 1: 调整配置默认值**

- 把 `trailWidth = 0.13f` 改成 `0.0975f`

**Step 2: 调整外观快照默认回退值**

- 把 `config != null ? config.trailWidth : 0.13f` 改成 `0.0975f`

**Step 3: 调整线段运行时与存读档回退值**

- `BeamTrailSegment.Reset(...)` 的空外观回退值改成 `0.0975f`
- `Scribe_Values.Look(ref TrailWidth, "trailWidth", ...)` 默认值改成 `0.0975f`

**Step 4: 调整投射物 Def**

- `BDP_TestBulletSemantic` 的 `<trailWidth>0.13</trailWidth>` 改成 `<trailWidth>0.0975</trailWidth>`

### Task 3: 专项验证

**Files:**
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailVisualSmokeTests.ps1`
- Test: `模组工程/BorderDefenseProtocol/Source/BDP.Tests/DevHarnessBeamTrailLoadThreadSafetySmokeTests.ps1`
- Build: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness/BDP.DevHarness.csproj`

**Step 1: 跑拖尾视觉烟测**

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailVisualSmokeTests.ps1'
```

Expected:

- PASS

**Step 2: 跑相邻拖尾安全烟测**

```powershell
& '.\模组工程\BorderDefenseProtocol\Source\BDP.Tests\DevHarnessBeamTrailLoadThreadSafetySmokeTests.ps1'
```

Expected:

- PASS

**Step 3: 编译 DevHarness**

```powershell
dotnet msbuild '.\模组工程\BorderDefenseProtocol.DevHarness\Source\BDP.DevHarness\BDP.DevHarness.csproj'
```

Expected:

- Build succeeded

### Task 4: 补落实记录

**Files:**
- Create: `日志/2026-04-25-子弹拖尾宽度缩减落实记录.md`

**Step 1: 记录**

- 说明这次仅调整拖尾宽度，不改拖尾材质、颜色、寿命、透明曲线和主逻辑。

---

用户已明确要求“直接改”，当前按本会话直接执行，不单开新会话，不创建提交。
