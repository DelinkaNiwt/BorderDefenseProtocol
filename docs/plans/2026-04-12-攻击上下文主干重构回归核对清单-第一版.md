# 攻击上下文主干重构回归核对清单-第一版

日期：2026-04-12

## 一、自动验证已通过

- [x] 主干只剩 `AttackContext -> AttackContextSnapshot`
- [x] `SharedState（共享状态）` 已从攻击链主干移除
- [x] 请求边界只带 `AttackContextSnapshot`
- [x] 前半段阶段上下文统一暴露 `AttackContext`
- [x] `ProjectileInitPlan（投射物初始化计划）` 只带 `AttackContextSnapshot`
- [x] 后半段阶段上下文只读冻结快照
- [x] 阶段挂件只读 `AttackContextSnapshot`
- [x] 模块私有上下文已并入统一主干
- [x] `RangedModuleContextSnapshot` 已删除
- [x] 主模组编译通过

## 二、已跑 smoke tests

- [x] `Source/BDP.Tests/AttackContextBoundarySmokeTests.ps1`
- [x] `Source/BDP.Tests/AttackContextNoSharedStateSmokeTests.ps1`
- [x] `Source/BDP.Tests/AttackContextRangedProtocolCarrySmokeTests.ps1`
- [x] `Source/BDP.Tests/RangedModuleRuntimeSessionSmokeTests.ps1`
- [x] `Source/BDP.Tests/RangedModuleStageAddonBoundarySmokeTests.ps1`
- [x] `Source/BDP.Tests/RangedExecutionFreezeBoundarySmokeTests.ps1`
- [x] `Source/BDP.Tests/RangedTargetingProtocolSmokeTests.ps1`
- [x] `Source/BDP.Tests/RangedImpactExecutionBoundarySmokeTests.ps1`
- [x] `Source/BDP.Tests/FormalHostVerbSmokeTests.ps1`
- [x] `Source/BDP.Tests/RangedAttackTrionConsumptionSmokeTests.ps1`
- [x] `Source/BDP.Tests/CombatLogPresentationSmokeTests.ps1`
- [x] `dotnet msbuild '.\Source\BDP\BDP.csproj'`

## 三、代码层结论

- [x] 目标交互链没有再挂独立确认主干
- [x] 远程协议前半段没有再挂独立确认主干
- [x] 投射物后半段没有再挂运行时会话主干
- [x] 旧测试已同步到新主干事实

## 四、仍需游戏内实测

以下项目本次只做到了代码与 smoke test 验证，尚未做 RimWorld 实机确认：

- [ ] 手动攻击完整手感
- [ ] 自动攻击完整手感
- [ ] 多轮目标交互实际体验
- [ ] 多发 / 齐射 / 逐发节奏体感
- [ ] 命中 / 掩体 / 护盾的游戏内边界
- [ ] 读档后在真实战斗中的续射体验

## 五、判定

当前可以确认：

- 代码主干已收干净；
- 自动验证通过；
- 没有靠兼容字段掩盖问题。

当前不能宣称：

- 已完成游戏内全实测。
