# 护盾常驻气泡关闭 Implementation Plan（实施计划）

> **For Claude（供 Claude 执行）:** REQUIRED SUB-SKILL（必须使用的子技能）: Use superpowers:executing-plans to implement this plan task-by-task（使用 executing-plans 按任务实施本计划）。

**Goal（目标）:** 让 DevHarness 测试护盾激活期间不显示常驻气泡，同时保持抵挡逻辑和命中反馈不变。

**Architecture（结构）:** 沿用现有 `showShieldBubble` 配置边界，只把 `BDP_TestShield` 的业务配置改为关闭。现有 C#（C#语言）绘制入口、抵挡路径与命中特效路径不改动。

**Tech Stack（技术栈）:** RimWorld 1.6 Def XML（定义文件）、PowerShell（脚本）冒烟测试、Markdown（文档）。

---

### Task 1：用冒烟测试锁定气泡关闭配置

**Files（文件）:**

- Modify（修改）: `模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness.Tests/ShieldChipSmokeTests.ps1`
- Modify（修改）: `模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Health/Shield/Test/HediffDefs_TestShield.xml`

**Step 1：先写失败测试**

在读取 `$props` 后加入：

```powershell
Assert-True ([string]$props.showShieldBubble -eq 'false') '测试护盾激活期间不得显示常驻护盾球。'
```

**Step 2：运行测试并确认失败**

Run（运行）:

```powershell
& 'C:\NiwtDatas\Projects\RimworldModStudio\模组工程\BorderDefenseProtocol.DevHarness\Source\BDP.DevHarness.Tests\ShieldChipSmokeTests.ps1'
```

Expected（预期）: FAIL（失败），错误为“测试护盾激活期间不得显示常驻护盾球”。

**Step 3：实施最小配置改动**

将测试护盾定义改为：

```xml
<!-- 激活期间不显示常驻护盾气泡；成功抵挡时的命中特效不受影响。 -->
<showShieldBubble>false</showShieldBubble>
```

**Step 4：运行测试并确认通过**

Run（运行）: 同 Step 2。

Expected（预期）: 输出 `Shield chip smoke tests passed.`（护盾芯片冒烟测试通过）。

**Step 5：提交配置与测试**

```powershell
git add -- '模组工程/BorderDefenseProtocol.DevHarness/Source/BDP.DevHarness.Tests/ShieldChipSmokeTests.ps1' '模组工程/BorderDefenseProtocol.DevHarness/1.6/Defs/Health/Shield/Test/HediffDefs_TestShield.xml'
git commit -m 'fix: 隐藏护盾常驻气泡'
```

### Task 2：更新实测口径并完成验证

**Files（文件）:**

- Modify（修改）: `模组工程/BorderDefenseProtocol.DevHarness/Docs/Testing/2026-07-21-护盾芯片实测清单.md`
- Modify（修改）: `日志/Agent工作日志/Agent工作日志*.md`

**Step 1：更新游戏实测清单**

- 单枚护盾激活后应确认 Pawn（角色）周围没有常驻气泡。
- 成功抵挡时仍需确认六边形特效、闪光、火花和拦截音效。
- 关闭与读档项目只检查 Hediff（健康状态）与 Severity（严重度）恢复，不再期待气泡。

**Step 2：运行完整相关验证**

Run（运行）:

```powershell
& 'C:\NiwtDatas\Projects\RimworldModStudio\模组工程\BorderDefenseProtocol.DevHarness\Source\BDP.DevHarness.Tests\ShieldChipSmokeTests.ps1'
git diff --check
```

Expected（预期）: 冒烟测试通过；`git diff --check` 无错误。

**Step 3：写工作日志**

在 `C:\NiwtDatas\Projects\RimworldModStudio\日志\Agent工作日志` 中按时间倒序记录本次修改、验证结果与提交号；遵守单文件最多 20 条。

**Step 4：提交文档与日志**

只暂存本计划涉及的实测清单、实施计划和工作日志，不包含工作区其他改动。
