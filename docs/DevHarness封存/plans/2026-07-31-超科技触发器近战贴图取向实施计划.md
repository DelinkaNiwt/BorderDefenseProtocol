# 超科技触发器近战贴图取向 Implementation Plan（实施计划）

> **For Claude（供执行代理）：** REQUIRED SUB-SKILL（必需子技能）：使用 `superpowers:executing-plans（执行计划）` 按任务逐项执行。

**Goal（目标）：** 以第一版触发器为基础，生成一张外轮廓优先、波浪边可见窄厚度带的 RW（RimWorld／边缘世界）超科技白底贴图。

**Architecture（方案）：** 使用内置图像生成工具保留触发器身份与刀柄式方向，只改视角、外轮廓层级和内部细节密度。生成后将背景边缘统一为纯白不透明，另存新版本，不覆盖已提交的 v2。

**Tech Stack（工具）：** 内置 ImageGen（图像生成）、PowerShell（命令行）、Pillow（图像像素核验）、Git（版本控制）。

---

### Task 1：按原版近战图取向重绘

**Files（文件）：**

- Read（读取）：`C:/Users/Niwt/.codex/generated_images/019fb62a-80d4-7a71-9ad1-b49009bc0952/exec-0499f175-f944-4354-8b55-7527e9955a6f.png`
- Create（创建）：内置生成工具的候选 PNG

**Step 1：固定不变量**

保留青蓝收腰长板、中央圆形节点、两端各两条圆端导线槽、左下握持尾与右上控制前端、纯白不透明背景。

**Step 2：固定视角改动**

主面改为近乎平面；波浪收腰边为近侧，显示一条窄深青灰厚度/硬阴影带；长直边仅保留单层外轮廓，取消双层长侧壳。

**Step 3：调用图像生成并目检**

确认生成结果只有一个触发器，外轮廓在缩小后优先可读，内部线条不与外轮廓竞争。

### Task 2：仅在不符合视角时做一次定向修正

**Files（文件）：**

- Modify（修改）：Task 1 的候选图（以新文件生成，不覆盖候选）

**Step 1：选择唯一主缺陷**

仅在厚度仍位于长直边、出现强透视/双侧壳、或内部导线过密三者之一时执行。

**Step 2：重复不变量并修正该缺陷**

明确要求只更改该项，不改变触发器方向、身份结构或白色背景。

**Step 3：目检**

确认波浪边窄厚度带仍可见，长直边未重新产生第二层外壳。

### Task 3：保存与验证

**Files（文件）：**

- Create（创建）：`日志/BDP_TriggerHandle_RW_White_v3.png`
- Modify（修改）：`日志/Agent工作日志/Agent日志16.md` 或下一个未满 20 条的日志文件

**Step 1：非破坏式保存**

把选择的结果保存为 v3；不得覆盖 v2 或原始截图。

**Step 2：像素与视觉核验**

检查 PNG（图片文件）可打开、四角与整圈边缘均为 `#FFFFFF`、Alpha（透明度）均为 255，且画面不含人物、投影或额外物体。

**Step 3：记录和提交**

按时间倒序写入工作日志，并只提交本次 v3、相关文档与日志。
