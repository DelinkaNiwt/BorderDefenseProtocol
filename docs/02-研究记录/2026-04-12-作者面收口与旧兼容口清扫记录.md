# 2026-04-12-作者面收口与旧兼容口清扫记录

## 本次结论

- 主模组表达配置层已经不再接受 `ExecutionStyle` 旧作者入口。
- 主模组作者输入面现在只承认 `Execution` 统一执行配置。
- `tools` 不是兼容垃圾，它仍是正式多 Tool 近战声明面，继续保留。

## 本次改动

- 删除 `ChipExpressionEntryConfig` 上的 `ExecutionStyle` 字段。
- 删除 `ComboExpressionEntryConfig` 上的 `ExecutionStyle` 字段与映射。
- 解释器与校验器停止读取配置层旧 `ExecutionStyle`。
- 清理多处“兼容入口 / 旧链路 / 向后兼容”残留表述。
- 补 `ExpressionAuthoringBoundarySmokeTests`，专门锁这轮收口。
- 补样例 smoke test，锁 `DevHarness` 继续只使用 `Execution`，不回潮到旧写法。

## 保留项

- 保留 `ChipExpressionEntryConfig.tools`。
- 保留 `ComboExpressionEntryConfig.tools`。
- 原因很简单：这不是旧兼容口，而是当前近战多 Tool 的正式作者声明面。

## 回归结果

- `ExpressionAuthoringBoundarySmokeTests` 通过。
- `RangedModuleSampleModulesSmokeTests` 通过。
- `RangedModuleAuthorSurfaceBoundarySmokeTests` 通过。
- `RangedModuleStageAddonBoundarySmokeTests` 通过。
- `BDP.csproj` 编译通过。

## 额外观察

- `ComboDefinitionBoundarySmokeTests` 当前在本地仍失败，失败点是：
  - `Snapshot builder must create combo composite results.`
- 这次收口未改 `ExpressionSnapshotBuilder` 相关链路，因此先记为独立旧问题，不混进本轮修复。
