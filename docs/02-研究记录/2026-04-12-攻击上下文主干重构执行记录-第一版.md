# 攻击上下文主干重构执行记录-第一版

日期：2026-04-12

## 最新记录

### 1. 本次目标

按既定实施计划，把攻击链里分散的上下文碎片收成唯一主干：

```text
AttackContext
  ↓
AttackContextSnapshot
```

### 2. 本次实际执行

- 补齐 `RangedStageAddonContext` 全链调用点，全部改成传 `AttackContextSnapshot`
- 删除攻击链对 `SharedState（共享状态）` 的剩余读取
- 把确认下单改成“复制 `AttackContext` → 补确认节点 → 冻结快照”
- 把 `ProjectileInitStageService` 改成直接冻结 `entry.AttackContext`
- 翻新旧 smoke tests，让测试只盯真实主干事实
- 跑完主干、冻结边界、目标交互、投射物后半段、战斗日志、Trion（能量）等核心回归

### 3. 本次验证结果

已通过：

- `AttackContextBoundarySmokeTests.ps1`
- `AttackContextNoSharedStateSmokeTests.ps1`
- `AttackContextRangedProtocolCarrySmokeTests.ps1`
- `RangedModuleRuntimeSessionSmokeTests.ps1`
- `RangedModuleStageAddonBoundarySmokeTests.ps1`
- `RangedExecutionFreezeBoundarySmokeTests.ps1`
- `RangedTargetingProtocolSmokeTests.ps1`
- `RangedImpactExecutionBoundarySmokeTests.ps1`
- `FormalHostVerbSmokeTests.ps1`
- `RangedAttackTrionConsumptionSmokeTests.ps1`
- `CombatLogPresentationSmokeTests.ps1`
- `dotnet msbuild '.\Source\BDP\BDP.csproj'`

### 4. 本次结论

- 攻击链正式主干已收成一条线
- 旧共享态通路已清掉
- 旧测试残留已同步
- 当前剩下的是游戏内实测，不是代码主干未收口
