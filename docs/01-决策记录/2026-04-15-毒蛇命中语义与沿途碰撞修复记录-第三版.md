# 2026-04-15-毒蛇命中语义与沿途碰撞修复记录-第三版

## 本次落地

- `PathLatchModule` 不再在 `Aim（瞄准阶段）` 用 `OverrideFinalTarget（覆盖最终目标）` 传首段锚点
- `ProjectileInitPlanContribution` 新增中性字段：
  - `HasOverrideLaunchTarget`
  - `OverrideLaunchTarget`
- `PathLatchModule` 在 `ProjectileInit（投射物初始化阶段）` 明确三值分层：
  - `LaunchTarget（发射导航目标） = firstTarget（首段锚点）`
  - `AimTarget（瞄准目标） = confirmedSnapshot.FinalTarget（确认的最终目标）`
  - `CurrentTarget（当前正式目标） = confirmedSnapshot.FinalTarget（确认的最终目标）`
- `BdpVerb_Shoot` 的 `semanticTarget（语义目标）` 回退链补成：
  - `plan.CurrentTarget`
  - `plan.AimTarget`
  - `currentTarget`

## 这次没有做的事

- 没有新增毒蛇专用碰撞扫描
- 没有改原版 `Projectile（投射物）` 的碰撞规则
- 没有改现有 `ComputeContinuationOrigin(...)` 的 `6f` 数值

## 静态验证

- `Source/BDP.Tests/ViperPathLatchBoundarySmokeTests.ps1`
- `Source/BDP.Tests/ViperPathLatchCollisionParitySmokeTests.ps1`
- `Source/BDP.Tests/ViperPathLatchHitFlagsBranchSmokeTests.ps1`
- `Source/BDP.Tests/ViperPathLatchContinuationParitySmokeTests.ps1`

结果：全部 `PASS（通过）`

## 编译验证

- `dotnet msbuild '.\Source\BDP\BDP.csproj'`
- `dotnet msbuild '.\Source\BDP.DevHarness\BDP.DevHarness.csproj'`

结果：全部通过

## 还需要你游戏内实测确认的点

- `pawn（小人）` 反复穿越
- `wall（墙）` / `door（门）` 拦截
- `cover（掩体）` 干预
- 短段密锚点场景下 `6f` 是否仍足够
