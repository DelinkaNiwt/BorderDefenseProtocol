# 毒蛇路线引导职责边界审查

- 日期：`2026-04-18`
- 范围：`BorderDefenseProtocol` 主模组运行时与烟雾测试、`BorderDefenseProtocol.DevHarness` 路线引导样本与测试芯片定义

## 结论

- 主模组运行时骨架整体分层基本正确：核心层提供中性模块接口、阶段记录、段合法性查询、导航/语义目标分流、投射物续段桥。
- 路线引导业务主体基本都留在测试模组：`PathLatchModule / PathLatchState / PathLatchSegmentResolver / PathLatchConfig / BDP_TestChipPathLatch / BDP_TestRangedPathLatchModule`。
- 主模组真正的业务污染主要在两处：一是源码注释直接点名“毒蛇”，二是 `Source/BDP.Tests` 下存在一批直接绑死 `PathLatchModule` 的业务烟雾测试。
- 上述两处已在同日清理：核心注释改为中性模块表述，业务绑定烟雾测试脚本从主模组测试目录移除。
- 本轮未发现“测试模组反向托管核心架构”的硬越界运行时代码；灰区主要是测试模组同时承担了“教学骨架样例”和“具体业务样例”两种职责，容易让架构说明与业务样本混住。

## 主模组污染点与处理

- `Source/BDP/Core/AttackExecution/AttackExecutionTargetingSource.cs`
  - 注释直接点名“毒蛇”：
    - `:806`
    - `:1086`
  - 影响：运行时代码语义仍是中性的，但源码说明层已经带入具体业务背景。
  - 处理：已改为中性模块表述。

- `Source/BDP.Tests`
  - 直接耦合 `PathLatchModule` 的业务烟雾测试：
    - `ViperPathLatchAnchorWalkableSmokeTests.ps1`
    - `ViperPathLatchBaseCastTargetAdapterSmokeTests.ps1`
    - `ViperPathLatchBoundarySmokeTests.ps1`
    - `ViperPathLatchCollisionParitySmokeTests.ps1`
    - `ViperPathLatchContinuationParitySmokeTests.ps1`
    - `ViperPathLatchFirstAnchorContinuitySmokeTests.ps1`
    - `ViperPathLatchHitFlagsBranchSmokeTests.ps1`
    - `ViperPathLatchLegacyVisualBaselineSmokeTests.ps1`
    - `ViperPathLatchSegmentLosBoundarySmokeTests.ps1`
    - `ViperPathLatchTargetingContinuitySmokeTests.ps1`
    - `DualRangedSemanticTargetIsolationSmokeTests.ps1`
  - 影响：主模组测试层要通过具体业务样本才能验证一部分核心桥接，边界意识是对的，但耦合点偏重。
  - 处理：已从主模组测试目录删除这些脚本。

## 测试模组灰区

- `BorderDefenseProtocol.DevHarness/1.6/Defs/Pawn/Expressions/Test/RangedAttackModuleDefs_Test.xml`
  - 同时放了：
    - `BDP_TestRangedTeachingSkeletonModule`
    - `BDP_TestRangedPathLatchModule`
  - 影响：教学骨架样例和毒蛇业务样例混在同一份 Def 文件里，测试模组承担了部分“架构教学样板”职责。

## 本轮判断

- `PathLatchModule` 这条运行时业务线本身没有把核心协议反包回测试模组。
- 主模组需要优先清理的是“业务名进入核心注释”和“核心测试直接绑死具体业务样本”这两类污染。
