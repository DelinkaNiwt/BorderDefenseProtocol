# 动作打断功能实现说明

## 更新日期
2026-03-05

## 需求
在两个关键时刻打断Pawn的当前动作：
1. 进入延时破裂阶段时
2. 破裂流程结束时

## 目的
- 避免延时破裂期间还在继续攻击
- 避免破裂完成后还在继续破裂前的动作

## 实现方案

### 打断动作的标准方法
使用RimWorld的三个核心API：
1. `pawn.jobs.EndCurrentJob(JobCondition.InterruptForced)` - 结束当前Job
2. `pawn.stances.CancelBusyStanceSoft()` - 取消战斗姿态
3. `pawn.pather.StopDead()` - 停止移动

### 实现位置

#### 1. 进入延时破裂时
**文件**：`Combat/Damage/CombatBodyDamageHandler.cs`

**位置**：转换到Collapsing状态后立即调用

```csharp
// 转换到Collapsing状态
ctx.Runtime.State.TransitionToCollapsing(collapseReason);

// 打断当前动作（避免延时破裂期间继续攻击等动作）
InterruptCurrentAction(pawn, "进入延时破裂");
```

#### 2. 破裂流程结束时
**文件**：`Combat/Hediff_CombatBodyCollapsing.cs`

**位置**：ExecuteCollapse()方法开始时调用

```csharp
private void ExecuteCollapse()
{
    Log.Warning($"[BDP] 战斗体破裂执行: {pawn.LabelShort} (原因: {collapseReason})");

    // 打断当前动作（避免破裂完成后继续破裂前的动作）
    InterruptCurrentAction(pawn, "破裂流程结束");

    // ... 后续清理逻辑
}
```

### 辅助方法实现

在两个文件中都添加了相同的辅助方法：

```csharp
/// <summary>
/// 打断Pawn的当前动作。
/// 用于破裂时强制停止攻击、移动等动作。
/// </summary>
/// <param name="pawn">目标Pawn</param>
/// <param name="reason">打断原因（用于日志）</param>
private static void InterruptCurrentAction(Pawn pawn, string reason)
{
    if (pawn == null) return;

    Log.Message($"[BDP]   打断当前动作: {pawn.LabelShort} (原因: {reason})");

    // 1. 结束当前Job
    if (pawn.jobs?.curJob != null)
    {
        pawn.jobs.EndCurrentJob(Verse.AI.JobCondition.InterruptForced);
    }

    // 2. 取消战斗姿态
    if (pawn.stances != null)
    {
        pawn.stances.CancelBusyStanceSoft();
    }

    // 3. 停止移动
    if (pawn.pather != null)
    {
        pawn.pather.StopDead();
    }
}
```

## 修改的文件

1. `Combat/Damage/CombatBodyDamageHandler.cs`
   - 添加InterruptCurrentAction()辅助方法
   - 在转换到Collapsing状态后调用打断动作

2. `Combat/Hediff_CombatBodyCollapsing.cs`
   - 添加InterruptCurrentAction()辅助方法
   - 在ExecuteCollapse()开始时调用打断动作

## 测试验证

### 测试场景
1. 小人正在攻击敌人时触发破裂
2. 验证立即停止攻击动作
3. 验证日志显示："打断当前动作: [小人名] (原因: 进入延时破裂)"
4. 等待90 ticks破裂完成
5. 验证日志显示："打断当前动作: [小人名] (原因: 破裂流程结束)"
6. 验证小人不会继续破裂前的攻击动作

### 预期日志
```
[BDP] ⚠ 战斗体触发破裂！
[BDP]   打断当前动作: [小人名] (原因: 进入延时破裂)
... (90 ticks后)
[BDP] 战斗体破裂执行: [小人名] (原因: [破裂原因])
[BDP]   打断当前动作: [小人名] (原因: 破裂流程结束)
```

## 编译状态
✅ 编译成功，无错误

## 设计优势

1. **双重保险**：在进入和结束两个时刻都打断，确保不会有遗漏
2. **完整打断**：同时处理Job、Stance、Path三个层面
3. **清晰日志**：每次打断都有明确的日志输出，便于调试
4. **代码复用**：使用辅助方法避免重复代码

---

**实现者**: Claude Sonnet 4.6
